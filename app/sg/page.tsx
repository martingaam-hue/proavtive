// Phase 1 / Plan 01-01 — sg placeholder page. Copy is verbatim per UI-SPEC §Copywriting Contract (line 117 "executor MUST use these exact strings").
export default function SGPage() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center px-6 py-16">
      <section className="w-full max-w-2xl rounded-lg bg-muted p-6">
        <h1 className="text-2xl font-semibold leading-tight">ProActiv Sports — Singapore</h1>
        <p className="mt-4 text-base leading-relaxed">
          Placeholder for sg.proactivsports.com. Katong Point, zones, and camps arrive in Phase 5.
        </p>
        {/* <Button>Example primitive</Button> — enabled after Plan 01-02 lands the shadcn Button primitive. */}
      </section>
    </main>
  );
}

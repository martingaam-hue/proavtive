---
phase: 01-next-js-foundation-subdomain-middleware-sanity-studio-scaffold-vercel-previews
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - components.json
  - components/ui/button.tsx
  - lib/utils.ts
  - app/globals.css
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - FOUND-02
must_haves:
  truths:
    - "shadcn/ui CLI is wired with the preset style=new-york, baseColor=neutral, cssVariables=true, rsc=true, tsx=true (UI-SPEC §Design System table)"
    - "`components/ui/button.tsx` exists and exports a Button component (scaffolded by `npx shadcn@latest add button`)"
    - "`lib/utils.ts` exports `cn()` merging `clsx` + `tailwind-merge`"
    - "`components.json` declares the aliases @/components and @/lib/utils (resolve under tsconfig @/* → ./*)"
    - "`app/globals.css` declares shadcn CSS variables (--background, --foreground, --primary, --muted, --accent, --destructive, etc.) so Plan 01-01's `bg-muted` class resolves"
    - "`pnpm build` exits 0 — shadcn dependencies compile under Next.js 15 + React 19"
  artifacts:
    - path: components.json
      provides: "shadcn/ui registry config (style/baseColor/cssVariables/rsc/tsx + aliases)"
      contains: "new-york"
    - path: components/ui/button.tsx
      provides: "Button primitive scaffolded by shadcn CLI (single example; full primitive library lands in Phase 2 DS-03)"
      contains: "buttonVariants"
    - path: lib/utils.ts
      provides: "`cn()` helper (clsx + tailwind-merge)"
      contains: "export function cn"
    - path: app/globals.css
      provides: "shadcn CSS variable definitions for neutral baseColor"
      contains: "--background"
    - path: package.json
      provides: "shadcn runtime deps (class-variance-authority, clsx, tailwind-merge, lucide-react, @radix-ui/react-slot)"
      contains: "class-variance-authority"
  key_links:
    - from: components/ui/button.tsx
      to: lib/utils.ts
      via: "import { cn } from \"@/lib/utils\""
      pattern: "import \\{ cn \\} from"
    - from: app/globals.css
      to: components/ui/button.tsx
      via: "CSS variable consumption (--primary, --background, etc. via Tailwind utility classes)"
      pattern: "--primary"
    - from: tsconfig.json
      to: components.json
      via: "@/* path alias shared by both"
      pattern: "@/\\*"
---

<objective>
Initialize shadcn/ui CLI with the UI-SPEC §Design System preset (`style=new-york`, `baseColor=neutral`, `cssVariables=true`, `rsc=true`, `tsx=true`, aliases `@/components` + `@/lib/utils`) and install ONE primitive (Button) — proving the shadcn registry pipe for Phase 2's DS-03 primitive-library expansion. Satisfies the remaining portion of FOUND-02 (Tailwind + shadcn-pattern base typed; Plan 01-01 already landed Next.js 15 + the deployable App Router shell).

Purpose: CSS variables (`cssVariables=true`) is the non-negotiable flag — Phase 2 (DS-01) will override `--background`, `--foreground`, `--primary`, `--muted` etc. with real ProActiv brand tokens (`#0f206c` navy, `#ec1c24` red) WITHOUT refactoring any component classnames. Installing Button here also makes the `bg-muted` utility on Plan 01-01's placeholder pages resolve to a concrete neutral color (today) that cleanly swaps to a brand token (Phase 2).
Output: `components.json` + `components/ui/button.tsx` + `lib/utils.ts` + updated `app/globals.css` (CSS vars) + updated `package.json`/`pnpm-lock.yaml` (shadcn runtime deps).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-next-js-foundation-subdomain-middleware-sanity-studio-scaffold-vercel-previews/01-CONTEXT.md
@.planning/phases/01-next-js-foundation-subdomain-middleware-sanity-studio-scaffold-vercel-previews/01-UI-SPEC.md
@.planning/phases/01-next-js-foundation-subdomain-middleware-sanity-studio-scaffold-vercel-previews/01-PATTERNS.md
@.planning/phases/01-next-js-foundation-subdomain-middleware-sanity-studio-scaffold-vercel-previews/01-01-SUMMARY.md

<interfaces>
<!-- Key shadcn CLI contract the executor needs — extracted from shadcn docs + UI-SPEC. -->

shadcn CLI (v2+ — React 19 + Tailwind v4 compatible):
```bash
# Non-interactive init with explicit flags (UI-SPEC §Design System table, lines 19–25):
npx shadcn@latest init \
  --style new-york \
  --base-color neutral \
  --css-variables \
  --rsc \
  --tsx

# If the --flag names differ in the current CLI version, the executor uses the interactive prompt and
# answers with these exact values:
#   Which style? → new-york
#   Which base color? → neutral
#   Use CSS variables for colors? → yes
#   Using RSC (React Server Components)? → yes
#   Use TypeScript? → yes
#   Components alias: @/components
#   Utils alias: @/lib/utils

# Add the Button primitive:
npx shadcn@latest add button
```

Expected components/ui/button.tsx export shape (shadcn v2 new-york style):
```typescript
import { cva, type VariantProps } from "class-variance-authority";
export const buttonVariants = cva(/* ... */);
export interface ButtonProps
  extends React.ButtonHTMLAttributes<HTMLButtonElement>,
    VariantProps<typeof buttonVariants> {
  asChild?: boolean;
}
export const Button: React.ForwardRefExoticComponent<ButtonProps>;
```

From tsconfig.json (existing, DO NOT edit — shadcn aliases resolve through this):
```json
"paths": {
  "@/*": ["./*"]
}
```
→ `@/components` resolves to `./components`, `@/lib/utils` resolves to `./lib/utils`. shadcn's `components.json` MUST emit aliases that match this mapping.

From Tailwind v4 (active in repo via @tailwindcss/postcss):
Tailwind v4 uses CSS-first config (no tailwind.config.js/ts needed). shadcn v2's `cssVariables=true` emits `:root { --background: ...; --foreground: ...; ... }` directly into `app/globals.css`. The CLI may also add `@theme inline { ... }` bindings that expose the CSS vars as Tailwind utilities (so `bg-muted` resolves to `var(--muted)`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run shadcn init with preset flags and verify components.json + aliases</name>
  <read_first>
    - package.json (current state — confirm shadcn/CLI deps are NOT yet installed; post-install, compare)
    - tsconfig.json (lines 21–22 — `@/*` path alias must resolve shadcn's emitted `@/components` and `@/lib/utils` without a tsconfig edit)
    - app/globals.css (current state — confirm what shadcn init appends/replaces)
    - postcss.config.mjs (Tailwind v4 pipeline — shadcn must coexist with the existing `@tailwindcss/postcss` plugin)
    - .planning/phases/01-next-js-foundation-subdomain-middleware-sanity-studio-scaffold-vercel-previews/01-UI-SPEC.md §Design System (lines 17–34) — exact preset flags, non-negotiable cssVariables=true
    - .planning/phases/01-next-js-foundation-subdomain-middleware-sanity-studio-scaffold-vercel-previews/01-PATTERNS.md §`components/ui/button.tsx` + `lib/utils.ts` + `components.json` (lines 431–465) — treat CLI output as authoritative
  </read_first>
  <files>components.json, lib/utils.ts, app/globals.css (modified by CLI), package.json, pnpm-lock.yaml</files>
  <action>
Run the shadcn init command with the exact UI-SPEC preset. shadcn's CLI writes `components.json` and creates `lib/utils.ts` automatically.

Use `pnpm dlx` (not `npx`) to be consistent with Phase 0's pnpm-locked discipline (CONTEXT.md D-13):

```bash
pnpm dlx shadcn@latest init \
  --style new-york \
  --base-color neutral \
  --css-variables \
  --yes
```

Note: the current shadcn v2 CLI may not expose `--rsc` or `--tsx` as explicit flags; those are auto-detected from `package.json` (`next` + `typescript` present → both on). `--yes` accepts sensible defaults. After running, read `components.json` and verify the following fields are set:

Expected `components.json` contents (exact keys — if any differ, report and fix):
```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "app/globals.css",
    "baseColor": "neutral",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "iconLibrary": "lucide"
}
```

Critical checks after `init`:

1. **`components.json` exists** at repo root with `style: "new-york"`, `tailwind.baseColor: "neutral"`, `tailwind.cssVariables: true`, `aliases.components: "@/components"`, `aliases.utils: "@/lib/utils"`. If any value is wrong, hand-edit the JSON to match (CLI output is authoritative, but the preset is non-negotiable per UI-SPEC).

2. **`lib/utils.ts` exists** with the standard `cn()` helper:
```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```
Do NOT modify — CLI output is authoritative.

3. **`app/globals.css` has been updated** by the CLI to include the shadcn CSS variable block. The exact structure depends on Tailwind v4 (shadcn v2 CLI writes an `@theme` inline block plus `:root` + `.dark` variable definitions). Confirm the file contains at minimum:
   - `--background`, `--foreground`, `--primary`, `--primary-foreground`, `--secondary`, `--muted`, `--muted-foreground`, `--accent`, `--accent-foreground`, `--destructive`, `--border`, `--input`, `--ring` CSS variables defined under a `:root` or `@theme` block.
   - A `.dark { ... }` block mirroring the variables for dark mode (shadcn v2 default).
   - An existing Tailwind v4 import line — if the CLI removed `@import "tailwindcss";` from the pre-init globals.css, restore it (shadcn's init should not remove Tailwind imports, but Tailwind v4 + shadcn v2 is a newer combination — verify).

4. **`package.json` runtime deps added by the CLI**:
   - `class-variance-authority` (for Button's variant system)
   - `clsx` (used by `cn()`)
   - `tailwind-merge` (used by `cn()`)
   - `tw-animate-css` (shadcn v2 default — for Tailwind v4 animation utilities)
   - `lucide-react` (icon library — UI-SPEC confirms this is the default)
   - `@radix-ui/react-slot` (added when the first primitive is installed in Task 2; may already be present after init)

   All should land in `dependencies` (not devDependencies) — they're imported by runtime React components. Confirm via `cat package.json | jq '.dependencies'` or equivalent.

5. **Tailwind preflight sanity**: run `pnpm typecheck && pnpm build`. If the build fails with "Cannot find module 'tailwindcss/typography'" or similar, the shadcn init introduced a mismatch with the existing Tailwind v4 setup — fix by removing unnecessary shadcn CLI additions (shadcn v1 added tailwind.config.js plugins; shadcn v2 does not).

If the CLI reports any errors or writes files outside `files_modified`, STOP and diagnose before proceeding to Task 2.
  </action>
  <verify>
    <automated>
      test -f components.json && \
      grep -q '"style": "new-york"' components.json && \
      grep -q '"baseColor": "neutral"' components.json && \
      grep -q '"cssVariables": true' components.json && \
      grep -q '"components": "@/components"' components.json && \
      grep -q '"utils": "@/lib/utils"' components.json && \
      test -f lib/utils.ts && \
      grep -q "export function cn" lib/utils.ts && \
      grep -q "twMerge" lib/utils.ts && \
      grep -q "class-variance-authority" package.json && \
      grep -q "tailwind-merge" package.json && \
      grep -q "lucide-react" package.json && \
      grep -q -- "--background" app/globals.css && \
      grep -q -- "--primary" app/globals.css && \
      pnpm typecheck && \
      pnpm build
    </automated>
  </verify>
  <acceptance_criteria>
    - `components.json` exists at repo root with the UI-SPEC preset values (style/baseColor/cssVariables/aliases)
    - `lib/utils.ts` exists exporting `cn()` that combines `clsx` + `twMerge`
    - `app/globals.css` contains shadcn CSS variable definitions (`--background`, `--primary`, `--muted`, etc.) — these are what make `bg-muted` resolve on Plan 01-01's placeholder pages
    - `package.json` has new runtime deps: `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react` (all in `dependencies`, not `devDependencies`)
    - `pnpm-lock.yaml` updated to reflect new deps (no drift between lockfile and package.json)
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0 — shadcn dependencies compile cleanly alongside Next.js 15 + React 19 + Tailwind v4
  </acceptance_criteria>
  <done>shadcn/ui CLI is initialized with the correct preset; `cn()` helper and CSS variable block are present; the build still succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Install the Button primitive and enable the commented `<Button>` placeholder on one route-group page</name>
  <read_first>
    - components.json (just-created; confirms `aliases.ui: "@/components/ui"` — Button will land at `components/ui/button.tsx`)
    - lib/utils.ts (from Task 1 — Button imports `cn` from here)
    - app/hk/page.tsx (Plan 01-01 landed a commented-out `<Button>Example primitive</Button>` — this task uncomments it on ONE page)
    - .planning/phases/01-next-js-foundation-subdomain-middleware-sanity-studio-scaffold-vercel-previews/01-UI-SPEC.md §Copywriting Contract line 109 (Button label must be "Example primitive" — no onClick) and §Registry Safety (first-party registry only)
    - .planning/phases/01-next-js-foundation-subdomain-middleware-sanity-studio-scaffold-vercel-previews/01-PATTERNS.md §`components/ui/button.tsx` (lines 432–449) — CLI output is authoritative, Button is a server component
  </read_first>
  <files>components/ui/button.tsx, app/root/page.tsx (uncomment Button line or add client wrapper), package.json, pnpm-lock.yaml</files>
  <action>
1. Install the Button primitive from the shadcn OFFICIAL registry only (no third-party registries per UI-SPEC §Registry Safety):

```bash
pnpm dlx shadcn@latest add button
```

The CLI creates `components/ui/button.tsx` (scaffolded code — DO NOT hand-edit after) and adds `@radix-ui/react-slot` to dependencies if not already present.

2. Confirm `components/ui/button.tsx` contains:
   - `import { Slot } from "@radix-ui/react-slot";`
   - `import { cva, type VariantProps } from "class-variance-authority";`
   - `import { cn } from "@/lib/utils";`
   - An exported `buttonVariants` via `cva(...)` with variant slots for `default`, `destructive`, `outline`, `secondary`, `ghost`, `link` and size slots for `default`, `sm`, `lg`, `icon`.
   - An exported `Button` component that accepts `asChild`, `variant`, `size`, and forwards all HTML button attrs.

   Do NOT modify the file — CLI output is the contract.

3. Uncomment the `<Button>` line on **EXACTLY ONE** placeholder page to visibly prove the primitive is wired (pick `app/root/page.tsx` for the visible demo — the root placeholder is what appears on plain `localhost:3000` so the Plan 01-04 build-smoke and any Martin ad-hoc check will see it).

   Edit `app/root/page.tsx`:
   - Add `import { Button } from "@/components/ui/button";` at the top (it's fine for a Server Component to import and render a Server-or-Client component; shadcn's Button is client-interactive via Radix but renders OK server-side — the forwardRef + Slot machinery may require a `"use client"` in the import path; if so, the executor adds `"use client"` to Button's file is NOT an option — the CLI owns that file. Instead: create a tiny wrapper OR import Button directly; if build fails with a "useRef is only supported in client components" error, revert to the commented-out state and leave the single Button visible as a future Phase 2 task. UI-SPEC §Performance baseline line 192: "server component with zero 'use client' in the Phase 1 import path **if possible**" — "if possible" is the escape hatch.)

   - Replace the `{/* <Button>Example primitive</Button> */}` comment with `<Button>Example primitive</Button>` (no onClick handler per UI-SPEC §Copywriting Contract line 109).

   - Leave the commented-out Button lines on `app/hk/page.tsx` and `app/sg/page.tsx` as-is — only one visible instance is needed to prove wire-up.

4. If step 3 triggers a "Cannot use Slot / forwardRef in a Server Component" build error (common with React 19 + RSC), convert ONLY the Button instance to a client boundary by creating a tiny wrapper at `app/root/_components/example-button.tsx`:
```typescript
"use client";
import { Button } from "@/components/ui/button";
export function ExampleButton() {
  return <Button>Example primitive</Button>;
}
```
Then `app/root/page.tsx` imports `ExampleButton` and renders `<ExampleButton />`. This contains the `"use client"` boundary to one tiny wrapper — the page itself stays server-component.

5. Run `pnpm build` — must succeed. If SSR-hydration errors appear in the console on the smoke run, they are out-of-scope for Phase 1 (UI-SPEC §Performance baseline is Phase 7's problem — Phase 1 just needs a clean build).
  </action>
  <verify>
    <automated>
      test -f components/ui/button.tsx && \
      grep -q "buttonVariants" components/ui/button.tsx && \
      grep -q "cva" components/ui/button.tsx && \
      grep -q "@radix-ui/react-slot" package.json && \
      grep -q '<Button>Example primitive</Button>' 'app/root/page.tsx' 2>/dev/null || \
        grep -q 'ExampleButton' 'app/root/page.tsx' && \
      pnpm typecheck && \
      pnpm build
    </automated>
  </verify>
  <acceptance_criteria>
    - `components/ui/button.tsx` exists and exports `Button` + `buttonVariants` via `cva`
    - `@radix-ui/react-slot` is in `package.json` dependencies (added automatically by `shadcn add button`)
    - `app/root/page.tsx` renders a visible `<Button>Example primitive</Button>` (direct import) OR imports an `ExampleButton` wrapper from `app/root/_components/example-button.tsx` that contains the client boundary
    - `app/hk/page.tsx` and `app/sg/page.tsx` still have the commented-out `{/* <Button>... */}` placeholder (not uncommented — only one visible instance across Phase 1)
    - `pnpm build` exits 0 — Button renders cleanly under Next.js 15 + React 19 RSC
    - No third-party registry was used (search `components.json` for `registries` — if the CLI added any, remove them per UI-SPEC §Registry Safety)
  </acceptance_criteria>
  <done>shadcn Button primitive is scaffolded at `components/ui/button.tsx`, one visible instance renders on the root placeholder page, `pnpm build` succeeds — the shadcn pipe is proven for Phase 2's DS-03 primitive-library expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn registry (ui.shadcn.com) → repo | CLI downloads component source code from a remote registry and writes it to `components/ui/`. Untrusted registry = arbitrary code in repo. |
| npm registry → node_modules | New runtime deps (`class-variance-authority`, `lucide-react`, `@radix-ui/react-slot`, `tailwind-merge`, `clsx`, `tw-animate-css`) are installed and bundled into the production build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering / Supply chain | shadcn registry | mitigate | UI-SPEC §Registry Safety (lines 150–157) locks Phase 1 to the **first-party shadcn registry only** (`https://ui.shadcn.com/`). The CLI embeds the registry URL in `components.json`. Third-party registries (tremor, aceternity, magicui) are explicitly deferred to Phase 2 DS-03 with a vetting gate. Verified at acceptance: `components.json` has no `registries` entry pointing elsewhere. |
| T-01-07 | Tampering / Supply chain | npm dependencies (shadcn runtime deps) | mitigate | `pnpm install --frozen-lockfile` on CI (per `.github/workflows/ci.yml` and `vercel.json`) pins every transitive dep to the lockfile. Phase 0 Dependabot config (D-12) surfaces CVE updates. Phase 9 MIG-03 will audit for high/critical CVEs before cutover — Phase 1's new deps enter that audit surface. |
| T-01-08 | Information disclosure | Button component — client boundary if created | accept | If a `"use client"` wrapper is introduced in Task 2 step 4, the Button ships to the browser bundle. Button has no inputs, no event handlers, no secrets — it renders the label "Example primitive" and takes no user input. Information disclosure surface = the component's own source code, which is non-sensitive (public shadcn registry code). No mitigation needed. |
| T-01-09 | Tampering | `app/globals.css` overwrite by shadcn init | mitigate | Task 1 explicitly verifies that the Tailwind v4 `@import "tailwindcss";` directive survives the init, and that the CSS variable block is additive (not replacing existing styles). If the init overwrites or corrupts the file, the executor diffs against git HEAD and restores Tailwind bootstrap lines. Build verification (`pnpm build`) catches any CSS-pipeline breakage at acceptance. |
</threat_model>

<verification>
Overall plan-level checks before closing 01-02:

- `test -f components.json && test -f lib/utils.ts && test -f components/ui/button.tsx` — three artifact files exist
- `grep "class-variance-authority" package.json && grep "lucide-react" package.json && grep "tailwind-merge" package.json` — runtime deps landed
- `grep -- "--background\\|--primary\\|--muted" app/globals.css` — shadcn CSS vars defined (so Plan 01-01's `bg-muted` resolves)
- `pnpm build` exits 0
- `pnpm typecheck` exits 0
- No files touched outside `files_modified`
- The visible Button appears on the root placeholder when `pnpm dev` runs and http://root.localhost:3000/ is hit (ad-hoc visual check — not blocking, but recommended before closing the plan)
</verification>

<success_criteria>
Plan complete when:
- [ ] `components.json` at repo root with `style: new-york`, `baseColor: neutral`, `cssVariables: true`, aliases `@/components` + `@/lib/utils`
- [ ] `lib/utils.ts` with `cn()` helper (CLI-scaffolded)
- [ ] `components/ui/button.tsx` with `buttonVariants` + `Button` export (CLI-scaffolded, unmodified)
- [ ] `app/globals.css` augmented with shadcn CSS variable block (`--background`, `--primary`, `--muted`, etc.)
- [ ] `package.json` has new dependencies: `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`, `@radix-ui/react-slot`, `tw-animate-css`
- [ ] `pnpm-lock.yaml` in sync with `package.json` (no drift)
- [ ] `pnpm build` exits 0
- [ ] One visible `<Button>` instance renders on `app/root/page.tsx` (direct or via small client wrapper)
- [ ] `app/hk/page.tsx` + `app/sg/page.tsx` keep their commented-out `{/* <Button> */}` placeholders
- [ ] No third-party registry entries in `components.json`
</success_criteria>

<output>
After completion, create `.planning/phases/01-next-js-foundation-subdomain-middleware-sanity-studio-scaffold-vercel-previews/01-02-SUMMARY.md` recording:
- Exact shadcn CLI version used (`pnpm dlx shadcn@latest --version`)
- Whether Button rendered directly in the Server Component or required a tiny client wrapper
- Actual `components.json` contents (copy verbatim) for Phase 2 reference
- Any CSS variable naming quirks (Tailwind v4 + shadcn v2 combined behaviour)
</output>

---
phase: 05-singapore-market
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/sg/layout.tsx
  - app/sg/opengraph-image.tsx
  - components/sg/sg-nav.tsx
  - components/sg/sg-nav-mobile.tsx
  - components/sg/sg-footer.tsx
  - components/sg/venue-map.tsx
  - components/sg/venue-chip-row.tsx
  - components/sg/active-venue-chip.tsx
autonomous: true
requirements: [SG-01]
must_haves:
  truths:
    - "Every SG page in Waves 3+ inherits SGNav (top) and SGFooter (bottom) via app/sg/layout.tsx"
    - "SGNav shows 6 primary nav items + sticky red Book-a-Free-Trial CTA, dropdowns for Weekly Classes and Prodigy Camps"
    - "SGFooter shows single Katong Point NAP + WhatsApp + cross-market HK/Root links + social icons"
    - "Default SG opengraph-image renders Prodigy-green background distinguishing SG from HK navy in social shares"
    - "Baloo 2 font activates on the SG tree via app/sg/layout.tsx the same way Phase 4 activates it on HK"
    - "Katong-point venue chip and single-chip row composition available for homepage consumption"
  artifacts:
    - path: "app/sg/layout.tsx"
      provides: "SGGroupLayout with metadataBase + skip link + SGNav + children + SGFooter + Baloo 2 variable scope"
      contains: "metadataBase: new URL"
    - path: "components/sg/sg-nav.tsx"
      provides: "RSC nav with NavigationMenu dropdowns + sticky Book CTA"
      contains: "Book a Free Trial"
    - path: "components/sg/sg-nav-mobile.tsx"
      provides: "Client Sheet drawer with bottom-sticky Book CTA"
      contains: "\"use client\""
    - path: "components/sg/sg-footer.tsx"
      provides: "RSC footer with 4-column grid, single Katong NAP, WhatsApp CTA, cross-market links"
      contains: "Katong Point"
    - path: "components/sg/venue-map.tsx"
      provides: "Lazy iframe wrapper component consumed by Katong Point page (Plan 05-05)"
      contains: "loading=\"lazy\""
    - path: "components/sg/venue-chip-row.tsx"
      provides: "Single-venue chip composition for SG homepage §3.2"
      contains: "Katong Point"
    - path: "app/sg/opengraph-image.tsx"
      provides: "Default SG OG image (Prodigy-green background)"
      contains: "createSGOgImage"
  key_links:
    - from: "app/sg/layout.tsx"
      to: "components/sg/sg-nav.tsx + components/sg/sg-footer.tsx"
      via: "import + JSX render"
      pattern: "<SGNav />.*<SGFooter />"
    - from: "components/sg/sg-nav.tsx"
      to: "lib/sg-data.ts SG_ZONES + SG_CAMP_TYPES"
      via: "imported for dropdown items"
      pattern: "from \"@/lib/sg-data\""
    - from: "components/sg/sg-footer.tsx"
      to: "lib/sg-data.ts KATONG_POINT_NAP"
      via: "imported for NAP block"
      pattern: "KATONG_POINT_NAP"
    - from: "app/sg/opengraph-image.tsx"
      to: "lib/og-image.tsx createSGOgImage"
      via: "import + default export function"
      pattern: "createSGOgImage"
---

<objective>
Replace the Phase 1 SG stub layout with production layout chrome: `app/sg/layout.tsx` (metadataBase + skip link + SGNav + SGFooter + Baloo 2 font scope per D-01), SGNav/SGNavMobile (6 primary items + 2 dropdowns + sticky red CTA per D-02/D-03/D-04/D-05), SGFooter (single Katong NAP + cross-market links per D-08), VenueMap + VenueChipRow + ActiveVenueChip Phase 5-local compositions, and the default `app/sg/opengraph-image.tsx` wired to `createSGOgImage`. Per CONTEXT D-01, D-02, D-03, D-04, D-05, D-06, D-08, D-09 + UI-SPEC §5.1, §5.2, §5.3, §5.4 + PATTERNS.md §app/sg/layout.tsx + §components/sg/sg-nav.tsx + §components/sg/sg-footer.tsx + §components/sg/venue-map.tsx.

Purpose: Every SG page in Wave 3 (05-03 homepage, 05-04 pillars, 05-05 location/supporting, 05-06 booking) renders inside this layout. Without it, pages would each need to wire their own nav/footer. This is the "single-write, many-reuse" chrome that makes the rest of Phase 5 parallelizable.

Output: 8 files — 1 modified (`app/sg/layout.tsx` replaces Phase 1 stub), 7 created (SGNav, SGNavMobile, SGFooter, VenueMap, VenueChipRow, ActiveVenueChip, default opengraph-image). Mostly verbatim mirror of Phase 4 HK chrome — structural copy + rename tokens + swap copy text per strategy PART 6C.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-singapore-market/05-CONTEXT.md
@.planning/phases/05-singapore-market/05-PATTERNS.md
@.planning/phases/05-singapore-market/05-UI-SPEC.md
@.planning/phases/05-singapore-market/05-RESEARCH.md
@.planning/phases/05-singapore-market/05-01-SUMMARY.md
@app/hk/layout.tsx
@components/hk/hk-nav.tsx
@components/hk/hk-nav-mobile.tsx
@components/hk/hk-footer.tsx
@components/hk/venue-map.tsx
@components/hk/venue-chip-row.tsx
@components/hk/active-venue-chip.tsx
@app/hk/opengraph-image.tsx
@app/fonts.ts
@lib/sg-data.ts
@lib/og-image.tsx

<interfaces>
<!-- Key contracts from Plan 05-01 (consumed directly — no exploration needed) -->

From lib/sg-data.ts (Plan 05-01):
- `export const SG_ZONES: readonly SGZone[]` — 3 entries: { slug, href: "/weekly-classes/{slug}/", label, ageBand, tag?, h1, metaTitle, metaDescription, whatTheyLearn, classStructure }
- `export const SG_CAMP_TYPES: readonly SGCampType[]` — 3 entries with same shape plus camp-specific fields
- `export const KATONG_POINT_NAP` — { nameFull: "Prodigy by ProActiv Sports — Katong Point", addressStreet, addressLocality, addressCountry }
- `export const KATONG_POINT` — full venue shape (imported by venue-chip-row)

From lib/og-image.tsx (Plan 05-01):
- `export async function createSGOgImage({ title, tagline }: OgImageOptions): Promise<ImageResponse>` — Prodigy-green background

From app/fonts.ts (existing Phase 2):
- `export const baloo` — next/font/google Baloo 2 load at weight 600 with `.variable = "--font-accent"`

From components/ui/ (Phase 2 primitives):
- `Button` (with `size="touch"` variant = 44px)
- `ContainerEditorial` (max-width wrapper)
- `Section` (with `size="sm|md|lg"` vertical padding)
- `Sheet`, `SheetContent`, `SheetTrigger`, `SheetHeader`, `SheetTitle` (mobile drawer)
- `NavigationMenu`, `NavigationMenuList`, `NavigationMenuItem`, `NavigationMenuTrigger`, `NavigationMenuContent`, `NavigationMenuLink`
- `Separator`
- `Card`

From app/hk/layout.tsx (analog, do NOT modify — MIRROR structure):
- HK_PRODUCTION_BASE constant + metadataBase fallback chain (VERCEL_ENV → VERCEL_PROJECT_PRODUCTION_URL → VERCEL_URL → localhost)
- metadata.openGraph declares siteName + locale + type
- Body returns `<div className={baloo.variable}>` wrapping skip-link + HKNav + main + HKFooter
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Layout + SGNav + SGNavMobile + SGFooter (chrome)</name>
  <files>app/sg/layout.tsx, components/sg/sg-nav.tsx, components/sg/sg-nav-mobile.tsx, components/sg/sg-footer.tsx</files>
  <read_first>
    - app/hk/layout.tsx (analog — 76 lines, exact structural mirror)
    - components/hk/hk-nav.tsx (analog — 152 lines, mirror for SGNav)
    - components/hk/hk-nav-mobile.tsx (analog — 118 lines, mirror for SGNavMobile)
    - components/hk/hk-footer.tsx (analog — 245 lines, mirror for SGFooter with single-venue diff)
    - .planning/phases/05-singapore-market/05-PATTERNS.md lines 75-375 (precise HK→SG substitution rules for layout + nav + footer)
    - .planning/phases/05-singapore-market/05-UI-SPEC.md §5.1 (SGNav spec: 6 items, 2 dropdowns, sticky red CTA, mobile bottom-sticky), §5.2 (SGFooter spec), §3.0 (wrapper uses `<div className={baloo.variable}>`)
    - .planning/phases/05-singapore-market/05-CONTEXT.md D-02, D-03, D-04, D-05, D-06, D-08 (nav + footer decisions)
    - lib/sg-data.ts (Plan 05-01 output — imports SG_ZONES, SG_CAMP_TYPES, KATONG_POINT_NAP)
    - .planning/inputs/strategy.md §PART 6C §Hero (tagline copy for footer brand block)
  </read_first>
  <behavior>
    - `app/sg/layout.tsx` replaces the Phase 1 teal-stripe stub. Exports named `metadata: Metadata` with metadataBase for `https://sg.proactivsports.com` (via VERCEL_ENV→VERCEL_PROJECT_PRODUCTION_URL→VERCEL_URL→`http://sg.localhost:3000` fallback), title template "%s | Prodigy by ProActiv Sports", description about Prodigy Katong Point MultiBall, openGraph siteName "Prodigy by ProActiv Sports — Singapore" + `locale: "en_SG"`. Default export `SGGroupLayout` renders `<div className={baloo.variable}>` wrapping skip link + `<SGNav />` + `<main id="main-content">{children}</main>` + `<SGFooter />`.
    - `components/sg/sg-nav.tsx` is an RSC with sticky header (`sticky top-0 z-50 bg-background/95 backdrop-blur-sm border-b border-brand-navy/10`), brand lockup linking `/` ("Prodigy" in Baloo accent + "SG" in display font), 6 primary items per D-02: Weekly Classes dropdown (3 zones from SG_ZONES), Prodigy Camps dropdown (3 camp types from SG_CAMP_TYPES), Katong Point, Coaches, FAQ links, then red sticky Book-a-Free-Trial Button (`size="touch"`, `bg-brand-red`) linking `/book-a-trial/` using `<a href>` (NOT `<Link>`) to preserve trailing slash per PATTERNS §Trailing Slash Preservation. Dropdown for Sports+MultiBall zone entry shows `<Badge className="bg-brand-green text-white">Singapore's only</Badge>` inline per UI-SPEC §5.1. Mobile: renders `<SGNavMobile />` (client sub-component).
    - `components/sg/sg-nav-mobile.tsx` is `"use client"` using `React.useState` for open/close. `<Sheet side="right">` with `<SheetTrigger>` Menu hamburger (hidden on `lg`), SheetContent contains top-sticky red Book CTA + grouped nav: Group 1 "Weekly Classes" maps SG_ZONES with ageBand subline, Group 2 "Prodigy Camps" maps SG_CAMP_TYPES, Group 3 flat links (Katong Point, Parties, Schools, Events, Coaches, Blog, FAQ). D-05 mobile variant: large sticky red CTA at the BOTTOM of Sheet too (per UI-SPEC §5.1 mobile variant). All nav-link `onClick` calls `close()` to dismiss the sheet on navigation.
    - `components/sg/sg-footer.tsx` is an RSC with 4-col grid (`grid-cols-1 md:grid-cols-2 lg:grid-cols-4`) on `bg-brand-navy`. Col 1: Brand wordmark "Prodigy by ProActiv Sports" (Baloo accent on "Prodigy") + tagline "Kids' sports classes, holiday camps & birthday parties at Katong Point — home of Singapore's only MultiBall wall." Col 2: Single Katong Point NAP block (nameFull with Baloo accent, MapPin + "451 Joo Chiat Road, Level 3, Singapore 427664", conditional Phone + WhatsApp chips reading `process.env.NEXT_PUBLIC_SG_PHONE` and `NEXT_PUBLIC_WHATSAPP_SG`). Col 3: Quick links (Weekly Classes, Prodigy Camps, Parties, Schools, Coaches, Blog, FAQ — same-subdomain `<Link>`). Col 4: Connect — WhatsApp CTA (green icon + "Chat on WhatsApp" + href `https://wa.me/{env}`), email chip, cross-market `<a href={process.env.NEXT_PUBLIC_HK_URL}>Also in Hong Kong ↗</a>` and `<a href={process.env.NEXT_PUBLIC_ROOT_URL}>ProActiv Sports Group ↗</a>` (Pitfall 7 — always `<a href>` cross-subdomain, never `<Link>`). Below the 4 cols: Social icon row (Facebook/Instagram/LinkedIn SVGs copied from HKFooter), Separator, bottom bar with "© 2026 ProActiv Sports Singapore" + privacy/terms links as `<a href={NEXT_PUBLIC_ROOT_URL}/privacy>` etc.
  </behavior>
  <action>
    **Step 1 — app/sg/layout.tsx (MODIFY, replace stub):**
    Read `app/hk/layout.tsx`. Copy the file verbatim into `app/sg/layout.tsx` replacing the existing Phase 1 stub. Apply these 4 substitutions:
    1. `hk-data` → `sg-data` (import path) — but this file doesn't import hk-data; it imports components only. So: `@/components/hk/hk-nav` → `@/components/sg/sg-nav`, `@/components/hk/hk-footer` → `@/components/sg/sg-footer`.
    2. `HKNav` → `SGNav`, `HKFooter` → `SGFooter`, `HKGroupLayout` → `SGGroupLayout` (function name).
    3. `hk.proactivsports.com` → `sg.proactivsports.com` (in HK_PRODUCTION_BASE constant rename: `HK_PRODUCTION_BASE` → `SG_PRODUCTION_BASE`); `hk.localhost:3000` → `sg.localhost:3000`; `en_HK` → `en_SG`; `ProGym by ProActiv Sports` → `Prodigy by ProActiv Sports`; `Hong Kong` in strings → `Singapore`.
    4. Copy text per UI-SPEC §3 + strategy PART 6C: title default = "Kids' Sports Classes, Camps & Parties Singapore | Prodigy by ProActiv Sports"; template = "%s | Prodigy by ProActiv Sports"; description = "Kids' sports classes, holiday camps & birthday parties at Prodigy by ProActiv Sports — Katong Point, Singapore. Home of the only MultiBall wall. Book a free trial."; openGraph siteName = "Prodigy by ProActiv Sports — Singapore".

    The existing Phase 1 stub at `app/sg/layout.tsx` is deleted wholesale and replaced.

    **Step 2 — components/sg/sg-nav.tsx (CREATE):**
    Read `components/hk/hk-nav.tsx` lines 1-152. Mirror file into `components/sg/sg-nav.tsx` with the following substitutions:
    1. Imports: `@/components/hk/hk-nav-mobile` → `@/components/sg/sg-nav-mobile`; `@/lib/hk-data` → `@/lib/sg-data`; `HKNavMobile` → `SGNavMobile`; data imports become `SG_ZONES, SG_CAMP_TYPES` per UI-SPEC §5.1.
    2. Brand name: `<span className="font-accent text-brand-green">Prodigy</span> SG` (HK says `ProGym` or similar — replace). `aria-label` = "Prodigy by ProActiv Sports Singapore — home".
    3. `FLAT_NAV_LINKS` const (desktop visible + used by mobile component too): `[{ href: "/katong-point/", label: "Katong Point" }, { href: "/coaches/", label: "Coaches" }, { href: "/faq/", label: "FAQ" }]` per D-02 (this is the visible-on-desktop subset; Parties/Schools/Events/Blog are in mobile Sheet only per UI-SPEC §5.1 since SGNav desktop shows only 6 items).
    4. First dropdown: `<NavigationMenuTrigger>Weekly Classes</NavigationMenuTrigger>` with `<NavigationMenuContent>` mapping `SG_ZONES.map((z) => <NavigationMenuLink>Link href={z.href}>label/ageBand`). For Sports+MultiBall zone (`z.slug === "sports-multiball"`), render `<Badge className="bg-brand-green text-white ml-2">Singapore's only</Badge>` inline. Include bottom "See all zones →" link to `/weekly-classes/`.
    5. Second dropdown: `<NavigationMenuTrigger>Prodigy Camps</NavigationMenuTrigger>` with `SG_CAMP_TYPES.map(...)`. Bottom link "See all camps →" to `/prodigy-camps/`.
    6. Sticky red CTA (per D-05): `<Button asChild size="touch" className="bg-brand-red text-white hover:bg-brand-red/90 focus-visible:ring-2 focus-visible:ring-brand-red focus-visible:ring-offset-2"><a href="/book-a-trial/">Book a Free Trial <ArrowRight className="ml-2 size-4" /></a></Button>` (plain `<a>` not `<Link>` — PATTERNS §Trailing Slash Preservation).
    7. Mobile: `<SGNavMobile />` renders at `lg:hidden`.

    **Step 3 — components/sg/sg-nav-mobile.tsx (CREATE):**
    Read `components/hk/hk-nav-mobile.tsx` lines 1-118. Mirror with swaps:
    1. `"use client"` top directive (mandatory).
    2. Imports: `@/lib/hk-data` → `@/lib/sg-data`; `HK_VENUES / HK_GYMNASTICS_PROGRAMMES` → `SG_ZONES, SG_CAMP_TYPES`.
    3. Export name: `HKNavMobile` → `SGNavMobile`.
    4. Sheet structure verbatim: `<Sheet open={open} onOpenChange={setOpen}>` with SheetTrigger Menu icon + SheetContent side="right" (D-05 mobile: keep TOP sticky Book CTA AND add BOTTOM sticky Book CTA per UI-SPEC §5.1 mobile variant).
    5. Grouped nav:
       - Group 1 heading "Weekly Classes" → map SG_ZONES with label + ageBand subline; Sports+MultiBall gets `<Badge>Singapore's only</Badge>` inline.
       - Group 2 heading "Prodigy Camps" → map SG_CAMP_TYPES with label + ageBand + `tag` (if present, e.g. "Ninja · Pokémon · ...").
       - Group 3 (flat, under `border-t border-border pt-4 mt-4`): Katong Point `/katong-point/`, Parties `/birthday-parties/`, Schools `/school-partnerships/`, Events `/events/`, Coaches `/coaches/`, Blog `/blog/`, FAQ `/faq/`.
    6. Every `<Link>` calls `onClick={close}` to dismiss the sheet.
    7. Bottom of Sheet: second sticky `<Button asChild size="touch" className="bg-brand-red text-white hover:bg-brand-red/90 w-full mt-6"><Link href="/book-a-trial/" onClick={close}>Book a Free Trial</Link></Button>` (per D-05 mobile variant).

    **Step 4 — components/sg/sg-footer.tsx (CREATE):**
    Read `components/hk/hk-footer.tsx` lines 1-245. Mirror the file with these differences per D-08 + UI-SPEC §5.2:
    1. Imports: `@/lib/hk-data` → `@/lib/sg-data`; `HK_VENUES` array import → `KATONG_POINT_NAP` single-venue import.
    2. Export name: `HKFooter` → `SGFooter`.
    3. Env var reads: `NEXT_PUBLIC_WHATSAPP_HK` → `NEXT_PUBLIC_WHATSAPP_SG`; `NEXT_PUBLIC_HK_PHONE` → `NEXT_PUBLIC_SG_PHONE`; `NEXT_PUBLIC_HK_URL` → keep (cross-market link TO HK); add `NEXT_PUBLIC_ROOT_URL` for group link.
    4. Col 1 brand block: `<span className="font-accent text-brand-yellow">Prodigy</span>` on white text (navy background) for the wordmark + tagline "Kids' sports classes, holiday camps &amp; birthday parties at Katong Point — home of Singapore's only MultiBall wall."
    5. Col 2 venue block — REPLACE the HK `.map(HK_VENUES)` 2-venue rendering with a SINGLE block per D-08:
    ```tsx
    <div>
      <h3 className="font-display text-lg text-white mb-4">Venue</h3>
      <div>
        <div className="font-sans font-semibold text-white">
          <span className="font-accent">Prodigy</span> @ Katong Point
        </div>
        <p className="text-sm text-white/80 flex items-start gap-1 mt-1">
          <MapPin className="size-3.5 mt-0.5 shrink-0" aria-hidden="true" />
          <span>{KATONG_POINT_NAP.addressStreet}, Singapore 427664</span>
        </p>
        {whatsappSg && (
          <a href={`https://wa.me/${whatsappSg.replace(/\D/g, "")}`} className="text-sm text-white/80 hover:text-white flex items-center gap-1 mt-1">
            <MessageCircle className="size-3.5 text-brand-green" aria-hidden="true" />
            <span>WhatsApp Prodigy</span>
          </a>
        )}
      </div>
    </div>
    ```
    6. Col 3 Quick links: Weekly Classes `/weekly-classes/`, Prodigy Camps `/prodigy-camps/`, Parties `/birthday-parties/`, Schools `/school-partnerships/`, Events `/events/`, Coaches `/coaches/`, Blog `/blog/`, FAQ `/faq/` (same-subdomain `<Link>`).
    7. Col 4 Connect — WhatsApp CTA + email + cross-market `<a href={hkUrl}>ProGym Hong Kong ↗</a>` (label change per PATTERNS §cross-subdomain; user going from SG→HK sees HK's ProGym brand) + `<a href={rootUrl}>ProActiv Sports Group ↗</a>`.
    8. Social icons row: copy HK's Facebook/Instagram/LinkedIn inline SVG components verbatim (lines 25-63 of HK analog).
    9. Bottom bar: `© 2026 ProActiv Sports Singapore` + privacy/terms as `<a href={rootUrl}/privacy>` / `<a href={rootUrl}/terms>`.

    After creating all 4 files: `pnpm typecheck` (zero errors), `pnpm build` dry-run NOT required at this stage (Plan 05-03 will add homepage).
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `app/sg/layout.tsx` contains "SGNav" AND "SGFooter"
    - File `app/sg/layout.tsx` contains "sg.proactivsports.com"
    - File `app/sg/layout.tsx` contains "en_SG"
    - File `app/sg/layout.tsx` contains "baloo.variable"
    - File `app/sg/layout.tsx` contains "Prodigy by ProActiv Sports"
    - File `app/sg/layout.tsx` contains "metadataBase: new URL"
    - File `app/sg/layout.tsx` contains "#main-content" (skip link target)
    - File `components/sg/sg-nav.tsx` contains "Book a Free Trial"
    - File `components/sg/sg-nav.tsx` contains "SG_ZONES" AND "SG_CAMP_TYPES"
    - File `components/sg/sg-nav.tsx` contains "bg-brand-red"
    - File `components/sg/sg-nav.tsx` contains "NavigationMenu"
    - File `components/sg/sg-nav.tsx` contains "href=\"/book-a-trial/\""
    - File `components/sg/sg-nav.tsx` contains "Katong Point"
    - File `components/sg/sg-nav-mobile.tsx` starts with "\"use client\""
    - File `components/sg/sg-nav-mobile.tsx` contains "Sheet" AND "SheetContent"
    - File `components/sg/sg-nav-mobile.tsx` contains "onClick={close}"
    - File `components/sg/sg-footer.tsx` contains "KATONG_POINT_NAP"
    - File `components/sg/sg-footer.tsx` contains "451 Joo Chiat" (via NAP import chain)
    - File `components/sg/sg-footer.tsx` contains "NEXT_PUBLIC_HK_URL"
    - File `components/sg/sg-footer.tsx` contains "NEXT_PUBLIC_ROOT_URL"
    - File `components/sg/sg-footer.tsx` contains "bg-brand-navy"
    - File `components/sg/sg-footer.tsx` contains "font-accent" (Baloo accent on Prodigy)
    - `grep -c "sg-placeholder" components/sg/*.tsx app/sg/layout.tsx` returns 0 — placeholder guard remains clean
    - `pnpm typecheck` passes with zero errors
    - `pnpm test:unit -- --run tests/no-sg-placeholder-leak.test.ts` passes
  </acceptance_criteria>
  <done>Layout + nav + footer live. All Wave-3 plans can mount pages under app/sg/ and inherit chrome automatically. Mobile Sheet + desktop NavigationMenu dropdowns both functional from SG_ZONES/SG_CAMP_TYPES data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VenueMap + VenueChipRow + ActiveVenueChip + default opengraph-image</name>
  <files>components/sg/venue-map.tsx, components/sg/venue-chip-row.tsx, components/sg/active-venue-chip.tsx, app/sg/opengraph-image.tsx</files>
  <read_first>
    - components/hk/venue-map.tsx (analog — 51 lines, verbatim mirror)
    - components/hk/venue-chip-row.tsx (analog — 2-venue pattern; SG adapts to 1-venue)
    - components/hk/active-venue-chip.tsx (analog — client pathname compare)
    - app/hk/opengraph-image.tsx (analog — 25 lines; default OG wiring pattern)
    - .planning/phases/05-singapore-market/05-PATTERNS.md lines 378-460 (venue-map + venue-chip-row SG simplification, single-chip composition)
    - .planning/phases/05-singapore-market/05-UI-SPEC.md §5.3 (VenueChipRow single-chip spec), §5.4 (VenueMap fallback spec), §7 (OG template rules)
    - lib/og-image.tsx (Plan 05-01 output — createSGOgImage consumer)
    - lib/sg-data.ts (Plan 05-01 output — KATONG_POINT consumer)
  </read_first>
  <behavior>
    - `components/sg/venue-map.tsx` is an RSC exporting `VenueMap({ embedSrc, title, className? })`. If `embedSrc` empty or contains "PLACEHOLDER", renders `<div role="status" aria-live="polite">Map loading — see venue address below.</div>` fallback. Otherwise renders `<iframe loading="lazy" referrerPolicy="no-referrer-when-downgrade" title={title}>`.
    - `components/sg/venue-chip-row.tsx` is an RSC rendering a SINGLE centred chip (simpler than HK's 2-chip `.map`). Chip = `<ActiveVenueChip href="/katong-point/" label="Prodigy @ Katong Point" address="451 Joo Chiat Road, Level 3" />` with `<MapPin>` icon and Baloo accent on "Prodigy" word. Outer wrapper `<div className="flex justify-center">` so mobile is full-width, desktop is centred.
    - `components/sg/active-venue-chip.tsx` is `"use client"` using `usePathname()` to compare against `/katong-point/` href. Active state: `bg-brand-navy text-white`. Inactive: `bg-background border border-brand-navy/20 hover:bg-brand-navy/5`. Renders `<Link href={href}>` wrapping `<Card className={activeStateClasses}>`.
    - `app/sg/opengraph-image.tsx` exports runtime="nodejs", dynamic="force-dynamic", size={width:1200,height:630}, contentType="image/png". Default export calls `createSGOgImage({ title: "Prodigy by ProActiv Sports Singapore", tagline: "Kids' sports, camps, and parties at Katong Point — home of Singapore's only MultiBall wall." })`.
  </behavior>
  <action>
    **Step 1 — components/sg/venue-map.tsx (CREATE):**
    Read `components/hk/venue-map.tsx` lines 1-51. Copy the file verbatim into `components/sg/venue-map.tsx`. No substitutions needed — the component is market-agnostic (generic `{ embedSrc, title, className? }` props). Add Phase 5 comment header: `// Phase 5 / Plan 05-02 — SG venue map iframe. Mirrors components/hk/venue-map.tsx verbatim (component is market-agnostic).`

    The component must use `loading="lazy"` on the iframe (performance — avoids Google Maps JS blocking initial render), `referrerPolicy="no-referrer-when-downgrade"`, and `title` attribute (a11y). If `embedSrc` is empty or includes the string "PLACEHOLDER" (which it will at execute-time until user drops real embed URL into `NEXT_PUBLIC_MAP_EMBED_KATONG_POINT`), render a `<div role="status" aria-live="polite" aria-label="{title} — map unavailable, see address below">` with copy "Map loading — see venue address below."

    **Step 2 — components/sg/active-venue-chip.tsx (CREATE):**
    Read `components/hk/active-venue-chip.tsx`. Mirror with `"use client"` directive + `usePathname()`. Props: `{ href, label, address }`. Active state when `pathname === href` OR `pathname === href.replace(/\/$/, "")`. Apply classes: active → `bg-brand-navy text-white`; inactive → `bg-background border border-brand-navy/20 hover:-translate-y-1 hover:shadow-md transition-transform`. Inside the chip render `<MapPin className="size-4 mt-0.5 shrink-0" />` + `<div>` stack with label (Manrope SemiBold, "Prodigy" word wrapped in `<span className="font-accent">`) + address (text-small text-muted-foreground).

    **Step 3 — components/sg/venue-chip-row.tsx (CREATE):**
    Read `components/hk/venue-chip-row.tsx`. SG simplification per D-08 (single venue): NO `.map(HK_VENUES)` pattern. Instead single `<ActiveVenueChip href="/katong-point/" label="Prodigy @ Katong Point" address="451 Joo Chiat Road, Level 3" />` centred via `<div className="flex justify-center">`. Pull label + address from `KATONG_POINT_NAP` (Plan 05-01 export) — do NOT hardcode. Wrap entire chip in `<nav aria-label="Prodigy Singapore venues">` for a11y.

    **Step 4 — app/sg/opengraph-image.tsx (CREATE):**
    Read `app/hk/opengraph-image.tsx` lines 1-25. Mirror file verbatim with these substitutions:
    1. `createHKOgImage` → `createSGOgImage` (import + call).
    2. Title text: `"ProActiv Sports Hong Kong"` → `"Prodigy by ProActiv Sports Singapore"`.
    3. Tagline: `"Children's sports, camps, and parties at ProGym..."` → `"Kids' sports, camps, and parties at Katong Point — home of Singapore's only MultiBall wall."`
    4. Keep `export const runtime = "nodejs"; export const dynamic = "force-dynamic"; export const size = { width: 1200, height: 630 }; export const contentType = "image/png";` exports verbatim.

    After all 4 files: `pnpm typecheck`, `pnpm build` dry-run the opengraph-image route to confirm createSGOgImage is callable (the full build will run in Plan 05-03 integration; this plan just confirms imports resolve).
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `components/sg/venue-map.tsx` contains "loading=\"lazy\""
    - File `components/sg/venue-map.tsx` contains "PLACEHOLDER" (empty-state check)
    - File `components/sg/venue-map.tsx` contains "role=\"status\"" (a11y fallback)
    - File `components/sg/active-venue-chip.tsx` starts with "\"use client\""
    - File `components/sg/active-venue-chip.tsx` contains "usePathname"
    - File `components/sg/active-venue-chip.tsx` contains "bg-brand-navy"
    - File `components/sg/venue-chip-row.tsx` contains "ActiveVenueChip"
    - File `components/sg/venue-chip-row.tsx` contains "/katong-point/"
    - File `components/sg/venue-chip-row.tsx` contains "font-accent" (Baloo on "Prodigy")
    - File `app/sg/opengraph-image.tsx` contains "createSGOgImage"
    - File `app/sg/opengraph-image.tsx` contains "Prodigy by ProActiv Sports Singapore"
    - File `app/sg/opengraph-image.tsx` contains "Singapore's only MultiBall wall"
    - File `app/sg/opengraph-image.tsx` contains "runtime = \"nodejs\""
    - File `app/sg/opengraph-image.tsx` contains "size = { width: 1200, height: 630 }"
    - `grep -c "sg-placeholder" components/sg/*.tsx app/sg/**/*.tsx` returns 0
    - `pnpm typecheck` passes
    - `pnpm test:unit -- --run tests/no-sg-placeholder-leak.test.ts` passes
  </acceptance_criteria>
  <done>All chrome components plus default SG OG image live. SGNav + SGFooter + VenueMap + VenueChipRow ready for Wave-3 page consumption. Plan 05-03 (homepage) can import VenueChipRow directly; Plan 05-05 (Katong Point) can import VenueMap directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public browser → SG pages via middleware | Every SG request flows through `middleware.ts` (Phase 1 rewrite) into `app/sg/layout.tsx` which wraps every child page. Hostile request `host: sg.*` on an HK-intended path triggers cross-tree leak risk (Phase 1 D-16 invariant — already tested). |
| `app/sg/opengraph-image.tsx` → filesystem | Reads `app/fonts/bloc-bold.ttf` + `app/assets/logo-white.svg`. Both are trusted build-time assets; no user input reaches file paths. |
| Mobile Sheet client state | `SGNavMobile` holds open/close in React state; no server data persisted. Closing sheet on nav click prevents stuck-open state after transition. |
| Cross-subdomain `<a href={NEXT_PUBLIC_HK_URL}>` | User-clickable external link; `NEXT_PUBLIC_HK_URL` is build-time env var, not user-supplied. No XSS surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Information Disclosure | `components/sg/sg-footer.tsx` if `NEXT_PUBLIC_HK_URL` env var leaks a private preview URL into production | accept | Preview-URL exposure is expected in Phase 5 (build-on-previews posture per STATE.md); ROADMAP Phase 10 swaps to `hk.proactivsports.com` canonical URL. No user PII in env vars. |
| T-05-11 | Tampering | Malicious `embedSrc` passed to `<VenueMap>` (XSS via iframe src) | mitigate | `embedSrc` sourced ONLY from `lib/sg-data.ts` `KATONG_POINT_MAP_EMBED` (env-driven, not user-editable at runtime). Component does NOT accept user-supplied src — prop is internally constructed from `process.env.NEXT_PUBLIC_MAP_EMBED_KATONG_POINT`. |
| T-05-12 | Spoofing | Mobile Sheet click handler triggering unintended navigation while drawer animation completes | accept | Phase 4 tested; `onClick={close}` fires synchronously before `<Link>` navigation, no race. |
| T-05-13 | Denial of Service | `app/sg/opengraph-image.tsx` build-time font-load failure cascading | mitigate | Graceful `try/catch` on Bloc Bold + logo SVG reads (inherited from Plan 05-01 `createSGOgImage`). Failed read → system-ui fallback renders (visual regression, not build failure). |
| T-05-14 | Information Disclosure | Skip link focus state exposing `#main-content` anchor to DOM inspector | accept | Standard WCAG 2.2 AA skip-link pattern; intentionally discoverable for keyboard/screen-reader users. |
</threat_model>

<verification>
Post-task phase-level checks (run after both tasks complete):

1. `pnpm typecheck` — zero errors across all 8 touched files.
2. `pnpm test:unit -- --run tests/no-sg-placeholder-leak.test.ts` — green (no placeholder in new files).
3. `pnpm build` — full Next.js build succeeds; `app/sg/opengraph-image` route compiles; no SGNav/SGFooter dep errors.
4. Smoke via `pnpm dev` (Martin or executor): hit `http://sg.localhost:3000/` — SGNav renders with 6 items, red Book CTA visible top-right; SGFooter renders at bottom with single Katong NAP block; mobile viewport shows hamburger → Sheet opens.
5. `grep -c "createHKOgImage\|HKNav\|HKFooter" components/sg/ app/sg/layout.tsx app/sg/opengraph-image.tsx` returns 0 (no stale HK references). Exception: `components/hk/` itself is untouched.
6. File counts: `components/sg/` should contain exactly 6 `.tsx` files after Task 2 (sg-nav, sg-nav-mobile, sg-footer, venue-map, venue-chip-row, active-venue-chip).
</verification>

<success_criteria>
- Requirement SG-01 partially addressed: layout + nav + footer chrome ready (homepage content is Plan 05-03).
- `app/sg/layout.tsx` replaces the Phase 1 teal-stripe stub completely.
- Mobile Sheet drawer functional: top-sticky AND bottom-sticky red Book CTA per D-05 variant.
- SGFooter renders single Katong Point NAP (not 2-venue map) per D-08.
- Default SG opengraph-image calls `createSGOgImage` and produces Prodigy-green OG (`#0f9733`) per D-09.
- Placeholder-leak guard remains green. No `sg-placeholder` strings in any file touched.
- `/studio` (Phase 1 pass-through) continues to work — middleware unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/05-singapore-market/05-02-SUMMARY.md` with:
- Task 1 + Task 2 outputs
- Any SGNav D-03/D-04 slug reconciliation decisions (executor confirms consumption of Plan 05-01 slugs: movement, sports-multiball, climbing for zones; themed, multi-activity, gymnastics for camps)
- HUMAN-ACTION list pending (WhatsApp number config, HK/Root URL env vars, map embed, Mux playback ID — all optional at this plan since chrome renders without them)
- Link to 05-03-PLAN.md (homepage — Wave 3)
- Visual smoke-check notes if executor ran `pnpm dev` — SGNav + SGFooter visible at `http://sg.localhost:3000/`
</output>

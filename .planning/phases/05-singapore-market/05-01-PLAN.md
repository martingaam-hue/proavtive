---
phase: 05-singapore-market
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sg-data.ts
  - lib/og-image.tsx
  - app/api/contact/route.ts
  - .env.example
  - app/sg/page.test.ts
  - app/sg/katong-point/page.test.ts
  - app/sg/weekly-classes/pillar.test.ts
  - app/sg/weekly-classes/sports-multiball/page.test.ts
  - app/sg/prodigy-camps/pillar.test.ts
  - app/sg/school-partnerships/page.test.ts
  - app/sg/coaches/page.test.ts
  - app/sg/faq/page.test.ts
  - app/sg/book-a-trial/booking-form.test.ts
  - tests/no-sg-placeholder-leak.test.ts
autonomous: true
requirements: [SG-01, SG-02, SG-03, SG-04, SG-06, SG-08, SG-10, SG-11]
user_setup:
  - service: Resend (existing from Phase 3)
    why: "Booking form submissions routed to CONTACT_INBOX_SG — no new service, just SG inbox env var."
    env_vars:
      - name: CONTACT_INBOX_SG
        source: "User-owned SG inbox email (already documented Phase 3 .env.example)."
      - name: NEXT_PUBLIC_WHATSAPP_SG
        source: "Strategy PART 8.3: +6598076827. User confirms at execute-time."
      - name: NEXT_PUBLIC_HK_URL
        source: "Phase 4 HK preview URL (e.g. https://hk-<sha>.vercel.app)."
      - name: NEXT_PUBLIC_ROOT_URL
        source: "Phase 3 root preview URL."
      - name: NEXT_PUBLIC_MAP_EMBED_KATONG_POINT
        source: "Google Maps → 451 Joo Chiat Rd, Level 3 → Share → Embed a map → copy src URL."
      - name: NEXT_PUBLIC_MUX_SG_HERO_PLAYBACK_ID
        source: "Mux dashboard — Prodigy hero montage playback ID. Optional at Phase 5 (poster fallback)."

must_haves:
  truths:
    - "SG data module exports all data arrays, interfaces, and constants downstream plans consume"
    - "createSGOgImage utility produces Prodigy-green OG images distinct from HK navy"
    - "/api/contact accepts venue 'katong-point' without 400-rejecting SG booking submissions"
    - "Wave-0 test scaffolds exist in RED state, failing until downstream plans implement"
    - "grep 'sg-placeholder' in app/sg/** returns zero matches (guard test live)"
  artifacts:
    - path: "lib/sg-data.ts"
      provides: "KATONG_POINT, KATONG_POINT_NAP, KATONG_POINT_MAP_EMBED, SG_COACHES, SG_FAQ_ITEMS, SG_BLOG_POSTS_STUB, SG_ZONES, SG_CAMP_TYPES, IFS_PARTNERSHIP_COPY exports + interfaces"
      min_lines: 300
      contains: "export const SG_ZONES"
    - path: "lib/og-image.tsx"
      provides: "createSGOgImage appended to existing exports"
      contains: "export async function createSGOgImage"
    - path: "app/api/contact/route.ts"
      provides: "ALLOWED_VENUES extended with 'katong-point'"
      contains: "\"katong-point\""
    - path: "tests/no-sg-placeholder-leak.test.ts"
      provides: "Grep-regression guard against sg-placeholder leaking into SG page source"
      contains: "sg-placeholder"
  key_links:
    - from: "downstream plans 05-02..05-06"
      to: "lib/sg-data.ts"
      via: "import statements"
      pattern: "from \"@/lib/sg-data\""
    - from: "app/sg/opengraph-image.tsx + per-route OGs (future plans)"
      to: "lib/og-image.tsx"
      via: "createSGOgImage import"
      pattern: "createSGOgImage"
    - from: "app/sg/book-a-trial/booking-form.tsx (Plan 05-06)"
      to: "app/api/contact/route.ts"
      via: "POST with venue: 'katong-point'"
      pattern: "\"katong-point\""
---

<objective>
Establish the Phase 5 foundation layer: (a) `lib/sg-data.ts` static data module with all SG interfaces + data arrays downstream plans consume, (b) `createSGOgImage()` utility appended to `lib/og-image.tsx` (Prodigy-green background per D-09), (c) `/api/contact/route.ts` `ALLOWED_VENUES` extension to accept `"katong-point"` (single backend edit for Phase 5 per RESEARCH §Shared Patterns — Booking), (d) `.env.example` documentation of SG env vars, and (e) 10 Wave-0 Vitest test scaffolds in RED state that downstream plans will make GREEN. Per CONTEXT D-07, D-08, D-10, D-11 + UI-SPEC §5.8.

Purpose: Every subsequent plan imports from `lib/sg-data.ts` (data/types), depends on the `ALLOWED_VENUES` change (booking form) and on `createSGOgImage` (OG routes). Ship this first so Waves 2–3 can run without data-layer churn.

Output: `lib/sg-data.ts` (~320 lines with all 8 interfaces + 8 data constants), `lib/og-image.tsx` extended additively, `app/api/contact/route.ts` one-line `ALLOWED_VENUES` edit, `.env.example` SG block, 10 Wave-0 `.test.ts` files totalling ~200 lines of RED-state scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-singapore-market/05-CONTEXT.md
@.planning/phases/05-singapore-market/05-RESEARCH.md
@.planning/phases/05-singapore-market/05-PATTERNS.md
@.planning/phases/05-singapore-market/05-UI-SPEC.md
@.planning/phases/05-singapore-market/05-VALIDATION.md
@lib/hk-data.ts
@lib/og-image.tsx
@app/api/contact/route.ts
@.planning/inputs/strategy.md

<interfaces>
<!-- Key contracts extracted from the existing HK analog (lib/hk-data.ts, ~511 lines) and existing og-image utility. -->
<!-- Executor uses these shapes verbatim, swapping HK identifiers for SG. -->

From lib/hk-data.ts (analog, do NOT modify):
- `HKVenue` interface (id, nameShort, nameFull, addressStreet, addressLocality, addressRegion, addressCountry, geo, hours[], phoneEnvVar, mapEmbedEnvVar, heroImage, serviceArea[], apparatus[])
- `HK_VENUES` array (2 venues: wan-chai, cyberport)
- `HK_COACHES`, `HK_FAQ_ITEMS`, `HK_BLOG_POSTS_STUB`, `HK_GYMNASTICS_PROGRAMMES` static arrays
- Pattern: discriminated-union literal id types (`id: "wan-chai" | "cyberport"`)
- Pattern: `readonly` on array exports; `as const` on string-literal collections

From lib/og-image.tsx (existing):
- `export async function createRootOgImage({ title, tagline }: OgImageOptions): Promise<ImageResponse>`
- `export async function createHKOgImage({ title, tagline }: OgImageOptions): Promise<ImageResponse>` — lines 131-243 of analog
- `OgImageOptions` type already defined
- Font-fallback pattern: try/catch read of `app/fonts/bloc-bold.ttf` + `app/assets/logo-white.svg`
- Return shape: `new ImageResponse(<JSX>, { width: 1200, height: 630, fonts: [...] })`

From app/api/contact/route.ts (existing):
- Line 30: `const ALLOWED_VENUES = ["wan-chai", "cyberport", "no-preference"] as const;`
- Line 31: `type AllowedVenue = (typeof ALLOWED_VENUES)[number];`
- Accepts `market: "hk" | "sg"` since Phase 3 (CONTACT_INBOX_SG already routed)

From tests/setup.ts + vitest.config.ts (existing):
- Vitest 4.1.5 with jsdom environment
- React Testing Library available
- `pnpm test:unit` runs the full suite in ~15s
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Wave-0 test scaffolds (RED state) + no-placeholder-leak guard</name>
  <files>app/sg/page.test.ts, app/sg/katong-point/page.test.ts, app/sg/weekly-classes/pillar.test.ts, app/sg/weekly-classes/sports-multiball/page.test.ts, app/sg/prodigy-camps/pillar.test.ts, app/sg/school-partnerships/page.test.ts, app/sg/coaches/page.test.ts, app/sg/faq/page.test.ts, app/sg/book-a-trial/booking-form.test.ts, tests/no-sg-placeholder-leak.test.ts</files>
  <read_first>
    - .planning/phases/05-singapore-market/05-VALIDATION.md (Wave 0 requirements, lines 54-67 — authoritative list of test files + what each asserts)
    - app/hk/page.test.tsx (HK analog — regression shape, RTL render + screen.getByRole assertions)
    - .planning/phases/05-singapore-market/05-RESEARCH.md §Shared Patterns — Photography HUMAN-ACTION Gates (why the placeholder-leak grep test matters)
    - .planning/phases/05-singapore-market/05-UI-SPEC.md §3.1 (hero H1 verbatim), §3.12 (FAQ copy), §5.1 (SGNav structure — to know what nav assertions the homepage test needs)
    - tests/setup.ts (existing Vitest setup — understand available imports and matchers)
  </read_first>
  <behavior>
    - All 10 test files exist and fail immediately with clear "module not found" or "element not found" errors — RED state.
    - `app/sg/page.test.ts` asserts: renders H1 "Where Singapore's kids come to move, play, and grow."; Katong Point venue chip link to `/katong-point/`; "Singapore's only MultiBall wall" trust-line text; primary CTA link to `/book-a-trial/`.
    - `app/sg/katong-point/page.test.ts` asserts: renders address "451 Joo Chiat Road, Level 3"; Google Maps iframe present (`<iframe title=...>`); "Singapore's only MultiBall wall" mention.
    - `app/sg/weekly-classes/pillar.test.ts` asserts: pillar page renders `<nav aria-label="Prodigy zones">` with 3 links matching hrefs `/weekly-classes/movement/`, `/weekly-classes/sports-multiball/`, `/weekly-classes/climbing/`.
    - `app/sg/weekly-classes/sports-multiball/page.test.ts` asserts: H1 contains "Sports" and "MultiBall"; Badge "Singapore's only" present.
    - `app/sg/prodigy-camps/pillar.test.ts` asserts: 3 camp-type links matching `/prodigy-camps/themed/`, `/prodigy-camps/multi-activity/`, `/prodigy-camps/gymnastics/`.
    - `app/sg/school-partnerships/page.test.ts` asserts: text "International French School" OR "IFS" present.
    - `app/sg/coaches/page.test.ts` asserts: three coach names "Haikel", "Mark", "Coach King" all present.
    - `app/sg/faq/page.test.ts` asserts: at least 10 FAQ questions rendered; JSON-LD `<script type="application/ld+json">` with `FAQPage` @type.
    - `app/sg/book-a-trial/booking-form.test.ts` asserts: form submits with `market: "sg"` and `venue: "katong-point"` hardcoded; `?subject=birthday-party` pre-fills the subject input. Uses `vi.fn()` for fetch mock.
    - `tests/no-sg-placeholder-leak.test.ts` asserts: recursive file scan of `app/sg/**/*.{ts,tsx}` contains zero occurrences of the string `sg-placeholder`. Implementation: `import { readdirSync, readFileSync, statSync } from "node:fs"; import { join } from "node:path";` — walk `app/sg`, read every `.ts`/`.tsx`, assert `content.includes("sg-placeholder") === false`.
  </behavior>
  <action>
    Create 10 test files using the HK `app/hk/page.test.tsx` file shape (describe/it/expect with RTL `render(<Component />)` + `screen.getByRole`/`screen.getByText`). These files will fail at module-import time until downstream plans (05-02..05-06) create the page source files — that RED state is intentional and is what Wave-0 scaffolds means. Each file uses strict assertions that encode the verbatim copy from strategy PART 6C + UI-SPEC §3 so the executor cannot ship a paraphrased stub and pass the test.

    For `tests/no-sg-placeholder-leak.test.ts` (the critical Pitfall 3 guard per RESEARCH §Shared Patterns — Photography): implement as a pure Node fs walk of `app/sg/` (skip if directory does not exist yet — empty app/sg/ passes trivially; once pages are created the guard activates). The test must NOT require any module imports beyond `node:fs` and `node:path` so it runs fast and never depends on SG page source being valid TSX.

    Per D-07 photography policy: the phrase `sg-placeholder` must NEVER appear in `app/sg/**` source (it may appear in `lib/sg-data.ts` comments documenting the Phase 2 D-07 precedent, but NOT in page source). The guard test enforces this at commit time.

    Use `vi.mock("next/navigation", () => ({ useSearchParams: () => new URLSearchParams("subject=birthday-party") }))` for the booking-form subject pre-fill test.
  </action>
  <verify>
    <automated>pnpm test:unit 2>&amp;1 | grep -E "(no-sg-placeholder-leak|sg/.+test)" || echo "Tests registered in Vitest runner"</automated>
  </verify>
  <acceptance_criteria>
    - File `app/sg/page.test.ts` contains string "Where Singapore's kids come to move, play, and grow."
    - File `app/sg/page.test.ts` contains string "/book-a-trial/"
    - File `app/sg/katong-point/page.test.ts` contains string "451 Joo Chiat Road"
    - File `app/sg/weekly-classes/pillar.test.ts` contains string "/weekly-classes/movement/"
    - File `app/sg/weekly-classes/pillar.test.ts` contains string "/weekly-classes/sports-multiball/"
    - File `app/sg/weekly-classes/sports-multiball/page.test.ts` contains string "Singapore's only"
    - File `app/sg/prodigy-camps/pillar.test.ts` contains string "/prodigy-camps/themed/"
    - File `app/sg/school-partnerships/page.test.ts` contains "International French School" OR "IFS"
    - File `app/sg/coaches/page.test.ts` contains "Haikel" AND "Mark" AND "Coach King"
    - File `app/sg/faq/page.test.ts` contains "FAQPage"
    - File `app/sg/book-a-trial/booking-form.test.ts` contains "market" AND "\"sg\"" AND "katong-point"
    - File `app/sg/book-a-trial/booking-form.test.ts` contains "subject=birthday-party"
    - File `tests/no-sg-placeholder-leak.test.ts` contains "sg-placeholder"
    - File `tests/no-sg-placeholder-leak.test.ts` uses `node:fs` imports (walks the filesystem, no fragile module imports)
    - Running `pnpm test:unit` surfaces all 10 new test files in the Vitest listing (may be failing — that is expected Wave-0 RED state)
    - The placeholder-leak test passes currently (no app/sg/ pages exist yet) — confirms guard is live
  </acceptance_criteria>
  <done>All 10 Wave-0 test files created and registered in Vitest. Placeholder-leak guard passes (zero matches). Other 9 tests are RED and explicitly waiting for Waves 2-3 to produce page source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Foundation — lib/sg-data.ts + createSGOgImage + ALLOWED_VENUES extension + .env.example</name>
  <files>lib/sg-data.ts, lib/og-image.tsx, app/api/contact/route.ts, .env.example</files>
  <read_first>
    - lib/hk-data.ts (exact structural analog — mirror interface patterns, discriminated-union id literals, readonly arrays)
    - lib/og-image.tsx (analog `createHKOgImage` at lines 131-243 — copy line-for-line changing ONLY background color, superscript color, and superscript text)
    - app/api/contact/route.ts (ALLOWED_VENUES is at line 30 — ONE additive entry)
    - .planning/phases/05-singapore-market/05-UI-SPEC.md §5.8 (data module shape specification) + §7 (OG template rules)
    - .planning/phases/05-singapore-market/05-PATTERNS.md lines 640-760 (lib/sg-data.ts pattern with KATONG_POINT, SG_COACHES, SG_ZONES, SG_CAMP_TYPES exact shapes)
    - .planning/phases/05-singapore-market/05-PATTERNS.md lines 540-612 (createSGOgImage extension pattern with exact color values)
    - .planning/inputs/strategy.md §PART 6C (verbatim source for coach bios, FAQ questions/answers, zone descriptions, camp types, IFS copy)
    - .planning/inputs/strategy.md §PART 8 (NAP + phone number: +6598076827)
  </read_first>
  <behavior>
    - `lib/sg-data.ts` exports: `KatongPointVenue` interface, `KATONG_POINT` const, `KATONG_POINT_NAP` const (subset used by SGFooter), `KATONG_POINT_MAP_EMBED` const (reads `NEXT_PUBLIC_MAP_EMBED_KATONG_POINT` with "PLACEHOLDER_KATONG_EMBED" fallback), `SGCoach` interface + `SG_COACHES` array (3 entries: Haikel, Mark, Coach King — bios verbatim from strategy PART 6C §8), `SGFAQItem` interface + `SG_FAQ_ITEMS` array (10 entries verbatim from strategy PART 6C §11, grouped: about/venue/classes/camps/multiball/parties/schools), `SGBlogPost` interface + `SG_BLOG_POSTS_STUB` array (1 stub: "What makes MultiBall different — interactive sport for kids in Singapore"), `SGZone` interface + `SG_ZONES` array (3 entries: movement / sports-multiball / climbing), `SGCampType` interface + `SG_CAMP_TYPES` array (3 entries: themed / multi-activity / gymnastics), `IFS_PARTNERSHIP_COPY` string (verbatim from strategy PART 6C §3 partnership paragraph).
    - Slug reconciliation (D-03 + D-04 authoritative per UI-SPEC §5.1 note): zones `movement`, `sports-multiball`, `climbing`; camps `themed`, `multi-activity`, `gymnastics`. Hrefs formatted as `/weekly-classes/{slug}/` and `/prodigy-camps/{slug}/`.
    - `createSGOgImage({ title, tagline })` appended to `lib/og-image.tsx` — EXACT line-for-line mirror of `createHKOgImage` with THREE changes only: (1) `backgroundColor: '#0f9733'` instead of `'#0f206c'`, (2) superscript `color: '#fff3dd'` (brand-cream) instead of `'#fac049'`, (3) superscript text `'Prodigy by ProActiv Sports Singapore'` instead of `'ProActiv Sports Hong Kong'`.
    - `app/api/contact/route.ts` line 30: add `"katong-point"` to `ALLOWED_VENUES` array.
    - `.env.example` appends SG block: `CONTACT_INBOX_SG`, `NEXT_PUBLIC_WHATSAPP_SG`, `NEXT_PUBLIC_MUX_SG_HERO_PLAYBACK_ID`, `NEXT_PUBLIC_MAP_EMBED_KATONG_POINT`, `NEXT_PUBLIC_HK_URL`, `NEXT_PUBLIC_ROOT_URL` with comment explaining each.
    - `pnpm test:unit` passes the placeholder-leak guard (no placeholder string in sg-data.ts page-source consumers; sg-data.ts itself may contain the word in a comment — guard is scoped to `app/sg/`).
  </behavior>
  <action>
    **Step 1 — lib/sg-data.ts creation (per D-07 + D-08 + D-11 + UI-SPEC §5.8):**
    Read `lib/hk-data.ts` end-to-end. Mirror every interface pattern (discriminated-union id literals, `readonly` modifiers, `as const` on literal collections). Build the 8 interfaces and 8 data constants enumerated above. For `SG_FAQ_ITEMS`, pull 10 Q&A verbatim from strategy §PART 6C §11 — do NOT paraphrase; cite anchor in comment. For `SG_COACHES`, use the 3 names per D-07 gate 3; bios from strategy §PART 6C §8. Portraits reference `/photography/coach-{haikel,mark,king}-portrait.webp` (files will NOT exist at execute time — D-07 HUMAN-ACTION gate; comment them with `// HUMAN-ACTION D-07 gate 3`).

    For `KATONG_POINT`: id literal `"katong-point"`, addressStreet `"451 Joo Chiat Road, Level 3"`, addressLocality `"Katong"`, addressRegion `"Singapore"`, addressCountry `"SG"`, geo `{ lat: 1.3113, lng: 103.9011 }` (comment `// HUMAN-ACTION A5 — verify pre-Phase-7`), hours per RESEARCH §lib/sg-data.ts Pattern 2 (weekday 09:00–19:00, weekend 09:00–17:00, commented `// HUMAN-ACTION A4 — confirm`), phoneEnvVar `"NEXT_PUBLIC_WHATSAPP_SG"`, mapEmbedEnvVar `"NEXT_PUBLIC_MAP_EMBED_KATONG_POINT"`, heroImage `"/photography/sg-venue-katong-hero.webp"` (HUMAN-ACTION D-07 gate 1), serviceArea `["Katong","Marine Parade","East Coast","Joo Chiat","Kembangan","Tanjong Katong"]`, apparatus `["MultiBall wall","Climbing wall","Movement floor","Sports court"]`.

    `KATONG_POINT_NAP` is a derived subset: `{ nameFull, addressStreet, addressLocality, addressCountry }` for SGFooter consumption.

    `KATONG_POINT_MAP_EMBED`: `process.env.NEXT_PUBLIC_MAP_EMBED_KATONG_POINT ?? "PLACEHOLDER_KATONG_EMBED"` (the string "PLACEHOLDER" is the VenueMap component's trigger to render fallback state — see Phase 4 pattern).

    For `SG_ZONES`:
    ```typescript
    export const SG_ZONES: readonly SGZone[] = [
      { slug: "movement", href: "/weekly-classes/movement/", label: "Movement Zone", ageBand: "Ages 2–5", metaTitle: "...", metaDescription: "...", h1: "Movement Zone — Ages 2–5 at Prodigy Singapore.", whatTheyLearn: [...], classStructure: "..." },
      { slug: "sports-multiball", href: "/weekly-classes/sports-multiball/", label: "Sports + MultiBall", ageBand: "Ages 5–12", tag: "Singapore's only", metaTitle: "...", metaDescription: "...", h1: "Sports + MultiBall Zone — with Singapore's Only Wall.", whatTheyLearn: [...], classStructure: "..." },
      { slug: "climbing", href: "/weekly-classes/climbing/", label: "Climbing", ageBand: "All ages", metaTitle: "...", metaDescription: "...", h1: "Climbing Zone — Rock + Boulder for All Ages.", whatTheyLearn: [...], classStructure: "..." },
    ] as const;
    ```
    Copy (metaTitle/metaDescription/whatTheyLearn/classStructure) sourced verbatim from strategy §PART 6C §4 per-zone blocks.

    For `SG_CAMP_TYPES`: mirror shape with 3 entries (themed with tag `"Ninja · Pokémon · Superhero · LEGO · STEAM"` per UI-SPEC §5.1 dropdown copy, multi-activity, gymnastics). Copy from strategy §PART 6C §6.

    `IFS_PARTNERSHIP_COPY`: verbatim paragraph from strategy §PART 6C §3 partnership section (per D-11).

    **Step 2 — lib/og-image.tsx additive extension (per D-09):**
    Open `lib/og-image.tsx`. Do NOT modify `createRootOgImage` or `createHKOgImage`. Append `export async function createSGOgImage({ title, tagline }: OgImageOptions): Promise<ImageResponse>` at the bottom. Copy the ENTIRE function body of `createHKOgImage` and change exactly three things:
    - Outer `<div>` `backgroundColor: '#0f206c'` → `'#0f9733'` (Prodigy-green per D-09)
    - Superscript `<div>` `color: '#fac049'` → `'#fff3dd'` (brand-cream — yellow clashes on green per PATTERNS.md line 544)
    - Superscript text `'ProActiv Sports Hong Kong'` → `'Prodigy by ProActiv Sports Singapore'`
    Keep Bloc Bold font-load try/catch, logo-SVG try/catch, brand-rainbow bottom strip — all identical to HK. Verify contrast: white title on `#0f9733` = 3.82:1 (passes AA large-text per UI-SPEC §8.1).

    **Step 3 — app/api/contact/route.ts one-line edit (per D-10):**
    Line 30: `const ALLOWED_VENUES = ["wan-chai", "cyberport", "no-preference"] as const;`
    Change to: `const ALLOWED_VENUES = ["wan-chai", "cyberport", "no-preference", "katong-point"] as const;`
    Add inline comment: `// Phase 5 / Plan 05-01 — added "katong-point" per D-10 (single SG venue; BookingForm submits with venue hardcoded).`
    Do NOT touch any other lines in the file.

    **Step 4 — .env.example SG block append:**
    Append the following block to `.env.example` (after any existing HK block):
    ```
    # --- Singapore market (Phase 5) ---
    # Resend inbox for SG booking/contact submissions (already wired in /api/contact since Phase 3).
    CONTACT_INBOX_SG=
    # WhatsApp number for SG CTA chip (per strategy PART 8.3). Format: E.164 without +, e.g. 6598076827.
    NEXT_PUBLIC_WHATSAPP_SG=
    # Mux playback ID for SG hero montage. Leave empty to fall back to poster image (D-07).
    NEXT_PUBLIC_MUX_SG_HERO_PLAYBACK_ID=
    # Google Maps embed URL for 451 Joo Chiat Road, Level 3 (Katong Point). Generated at execute-time by user per D-07.
    NEXT_PUBLIC_MAP_EMBED_KATONG_POINT=
    # Cross-market links (rendered as absolute <a href>, NEVER <Link> — cross-subdomain).
    NEXT_PUBLIC_HK_URL=
    NEXT_PUBLIC_ROOT_URL=
    ```

    **Step 5 — run typecheck + test suite:**
    `pnpm typecheck` (must pass — all new exports typed), `pnpm test:unit` (placeholder-leak guard passes; 9 other SG tests still RED waiting for Waves 2-3).
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm test:unit -- --run tests/no-sg-placeholder-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/sg-data.ts` contains "export const SG_ZONES"
    - File `lib/sg-data.ts` contains "export const SG_CAMP_TYPES"
    - File `lib/sg-data.ts` contains "export const KATONG_POINT"
    - File `lib/sg-data.ts` contains "export const KATONG_POINT_NAP"
    - File `lib/sg-data.ts` contains "export const SG_COACHES"
    - File `lib/sg-data.ts` contains "export const SG_FAQ_ITEMS"
    - File `lib/sg-data.ts` contains "export const SG_BLOG_POSTS_STUB"
    - File `lib/sg-data.ts` contains "export const IFS_PARTNERSHIP_COPY"
    - File `lib/sg-data.ts` contains "Haikel" AND "Mark" AND "Coach King"
    - File `lib/sg-data.ts` contains "451 Joo Chiat Road"
    - File `lib/sg-data.ts` contains "/weekly-classes/movement/" AND "/weekly-classes/sports-multiball/" AND "/weekly-classes/climbing/"
    - File `lib/sg-data.ts` contains "/prodigy-camps/themed/" AND "/prodigy-camps/multi-activity/" AND "/prodigy-camps/gymnastics/"
    - File `lib/og-image.tsx` contains "export async function createSGOgImage"
    - File `lib/og-image.tsx` contains "#0f9733" (Prodigy-green background)
    - File `lib/og-image.tsx` contains "Prodigy by ProActiv Sports Singapore"
    - File `lib/og-image.tsx` still contains "createHKOgImage" and "createRootOgImage" (additive-only: prior exports untouched)
    - File `app/api/contact/route.ts` contains "\"katong-point\"" inside `ALLOWED_VENUES`
    - File `.env.example` contains "NEXT_PUBLIC_WHATSAPP_SG"
    - File `.env.example` contains "NEXT_PUBLIC_MAP_EMBED_KATONG_POINT"
    - File `.env.example` contains "NEXT_PUBLIC_MUX_SG_HERO_PLAYBACK_ID"
    - `pnpm typecheck` passes with zero errors
    - `pnpm test:unit -- --run tests/no-sg-placeholder-leak.test.ts` passes
    - `grep -c "sg-placeholder" app/sg/` returns 0 (app/sg currently empty of new pages; guard still active)
  </acceptance_criteria>
  <done>Foundation layer ready: lib/sg-data.ts exports all 8 data constants + 8 interfaces; createSGOgImage appended additively; ALLOWED_VENUES accepts katong-point; .env.example documents SG vars; typecheck + placeholder-leak guard green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public browser → /api/contact handler | Untrusted JSON payload (venue + market fields) crosses into server-side Resend-dispatch logic; `ALLOWED_VENUES` enum is the whitelist enforcement point. |
| Public-tree fs scan (no-sg-placeholder-leak test) | Node fs reads every `app/sg/**/*.{ts,tsx}` string content; test runs in dev only, not in production runtime — boundary is dev-CI, not public. |
| Environment variables crossing process boundary | `NEXT_PUBLIC_*` vars ship to client bundle (SG WhatsApp number + preview URLs). Server-only vars (`CONTACT_INBOX_SG`) never ship to client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `/api/contact` venue field (now `katong-point` valid) | mitigate | `ALLOWED_VENUES` whitelist (enum) rejects any non-listed venue at route handler with 400. One-line change is purely additive — HK venue values retained for backward compat (Plan 04-07 is still in production). |
| T-05-02 | Information Disclosure | `sg-placeholder-climbing-unsplash-trinks.*` leaking from Phase 2 `/_design/` gallery into production SG page code | mitigate | `tests/no-sg-placeholder-leak.test.ts` grep guard runs in CI; any SG page source string `sg-placeholder` fails the test. Enforces D-07 policy verbatim. |
| T-05-03 | Tampering | `NEXT_PUBLIC_*` client env vars (SG WhatsApp, HK/Root URLs, Mux ID) | accept | Documented as user-provided; no PII; public-by-design. Mux playback IDs are intentionally public. WhatsApp number is marketing-public per strategy PART 8.3. |
| T-05-04 | Denial of Service | `lib/og-image.tsx` font-load I/O (Bloc Bold TTF, logo SVG) | accept | Graceful try/catch fallback already in Phase 3 pattern; failed font-load renders system-ui (visual regression, not crash). No new attack surface introduced by SG extension. |
| T-05-05 | Spoofing | Email-header injection via `subject` field now used by SG CTAs with `?subject=` pre-fill | mitigate | Route handler sanitizes `subject` (≤100 chars, newlines stripped per UI-SPEC §6 + RESEARCH §Security). Plan 05-06 BookingForm passes user-editable subject through same handler — same mitigation applies. This plan adds the `"katong-point"` venue; subject sanitization is a Plan 05-06 concern, logged here for traceability. |
</threat_model>

<verification>
Post-task phase-level checks (run after both tasks complete):

1. `pnpm typecheck` — zero TS errors. SG-typed exports resolve cleanly. `ALLOWED_VENUES` literal-type union includes `"katong-point"`.
2. `pnpm test:unit -- --run tests/no-sg-placeholder-leak.test.ts` — green.
3. `pnpm test:unit` — 10 new Vitest entries registered in output; 9 SG page tests RED (expected Wave-0 state); placeholder-leak green.
4. `grep -c "createSGOgImage" lib/og-image.tsx` → 1 (added); `grep -c "createHKOgImage" lib/og-image.tsx` → at least 1 (preserved); `grep -c "createRootOgImage" lib/og-image.tsx` → at least 1 (preserved).
5. `grep -c "export const" lib/sg-data.ts` ≥ 8 (8 major data exports).
6. `node -e "console.log(require('./app/api/contact/route.ts').ALLOWED_VENUES)"` — not a clean Node import (it's Next route), instead: `grep 'katong-point' app/api/contact/route.ts` → 1 match inside ALLOWED_VENUES.
</verification>

<success_criteria>
- All frontmatter `requirements` IDs (SG-01 SG-02 SG-03 SG-04 SG-06 SG-08 SG-10 SG-11) have downstream plan dependencies established via lib/sg-data.ts imports.
- Downstream plans (05-02, 05-03, 05-04, 05-05, 05-06) can import from `@/lib/sg-data` without ambiguity.
- Wave-0 placeholder-leak guard is live and passing. Other 9 Wave-0 tests are RED (pending downstream plans), but registered.
- `/api/contact` accepts `venue: "katong-point"` (per D-10). No other backend changes needed in Phase 5.
- `createSGOgImage` ready for Plan 05-02 (default OG) + per-route OGs in Plans 05-03..05-06.
- Zero raw `sg-placeholder` string in any `app/sg/` source (currently empty of pages — guard active for future plans).
</success_criteria>

<output>
After completion, create `.planning/phases/05-singapore-market/05-01-SUMMARY.md` with:
- Task 1 + Task 2 outputs
- HUMAN-ACTION list surfaced for execute-time: NEXT_PUBLIC_WHATSAPP_SG, NEXT_PUBLIC_MAP_EMBED_KATONG_POINT, NEXT_PUBLIC_HK_URL, NEXT_PUBLIC_ROOT_URL, CONTACT_INBOX_SG, NEXT_PUBLIC_MUX_SG_HERO_PLAYBACK_ID (optional)
- Test status report: placeholder-leak green; 9 SG page tests RED pending Waves 2-3
- Any deviations from plan (Rule 1 adaptations)
- Link to 05-02-PLAN.md (next plan in wave)
</output>

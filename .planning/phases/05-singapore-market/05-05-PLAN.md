---
phase: 05-singapore-market
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - app/sg/katong-point/page.tsx
  - app/sg/katong-point/opengraph-image.tsx
  - app/sg/birthday-parties/page.tsx
  - app/sg/birthday-parties/opengraph-image.tsx
  - app/sg/school-partnerships/page.tsx
  - app/sg/events/page.tsx
  - app/sg/coaches/page.tsx
  - app/sg/coaches/opengraph-image.tsx
  - app/sg/blog/page.tsx
  - app/sg/blog/opengraph-image.tsx
  - app/sg/faq/page.tsx
autonomous: true
requirements: [SG-02, SG-05, SG-06, SG-07, SG-08, SG-09, SG-10]

must_haves:
  truths:
    - "Katong Point location page at /katong-point/ renders verbatim NAP (451 Joo Chiat Road, Level 3), lazy Google Maps iframe, opening hours, apparatus badges, MultiBall headline badge, SportsActivityLocation + BreadcrumbList JSON-LD"
    - "Coaches page at /coaches/ renders 3 coach cards (Haikel, Mark, Coach King) with portrait Avatars + Coach Person JSON-LD"
    - "School Partnerships page at /school-partnerships/ surfaces IFS inline as featured-partner block per D-11 (no separate /international-french-school/ route)"
    - "Events page at /events/ ships as evergreen editorial with empty-state pattern (Phase 6 CMS adds dated Event schema)"
    - "Birthday Parties page at /birthday-parties/ includes MultiBall-access callout + Send-an-Enquiry CTA with subject pre-fill"
    - "Blog page at /blog/ handles 0-1 stub posts gracefully with empty state; NEVER uses sg-placeholder-* as imageUrl (Pitfall 9)"
    - "FAQ page at /faq/ renders 10 Q&A grouped by category with FAQPage JSON-LD char-for-char DOM match"
  artifacts:
    - path: "app/sg/katong-point/page.tsx"
      provides: "Katong Point location page with NAP, map iframe, hours, SportsActivityLocation JSON-LD, MultiBall headline"
      min_lines: 200
      contains: "451 Joo Chiat Road"
    - path: "app/sg/katong-point/opengraph-image.tsx"
      provides: "Per-route OG for Katong Point"
      contains: "createSGOgImage"
    - path: "app/sg/coaches/page.tsx"
      provides: "3 coach bios with Person JSON-LD"
      contains: "Haikel"
    - path: "app/sg/school-partnerships/page.tsx"
      provides: "School partnerships hub with IFS inline featured block (D-11)"
      contains: "International French School"
    - path: "app/sg/events/page.tsx"
      provides: "Evergreen events page with empty-state pattern"
      contains: "Events"
    - path: "app/sg/birthday-parties/page.tsx"
      provides: "Parties hub with MultiBall access + enquiry CTA"
      contains: "/book-a-trial/?subject=birthday-party"
    - path: "app/sg/blog/page.tsx"
      provides: "Blog hub with 0-1 stub or empty-state"
      contains: "SG_BLOG_POSTS_STUB"
    - path: "app/sg/faq/page.tsx"
      provides: "Full FAQ hub with FAQPage JSON-LD, 10 grouped Q&A"
      contains: "FAQPage"
  key_links:
    - from: "app/sg/katong-point/page.tsx"
      to: "components/sg/venue-map.tsx (Plan 05-02)"
      via: "import + render with KATONG_POINT_MAP_EMBED"
      pattern: "VenueMap"
    - from: "app/sg/coaches/page.tsx"
      to: "lib/sg-data.ts SG_COACHES"
      via: "import + map"
      pattern: "SG_COACHES"
    - from: "app/sg/school-partnerships/page.tsx"
      to: "lib/sg-data.ts IFS_PARTNERSHIP_COPY"
      via: "import + inline render"
      pattern: "IFS_PARTNERSHIP_COPY"
    - from: "app/sg/faq/page.tsx"
      to: "visible DOM FAQ questions"
      via: "same filtered SG_FAQ_ITEMS drives JSON-LD + DOM"
      pattern: "SG_FAQ_ITEMS"
---

<objective>
Ship the 7 SG supporting pages: Katong Point location (SG-02), Birthday Parties (SG-05), School Partnerships with IFS inline (SG-06), Events (SG-07), Coaches (SG-08), Blog (SG-09), FAQ (SG-10). Per UI-SPEC §4 supporting page rows + §5 component consumption + strategy PART 6C copy.

Covers Pattern 11 MultiBall placements #5 (FAQ entry from SG_FAQ_ITEMS group "multiball") and #6 (Katong Point hero headline "Singapore's only MultiBall wall"). Together with Plan 05-03 (hero + why + three-zones = 3 placements) and Plan 05-04 (sports-multiball spotlight = 1 placement), completes all 6 Pattern 11 placements.

Output: 11 files — 7 page.tsx + 4 per-route opengraph-image.tsx (Katong Point, Birthday Parties, Coaches, Blog — high-priority per UI-SPEC §7). Events, School Partnerships, FAQ inherit default OG from Plan 05-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-singapore-market/05-CONTEXT.md
@.planning/phases/05-singapore-market/05-PATTERNS.md
@.planning/phases/05-singapore-market/05-UI-SPEC.md
@.planning/phases/05-singapore-market/05-RESEARCH.md
@.planning/phases/05-singapore-market/05-01-SUMMARY.md
@.planning/phases/05-singapore-market/05-02-SUMMARY.md
@app/hk/wan-chai/page.tsx
@app/hk/birthday-parties/page.tsx
@app/hk/school-partnerships/page.tsx
@app/hk/competitions-events/page.tsx
@app/hk/coaches/page.tsx
@app/hk/blog/page.tsx
@app/hk/faq/page.tsx
@lib/sg-data.ts
@lib/og-image.tsx
@components/sg/venue-map.tsx
@.planning/inputs/strategy.md

<interfaces>
From lib/sg-data.ts (Plan 05-01):
- KATONG_POINT (full venue shape: addressStreet, addressLocality, addressRegion, addressCountry, geo{lat,lng}, hours[], phoneEnvVar, mapEmbedEnvVar, heroImage, serviceArea[], apparatus[])
- KATONG_POINT_NAP (subset for header/chip)
- KATONG_POINT_MAP_EMBED (string — reads env with PLACEHOLDER fallback)
- SG_COACHES: readonly SGCoach[] — 3 entries { name, role, bio, portrait }
- SG_FAQ_ITEMS: readonly SGFAQItem[] — 10 items, groups: about|venue|classes|camps|multiball|parties|schools
- SG_BLOG_POSTS_STUB: readonly SGBlogPost[] — 0-1 entries
- IFS_PARTNERSHIP_COPY: string — verbatim PART 6C §3 paragraph

From components/sg/venue-map.tsx (Plan 05-02):
- `<VenueMap embedSrc={string} title={string} className?={string} />` — renders iframe or fallback

From components/ui/ (Phase 2):
- Section, ContainerEditorial, Button, Card, Badge, Avatar, FAQItem, LogoWall, Separator

From lib/og-image.tsx (Plan 05-01):
- createSGOgImage
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Katong Point location page + Coaches page (both high-SEO-priority; both with JSON-LD)</name>
  <files>app/sg/katong-point/page.tsx, app/sg/katong-point/opengraph-image.tsx, app/sg/coaches/page.tsx, app/sg/coaches/opengraph-image.tsx</files>
  <read_first>
    - app/hk/wan-chai/page.tsx (analog for katong-point — location page with NAP + map + hours + LocalBusiness JSON-LD + BreadcrumbList)
    - app/hk/coaches/page.tsx (analog for coaches — ~150 lines with Person JSON-LD schema)
    - .planning/phases/05-singapore-market/05-PATTERNS.md lines 879-949 (katong-point page pattern — SportsActivityLocation JSON-LD), lines 1042-1068 (coaches page pattern)
    - .planning/phases/05-singapore-market/05-UI-SPEC.md §4 /katong-point/ + /coaches/ rows, §3.9 (homepage coaches section — reuse for /coaches/)
    - .planning/phases/05-singapore-market/05-CONTEXT.md D-07 (coach portraits HUMAN-ACTION gate 3)
    - .planning/inputs/strategy.md §PART 6C §8 (coach bios verbatim for Haikel, Mark, Coach King), §PART 9.4 (LocalBusiness skeleton), §PART 8 (NAP + transit copy)
    - lib/sg-data.ts (Plan 05-01: KATONG_POINT, KATONG_POINT_NAP, KATONG_POINT_MAP_EMBED, SG_COACHES, SG_FAQ_ITEMS group "venue")
    - components/sg/venue-map.tsx (Plan 05-02)
    - app/sg/katong-point/page.test.ts (Plan 05-01 Wave-0)
    - app/sg/coaches/page.test.ts (Plan 05-01 Wave-0)
  </read_first>
  <behavior>
    - `app/sg/katong-point/page.tsx`: RSC. Metadata (title "Prodigy @ Katong Point | Singapore's Only MultiBall Wall", description from strategy, openGraph, canonical). JSON-LD `@graph` with SportsActivityLocation (parentOrganization ref to root `#organization`, address from KATONG_POINT fields, geo coords, openingHoursSpecification from KATONG_POINT.hours, hasOfferCatalog with 4 offers, telephone from env) + BreadcrumbList (Home → Katong Point). Page body: hero section with H1 "Prodigy @ Katong Point" (Baloo accent on "Prodigy"), MultiBall headline Badge `<Badge className="bg-brand-yellow text-brand-navy">Singapore's only MultiBall wall</Badge>`, address block with MapPin, opening hours table, transit copy (East Coast / Marine Parade / Tanjong Katong from KATONG_POINT.serviceArea), VenueMap iframe, apparatus badges `KATONG_POINT.apparatus.map((a) => <Badge>)`, 3-zone overview cards linking to `/weekly-classes/{slug}/`, venue FAQ filtered from SG_FAQ_ITEMS group "venue", booking CTA.
    - `app/sg/katong-point/opengraph-image.tsx`: per-route OG with title "Prodigy @ Katong Point" + tagline "Singapore's only MultiBall wall · 451 Joo Chiat Road, Level 3."
    - `app/sg/coaches/page.tsx`: RSC. Metadata. JSON-LD `@graph` of Person entries from SG_COACHES (worksFor ref to root `#organization`, name, jobTitle, image URL, description). Page body: hero H1 "Meet the Prodigy Singapore team.", 3-col grid of coach cards (portrait Avatar 128×128 + name H3 + role + 2-line bio + optional "Read full bio →" anchor). Booking CTA footer.
    - `app/sg/coaches/opengraph-image.tsx`: per-route OG with title "Meet the Prodigy Singapore Team" + tagline "Haikel, Mark, and Coach King — at Katong Point."
  </behavior>
  <action>
    **Step 1 — app/sg/katong-point/page.tsx (CREATE):**
    Read `app/hk/wan-chai/page.tsx` (analog). Mirror structure with these substitutions:
    - Imports: `@/lib/hk-data` → `@/lib/sg-data` (KATONG_POINT, SG_ZONES, SG_FAQ_ITEMS, KATONG_POINT_MAP_EMBED); `@/components/hk/venue-map` → `@/components/sg/venue-map`
    - `const VENUE = HK_VENUES.find((v) => v.id === "wan-chai")!` → `const VENUE = KATONG_POINT;` (direct import; single venue, no `.find`)
    - `const VENUE_FAQS = HK_FAQ_ITEMS.filter(...)` → `const VENUE_FAQS = SG_FAQ_ITEMS.filter((i) => i.group === "venue").slice(0, 6);`
    - Metadata: SG-specific title/description per UI-SPEC §4 katong-point row
    - JSON-LD: `@type: "LocalBusiness"` from HK analog → `@type: "SportsActivityLocation"` per PATTERNS §katong-point + strategy §PART 9.4; address, geo, openingHoursSpecification all from KATONG_POINT fields; hasOfferCatalog with 4 offers (Weekly Sports Classes, Prodigy Holiday Camps, Birthday Parties, School Partnerships); parentOrganization `@id` reference to `https://proactivsports.com/#organization`
    - BreadcrumbList: Home → Katong Point (2 items)
    - MultiBall headline per Pattern 11 #6: `<Badge className="bg-brand-yellow text-brand-navy">Singapore's only MultiBall wall</Badge>` in hero area
    - Address: verbatim `{KATONG_POINT.addressStreet}, Singapore 427664` (hardcoded postal suffix since not in data shape)
    - Transit copy (UI-SPEC §4): `KATONG_POINT.serviceArea.join(" · ")` → "Katong · Marine Parade · East Coast · Joo Chiat · Kembangan · Tanjong Katong"
    - VenueMap: `<VenueMap embedSrc={KATONG_POINT_MAP_EMBED} title="Map showing Prodigy at 451 Joo Chiat Road, Level 3, Singapore 427664" className="my-8" />`
    - 3-zone overview: map SG_ZONES to linked cards (per UI-SPEC §4 katong-point key sections)
    - Opening hours table: map KATONG_POINT.hours to `<tr>` rows
    - Apparatus badges: `KATONG_POINT.apparatus.map((a) => <Badge variant="outline">{a}</Badge>)`
    - Booking CTA: `<Button className="bg-brand-red"><a href="/book-a-trial/?subject=katong-point">Book a Free Trial</a></Button>`

    **Step 2 — app/sg/katong-point/opengraph-image.tsx (CREATE):**
    Per Plan 05-02 pattern. Title "Prodigy @ Katong Point" + tagline "Singapore's only MultiBall wall · 451 Joo Chiat Road, Level 3, Singapore."

    **Step 3 — app/sg/coaches/page.tsx (CREATE):**
    Read `app/hk/coaches/page.tsx` (analog ~150 lines). Mirror with:
    - Imports: `HK_COACHES` → `SG_COACHES`
    - H1: "Meet the Prodigy Singapore team." (UI-SPEC §3.9 reused for /coaches/)
    - Simpler layout: 3-col grid (no HK "lead vs team" bifurcation since SG has 3 coaches — per PATTERNS lines 1066-1068)
    - Person JSON-LD: map SG_COACHES to Person entries with worksFor `@id` pointing to `https://proactivsports.com/#organization`, image URL `https://sg.proactivsports.com${c.portrait}`, name, jobTitle, description (bio)
    - Coach cards: Avatar (HUMAN-ACTION D-07 gate 3 — portrait may not exist; Avatar falls back gracefully with initials OR broken-image per D-07 precedent; prefer explicit `fallback` prop if Phase 2 Avatar supports it, else acceptable degradation)
    - Each card: name H3 Unbounded + role Manrope SemiBold small + 2-line bio + optional "Read full bio →" (at Phase 5 links to `#{slug}` within the page since no separate bio pages exist)
    - Booking CTA at bottom

    **Step 4 — app/sg/coaches/opengraph-image.tsx (CREATE):**
    Per-route OG: title "Meet the Prodigy Singapore Team" + tagline "Haikel, Mark, and Coach King — at Katong Point."

    **Step 5 — Tests:**
    Run `pnpm test:unit -- --run app/sg/katong-point/ app/sg/coaches/` — Wave-0 scaffolds turn GREEN (address + 3 coach names assertions).
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm test:unit -- --run app/sg/katong-point/ app/sg/coaches/ tests/no-sg-placeholder-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `app/sg/katong-point/page.tsx` contains "451 Joo Chiat Road"
    - File `app/sg/katong-point/page.tsx` contains "Singapore's only MultiBall wall"
    - File `app/sg/katong-point/page.tsx` contains `SportsActivityLocation` (JSON-LD)
    - File `app/sg/katong-point/page.tsx` contains `BreadcrumbList`
    - File `app/sg/katong-point/page.tsx` contains `VenueMap` (imported + rendered)
    - File `app/sg/katong-point/page.tsx` contains `KATONG_POINT` (import + usage)
    - File `app/sg/katong-point/page.tsx` contains `openingHoursSpecification`
    - File `app/sg/katong-point/page.tsx` contains `hasOfferCatalog`
    - File `app/sg/katong-point/page.tsx` contains "Singapore 427664" (hardcoded postal suffix)
    - File `app/sg/katong-point/opengraph-image.tsx` contains `createSGOgImage`
    - File `app/sg/coaches/page.tsx` contains "Haikel" AND "Mark" AND "Coach King"
    - File `app/sg/coaches/page.tsx` contains `Person` (JSON-LD type)
    - File `app/sg/coaches/page.tsx` contains `SG_COACHES`
    - File `app/sg/coaches/page.tsx` contains `proactivsports.com/#organization` (worksFor ref)
    - File `app/sg/coaches/opengraph-image.tsx` contains `createSGOgImage`
    - `grep -r "sg-placeholder" app/sg/katong-point/ app/sg/coaches/` returns 0
    - `pnpm test:unit -- --run app/sg/katong-point/page.test.ts` passes
    - `pnpm test:unit -- --run app/sg/coaches/page.test.ts` passes
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Katong Point location page (SG-02 fully addressed) + Coaches page (SG-08 fully addressed). Pattern 11 #6 MultiBall placement live on Katong Point page. Wave-0 katong-point + coaches tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FAQ hub + Blog hub + Events page</name>
  <files>app/sg/faq/page.tsx, app/sg/blog/page.tsx, app/sg/blog/opengraph-image.tsx, app/sg/events/page.tsx</files>
  <read_first>
    - app/hk/faq/page.tsx (analog — ~80 lines with grouped FAQ + FAQPage JSON-LD char-for-char match)
    - app/hk/blog/page.tsx (analog — blog hub with empty-state branch)
    - app/hk/competitions-events/page.tsx (partial analog for /events/ — structure only, different content scope)
    - .planning/phases/05-singapore-market/05-PATTERNS.md lines 1071-1082 (blog + faq patterns), lines 1036-1041 (events pattern)
    - .planning/phases/05-singapore-market/05-UI-SPEC.md §4 /faq/ + /blog/ + /events/ rows, §Copywriting empty-state copy for blog + events
    - .planning/phases/05-singapore-market/05-RESEARCH.md §Pitfall 9 (no sg-placeholder-* in blog imageUrl)
    - .planning/inputs/strategy.md §PART 6C §11 (10 FAQ Q&A verbatim — same source as SG_FAQ_ITEMS in lib/sg-data.ts)
    - lib/sg-data.ts (SG_FAQ_ITEMS grouped, SG_BLOG_POSTS_STUB)
    - app/sg/faq/page.test.ts + app/sg/page.test.ts (Wave-0 scaffolds Plan 05-01)
  </read_first>
  <behavior>
    - `app/sg/faq/page.tsx`: RSC. Metadata. Inline FAQPage + BreadcrumbList JSON-LD. DOM: grouped FAQ by category (about / venue / classes / multiball / camps / parties / schools — follow UI-SPEC §4 faq row grouping). Each group has H2 + `<div className="space-y-2">` of `<FAQItem>`. FAQPage JSON-LD `mainEntity` order matches DOM order char-for-char (Google rich-result rule per PATTERNS §JSON-LD Data Order).
    - `app/sg/blog/page.tsx`: RSC. Metadata. `grid-cols-1 md:grid-cols-3 gap-6` blog card grid. If `SG_BLOG_POSTS_STUB.length === 0` render empty-state per UI-SPEC §Copywriting. If `>= 1`, map posts to `<Card>` (text-only card treatment if no imageUrl — Pitfall 9 guards against `sg-placeholder-*` in imageUrl). CTAs link to `/blog/` (stubs are unlinkable at Phase 5).
    - `app/sg/blog/opengraph-image.tsx`: per-route OG with title "Prodigy Singapore Blog" + tagline "Guides, tips, and stories from Katong Point."
    - `app/sg/events/page.tsx`: RSC. Metadata. Evergreen editorial: hero + intro (sports days, community events, inter-school meets verbatim from strategy if available or per UI-SPEC §Copywriting "Upcoming events will appear here" empty state if no current events), KidsFirst sports-day proof point from strategy §PART 6C testimonial, Send-an-Enquiry CTA `/book-a-trial/?subject=events`.
  </behavior>
  <action>
    **Step 1 — app/sg/faq/page.tsx (CREATE):**
    Read `app/hk/faq/page.tsx` (analog ~80 lines). Mirror structure:
    - Imports: `HK_FAQ_ITEMS` → `SG_FAQ_ITEMS`
    - `GROUP_LABELS` const: map SG group keys to display labels — `{ about: "About Prodigy", venue: "Katong Point", classes: "Classes", multiball: "MultiBall", camps: "Camps", parties: "Birthday Parties", schools: "School Partnerships" }`
    - `GROUP_ORDER` const: `["about", "venue", "classes", "multiball", "camps", "parties", "schools"] as const` — reading order for DOM + JSON-LD
    - FAQPage JSON-LD: `mainEntity: GROUP_ORDER.flatMap((group) => SG_FAQ_ITEMS.filter((i) => i.group === group).map((i) => ({ "@type": "Question", name: i.question, acceptedAnswer: { "@type": "Answer", text: i.answer } })))` — same ordering drives DOM render below
    - DOM: `GROUP_ORDER.map((group) => <section><h2>{GROUP_LABELS[group]}</h2><div className="space-y-2">{SG_FAQ_ITEMS.filter((i) => i.group === group).map((item) => <FAQItem ...>)}</div></section>)`
    - BreadcrumbList (Home → FAQ)
    - CRITICAL: DOM order MUST match JSON-LD order char-for-char (Google rich-result rule)

    **Step 2 — app/sg/blog/page.tsx (CREATE):**
    Read `app/hk/blog/page.tsx` (analog). Mirror:
    - Imports: `HK_BLOG_POSTS_STUB` → `SG_BLOG_POSTS_STUB`
    - Empty-state branch: if `SG_BLOG_POSTS_STUB.length === 0`, render centred empty-state per UI-SPEC §Copywriting: "New posts coming soon." + body + Chat on WhatsApp CTA (conditional)
    - Stub branch: `grid-cols-1 md:grid-cols-3 gap-6` with `<Card>` per post
    - Card composition: if post has `imageUrl` AND NOT placeholder → render `<Image>`; else text-only card per Pitfall 9
    - Card fields: Badge category + H3 title + excerpt + `<time>` date + read-time + stub `<Link>` to `/blog/` (no per-post routes at Phase 5)

    **Step 3 — app/sg/blog/opengraph-image.tsx (CREATE):**
    Per-route OG with title "Prodigy Singapore Blog" + tagline "Guides, tips, and stories from Katong Point."

    **Step 4 — app/sg/events/page.tsx (CREATE):**
    Read `app/hk/competitions-events/page.tsx` (partial analog). The HK page focuses on gymnastics competitive pathway; SG /events/ is evergreen sports days / community events per UI-SPEC §4 events row.
    Structure:
    - Metadata (title "Events at Prodigy Singapore — Sports Days & Community", description per UI-SPEC §Copywriting)
    - Hero section: H1 "Events at Prodigy Singapore." + intro paragraph
    - Upcoming events section with empty-state per UI-SPEC §Copywriting: heading "Upcoming events will appear here." + body + CTA
    - KidsFirst sports day proof point (from strategy §PART 6C testimonial — single reference as social proof)
    - Future event categories editorial (sports days, community events, inter-school meets) — evergreen copy
    - Send-an-Enquiry CTA: `<a href="/book-a-trial/?subject=events">Send an Enquiry</a>`
    - BreadcrumbList JSON-LD (Home → Events)
    - NO Event JSON-LD at Phase 5 (Phase 6 CMS adds dated events with start/end/offers)

    **Step 5 — Tests:**
    Run `pnpm test:unit -- --run app/sg/faq/ app/sg/blog/ app/sg/events/` — Wave-0 FAQ scaffold GREEN (10 questions + FAQPage JSON-LD). No events.test.ts in Wave-0 scaffold (events is not listed in VALIDATION.md) but placeholder-leak guard still runs.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm test:unit -- --run app/sg/faq/ app/sg/blog/ app/sg/events/ tests/no-sg-placeholder-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `app/sg/faq/page.tsx` contains `FAQPage`
    - File `app/sg/faq/page.tsx` contains `SG_FAQ_ITEMS`
    - File `app/sg/faq/page.tsx` contains `FAQItem`
    - File `app/sg/faq/page.tsx` does NOT contain `<Accordion` (FAQItem composes its own; nesting breaks state per PATTERNS)
    - File `app/sg/faq/page.tsx` contains "MultiBall" (group label)
    - File `app/sg/faq/page.tsx` contains `BreadcrumbList`
    - File `app/sg/blog/page.tsx` contains `SG_BLOG_POSTS_STUB`
    - File `app/sg/blog/page.tsx` contains `length === 0` OR `length < 1` (empty-state branch)
    - File `app/sg/blog/page.tsx` does NOT contain `sg-placeholder` (Pitfall 9)
    - File `app/sg/blog/opengraph-image.tsx` contains `createSGOgImage`
    - File `app/sg/events/page.tsx` contains "Events" (H1)
    - File `app/sg/events/page.tsx` contains `/book-a-trial/?subject=events`
    - File `app/sg/events/page.tsx` does NOT contain `"Event"` (standalone — avoid raw Event JSON-LD at Phase 5) OR if it does contain "Event", it is inside an allowed context like "Events" or "Event series". Specifically `grep -c "\"@type\": \"Event\"" app/sg/events/page.tsx` returns 0
    - `grep -r "sg-placeholder" app/sg/faq/ app/sg/blog/ app/sg/events/` returns 0
    - `pnpm test:unit -- --run app/sg/faq/page.test.ts` passes
    - `pnpm test:unit -- --run tests/no-sg-placeholder-leak.test.ts` passes
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>FAQ + Blog + Events pages ship. SG-07 + SG-09 + SG-10 addressed. Pattern 11 #5 MultiBall placement (FAQ group "multiball") surfaces via FAQ hub. Wave-0 faq.test.ts GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Birthday Parties + School Partnerships with IFS inline</name>
  <files>app/sg/birthday-parties/page.tsx, app/sg/birthday-parties/opengraph-image.tsx, app/sg/school-partnerships/page.tsx</files>
  <read_first>
    - app/hk/birthday-parties/page.tsx (analog)
    - app/hk/school-partnerships/page.tsx (analog)
    - .planning/phases/05-singapore-market/05-PATTERNS.md lines 1013-1033 (birthday-parties + school-partnerships patterns — IFS inline per D-11)
    - .planning/phases/05-singapore-market/05-UI-SPEC.md §4 /birthday-parties/ + /school-partnerships/ rows, §Copywriting IFS callout copy
    - .planning/phases/05-singapore-market/05-CONTEXT.md D-11 (IFS inline on hub, no separate route)
    - .planning/inputs/strategy.md §PART 6C §3 (IFS partnership paragraph verbatim — sourced from IFS_PARTNERSHIP_COPY in lib/sg-data.ts), §PART 6C §7 (birthday party copy)
    - lib/sg-data.ts (IFS_PARTNERSHIP_COPY)
    - app/sg/school-partnerships/page.test.ts (Wave-0 scaffold Plan 05-01)
  </read_first>
  <behavior>
    - `app/sg/birthday-parties/page.tsx`: RSC. Metadata. Hero section + 2-col "What's included" + MultiBall-access callout (dedicated subsection per UI-SPEC §4 parties row: `<Badge className="bg-brand-green text-white">Singapore's only</Badge>` near the MultiBall-access line). Send-an-Enquiry CTA `/book-a-trial/?subject=birthday-party`. BreadcrumbList JSON-LD.
    - `app/sg/birthday-parties/opengraph-image.tsx`: per-route OG.
    - `app/sg/school-partnerships/page.tsx`: RSC. Metadata. Hero + intro + "Our Partner Schools" LogoWall (IFS + KidsFirst — logos HUMAN-ACTION optional per D-11, text-fallback otherwise) + **Featured IFS block** (H2 "Featured partner: International French School (IFS).", body = `IFS_PARTNERSHIP_COPY`, optional IFS logo image, CTA `/book-a-trial/?subject=school-partnership`) + "What School Partnerships Offer" 4-bullet list + BreadcrumbList JSON-LD.
  </behavior>
  <action>
    **Step 1 — app/sg/birthday-parties/page.tsx (CREATE):**
    Read `app/hk/birthday-parties/page.tsx` (analog ~200 lines). Mirror structure with substitutions:
    - Remove 2-venue wording (HK mentions "Wan Chai & Cyberport") → replace with "at Katong Point"
    - Add MultiBall access section: after the "2-hour hosted · coach-led" bullet list, add a callout card: `<div><Badge className="bg-brand-green text-white mb-2">Singapore's only</Badge><h3>MultiBall wall access</h3><p>{verbatim copy from PART 6C §7 MultiBall party paragraph}</p></div>`
    - CTA href: `/book-a-trial/?subject=birthday-party`
    - Metadata per UI-SPEC §4 parties row
    - BreadcrumbList (Home → Birthday Parties) JSON-LD

    **Step 2 — app/sg/birthday-parties/opengraph-image.tsx (CREATE):**
    Per-route OG: title "Birthday Parties at Prodigy Singapore" + tagline "2-hour hosted parties with MultiBall wall access at Katong Point."

    **Step 3 — app/sg/school-partnerships/page.tsx (CREATE):**
    Read `app/hk/school-partnerships/page.tsx` (analog). Mirror with IFS inline per D-11:
    - Imports: `IFS_PARTNERSHIP_COPY` from `@/lib/sg-data`
    - Hero: H1 "School Partnerships with Prodigy Singapore." + intro
    - LogoWall section: `<LogoWall>` with IFS + KidsFirst partner logos (if permission assets exist at `public/brand/logo-ifs.svg` / `logo-kidsfirst.svg`, render; else text-only fallback — D-11 permits both)
    - **Featured IFS block** (per D-11 + Pattern 10):
      ```tsx
      <Section size="md" bg="muted">
        <ContainerEditorial width="default">
          <div className="grid grid-cols-1 md:grid-cols-2 gap-8 items-center">
            <div>
              <Badge variant="outline" className="mb-4">Featured partner</Badge>
              <h2 className="text-h2 font-display">Featured partner: International French School (IFS).</h2>
              <p className="text-body-lg mt-4 text-muted-foreground">{IFS_PARTNERSHIP_COPY}</p>
              <Button asChild className="mt-6 bg-brand-red text-white">
                <a href="/book-a-trial/?subject=school-partnership">Enquire about an IFS partnership</a>
              </Button>
            </div>
            <div className="relative aspect-[4/3] rounded-xl overflow-hidden">
              {/* Optional IFS logo or generic schools-partnership photo */}
              <Image src="/brand/logo-ifs.svg" alt="International French School (IFS)" fill className="object-contain p-8" />
            </div>
          </div>
        </ContainerEditorial>
      </Section>
      ```
    - "What School Partnerships Offer" 4-bullet list (term-time programmes, camps, inter-school meets, bespoke curriculum)
    - General CTA: `/book-a-trial/?subject=school-partnership`
    - BreadcrumbList JSON-LD
    - Do NOT create `app/sg/school-partnerships/international-french-school/` — per D-11 IFS is consolidated inline

    **Step 4 — Tests:**
    Run `pnpm test:unit -- --run app/sg/school-partnerships/ app/sg/birthday-parties/` — Wave-0 school-partnerships scaffold GREEN.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm test:unit -- --run app/sg/school-partnerships/ app/sg/birthday-parties/ tests/no-sg-placeholder-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `app/sg/birthday-parties/page.tsx` contains `/book-a-trial/?subject=birthday-party`
    - File `app/sg/birthday-parties/page.tsx` contains `MultiBall` (MultiBall access callout per UI-SPEC §4)
    - File `app/sg/birthday-parties/page.tsx` contains `Singapore's only` (Badge)
    - File `app/sg/birthday-parties/page.tsx` contains `BreadcrumbList`
    - File `app/sg/birthday-parties/page.tsx` does NOT contain "Wan Chai" (SG-specific, no HK leak)
    - File `app/sg/birthday-parties/page.tsx` does NOT contain "Cyberport"
    - File `app/sg/birthday-parties/opengraph-image.tsx` contains `createSGOgImage`
    - File `app/sg/school-partnerships/page.tsx` contains "International French School" OR "IFS"
    - File `app/sg/school-partnerships/page.tsx` contains `IFS_PARTNERSHIP_COPY`
    - File `app/sg/school-partnerships/page.tsx` contains `?subject=school-partnership`
    - File `app/sg/school-partnerships/page.tsx` contains "Enquire about an IFS partnership" (verb + noun CTA per UI-SPEC §Copywriting)
    - File `app/sg/school-partnerships/page.tsx` contains `BreadcrumbList`
    - Directory `app/sg/school-partnerships/international-french-school/` does NOT exist (per D-11 no separate route)
    - `grep -r "sg-placeholder" app/sg/birthday-parties/ app/sg/school-partnerships/` returns 0
    - `pnpm test:unit -- --run app/sg/school-partnerships/page.test.ts` passes
    - `pnpm test:unit -- --run tests/no-sg-placeholder-leak.test.ts` passes
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Birthday Parties + School Partnerships with IFS inline ship. SG-05 + SG-06 addressed. IFS consolidated per D-11. Wave-0 school-partnerships test GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Page source → JSON-LD emission (SportsActivityLocation + Person + BreadcrumbList + FAQPage) | Structured data is public-by-design; no user input in schema generation. |
| KATONG_POINT_MAP_EMBED env var → iframe src | VenueMap component validates for "PLACEHOLDER" and falls back to text-only address block; iframe src is user-owned, not user-supplied. |
| SG_COACHES data → Person JSON-LD image URL | Portrait paths are developer-authored; HUMAN-ACTION D-07 gate fills files; broken-image fallback at runtime. |
| IFS_PARTNERSHIP_COPY → SG school-partnerships DOM | Verbatim strategy PART 6C §3 copy; trusted source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-40 | Tampering | Malicious iframe src via env var injection on KATONG_POINT_MAP_EMBED | mitigate | VenueMap component checks for "PLACEHOLDER" substring + empty string → fallback renders; user-provided Google Maps embed URLs go through a standard CSP `frame-src` policy enforced at Phase 10 Cloudflare WAF level. |
| T-05-41 | Information Disclosure | Per-route OG images leaking pre-production poster photography to public CDN via Vercel preview | accept | Preview URLs are Vercel-Authenticated per Phase 0 deployment protection; OG previews require auth header. Phase 10 swaps to production with public OG by design. |
| T-05-42 | Spoofing | FAQ content drift from strategy PART 6C §11 after executor paraphrase | mitigate | SG_FAQ_ITEMS is sourced in lib/sg-data.ts (Plan 05-01) and consumed by FAQ hub — single source of truth for both homepage §3.12 and faq hub. Executor cannot paraphrase per-page without diverging from data module; FAQPage JSON-LD char-for-char DOM match rule enforces this downstream (Google rich-result validation). |
| T-05-43 | Tampering | `IFS_PARTNERSHIP_COPY` changing between Plan 05-01 and this plan | mitigate | Single source in lib/sg-data.ts; Plan 05-05 imports and renders without modification. If copy drifts, Plan 05-01 SUMMARY documents it. |
| T-05-44 | Information Disclosure | Real coach portraits (Haikel/Mark/Coach King) being indexed by crawlers before portrait consent confirmed | accept | HUMAN-ACTION D-07 gate 3 is on the user; Phase 5 ships with broken-image fallback if portraits not provided. If portraits land post-ship, they're already consent-gated by the user-provision workflow. |
</threat_model>

<verification>
1. `pnpm typecheck` — zero errors across 11 files.
2. `pnpm test:unit` — all Wave-0 scaffolds addressed by this plan turn GREEN (katong-point, coaches, school-partnerships, faq).
3. `pnpm build` — all 11 routes compile; 4 per-route OG files compile.
4. `grep -r "sg-placeholder" app/sg/` → 0 matches across all SG pages.
5. Lighthouse mobile audit for /katong-point/: target LCP < 3.5s (hero poster) + SportsActivityLocation JSON-LD validates in Google Rich Results Test.
6. FAQ JSON-LD validation: copy visible FAQ DOM text into Rich Results Test; mainEntity order matches.
</verification>

<success_criteria>
- Requirements SG-02, SG-05, SG-06, SG-07, SG-08, SG-09, SG-10 fully addressed.
- Pattern 11 MultiBall placements #5 (FAQ entry) and #6 (Katong Point hero badge) live — completes all 6 placements.
- IFS surfaced inline per D-11; no separate route created.
- Blog + Events handle empty-state gracefully per UI-SPEC §Copywriting.
- FAQ hub JSON-LD char-for-char matches DOM (Google rich-result rule).
- Placeholder-leak guard remains GREEN across all pages including katong-point climbing references.
- Wave-0 tests: katong-point, coaches, school-partnerships, faq scaffolds GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/05-singapore-market/05-05-SUMMARY.md` with:
- Task 1 + Task 2 + Task 3 outputs
- Test status: Wave-0 scaffolds (katong-point, coaches, school-partnerships, faq) GREEN
- HUMAN-ACTION list: Katong Point hero photo (D-07 gate 1), 3 coach portraits (D-07 gate 3), optional birthday party photo (D-07 gate 4), IFS logo asset (optional, D-11), KidsFirst logo asset (optional)
- JSON-LD validation notes (if Martin ran Rich Results Test)
- Any deviations from UI-SPEC or PATTERNS (Rule 1 adaptations)
- Link to 05-06 plan (booking form — last Wave-3 plan)
</output>

---
phase: 05-singapore-market
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - app/sg/page.tsx
  - components/sg/sg-hero-video.tsx
autonomous: false
requirements: [SG-01]
user_setup:
  - service: Mux (existing from Phase 2)
    why: "SG hero montage video — Prodigy camp day quick cuts per strategy PART 5 §1."
    env_vars:
      - name: NEXT_PUBLIC_MUX_SG_HERO_PLAYBACK_ID
        source: "Mux dashboard → upload Prodigy camp footage → copy public playback ID. Optional at Phase 5 — poster-only fallback ships cleanly if missing."
    dashboard_config:
      - task: "Upload Prodigy camp-day montage (climbing + MultiBall + sports + laughter per PART 5 §1)"
        location: "Mux dashboard → Assets → Upload"
      - task: "Provide Katong Point hero poster photo (D-07 gate 1)"
        location: "public/photography/sg-venue-katong-hero.{source}; run pnpm photos:process to generate AVIF/WebP/JPG"

must_haves:
  truths:
    - "SG homepage at app/sg/page.tsx replaces the Phase 1 stub and renders 13 sections per strategy PART 5"
    - "Hero section shows Prodigy camp-day video (or poster fallback) with H1 verbatim from PART 6C §Hero"
    - "Primary red Book-a-Free-Trial CTA is visible above the fold on mobile (375×667 viewport)"
    - "MultiBall differentiator surfaces at 3 homepage placements per Pattern 11: hero trust line, Why-Prodigy §3 first tile, Three-Zones §5 Sports Zone card"
    - "SGHeroVideo client component does NOT wrap VideoPlayer in dynamic({ssr:false}) — VideoPlayer wraps MuxPlayer internally per Phase 4 pattern"
    - "WebSite + FAQPage JSON-LD inline in page head (char-for-char DOM match per Google rich-result rule)"
    - "Katong Point venue chip composition linking to /katong-point/ shows in §3.2 (uses VenueChipRow from Plan 05-02)"
    - "FAQ section uses FAQItem composition directly (NOT nested inside parent Accordion — Accordion-nesting bug per PATTERNS §FAQItem Composition)"
  artifacts:
    - path: "app/sg/page.tsx"
      provides: "13-section SG homepage RSC with metadata, JSON-LD, all composed sections"
      min_lines: 400
      contains: "Where Singapore's kids come to move, play, and grow."
    - path: "components/sg/sg-hero-video.tsx"
      provides: "Client component rendering Image poster + VideoPlayer for Prodigy camp montage"
      contains: "\"use client\""
  key_links:
    - from: "app/sg/page.tsx"
      to: "components/sg/sg-hero-video.tsx + components/sg/venue-chip-row.tsx + lib/sg-data.ts"
      via: "import + JSX"
      pattern: "SGHeroVideo.*VenueChipRow.*SG_FAQ_ITEMS.*SG_ZONES"
    - from: "app/sg/page.tsx FAQ section"
      to: "DOM FAQ questions matching FAQPage JSON-LD mainEntity order"
      via: "single SG_FAQ_ITEMS filter drives both"
      pattern: "SG_HOMEPAGE_FAQS"
    - from: "app/sg/page.tsx hero CTA"
      to: "/book-a-trial/"
      via: "plain <a href> preserves trailing slash (PATTERNS §Trailing Slash)"
      pattern: "href=\"/book-a-trial/\""
---

<objective>
Replace the Phase 1 `app/sg/page.tsx` stub with the production SG homepage: 13 sections per strategy PART 5 wireframe composed from Phase 2 primitives + Phase 5-local components (SGHeroVideo, VenueChipRow from Plan 05-02). Inline WebSite + FAQPage JSON-LD. Copy verbatim from strategy PART 6C §Hero + §2–§12. MultiBall differentiator treatment at 3 placements per Pattern 11. Per UI-SPEC §3.0–§3.13 + §5 local components.

Purpose: The homepage is the single highest-conversion surface in the SG market. It must ship with every conversion signal active (red Book CTA above the fold mobile, "Singapore's only MultiBall wall" trust line, social proof, coaches, FAQ + final CTA) so parents land and can click Book a Free Trial in ≤2 taps per ROADMAP success criterion 1.

Output: 2 files — `app/sg/page.tsx` (~450 lines — 13 sections + metadata + JSON-LD + section function components) and `components/sg/sg-hero-video.tsx` (~35 lines — client Image + VideoPlayer wrapper).

This plan is NOT autonomous: ends with a `checkpoint:human-verify` gate (visual verification on mobile viewport + MultiBall spotlight treatment + above-the-fold Book CTA).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-singapore-market/05-CONTEXT.md
@.planning/phases/05-singapore-market/05-PATTERNS.md
@.planning/phases/05-singapore-market/05-UI-SPEC.md
@.planning/phases/05-singapore-market/05-RESEARCH.md
@.planning/phases/05-singapore-market/05-01-SUMMARY.md
@.planning/phases/05-singapore-market/05-02-SUMMARY.md
@app/hk/page.tsx
@components/hk/hk-hero-video.tsx
@lib/sg-data.ts
@components/sg/venue-chip-row.tsx
@components/sg/sg-nav.tsx
@.planning/inputs/strategy.md

<interfaces>
<!-- Consumed from Plan 05-01 -->
From lib/sg-data.ts:
- KATONG_POINT (venue), SG_FAQ_ITEMS (10 Q&A), SG_BLOG_POSTS_STUB, SG_ZONES, SG_CAMP_TYPES, SG_COACHES, IFS_PARTNERSHIP_COPY
- `SGFAQItem.group` enum includes "about" | "venue" | "classes" | "camps" | "multiball" | "parties" | "schools"

<!-- Consumed from Plan 05-02 -->
From components/sg/venue-chip-row.tsx:
- `export function VenueChipRow(): JSX.Element` — renders single Katong Point chip, centred

From components/sg/sg-nav.tsx + sg-footer.tsx (via layout, not imported here):
- Already wraps page via app/sg/layout.tsx

<!-- Consumed from Phase 2 primitives -->
From components/ui/:
- Section (size prop), ContainerEditorial (width prop), Button (size="touch" + variant), Card, Badge, FAQItem, ProgrammeTile, LogoWall, StatStrip, TestimonialCard, Avatar, Separator

<!-- Analog: app/hk/page.tsx (~790 lines, 12 sections) -->
Exports:
- `export const metadata: Metadata = { title, description, openGraph, alternates }`
- Default page function `HKHomePage(): JSX.Element` with 12 section function components
- Inline `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(hkHomeSchema) }} />` (WebSite + FAQPage)
- Section functions: `HeroSection`, `VenueChipRow` (Phase 4 version), `WhyProGymSection`, `ProgrammesSection`, `LocationSplitSection`, `SocialProofSection`, `CoachingMethodSection`, `CampsPartiesSection`, `AboutSection`, `BlogSection`, `FAQSection`, `FinalCTASection`

<!-- Analog: components/hk/hk-hero-video.tsx (65 lines) -->
Exports:
- `export interface HKHeroVideoProps { playbackId, posterSrc, posterAlt, title }`
- `export function HKHeroVideo(props)`: renders `<Image fill priority>` poster + `<VideoPlayer playbackId autoPlay loop muted aspect="video">` — does NOT wrap VideoPlayer in dynamic (VideoPlayer wraps MuxPlayer internally)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: components/sg/sg-hero-video.tsx (hero video client component)</name>
  <files>components/sg/sg-hero-video.tsx</files>
  <read_first>
    - components/hk/hk-hero-video.tsx (analog — 65 lines, verbatim mirror)
    - .planning/phases/05-singapore-market/05-PATTERNS.md lines 503-532 (SGHeroVideo pattern — critical note: do NOT wrap VideoPlayer in dynamic({ssr:false}))
    - .planning/phases/05-singapore-market/05-UI-SPEC.md §3.1 (hero video spec: 16:9 mobile / 21:9 desktop, poster fallback, autoplay loop muted)
    - components/ui/video-player.tsx (existing Phase 2 VideoPlayer — already internally handles dynamic wrapping)
  </read_first>
  <behavior>
    - File starts with `"use client"` directive (client component).
    - Exports `SGHeroVideoProps` interface: `{ playbackId: string; posterSrc: string; posterAlt: string; title: string }`.
    - Exports `SGHeroVideo({ playbackId, posterSrc, posterAlt, title })` function component.
    - Renders a React fragment containing: (1) `<Image src={posterSrc} alt={posterAlt} fill priority className="object-cover" sizes="100vw" />` (LCP element), (2) `<VideoPlayer playbackId={playbackId} title={title} poster={posterSrc} autoPlay aspect="video" className="absolute inset-0 w-full h-full" />`.
    - Does NOT wrap VideoPlayer in `dynamic({ ssr: false })` — VideoPlayer already wraps MuxPlayer internally per Phase 2 pattern (critical — nesting two `dynamic({ssr:false})` layers breaks Next 15 RSC/Client boundary).
  </behavior>
  <action>
    Read `components/hk/hk-hero-video.tsx` (analog, 65 lines). Mirror verbatim with 5 substitutions:
    1. Interface rename: `HKHeroVideoProps` → `SGHeroVideoProps`.
    2. Function rename: `export function HKHeroVideo` → `export function SGHeroVideo`.
    3. JSDoc comment: `Phase 4 / Plan 04-03 — HK hero video` → `Phase 5 / Plan 05-03 — SG hero video (Prodigy camp-day montage per strategy PART 5 §1)`.
    4. The `loop` and `muted` props: VideoPlayer internal defaults handle these, but verify they are set explicitly for clarity. Check the HK analog for exact prop arrangement.
    5. No other changes — file is market-agnostic in its core logic.

    Add comment block at top referencing:
    ```
    // Phase 5 / Plan 05-03 — SG hero video.
    // Mirrors components/hk/hk-hero-video.tsx verbatim — video primitive is market-agnostic.
    // CRITICAL (PATTERNS §SGHeroVideo): do NOT wrap VideoPlayer in dynamic({ssr:false}).
    // VideoPlayer already internally wraps MuxPlayer in dynamic; double-wrapping breaks RSC.
    // HUMAN-ACTION D-07 gate 1: posterSrc defaults to /photography/sg-venue-katong-hero.webp
    // (file may not exist at first render — graceful broken-image fallback per Phase 3 D-10 precedent).
    ```
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `components/sg/sg-hero-video.tsx` starts with `"use client"` directive
    - File `components/sg/sg-hero-video.tsx` contains `"use client"` as first non-comment line
    - File `components/sg/sg-hero-video.tsx` contains `export interface SGHeroVideoProps`
    - File `components/sg/sg-hero-video.tsx` contains `export function SGHeroVideo`
    - File `components/sg/sg-hero-video.tsx` contains `VideoPlayer` (imports + usage)
    - File `components/sg/sg-hero-video.tsx` contains `import Image from "next/image"`
    - File `components/sg/sg-hero-video.tsx` contains `priority` (on Image poster — LCP signal)
    - File `components/sg/sg-hero-video.tsx` contains `fill`
    - File `components/sg/sg-hero-video.tsx` does NOT contain `dynamic` (no double-wrapping)
    - `grep -c "dynamic(" components/sg/sg-hero-video.tsx` returns 0
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>SGHeroVideo client component ready for consumption in app/sg/page.tsx hero section.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: app/sg/page.tsx — 13-section SG homepage (MODIFY: replace Phase 1 stub)</name>
  <files>app/sg/page.tsx</files>
  <read_first>
    - app/hk/page.tsx (analog — 790 lines, 12 sections; use as structural template)
    - app/sg/page.tsx (existing Phase 1 stub — must be DELETED wholesale)
    - .planning/phases/05-singapore-market/05-PATTERNS.md lines 763-876 (app/sg/page.tsx full mirror instructions: imports, metadata, JSON-LD, 13 section layout, FAQ rendering pattern, hero trust line, MultiBall Pattern 11 placements)
    - .planning/phases/05-singapore-market/05-UI-SPEC.md §3 (ALL sub-sections §3.0 through §3.13 — exhaustive section-by-section specification: primitives, layout asymmetry slots, copy, CTAs, imagery, contrast, interaction states)
    - .planning/phases/05-singapore-market/05-UI-SPEC.md §Copywriting Contract (verbatim CTA labels), §13 (editorial asymmetry alternation table)
    - .planning/phases/05-singapore-market/05-CONTEXT.md D-07 (photography gate filenames), D-09 (Prodigy-green usage rules), §Specific Ideas (MultiBall differentiator approach)
    - .planning/inputs/strategy.md §PART 5 (13-section wireframe authoritative order + captions), §PART 6C (VERBATIM copy for all sections: Hero, §2 Why Prodigy, §3 Programmes, §4 Three Zones, §5 Social Proof, §6 Camps, §7 Parties, §8 Coaches, §9 About, §10 Blog, §11 FAQ ×10, §12 Final CTA)
    - lib/sg-data.ts (Plan 05-01 — SG_FAQ_ITEMS, SG_ZONES, SG_CAMP_TYPES, SG_COACHES, SG_BLOG_POSTS_STUB, KATONG_POINT)
    - components/sg/sg-hero-video.tsx (Task 1 output)
    - components/sg/venue-chip-row.tsx (Plan 05-02 output)
  </read_first>
  <behavior>
    - `app/sg/page.tsx` is RSC; replaces the Phase 1 teal-stripe stub.
    - Exports `metadata` with SG-specific title/description/openGraph/canonical per UI-SPEC §3.0 (NOT just inheriting layout — openGraph merges shallow per PATTERNS §Pitfall 2).
    - Inline WebSite + FAQPage JSON-LD block; FAQPage `mainEntity` filtered from SG_FAQ_ITEMS (groups: about/classes/venue/multiball), `.slice(0, 8)`. DOM FAQ rendering uses the SAME filtered array so order matches char-for-char.
    - 13 section function components composed top-to-bottom per UI-SPEC §3.0 order: HeroSection, VenueChipRowSection, WhyProdigySection, ProgrammesSection, ThreeZonesSection, SocialProofSection, CampsFeatureSection, BirthdayPartySection, CoachesSection, AboutSection, BlogSection, FAQSection, FinalCTASection.
    - HeroSection: Uses `<SGHeroVideo>` with `playbackId={process.env.NEXT_PUBLIC_MUX_SG_HERO_PLAYBACK_ID ?? ""}`, posterSrc `/photography/sg-venue-katong-hero.webp`, H1 verbatim "Where Singapore's kids come to move, play, and grow.", subhead verbatim from PART 6C §Hero, "Singapore's only MultiBall wall" trust line as `<p className="text-body-lg text-brand-cream"><span className="font-accent text-brand-green">Singapore's only MultiBall wall</span> · Katong Point</p>`, primary `<Button>` red Book a Free Trial linking `<a href="/book-a-trial/">`, secondary `<Button variant="outline">` Send an Enquiry linking `<a href="/book-a-trial/?subject=general-enquiry">`, trust line "Free trial · No obligation · Usually booked same week."
    - VenueChipRowSection: `<Section size="sm">` + `<ContainerEditorial>` + `<VenueChipRow />` from Plan 05-02.
    - WhyProdigySection: `<Section size="md" bg="muted">` + 4-tile grid. Tile 1 is MultiBall flagship with Zap icon, `<Badge className="bg-brand-green text-white">Singapore's only</Badge>` overlay, copy from PART 6C §2 first paragraph. Tiles 2–4: multi-sport, coaching standard, progression — copy from PART 6C §2.
    - ProgrammesSection: `<Section size="md">` + 4 `<ProgrammeTile>` cards (2+2 staggered grid per UI-SPEC §3.4: first+third tiles `mt-0`, second+fourth `mt-8`). Cards: Weekly Classes/Prodigy Camps/Birthday Parties/School Partnerships with age bands, hrefs, photo references per UI-SPEC §3.4.
    - ThreeZonesSection: 3 `<Card>` zones. Sports+MultiBall zone carries inline `<Badge className="bg-brand-green text-white">Singapore's only</Badge>`. Mobile reshuffle: Sports first on mobile, desktop keeps Movement → Sports → Climbing reading order (via Tailwind `order-first md:order-none`).
    - SocialProofSection: navy-surface section (`bg="navy"`) + `<LogoWall>` (IFS + KidsFirst — HUMAN-ACTION D-11 for permission) + 1–2 `<TestimonialCard>` per PART 6C §5. Pitfall 6: avoid duplicating Manjula testimonial across root+SG.
    - CampsFeatureSection: single-card feature block with photo-left-copy-right layout for upcoming Prodigy camp (stub data: "Ninja Warrior Camp · 16-20 June · Ages 5-12"), CTA → `/prodigy-camps/themed/`.
    - BirthdayPartySection: 2-col revenue block with MultiBall access mention + `Send an Enquiry` CTA → `/book-a-trial/?subject=birthday-party`.
    - CoachesSection: 3 `<Card>` coach bios (Haikel/Mark/Coach King from SG_COACHES), `<Avatar>` portraits (HUMAN-ACTION D-07 gate 3), "Read bio →" links to `/coaches/#{slug}`.
    - AboutSection: 2-col prose + photo with cross-market link to HK via `<a href={process.env.NEXT_PUBLIC_HK_URL}>14 years in Hong Kong →</a>`.
    - BlogSection: 1 stub card (recommended per Claude's Discretion) with graceful `grid-cols-1 md:grid-cols-3` empty-state per UI-SPEC §3.11; NO `sg-placeholder-*` in imageUrl.
    - FAQSection: narrow centred `<ContainerEditorial width="default">` + `<div className="space-y-2">` wrapping `SG_HOMEPAGE_FAQS.map((item) => <FAQItem ... />)`. NOT wrapped in parent `<Accordion>` (nesting bug per PATTERNS §FAQItem Composition).
    - FinalCTASection: full-bleed navy (`bg="navy"`) with centred CTA group: red Book a Free Trial + outline Chat on WhatsApp (conditionally rendered if `NEXT_PUBLIC_WHATSAPP_SG` set).
  </behavior>
  <action>
    **Step 1 — Delete existing stub.** Open `app/sg/page.tsx` (Phase 1 stub, ~30 lines of teal distinguisher) and replace with new content.

    **Step 2 — Mirror app/hk/page.tsx structure** end-to-end. Read the HK analog line-by-line (790 lines). For each section, copy the HK section function pattern (imports, JSX structure, primitive composition, ContainerEditorial widths, Section sizes) and apply the 4-step substitution:
    - `hk-data` → `sg-data` (`SG_FAQ_ITEMS`, `SG_ZONES`, `SG_CAMP_TYPES`, `SG_COACHES`, `SG_BLOG_POSTS_STUB`, `KATONG_POINT`)
    - `HKHeroVideo` → `SGHeroVideo`, `HK_VENUES` → `KATONG_POINT` (single venue, not `.map()`), `en_HK` → `en_SG`, `hk.proactivsports.com` → `sg.proactivsports.com`
    - Copy: swap HK copy (ProGym, Wan Chai, Cyberport, Monica) with Prodigy copy from strategy §PART 6C (Haikel, Mark, Coach King, Katong Point, MultiBall) — EVERY copy string updated.

    **Section-by-section requirements (UI-SPEC §3):**

    Metadata + JSON-LD (§3.0):
    ```typescript
    export const metadata: Metadata = {
      title: "Kids' Sports Classes Singapore | Prodigy by ProActiv Sports @ Katong Point",
      description: "Kids' sports classes, holiday camps & birthday parties at Prodigy, Katong Point. Home of Singapore's only MultiBall wall. Book a free trial.",
      openGraph: {
        title: "Kids' Sports Classes Singapore | Prodigy @ Katong Point",
        description: "Singapore's only MultiBall wall — kids' sport, camps, and parties at Katong Point.",
        url: "https://sg.proactivsports.com/",
        siteName: "Prodigy by ProActiv Sports — Singapore",
        locale: "en_SG",
        type: "website",
        images: [{ url: "/opengraph-image", width: 1200, height: 630, alt: "Prodigy by ProActiv Sports Singapore" }],
      },
      alternates: { canonical: "https://sg.proactivsports.com/" },
    };

    const SG_HOMEPAGE_FAQS = SG_FAQ_ITEMS.filter(
      (i) => i.group === "about" || i.group === "classes" || i.group === "venue" || i.group === "multiball",
    ).slice(0, 8);

    const sgHomeSchema = {
      "@context": "https://schema.org",
      "@graph": [
        { "@type": "WebSite", "@id": "https://sg.proactivsports.com/#website",
          url: "https://sg.proactivsports.com/",
          name: "Prodigy by ProActiv Sports — Singapore",
          publisher: { "@id": "https://proactivsports.com/#organization" },
          inLanguage: "en-SG" },
        { "@type": "FAQPage",
          mainEntity: SG_HOMEPAGE_FAQS.map((item) => ({
            "@type": "Question",
            name: item.question,
            acceptedAnswer: { "@type": "Answer", text: item.answer },
          })) },
      ],
    } as const;
    ```

    Hero (§3.1): Full-bleed container `relative w-full aspect-[16/9] md:aspect-[21/9] overflow-hidden rounded-2xl`, SGHeroVideo inside, navy/40 overlay, absolute-positioned copy block left-aligned (`absolute inset-0 flex flex-col items-start justify-center px-6 md:px-12`). Use H1 verbatim: "Where Singapore's kids come to move, play, and grow." Subhead verbatim from PART 6C §Hero. MultiBall trust line per UI-SPEC §3.1 with `<Zap size={16} className="text-brand-yellow" />` icon. Primary red CTA with `<ArrowRight>` suffix. Secondary CTA Send an Enquiry. Trust line "Free trial · No obligation · Usually booked same week." All CTAs use plain `<a href="/book-a-trial/">` (per PATTERNS §Trailing Slash).

    VenueChipRow (§3.2): `<Section size="sm"><ContainerEditorial><VenueChipRow /></ContainerEditorial></Section>` — Plan 05-02 component already built.

    Why Prodigy (§3.3): 4-tile grid on `bg-muted` with `border-y border-brand-navy/10`. Tile 1 (MultiBall flagship): `<Card className="p-6 relative">` + `<Zap className="size-8 text-brand-green mb-4" />` + `<h3 className="text-h3 font-display">The only MultiBall wall in Singapore</h3>` + `<p className="text-body text-muted-foreground mt-2">{body from PART 6C §2}</p>` + `<Badge className="bg-brand-green text-white absolute top-3 right-3">Singapore's only</Badge>`. Tiles 2–4: Trophy/BadgeCheck/ArrowUpRight icons in navy.

    Programmes (§3.4): 4 `<ProgrammeTile>` cards with 2+2 staggered grid (first+third `mt-0`, second+fourth `mt-8` md-breakpoint). Ages + hrefs + images + taglines per UI-SPEC §3.4 table (reference SG_ZONES + SG_CAMP_TYPES where data overlaps).

    Three Zones (§3.5): `<Section size="md">` + 3 `<Card>` zones mapping SG_ZONES. Sports+MultiBall card carries `<Badge className="bg-brand-green text-white absolute top-3 right-3">Singapore's only</Badge>`. Mobile order override: `className="order-first md:order-none"` on Sports+MultiBall card (UI-SPEC §3.5 mobile reshuffle).

    Social Proof (§3.6): `<Section bg="navy">` + `<LogoWall>` (IFS + KidsFirst — names with text fallback if logo asset missing) + 1-2 `<TestimonialCard>` on cream surface per PART 6C §5. Check root gateway `app/root/page.tsx` to avoid duplicating Manjula testimonial (Pitfall 6).

    Camps Feature (§3.7): single large photo-left-copy-right `<Card>` with upcoming camp stub "Ninja Warrior Camp · 16-20 June · Ages 5-12" (hardcoded at Phase 5; Phase 6 CMS replaces with dated Event schema). CTA `<Button variant="outline">Book Camp →</Button>` → `/prodigy-camps/themed/`.

    Birthday Parties (§3.8): 2-col revenue block. Left col: photo + 3-bullet "2hr hosted · coach-led · MultiBall wall access" + CTA `<a href="/book-a-trial/?subject=birthday-party">Send an Enquiry</a>`. Right col: "What's included" prose from PART 6C §7.

    Coaches (§3.9): `<Section bg="muted">` + 3-col coach grid mapping SG_COACHES (Haikel/Mark/Coach King). Each card: `<Avatar>` (128×128 round), name H3, role, 2-line bio, "Read bio →" link to `/coaches/#{slug}`. D-07 HUMAN-ACTION gate: if portraits missing at `/photography/coach-{slug}-portrait.webp`, surface in SUMMARY (but page still renders — broken-image acceptable per D-07 precedent).

    About (§3.10): 2-col prose + photo with About-Prodigy text verbatim from PART 6C §9 + cross-market `<a href={process.env.NEXT_PUBLIC_HK_URL ?? "#"}>14 years in Hong Kong →</a>`. Photo: coach-with-child action shot (HUMAN-ACTION).

    Blog (§3.11): `<Section size="md">` + `grid-cols-1 md:grid-cols-3 gap-6` + map `SG_BLOG_POSTS_STUB` → `<Card>` with image-less text-only card treatment per Pitfall 9 (NEVER use `sg-placeholder-*` as imageUrl). If empty, render empty-state copy from UI-SPEC §Copywriting "New posts coming soon." + Chat on WhatsApp CTA.

    FAQ (§3.12): `<Section size="md">` + `<ContainerEditorial width="default">` (narrow reading column) + `<div className="space-y-2 max-w-3xl mx-auto">{SG_HOMEPAGE_FAQS.map((item) => <FAQItem key={item.value} value={item.value} question={item.question} answer={item.answer} />)}</div>`. Do NOT wrap in parent `<Accordion>` — FAQItem internally composes Accordion; nesting breaks state (PATTERNS §FAQItem Composition).

    Final CTA (§3.13): `<Section size="lg" bg="navy">` + centred container + `<h2>Ready to try a free trial at Prodigy?</h2>` + body + primary red Book CTA + secondary outline Chat on WhatsApp button (conditional on env var `NEXT_PUBLIC_WHATSAPP_SG`).

    **Step 3 — JSON-LD inline.** Place `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(sgHomeSchema) }} />` at the top of the default export JSX tree (before HeroSection).

    **Step 4 — Verify placeholder leak guard.** After writing page, run `grep sg-placeholder app/sg/page.tsx` — MUST return 0 matches. All photo references use `/photography/sg-{topic}.webp` naming without `placeholder` in the path.

    **Step 5 — Run test.** `pnpm test:unit -- --run app/sg/page.test.ts` — from Wave-0 scaffold in Plan 05-01. Should now turn GREEN (H1 + chip + CTA + MultiBall trust line present). If fails, iterate until green.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm test:unit -- --run app/sg/page.test.ts tests/no-sg-placeholder-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `app/sg/page.tsx` contains "Where Singapore's kids come to move, play, and grow."
    - File `app/sg/page.tsx` contains "Singapore's only MultiBall wall" (appears at least 2 times — hero trust line + ThreeZones badge)
    - File `app/sg/page.tsx` contains `href="/book-a-trial/"` (primary CTA) at least 2 times (hero + final CTA)
    - File `app/sg/page.tsx` contains `SGHeroVideo`
    - File `app/sg/page.tsx` contains `VenueChipRow` (imported from Plan 05-02)
    - File `app/sg/page.tsx` contains `SG_FAQ_ITEMS` AND `SG_HOMEPAGE_FAQS`
    - File `app/sg/page.tsx` contains `"FAQPage"` (JSON-LD)
    - File `app/sg/page.tsx` contains `"WebSite"` (JSON-LD)
    - File `app/sg/page.tsx` contains `locale: "en_SG"` or `locale:"en_SG"`
    - File `app/sg/page.tsx` contains `sg.proactivsports.com`
    - File `app/sg/page.tsx` contains `bg-brand-green` (MultiBall badge fill)
    - File `app/sg/page.tsx` contains `Haikel` AND `Mark` AND `Coach King` (via SG_COACHES map)
    - File `app/sg/page.tsx` contains `?subject=birthday-party`
    - File `app/sg/page.tsx` does NOT contain raw `sg-placeholder` string
    - `grep -c "sg-placeholder" app/sg/page.tsx` returns 0
    - `grep -c "FAQItem" app/sg/page.tsx` returns at least 1 (composition without nested Accordion)
    - `grep -c "<Accordion" app/sg/page.tsx` returns 0 (FAQItem composes its own; PATTERNS §FAQItem Composition)
    - `pnpm test:unit -- --run app/sg/page.test.ts` passes (Wave-0 scaffold from Plan 05-01 turns GREEN)
    - `pnpm test:unit -- --run tests/no-sg-placeholder-leak.test.ts` passes
    - `pnpm typecheck` passes
    - `pnpm build` compiles `/sg/` route without errors
  </acceptance_criteria>
  <done>SG homepage (13 sections) ships. Homepage test turns GREEN. MultiBall Pattern 11 placements: hero trust line + Why-Prodigy §3 tile + ThreeZones §5 badge = 3 placements on homepage (Pattern 11 placements 4, 5, 6 land on Plan 05-04 sports-multiball page + Plan 05-05 Katong Point + FAQ hub).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual verification — SG homepage above-the-fold conversion + MultiBall treatment</name>
  <what-built>
    - app/sg/page.tsx with 13 sections per strategy PART 5
    - SGHeroVideo component with poster fallback
    - MultiBall differentiator at 3 placements (hero trust line, Why-Prodigy flagship tile, ThreeZones Sports badge)
    - Red Book-a-Free-Trial CTA above the fold on mobile
    - Homepage Vitest regression test passing (from Wave-0 scaffold)
  </what-built>
  <action>Pause execution and prompt the user to perform the visual verification steps below. Do not proceed to the next plan until the user replies with approval or iteration requests.</action>
  <how-to-verify>
    **Preconditions:**
    - `.env.local` may include `NEXT_PUBLIC_MUX_SG_HERO_PLAYBACK_ID` (optional — poster-only fallback is acceptable for this checkpoint)
    - `.env.local` may include `NEXT_PUBLIC_WHATSAPP_SG=+6598076827` (optional — WhatsApp chip conditionally renders)
    - Poster photo at `public/photography/sg-venue-katong-hero.webp` may not exist yet (broken-image fallback acceptable per D-07 precedent)

    **Start dev server:**
    ```bash
    pnpm dev
    ```
    Visit `http://sg.localhost:3000/` in Chrome (desktop, fully expanded).

    **Verification steps:**

    1. **Above-the-fold conversion check (desktop):**
       - Red "Book a Free Trial" button visible without scrolling? ✅ / ❌
       - Hero H1 reads exactly "Where Singapore's kids come to move, play, and grow."? ✅ / ❌
       - "Singapore's only MultiBall wall" text visible on hero? ✅ / ❌
       - SGNav sticky Book CTA also visible top-right? ✅ / ❌

    2. **Mobile viewport check (Chrome DevTools → Toggle device toolbar → 390×844 iPhone 14 Pro):**
       - Red Book CTA visible above the fold on mobile? ✅ / ❌ (ROADMAP SC #1 — mobile above-fold requirement)
       - Hamburger menu opens Sheet drawer when tapped? ✅ / ❌
       - Sheet drawer shows sticky red Book CTA at BOTTOM (per D-05 mobile variant)? ✅ / ❌
       - SGNav brand lockup "Prodigy SG" with Baloo accent renders correctly on "Prodigy" word? ✅ / ❌

    3. **MultiBall differentiator placement check:**
       - Hero: "Singapore's only MultiBall wall" trust line visible above CTAs? ✅ / ❌
       - Why-Prodigy §3 first tile: Zap icon + "The only MultiBall wall in Singapore" H3 + green "Singapore's only" badge overlay? ✅ / ❌
       - Three-Zones §5: Sports+MultiBall zone card has green "Singapore's only" badge? ✅ / ❌

    4. **Mobile editorial reshuffle check:**
       - On mobile viewport, Sports+MultiBall zone card is rendered FIRST in Three-Zones section (above Movement and Climbing)? ✅ / ❌ (UI-SPEC §3.5 mobile override)

    5. **Social proof + trust signals:**
       - Section 6 (Social Proof) renders on navy background with LogoWall + testimonials? ✅ / ❌
       - Coaches section shows Haikel, Mark, Coach King names (portraits may be broken-image placeholders if D-07 gate 3 photos not dropped — acceptable)? ✅ / ❌

    6. **Final CTA:**
       - Bottom of page: navy full-bleed section with "Ready to try a free trial at Prodigy?" + red Book CTA + optional WhatsApp chip (if env var set)? ✅ / ❌

    7. **Footer:**
       - Single Katong Point NAP visible (not 2-venue grid)? ✅ / ❌
       - "Also in Hong Kong ↗" cross-market link visible (href may be `#` if `NEXT_PUBLIC_HK_URL` unset)? ✅ / ❌

    8. **Console check (DevTools → Console):**
       - Zero React hydration warnings? ✅ / ❌
       - Zero missing-key warnings on mapped sections? ✅ / ❌

    9. **Terminal check:**
       ```bash
       grep -c "sg-placeholder" app/sg/page.tsx
       # Must return: 0
       ```

    10. **JSON-LD check (DevTools → Elements → `<head>`):**
        - `<script type="application/ld+json">` present with `WebSite` + `FAQPage` in `@graph`? ✅ / ❌
        - FAQPage `mainEntity[]` questions match FAQ section DOM text char-for-char? ✅ / ❌

    **If any ❌, document in checkpoint response with:**
    - Which step failed
    - Expected vs actual behavior
    - Screenshot path (if possible: `cmd+ctrl+shift+4` → Desktop)
    - Suggested fix direction

    **If all ✅ → approve.**
  </how-to-verify>
  <resume-signal>
    Type "approved" to proceed to Plan 05-04 (pillar pages + zone/camp sub-pages). Alternatively describe specific issues and request iteration.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client browser → SGHeroVideo → Mux CDN | `NEXT_PUBLIC_MUX_SG_HERO_PLAYBACK_ID` env var embedded in client bundle; playback IDs are public-by-design per Mux security model. |
| Page source → strategy copy | Copy is verbatim from strategy PART 6C (trusted developer-authored source); no user input in copy path. |
| FAQPage JSON-LD → DOM | Google rich-result rule requires char-for-char match between JSON-LD `mainEntity` order and visible DOM FAQ order. Drift exposes rich-result penalty risk, not security. |
| Cross-subdomain `<a href={NEXT_PUBLIC_HK_URL}>` | Build-time env var, not user-supplied. No XSS surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-20 | Information Disclosure | JSON-LD `sgHomeSchema` exposing site structure to competitors | accept | SEO/LLMO is a core Phase 5 goal per core value statement; JSON-LD is public-by-design. |
| T-05-21 | Tampering | Static copy drift from strategy PART 6C if executor paraphrases CTA labels | mitigate | Vitest Wave-0 scaffold (from Plan 05-01) asserts exact H1 + CTA text strings; `pnpm test:unit` blocks regression. |
| T-05-22 | Denial of Service | Hero poster image missing → layout shift + broken image | accept | D-07 broken-image fallback per Phase 3 D-10 precedent; CLS impact limited because `<Image fill>` reserves aspect-ratio space. |
| T-05-23 | Denial of Service | VideoPlayer hydration failure on old browsers | accept | VideoPlayer Phase 2 primitive wraps MuxPlayer with internal `dynamic({ssr:false})`; graceful non-play fallback to poster. |
</threat_model>

<verification>
1. `pnpm typecheck` — zero errors.
2. `pnpm test:unit -- --run app/sg/page.test.ts` — GREEN (turns from RED to GREEN in this plan).
3. `pnpm test:unit -- --run tests/no-sg-placeholder-leak.test.ts` — still GREEN.
4. `pnpm build` — `/sg/` route compiles; opengraph-image route (from Plan 05-02) renders; homepage route renders.
5. `grep -c "sg-placeholder" app/sg/page.tsx components/sg/sg-hero-video.tsx` → 0.
6. Human visual verification passes (Task 3 checkpoint).
7. Lighthouse mobile audit (optional, not blocking at Phase 5): target LCP < 3.5s with poster fallback; target Accessibility ≥ 95.
</verification>

<success_criteria>
- Requirement SG-01 fully addressed: SG homepage per PART 5 wireframe + PART 6C copy, 13 sections, MultiBall differentiator at 3 homepage placements, Book CTA above the fold mobile, FAQPage JSON-LD valid.
- Homepage Vitest regression turns GREEN.
- Placeholder-leak guard remains GREEN (no `sg-placeholder` in page source).
- Every CTA labelled per UI-SPEC §Copywriting (verb + noun pair): `Book a Free Trial`, `Send an Enquiry`, `Chat on WhatsApp`.
- JSON-LD `@graph` contains `WebSite` + `FAQPage`; FAQPage `mainEntity` order matches DOM FAQ order.
- Mobile reshuffle: Sports+MultiBall zone card renders first on mobile (order-first), reading order on desktop.
- Human checkpoint approval logged in 05-03-SUMMARY.md.
</success_criteria>

<output>
After completion, create `.planning/phases/05-singapore-market/05-03-SUMMARY.md` with:
- Task 1 + Task 2 + Task 3 outputs
- Test status: `app/sg/page.test.ts` transitioned from RED → GREEN
- HUMAN-ACTION satisfaction record: Mux playback ID (set / unset), hero poster (landed / broken-image fallback)
- Lighthouse report paste if Martin ran one (optional)
- Checkpoint approval quote from Martin
- Any copy/CTA deviations from UI-SPEC §Copywriting or strategy PART 6C (Rule 1 adaptations)
- Link to 05-04, 05-05, 05-06 plans (all Wave 3; can parallelize in execution)
</output>

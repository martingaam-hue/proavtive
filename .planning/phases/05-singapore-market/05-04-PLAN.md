---
phase: 05-singapore-market
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - app/sg/weekly-classes/page.tsx
  - app/sg/weekly-classes/opengraph-image.tsx
  - app/sg/weekly-classes/movement/page.tsx
  - app/sg/weekly-classes/sports-multiball/page.tsx
  - app/sg/weekly-classes/sports-multiball/opengraph-image.tsx
  - app/sg/weekly-classes/climbing/page.tsx
  - app/sg/prodigy-camps/page.tsx
  - app/sg/prodigy-camps/opengraph-image.tsx
  - app/sg/prodigy-camps/themed/page.tsx
  - app/sg/prodigy-camps/multi-activity/page.tsx
  - app/sg/prodigy-camps/gymnastics/page.tsx
  - components/sg/zones-pillar-nav.tsx
  - components/sg/camps-pillar-nav.tsx
  - components/sg/active-sg-nav-link.tsx
autonomous: true
requirements: [SG-03, SG-04]

must_haves:
  truths:
    - "Weekly Classes pillar at /weekly-classes/ renders 3-zone overview with ZonesPillarNav listing Movement / Sports+MultiBall / Climbing"
    - "Prodigy Camps pillar at /prodigy-camps/ renders 3-camp-type overview with CampsPillarNav listing Themed / Multi-Activity / Gymnastics"
    - "Each zone sub-page renders unique H1 from SG_ZONES data with age-band + class-structure copy"
    - "Each camp-type sub-page renders unique H1 from SG_CAMP_TYPES data with type-specific copy"
    - "/weekly-classes/sports-multiball/ carries a MultiBall spotlight section with green Singapore's-only badge + dedicated What-is-MultiBall block per Pattern 11 #4"
    - "Active-state pillar chip highlights current route correctly (exact-match, NOT startsWith — avoids pillar-root all-active bug per Pitfall 8)"
    - "All pillar + sub pages include BreadcrumbList JSON-LD; pillar pages include FAQPage JSON-LD from grouped SG_FAQ_ITEMS"
  artifacts:
    - path: "components/sg/zones-pillar-nav.tsx"
      provides: "RSC nav wrapping ActiveSGNavLink for 3 zone chips"
      contains: "SG_ZONES"
    - path: "components/sg/camps-pillar-nav.tsx"
      provides: "RSC nav wrapping ActiveSGNavLink for 3 camp-type chips"
      contains: "SG_CAMP_TYPES"
    - path: "components/sg/active-sg-nav-link.tsx"
      provides: "Client usePathname exact-match chip with active/inactive states"
      contains: "\"use client\""
    - path: "app/sg/weekly-classes/page.tsx"
      provides: "Weekly Classes pillar index with 3-zone overview + BreadcrumbList + FAQPage JSON-LD"
      contains: "/weekly-classes/movement/"
    - path: "app/sg/weekly-classes/movement/page.tsx"
      provides: "Movement zone sub-page ages 2–5"
      contains: "Movement Zone"
    - path: "app/sg/weekly-classes/sports-multiball/page.tsx"
      provides: "Sports+MultiBall zone sub-page WITH MultiBall spotlight (Pattern 11 #4)"
      contains: "What is"
      min_lines: 120
    - path: "app/sg/weekly-classes/climbing/page.tsx"
      provides: "Climbing zone sub-page — MUST NOT reference sg-placeholder-climbing-unsplash-trinks"
      contains: "Climbing Zone"
    - path: "app/sg/prodigy-camps/page.tsx"
      provides: "Prodigy Camps pillar index"
      contains: "/prodigy-camps/themed/"
    - path: "app/sg/prodigy-camps/themed/page.tsx"
      provides: "Themed Camps sub-page (Ninja · Pokémon · Superhero · LEGO · STEAM)"
      contains: "Themed"
    - path: "app/sg/prodigy-camps/multi-activity/page.tsx"
      provides: "Multi-Activity Camps sub-page"
      contains: "Multi-Activity"
    - path: "app/sg/prodigy-camps/gymnastics/page.tsx"
      provides: "Gymnastics Camps sub-page"
      contains: "Gymnastics"
  key_links:
    - from: "ZonesPillarNav"
      to: "ActiveSGNavLink (client) via props"
      via: "each zone chip = <ActiveSGNavLink href label ageBand>"
      pattern: "ActiveSGNavLink"
    - from: "Each zone sub-page"
      to: "SG_ZONES.find((z) => z.slug === ...)"
      via: "data-driven template"
      pattern: "SG_ZONES.find"
    - from: "sports-multiball page MultiBall spotlight"
      to: "strategy PART 6C FAQ §4"
      via: "verbatim copy"
      pattern: "What is.*MultiBall"
---

<objective>
Ship the two SG pillar architectures: Weekly Classes (3 zones: Movement / Sports+MultiBall / Climbing) and Prodigy Camps (3 types: Themed / Multi-Activity / Gymnastics). Per D-03, D-04, D-07 + UI-SPEC §4 pillar rows + §5.5 ZonesPillarNav + §5.6 CampsPillarNav + PATTERNS §app/sg/weekly-classes/ + PATTERNS §app/sg/prodigy-camps/.

Creates 11 page files + 3 nav components + 3 per-route OG files. Structure: one shared pillar-index page template + one shared sub-page template (both data-driven by SG_ZONES / SG_CAMP_TYPES arrays from Plan 05-01). Sports+MultiBall sub-page adds a dedicated MultiBall spotlight section per Pattern 11 #4 (the Pattern 11 placements 4 land here — 3 placements already covered by Plan 05-03 homepage).

Output: 14 files total — 11 page.tsx + 2 opengraph-image.tsx (pillar-level) + 1 opengraph-image.tsx (sports-multiball spotlight) + 3 new components (zones-pillar-nav, camps-pillar-nav, active-sg-nav-link).

Wait — recount based on files_modified: 14 files. 11 page files + 3 OG files + 3 components = 17 entries but actually per frontmatter: 11 file paths listed + 3 components. Let me recount in files_modified frontmatter: app/sg/weekly-classes + /opengraph-image + /movement + /sports-multiball + /sports-multiball/opengraph-image + /climbing = 6 files under weekly-classes; app/sg/prodigy-camps + /opengraph-image + /themed + /multi-activity + /gymnastics = 5 files under prodigy-camps; 3 components = 14 total. Verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-singapore-market/05-CONTEXT.md
@.planning/phases/05-singapore-market/05-PATTERNS.md
@.planning/phases/05-singapore-market/05-UI-SPEC.md
@.planning/phases/05-singapore-market/05-RESEARCH.md
@.planning/phases/05-singapore-market/05-01-SUMMARY.md
@.planning/phases/05-singapore-market/05-02-SUMMARY.md
@app/hk/gymnastics/page.tsx
@app/hk/gymnastics/toddlers/page.tsx
@components/hk/gymnastics-pillar-nav.tsx
@components/hk/active-gym-nav-link.tsx
@lib/sg-data.ts
@lib/og-image.tsx
@app/hk/wan-chai/opengraph-image.tsx
@.planning/inputs/strategy.md

<interfaces>
From lib/sg-data.ts (Plan 05-01):
- `SG_ZONES: readonly SGZone[]` — 3 entries with fields { slug, href, label, ageBand, tag?, metaTitle, metaDescription, h1, whatTheyLearn, classStructure }
- `SG_CAMP_TYPES: readonly SGCampType[]` — 3 entries with same shape
- `SG_FAQ_ITEMS: readonly SGFAQItem[]` — grouped: "classes" filter for zone pillar FAQs, "camps" filter for camps pillar FAQs

From lib/og-image.tsx (Plan 05-01):
- `createSGOgImage({ title, tagline }): Promise<ImageResponse>` — Prodigy-green background

From components/hk/gymnastics-pillar-nav.tsx (analog, 25 lines):
- RSC wrapper mapping data array to ActiveGymNavLink chips

From components/hk/active-gym-nav-link.tsx (analog, 43 lines):
- `"use client"` + usePathname exact-match pattern
- Active: bg-brand-navy text-white; Inactive: bg-muted hover:bg-brand-navy/10

From app/hk/gymnastics/page.tsx (pillar analog, ~200 lines):
- metadata, BreadcrumbList + FAQPage JSON-LD, pillar overview with 3-col grid of programme cards

From app/hk/gymnastics/toddlers/page.tsx (sub-page analog):
- Shared sub-page template: metadata from programme data + BreadcrumbList + hero + whatTheyLearn list + classStructure + CTA

From app/hk/wan-chai/opengraph-image.tsx (analog, 14 lines):
- Per-route OG: `import { createHKOgImage }...` + export default function calling with route-specific title
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pillar nav components — ZonesPillarNav + CampsPillarNav + ActiveSGNavLink</name>
  <files>components/sg/zones-pillar-nav.tsx, components/sg/camps-pillar-nav.tsx, components/sg/active-sg-nav-link.tsx</files>
  <read_first>
    - components/hk/gymnastics-pillar-nav.tsx (analog — 25 lines)
    - components/hk/active-gym-nav-link.tsx (analog — 43 lines)
    - .planning/phases/05-singapore-market/05-PATTERNS.md lines 423-497 (pillar nav + active-chip exact patterns)
    - .planning/phases/05-singapore-market/05-UI-SPEC.md §5.5 (ZonesPillarNav spec — Sports+MultiBall chip carries Singapore's-only Badge inline), §5.6 (CampsPillarNav spec — uniform, no special badge)
    - .planning/phases/05-singapore-market/05-RESEARCH.md §Pitfall 2 (usePathname returns pre-rewrite URL, no /sg/ prefix) and §Pitfall 8 (exact-match, NOT startsWith — otherwise pillar-root shows all chips active)
    - lib/sg-data.ts (Plan 05-01: SG_ZONES, SG_CAMP_TYPES)
  </read_first>
  <behavior>
    - `components/sg/active-sg-nav-link.tsx` is `"use client"`. Props: `{ href: string; label: string; ageBand: string; badge?: React.ReactNode }`. Uses `usePathname()`. Active when `pathname === href || pathname === href.replace(/\/$/, "")`. Active chip: `bg-brand-navy text-white`. Inactive: `bg-muted text-foreground hover:bg-brand-navy/10`. Renders `<Link href={href} aria-current={isActive ? "page" : undefined} className={...}>` wrapping `<span className="font-sans font-semibold">{label}</span>` + `<span className="text-[11px] opacity-70 mt-0.5">{ageBand}</span>` + optional `{badge}`.
    - `components/sg/zones-pillar-nav.tsx` is an RSC. Renders `<nav aria-label="Prodigy zones" className="flex flex-wrap gap-2 md:gap-3">{SG_ZONES.map((z) => <ActiveSGNavLink key={z.slug} href={z.href} label={z.label} ageBand={z.ageBand} badge={z.slug === "sports-multiball" ? <Badge className="bg-brand-green text-white ml-2">Singapore's only</Badge> : undefined} />)}</nav>`.
    - `components/sg/camps-pillar-nav.tsx` is an RSC. Same shape as ZonesPillarNav but for `SG_CAMP_TYPES`. No special badge treatment — camp nav uniform per UI-SPEC §5.6.
  </behavior>
  <action>
    **Step 1 — components/sg/active-sg-nav-link.tsx (CREATE):**
    Read `components/hk/active-gym-nav-link.tsx` lines 1-43. Copy verbatim and rename:
    - Export interface `ActiveGymNavLinkProps` → `ActiveSGNavLinkProps`
    - Export function `ActiveGymNavLink` → `ActiveSGNavLink`
    - Add optional `badge?: React.ReactNode` prop + render slot after ageBand text
    - Keep `"use client"` directive at top
    - Keep `usePathname()` exact-match logic verbatim (per Pitfall 8)
    - Add comment: `// Phase 5 / Plan 05-04 — active-state pillar nav chip. Pitfall 2: usePathname() returns browser URL pre-rewrite (no /sg/ prefix). Pitfall 8: exact-match, NOT startsWith, so pillar-root shows no active chip.`

    **Step 2 — components/sg/zones-pillar-nav.tsx (CREATE):**
    Read `components/hk/gymnastics-pillar-nav.tsx` lines 1-25. Mirror structure:
    ```typescript
    // Phase 5 / Plan 05-04 — Weekly Classes pillar nav (RSC wrapping client chips).
    import { SG_ZONES } from "@/lib/sg-data";
    import { ActiveSGNavLink } from "@/components/sg/active-sg-nav-link";
    import { Badge } from "@/components/ui/badge";

    export function ZonesPillarNav() {
      return (
        <nav aria-label="Prodigy zones" className="flex flex-wrap gap-2 md:gap-3">
          {SG_ZONES.map((z) => (
            <ActiveSGNavLink
              key={z.slug}
              href={z.href}
              label={z.label}
              ageBand={z.ageBand}
              badge={z.slug === "sports-multiball" ? (
                <Badge className="bg-brand-green text-white ml-2 text-[10px]">Singapore's only</Badge>
              ) : undefined}
            />
          ))}
        </nav>
      );
    }
    ```

    **Step 3 — components/sg/camps-pillar-nav.tsx (CREATE):**
    Same shape as ZonesPillarNav with `SG_CAMP_TYPES` + `aria-label="Prodigy camp types"`. No badge prop on any chip:
    ```typescript
    // Phase 5 / Plan 05-04 — Prodigy Camps pillar nav (RSC wrapping client chips).
    import { SG_CAMP_TYPES } from "@/lib/sg-data";
    import { ActiveSGNavLink } from "@/components/sg/active-sg-nav-link";

    export function CampsPillarNav() {
      return (
        <nav aria-label="Prodigy camp types" className="flex flex-wrap gap-2 md:gap-3">
          {SG_CAMP_TYPES.map((c) => (
            <ActiveSGNavLink
              key={c.slug}
              href={c.href}
              label={c.label}
              ageBand={c.ageBand}
            />
          ))}
        </nav>
      );
    }
    ```
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `components/sg/active-sg-nav-link.tsx` starts with `"use client"`
    - File `components/sg/active-sg-nav-link.tsx` contains `usePathname`
    - File `components/sg/active-sg-nav-link.tsx` contains `aria-current`
    - File `components/sg/active-sg-nav-link.tsx` contains `bg-brand-navy text-white` (active state)
    - File `components/sg/active-sg-nav-link.tsx` contains `badge?:` OR `badge:` (optional badge prop)
    - File `components/sg/zones-pillar-nav.tsx` contains `SG_ZONES`
    - File `components/sg/zones-pillar-nav.tsx` contains `ActiveSGNavLink`
    - File `components/sg/zones-pillar-nav.tsx` contains `Singapore's only` (Sports+MultiBall badge)
    - File `components/sg/zones-pillar-nav.tsx` contains `aria-label="Prodigy zones"`
    - File `components/sg/camps-pillar-nav.tsx` contains `SG_CAMP_TYPES`
    - File `components/sg/camps-pillar-nav.tsx` contains `aria-label="Prodigy camp types"`
    - File `components/sg/camps-pillar-nav.tsx` does NOT contain `bg-brand-green` (uniform, no badge)
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Three nav components ready. Pillar pages and sub-pages in Task 2 and Task 3 can import ZonesPillarNav / CampsPillarNav.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Weekly Classes pillar + 3 zone sub-pages + MultiBall spotlight (sports-multiball)</name>
  <files>app/sg/weekly-classes/page.tsx, app/sg/weekly-classes/opengraph-image.tsx, app/sg/weekly-classes/movement/page.tsx, app/sg/weekly-classes/sports-multiball/page.tsx, app/sg/weekly-classes/sports-multiball/opengraph-image.tsx, app/sg/weekly-classes/climbing/page.tsx</files>
  <read_first>
    - app/hk/gymnastics/page.tsx (pillar analog — ~200 lines)
    - app/hk/gymnastics/toddlers/page.tsx (sub-page analog — shared template across HK's 8 sub-pages)
    - .planning/phases/05-singapore-market/05-PATTERNS.md lines 952-1007 (weekly-classes pillar + zone sub-page patterns + Sports+MultiBall spotlight addition)
    - .planning/phases/05-singapore-market/05-UI-SPEC.md §4 /weekly-classes/ pillar row + movement-zone/sports-zone/climbing-zone sub-page rows (slug reconciliation: CONTEXT D-03 canonical slugs are `movement`, `sports-multiball`, `climbing` per UI-SPEC §5.1 note), §Spacing MultiBall spotlight `py-section-lg` exception, §3 MultiBall Pattern 11 #4
    - .planning/phases/05-singapore-market/05-CONTEXT.md D-03 (zone dropdown + slugs)
    - .planning/inputs/strategy.md §PART 6C §4 (zone copy verbatim: movement / sports+multiball / climbing descriptions), §FAQ §4 (What is MultiBall? answer verbatim for spotlight section)
    - lib/sg-data.ts (Plan 05-01: SG_ZONES + SG_FAQ_ITEMS with "classes" and "multiball" groups)
    - lib/og-image.tsx (Plan 05-01: createSGOgImage)
    - app/sg/weekly-classes/pillar.test.ts (Plan 05-01 Wave-0 scaffold — this plan makes it GREEN)
    - app/sg/weekly-classes/sports-multiball/page.test.ts (Plan 05-01 Wave-0 scaffold)
  </read_first>
  <behavior>
    - `app/sg/weekly-classes/page.tsx` (pillar index): Exports metadata; inlines BreadcrumbList (Home → Weekly Classes) + FAQPage (filter SG_FAQ_ITEMS where `group === "classes"`) JSON-LD; renders pillar intro H1 "Weekly Classes at Prodigy Singapore", ZonesPillarNav, 3-col `<Card>` grid of zones (links to sub-pages), shared pillar FAQ section, Book-a-Free-Trial CTA.
    - `app/sg/weekly-classes/opengraph-image.tsx`: calls `createSGOgImage({ title: "Weekly Classes — Prodigy Singapore", tagline: "Movement · Sports + MultiBall · Climbing zones at Katong Point." })`.
    - `app/sg/weekly-classes/movement/page.tsx`: zone sub-page — `const ZONE = SG_ZONES.find((z) => z.slug === "movement")!;` — uses ZONE.h1, ZONE.metaTitle, ZONE.metaDescription, ZONE.whatTheyLearn list, ZONE.classStructure. BreadcrumbList (Home → Weekly Classes → Movement). ZonesPillarNav active on Movement. CTA `/book-a-trial/?subject=movement-zone`.
    - `app/sg/weekly-classes/sports-multiball/page.tsx`: same template + dedicated MultiBall spotlight section per Pattern 11 #4 ABOVE the standard sub-page body. Spotlight: `<Section size="lg" bg="default">` + 2-col grid + left col: `<Badge className="bg-brand-green text-white mb-4">Singapore's only</Badge>` + H2 `What is <span className="font-accent text-brand-green">MultiBall</span>?` + body paragraph VERBATIM from strategy PART 6C FAQ §4 answer + right col: `<Image src="/photography/sg-multiball-action.webp" alt="..." fill priority>` inside `aspect-[4/3] rounded-xl overflow-hidden`.
    - `app/sg/weekly-classes/sports-multiball/opengraph-image.tsx`: per-route OG (high-priority per UI-SPEC §7) — `createSGOgImage({ title: "Sports + MultiBall Zone", tagline: "Singapore's only MultiBall wall — at Prodigy, Katong Point." })`.
    - `app/sg/weekly-classes/climbing/page.tsx`: zone sub-page for climbing. MUST NOT reference `/photography/sg-placeholder-climbing-unsplash-trinks.*` (Pitfall 3 — real Katong climbing photo required, HUMAN-ACTION D-07 gate 2).
    - Both sub-pages use the same shell template driven by SG_ZONES data — only sports-multiball has the extra spotlight section prepended.
  </behavior>
  <action>
    **Step 1 — Shared sub-page template (zone):**
    Design a reusable section shape the 3 zone sub-pages consume. Since Next.js RSC pages must be individual page.tsx files, "shared" means each page.tsx imports the same utility helpers and data:

    Each zone sub-page (3 files) does the following:
    ```typescript
    // app/sg/weekly-classes/{slug}/page.tsx
    import type { Metadata } from "next";
    import Link from "next/link";
    import Image from "next/image";
    import { ArrowRight } from "lucide-react";
    import { Section } from "@/components/ui/section";
    import { ContainerEditorial } from "@/components/ui/container-editorial";
    import { Button } from "@/components/ui/button";
    import { Badge } from "@/components/ui/badge";
    import { ZonesPillarNav } from "@/components/sg/zones-pillar-nav";
    import { SG_ZONES } from "@/lib/sg-data";

    const ZONE = SG_ZONES.find((z) => z.slug === "{slug}")!;  // "movement" | "sports-multiball" | "climbing"

    export const metadata: Metadata = {
      title: ZONE.metaTitle,
      description: ZONE.metaDescription,
      openGraph: {
        title: ZONE.metaTitle,
        description: ZONE.metaDescription,
        url: `https://sg.proactivsports.com${ZONE.href}`,
        siteName: "Prodigy by ProActiv Sports — Singapore",
        locale: "en_SG",
        type: "website",
        images: [{ url: `/opengraph-image`, width: 1200, height: 630, alt: ZONE.label }],
      },
      alternates: { canonical: `https://sg.proactivsports.com${ZONE.href}` },
    };

    const breadcrumbSchema = {
      "@context": "https://schema.org",
      "@type": "BreadcrumbList",
      itemListElement: [
        { "@type": "ListItem", position: 1, name: "Prodigy Singapore", item: "https://sg.proactivsports.com/" },
        { "@type": "ListItem", position: 2, name: "Weekly Classes", item: "https://sg.proactivsports.com/weekly-classes/" },
        { "@type": "ListItem", position: 3, name: ZONE.label, item: `https://sg.proactivsports.com${ZONE.href}` },
      ],
    };

    export default function ZonePage() {
      return (
        <>
          <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(breadcrumbSchema) }} />
          {/* Hero + zone-pillar-nav + whatTheyLearn + classStructure + CTA */}
        </>
      );
    }
    ```

    Body structure per UI-SPEC §4 zone sub-page row:
    1. `<Section size="md">` with ZonesPillarNav at top.
    2. `<Section size="md">` hero: H1 = ZONE.h1 (font-display). Subhead = ZONE.metaDescription. Badge = ZONE.tag ? `<Badge>{ZONE.tag}</Badge>` : null.
    3. Hero image: `<Image src={ZONE.heroImage ?? "/photography/sg-zone-{slug}.webp"} alt="..." width=... height=... />` — HUMAN-ACTION D-07 gate 2.
    4. "What children learn" section: `<ul>{ZONE.whatTheyLearn.map((x) => <li>)}</ul>` — bullet list from data.
    5. "Class structure" prose section: `<p>{ZONE.classStructure}</p>`.
    6. Safety note section (generic — same on all 3 zones).
    7. Booking CTA: `<Button asChild className="bg-brand-red"><a href={`/book-a-trial/?subject=${ZONE.slug}-zone`}>Book a Free Trial</a></Button>`.

    **Step 2 — Sports+MultiBall spotlight exception** (on app/sg/weekly-classes/sports-multiball/page.tsx only):
    ABOVE the ZonesPillarNav + hero, prepend:
    ```tsx
    {/* MultiBall spotlight per Pattern 11 #4 — dedicated section calling out Singapore's only MultiBall wall */}
    <Section size="lg" bg="default">
      <ContainerEditorial width="default">
        <div className="grid grid-cols-1 md:grid-cols-2 gap-8 items-center">
          <div>
            <Badge className="bg-brand-green text-white mb-4">Singapore's only</Badge>
            <h2 className="text-h2 font-display">
              What is <span className="font-accent text-brand-green">MultiBall</span>?
            </h2>
            <p className="text-body-lg mt-4 text-muted-foreground">
              {/* VERBATIM copy from strategy PART 6C FAQ §4 answer — do not paraphrase */}
            </p>
          </div>
          <div className="relative aspect-[4/3] rounded-xl overflow-hidden">
            <Image
              src="/photography/sg-multiball-action.webp"
              alt="Child playing on the MultiBall wall at Prodigy, Katong Point"
              fill
              priority
              sizes="(max-width: 768px) 100vw, 50vw"
              className="object-cover"
            />
          </div>
        </div>
      </ContainerEditorial>
    </Section>
    ```

    **Step 3 — Pillar index (app/sg/weekly-classes/page.tsx):**
    Read `app/hk/gymnastics/page.tsx` (pillar analog ~200 lines). Mirror structure:
    - Metadata per UI-SPEC §4 pillar row
    - Inline `BreadcrumbList` + `FAQPage` (from `SG_FAQ_ITEMS.filter((i) => i.group === "classes")`) as `@graph` pair
    - H1: "Weekly Classes at Prodigy Singapore."
    - Intro paragraph verbatim from strategy PART 6C §4 opening
    - `<ZonesPillarNav />` component
    - Feature cards: 3-col grid mapping SG_ZONES to `<Card>` with zone label (font-display H3), ageBand, oneLiner, "Explore zone →" link to ZONE.href. Sports+MultiBall card carries green badge.
    - Shared FAQ section (using filtered SG_FAQ_ITEMS) — `<div>` of `<FAQItem>` per PATTERNS §FAQItem Composition
    - Final Book CTA

    **Step 4 — OG images:**
    `app/sg/weekly-classes/opengraph-image.tsx`:
    ```typescript
    import { createSGOgImage } from "@/lib/og-image";
    export const runtime = "nodejs";
    export const dynamic = "force-dynamic";
    export const size = { width: 1200, height: 630 };
    export const contentType = "image/png";
    export default function OG() {
      return createSGOgImage({
        title: "Weekly Classes",
        tagline: "Movement · Sports + MultiBall · Climbing zones at Prodigy, Katong Point.",
      });
    }
    ```

    `app/sg/weekly-classes/sports-multiball/opengraph-image.tsx`: same pattern with title "Sports + MultiBall Zone" + tagline "Singapore's only MultiBall wall — at Prodigy, Katong Point.".

    **Step 5 — Climbing pitfall guard:**
    In `app/sg/weekly-classes/climbing/page.tsx` verify the image src uses real Katong climbing photo path (e.g. `/photography/sg-zone-climbing.webp` — HUMAN-ACTION D-07 gate 2). The string "sg-placeholder" MUST NOT appear anywhere in any sub-page file. Add explicit comment: `// D-07 gate 2: HUMAN-ACTION — real Katong climbing photo required. NEVER substitute sg-placeholder-climbing-unsplash-trinks.* here (Pitfall 3).`

    **Step 6 — Tests:**
    Run `pnpm test:unit -- --run app/sg/weekly-classes/` — should make:
    - `app/sg/weekly-classes/pillar.test.ts` GREEN (3 zone links with correct hrefs)
    - `app/sg/weekly-classes/sports-multiball/page.test.ts` GREEN (H1 + Singapore's only Badge)
    - `tests/no-sg-placeholder-leak.test.ts` still GREEN (no placeholder string in any sub-page)
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm test:unit -- --run app/sg/weekly-classes tests/no-sg-placeholder-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `app/sg/weekly-classes/page.tsx` contains `ZonesPillarNav`
    - File `app/sg/weekly-classes/page.tsx` contains `/weekly-classes/movement/`
    - File `app/sg/weekly-classes/page.tsx` contains `/weekly-classes/sports-multiball/`
    - File `app/sg/weekly-classes/page.tsx` contains `/weekly-classes/climbing/`
    - File `app/sg/weekly-classes/page.tsx` contains `BreadcrumbList` AND `FAQPage` (JSON-LD)
    - File `app/sg/weekly-classes/opengraph-image.tsx` contains `createSGOgImage`
    - File `app/sg/weekly-classes/movement/page.tsx` contains `SG_ZONES.find` AND `"movement"`
    - File `app/sg/weekly-classes/movement/page.tsx` contains `BreadcrumbList`
    - File `app/sg/weekly-classes/sports-multiball/page.tsx` contains `SG_ZONES.find` AND `"sports-multiball"`
    - File `app/sg/weekly-classes/sports-multiball/page.tsx` contains `Singapore's only` (Badge)
    - File `app/sg/weekly-classes/sports-multiball/page.tsx` contains `What is` AND `MultiBall` (spotlight H2)
    - File `app/sg/weekly-classes/sports-multiball/page.tsx` contains `size="lg"` (section for spotlight block per UI-SPEC §Spacing exception)
    - File `app/sg/weekly-classes/sports-multiball/opengraph-image.tsx` contains `createSGOgImage`
    - File `app/sg/weekly-classes/climbing/page.tsx` contains `SG_ZONES.find` AND `"climbing"`
    - `grep -r "sg-placeholder-climbing-unsplash-trinks" app/sg/weekly-classes/` returns 0 (critical Pitfall 3 guard)
    - `grep -r "sg-placeholder" app/sg/weekly-classes/` returns 0
    - `pnpm test:unit -- --run app/sg/weekly-classes/pillar.test.ts` passes (Wave-0 scaffold GREEN)
    - `pnpm test:unit -- --run app/sg/weekly-classes/sports-multiball/page.test.ts` passes
    - `pnpm test:unit -- --run tests/no-sg-placeholder-leak.test.ts` passes
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Weekly Classes pillar + 3 zone sub-pages + MultiBall spotlight ship. Pattern 11 #4 placement live on sports-multiball page. Wave-0 pillar + sports-multiball tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prodigy Camps pillar + 3 camp-type sub-pages</name>
  <files>app/sg/prodigy-camps/page.tsx, app/sg/prodigy-camps/opengraph-image.tsx, app/sg/prodigy-camps/themed/page.tsx, app/sg/prodigy-camps/multi-activity/page.tsx, app/sg/prodigy-camps/gymnastics/page.tsx</files>
  <read_first>
    - app/sg/weekly-classes/page.tsx (Task 2 output — mirror the pillar pattern for camps)
    - app/sg/weekly-classes/movement/page.tsx (Task 2 output — mirror the sub-page template for camps)
    - .planning/phases/05-singapore-market/05-PATTERNS.md lines 1003-1009 (prodigy-camps pillar + 3 camp-type subs — analog same as zones)
    - .planning/phases/05-singapore-market/05-UI-SPEC.md §4 /prodigy-camps/ pillar row + themed/multi-activity/gymnastics camp sub-page rows
    - .planning/phases/05-singapore-market/05-CONTEXT.md D-04 (camp dropdown + slugs: themed, multi-activity, gymnastics)
    - .planning/inputs/strategy.md §PART 6C §6 (camp type descriptions verbatim: themed theme list, multi-activity rotation, gymnastics focus)
    - lib/sg-data.ts (Plan 05-01: SG_CAMP_TYPES + SG_FAQ_ITEMS group "camps")
    - app/sg/prodigy-camps/pillar.test.ts (Plan 05-01 Wave-0 scaffold)
  </read_first>
  <behavior>
    - `app/sg/prodigy-camps/page.tsx`: pillar index mirroring Task 2 `app/sg/weekly-classes/page.tsx` structure. H1: "Prodigy Camps at Katong Point." CampsPillarNav component. 3-col card grid mapping SG_CAMP_TYPES. Shared FAQ (filter SG_FAQ_ITEMS group "camps"). CTA.
    - `app/sg/prodigy-camps/opengraph-image.tsx`: `createSGOgImage({ title: "Prodigy Camps", tagline: "Themed · Multi-Activity · Gymnastics camps at Katong Point every school holiday." })`.
    - `app/sg/prodigy-camps/themed/page.tsx`: camp sub-page driven by `SG_CAMP_TYPES.find((c) => c.slug === "themed")`. H1 = CAMP.h1. Theme list "Ninja · Pokémon · Superhero · LEGO · STEAM" per strategy PART 6C §6 + UI-SPEC §5.1 dropdown copy. "What's included" panel + age band + upcoming themes stub + Booking CTA `/book-a-trial/?subject=themed-camp`.
    - `app/sg/prodigy-camps/multi-activity/page.tsx`: same template for multi-activity camp. Copy from PART 6C §6.
    - `app/sg/prodigy-camps/gymnastics/page.tsx`: same template for gymnastics camps. Copy from PART 6C §6.
  </behavior>
  <action>
    **Step 1 — Pillar index (app/sg/prodigy-camps/page.tsx):**
    Copy the structure from Task 2's `app/sg/weekly-classes/page.tsx` (pillar) verbatim and swap:
    - Imports: `ZonesPillarNav` → `CampsPillarNav`; `SG_ZONES` → `SG_CAMP_TYPES`
    - H1 "Weekly Classes at Prodigy Singapore." → "Prodigy Camps at Katong Point."
    - Intro paragraph: swap in strategy PART 6C §6 opening
    - FAQ filter: `group === "classes"` → `group === "camps"`
    - BreadcrumbList: Weekly Classes → Prodigy Camps
    - Metadata: SG-camps-specific title/description
    - NO MultiBall badge on any card (camps don't carry the MultiBall differentiator the same way)
    - URLs: `/weekly-classes/` → `/prodigy-camps/`

    **Step 2 — 3 camp-type sub-pages:**
    Copy the structure from Task 2's `app/sg/weekly-classes/movement/page.tsx` verbatim for each of the 3 camp pages. Swap:
    - `ZonesPillarNav` → `CampsPillarNav`; `SG_ZONES` → `SG_CAMP_TYPES`
    - `.find((z) => z.slug === "movement")` → `.find((c) => c.slug === "{themed|multi-activity|gymnastics}")`
    - BreadcrumbList crumb: "Weekly Classes" → "Prodigy Camps"; zone slug → camp slug
    - Booking CTA subject: `?subject=${CAMP.slug}-camp` (becomes `?subject=themed-camp`, `?subject=multi-activity-camp`, `?subject=gymnastics-camp`)
    - Hero image: zone → camp (`/photography/sg-camp-{slug}.webp` — HUMAN-ACTION D-07)

    No spotlight section for camps (per UI-SPEC §4 camp rows — only sports-multiball has spotlight).

    **Step 3 — OG image:**
    `app/sg/prodigy-camps/opengraph-image.tsx` per Task 2 pattern with camps title/tagline. Sub-pages INHERIT the pillar-level OG (per UI-SPEC §7 OG image per-route budget — camp sub-pages don't need their own OG files).

    **Step 4 — Tests:**
    Run `pnpm test:unit -- --run app/sg/prodigy-camps/pillar.test.ts` — makes Wave-0 scaffold GREEN. Also run no-placeholder-leak guard.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm test:unit -- --run app/sg/prodigy-camps tests/no-sg-placeholder-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `app/sg/prodigy-camps/page.tsx` contains `CampsPillarNav`
    - File `app/sg/prodigy-camps/page.tsx` contains `/prodigy-camps/themed/`
    - File `app/sg/prodigy-camps/page.tsx` contains `/prodigy-camps/multi-activity/`
    - File `app/sg/prodigy-camps/page.tsx` contains `/prodigy-camps/gymnastics/`
    - File `app/sg/prodigy-camps/page.tsx` contains `BreadcrumbList` AND `FAQPage`
    - File `app/sg/prodigy-camps/opengraph-image.tsx` contains `createSGOgImage`
    - File `app/sg/prodigy-camps/themed/page.tsx` contains `SG_CAMP_TYPES.find` AND `"themed"`
    - File `app/sg/prodigy-camps/themed/page.tsx` contains `?subject=themed-camp`
    - File `app/sg/prodigy-camps/multi-activity/page.tsx` contains `SG_CAMP_TYPES.find` AND `"multi-activity"`
    - File `app/sg/prodigy-camps/multi-activity/page.tsx` contains `?subject=multi-activity-camp`
    - File `app/sg/prodigy-camps/gymnastics/page.tsx` contains `SG_CAMP_TYPES.find` AND `"gymnastics"`
    - File `app/sg/prodigy-camps/gymnastics/page.tsx` contains `?subject=gymnastics-camp`
    - `grep -r "sg-placeholder" app/sg/prodigy-camps/` returns 0
    - `pnpm test:unit -- --run app/sg/prodigy-camps/pillar.test.ts` passes (Wave-0 scaffold GREEN)
    - `pnpm test:unit -- --run tests/no-sg-placeholder-leak.test.ts` passes
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Prodigy Camps pillar + 3 camp sub-pages ship. Both pillar architectures (zones + camps) complete. SG-03 + SG-04 fully addressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client browser → ActiveSGNavLink.usePathname() | Reads browser URL client-side; no server data leakage. Pitfall 2 + 8 codified in implementation (exact-match, no /sg/ prefix). |
| Page source → strategy copy | Verbatim copy from trusted strategy.md source. |
| JSON-LD → SERP | Public-by-design rich-result markup; no user input in schema generation. |
| Per-route opengraph-image → Mux / filesystem | Same boundaries as Plan 05-02 default OG (build-time font + logo reads, graceful fallback). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-30 | Information Disclosure | BreadcrumbList JSON-LD exposing nested URL structure | accept | Public URL structure is discoverable anyway via crawl; JSON-LD is SEO-positive. |
| T-05-31 | Tampering | Wrong `SG_ZONES.find()` slug lookup returning undefined → TypeError at build | mitigate | `!` non-null assertion on find result is safe because slugs are enum-typed in SG_ZONES discriminated union (Plan 05-01); if slugs drift, typecheck catches at build. |
| T-05-32 | Spoofing | Stale `sg-placeholder-climbing-unsplash-trinks` URL leaking to climbing sub-page | mitigate | `tests/no-sg-placeholder-leak.test.ts` grep guard + explicit comment in climbing page warning against the specific filename. |
| T-05-33 | Denial of Service | Shared pillar + sub-page template pattern: one broken page template breaks all 3 zone sub-pages simultaneously | accept | All 3 sub-pages are separate page.tsx files; a typo in one affects only that route. Build catches syntax errors per-route. |
</threat_model>

<verification>
1. `pnpm typecheck` — zero errors across 14 files.
2. `pnpm test:unit` — Wave-0 scaffolds for pillar + sports-multiball + placeholder-leak guard all GREEN.
3. `pnpm build` — `/sg/weekly-classes/*` + `/sg/prodigy-camps/*` routes compile; pillar + sub-page + OG routes all resolve.
4. `grep -r "sg-placeholder" app/sg/weekly-classes/ app/sg/prodigy-camps/` → 0 matches.
5. `grep -c "createSGOgImage" app/sg/weekly-classes/opengraph-image.tsx app/sg/weekly-classes/sports-multiball/opengraph-image.tsx app/sg/prodigy-camps/opengraph-image.tsx` → 3 (per-route OG confirmed).
6. Smoke via `pnpm dev`: `http://sg.localhost:3000/weekly-classes/` shows 3 chips with Sports+MultiBall carrying green badge; click into each zone — H1 + whatTheyLearn list correct; sports-multiball spotlight section renders above the sub-page body.
</verification>

<success_criteria>
- Requirements SG-03 + SG-04 fully addressed: Weekly Classes pillar + 3 zones; Prodigy Camps pillar + 3 types.
- Pattern 11 #4 MultiBall placement live on `/weekly-classes/sports-multiball/`.
- Pillar navs (ZonesPillarNav + CampsPillarNav) render correct active state (exact-match, Pitfall 8).
- Per-route OG images for pillar + sports-multiball spotlight (3 OG files).
- Booking CTAs use correct subject pre-fill: `movement-zone`, `sports-multiball-zone`, `climbing-zone`, `themed-camp`, `multi-activity-camp`, `gymnastics-camp`, `general-enquiry` — taxonomy matches UI-SPEC §6 subject-pre-fill table.
- Placeholder-leak guard remains GREEN.
- Wave-0 tests for pillar.test.ts + sports-multiball/page.test.ts + pillar.test.ts (camps) + prodigy-camps/pillar.test.ts all GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/05-singapore-market/05-04-SUMMARY.md` with:
- Task 1 + Task 2 + Task 3 outputs
- Test status: all Wave-0 scaffolds addressed by this plan turn GREEN
- HUMAN-ACTION list: 4 zone hero images + 3 camp hero images + MultiBall spotlight action photo — all D-07 gate 2 items
- Any slug reconciliation notes (canonical: movement, sports-multiball, climbing for zones; themed, multi-activity, gymnastics for camps)
- Deviations from UI-SPEC (Rule 1 adaptations)
- Link to 05-05, 05-06 plans (Wave 3 parallelizable)
</output>

---
phase: 05-singapore-market
plan: 06
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - app/sg/book-a-trial/page.tsx
  - app/sg/book-a-trial/booking-form.tsx
  - app/sg/book-a-trial/opengraph-image.tsx
autonomous: false
requirements: [SG-11]
user_setup:
  - service: Resend (existing — Phase 3)
    why: "SG booking submissions dispatch via existing Resend integration; market: 'sg' routes to CONTACT_INBOX_SG."
    env_vars:
      - name: CONTACT_INBOX_SG
        source: "User-owned SG inbox — already documented in .env.example (Plan 05-01)."
      - name: NEXT_PUBLIC_WHATSAPP_SG
        source: "Strategy PART 8.3: +6598076827 — renders WhatsApp escape hatch on error."

must_haves:
  truths:
    - "/book-a-trial/ renders a single-page booking form (no hub→form two-step; SG has one venue so no venue selector)"
    - "Booking form submits payload with market: 'sg' and venue: 'katong-point' hardcoded (D-10)"
    - "Subject query param from any CTA (?subject=birthday-party, ?subject=themed-camp, etc.) pre-fills the subject field"
    - "Form includes honeypot 'bot-trap' field (Phase 3 D-04 required for existing /api/contact handler)"
    - "Submit flow hits /api/contact which is already extended with ALLOWED_VENUES including 'katong-point' (Plan 05-01)"
    - "Success/error states render per UI-SPEC §Copywriting (success heading 'Thanks — your free trial request is in.'; error retry + WhatsApp escape)"
    - "Booking page + form Wave-0 test turns GREEN (market:'sg' submission + subject pre-fill assertions)"
  artifacts:
    - path: "app/sg/book-a-trial/page.tsx"
      provides: "RSC shell with Suspense + metadata + hero copy, wrapping BookingForm client"
      contains: "Suspense"
    - path: "app/sg/book-a-trial/booking-form.tsx"
      provides: "Client form component with single-venue payload, subject pre-fill, honeypot, 6 fields + submit"
      contains: "\"use client\""
      min_lines: 200
    - path: "app/sg/book-a-trial/opengraph-image.tsx"
      provides: "Per-route OG for booking page"
      contains: "createSGOgImage"
  key_links:
    - from: "app/sg/book-a-trial/booking-form.tsx submit handler"
      to: "/api/contact POST endpoint"
      via: "fetch with market + venue payload"
      pattern: "\"sg\".*\"katong-point\""
    - from: "app/sg/book-a-trial/booking-form.tsx"
      to: "useSearchParams from next/navigation"
      via: "subject pre-fill from ?subject= query"
      pattern: "useSearchParams"
    - from: "app/sg/book-a-trial/page.tsx"
      to: "app/sg/book-a-trial/booking-form.tsx"
      via: "Suspense wrapper"
      pattern: "<BookingForm"
---

<objective>
Ship the SG booking conversion hub: `/book-a-trial/` as a single-page form (no two-step hub→form like HK, since SG has one venue — D-10 hardcodes `venue: "katong-point"`). RSC shell wraps client BookingForm with Suspense (required because form reads useSearchParams for subject pre-fill). Form reuses Phase 3/4 `/api/contact` handler with `market: "sg"` and extended `ALLOWED_VENUES` from Plan 05-01.

Per CONTEXT D-10 + UI-SPEC §4 /book-a-trial/ row + §5.7 BookingForm spec + §6 booking-form UX + PATTERNS §app/sg/book-a-trial/.

Output: 3 files — `page.tsx` (RSC shell), `booking-form.tsx` (client component, ~220 lines), `opengraph-image.tsx` (per-route OG).

This plan is NOT autonomous: ends with a `checkpoint:human-verify` gate confirming live booking submission works end-to-end (form → /api/contact → Resend → CONTACT_INBOX_SG inbox).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-singapore-market/05-CONTEXT.md
@.planning/phases/05-singapore-market/05-PATTERNS.md
@.planning/phases/05-singapore-market/05-UI-SPEC.md
@.planning/phases/05-singapore-market/05-RESEARCH.md
@.planning/phases/05-singapore-market/05-01-SUMMARY.md
@.planning/phases/05-singapore-market/05-02-SUMMARY.md
@app/hk/book-a-trial/free-assessment/page.tsx
@app/hk/book-a-trial/free-assessment/booking-form.tsx
@app/api/contact/route.ts
@emails/contact.tsx
@lib/sg-data.ts
@lib/og-image.tsx
@.planning/inputs/strategy.md

<interfaces>
From app/api/contact/route.ts (modified Plan 05-01):
- POST accepts JSON body with fields: `market: "hk" | "sg"`, `subject: string`, `venue?: "wan-chai" | "cyberport" | "no-preference" | "katong-point"`, `name`, `email`, `phone?`, `childAge?`, `message?`, `bot-trap` (honeypot)
- Returns: 200 success, 400 with `{ error | errors }` on validation failure, 200 silent on honeypot trip
- ALLOWED_VENUES extended with "katong-point" in Plan 05-01

From emails/contact.tsx (Phase 4 extended):
- ContactEmail props already accept `venue` + `childAge` — Phase 5 reuses without template changes (SG submissions render the same email template with Singapore subject prefix in the route handler)

From app/hk/book-a-trial/free-assessment/booking-form.tsx (analog, 367 lines):
- `"use client"` + `import { useSearchParams } from "next/navigation"`
- FormStatus = "idle" | "submitting" | "success" | "error"
- Uses uncontrolled form (native FormData) + `React.useState` for status + errors
- Fetch POST to /api/contact
- Venue selector at lines 225-276 — REMOVED for SG (single-venue simplification D-10)
- Honeypot input `name="bot-trap"` preserved verbatim
- Success + error cards per UI-SPEC §Copywriting

From app/hk/book-a-trial/free-assessment/page.tsx (analog, 55 lines):
- RSC with Suspense wrapping BookingForm (required for useSearchParams)
- Metadata + hero copy

From lib/sg-data.ts (Plan 05-01):
- KATONG_POINT.nameFull for success card copy
- SG market context for copy swaps
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BookingForm client component (single-venue, SG-market, subject pre-fill)</name>
  <files>app/sg/book-a-trial/booking-form.tsx</files>
  <read_first>
    - app/hk/book-a-trial/free-assessment/booking-form.tsx (analog — 367 lines)
    - .planning/phases/05-singapore-market/05-PATTERNS.md lines 1086-1200 (booking-form full mirror with venue-selector REMOVAL and single-venue payload, imports, state, submit handler, honeypot, success/error patterns)
    - .planning/phases/05-singapore-market/05-UI-SPEC.md §5.7 (field spec table: name, email, phone, childAge, subject, message, honeypot), §Copywriting (success/error verbatim copy), §6 (booking flow)
    - .planning/phases/05-singapore-market/05-CONTEXT.md D-10 (single-venue hardcoded, no selector)
    - app/sg/book-a-trial/booking-form.test.ts (Plan 05-01 Wave-0 scaffold — asserts market:"sg" + venue:"katong-point" + subject pre-fill)
  </read_first>
  <behavior>
    - File starts with `"use client"` directive.
    - Imports: `*, Link, useSearchParams, Loader2/CheckCircle2/AlertCircle/MessageCircle icons, Phase 2 primitives (Button, Card, Input, Label, Textarea), cn from lib/utils`.
    - Exports `BookingForm()` default function.
    - Reads `const searchParams = useSearchParams(); const prefilledSubject = searchParams?.get("subject") ?? "Free Assessment Request";`.
    - State: `status: "idle" | "submitting" | "success" | "error"` + `errors: Record<string, string>`.
    - `handleSubmit(e)`: preventDefault; setStatus("submitting"); read FormData; construct payload `{ market: "sg" as const, subject: prefilledSubject, venue: "katong-point" as const, name, email, phone?, childAge, message?, "bot-trap": formData.get("bot-trap") ?? "" }`; fetch POST to /api/contact; handle 200 → setStatus("success"); handle 400 with { errors } → setErrors; handle other failures → setStatus("error").
    - Success state: card with heading "Thanks — your free trial request is in." per UI-SPEC §Copywriting + body + two Follow-up CTAs (Back to Prodigy Singapore + Read the first-class guide).
    - Error state: card with heading "Something went wrong on our end." + retry button "Try sending again" + WhatsApp escape (conditional on `NEXT_PUBLIC_WHATSAPP_SG`).
    - Fields (idle state): Parent name (required), Email (required, type="email"), Phone (optional, type="tel"), Child age (required, type="number", 1-18), Subject (pre-filled editable text input, required), Message (optional textarea 500-char max), hidden honeypot `name="bot-trap"` (CSS-hidden per PATTERNS §Honeypot).
    - Submit button: red fill `bg-brand-red text-white` "Book free trial" label, becomes "Sending…" + Loader2 spinner + disabled during submission.
    - a11y: `aria-live="polite"` status region; label-for-input associations; focus management on state change.
    - NO venue selector (D-10 — remove analog lines 225-276).
  </behavior>
  <action>
    Read `app/hk/book-a-trial/free-assessment/booking-form.tsx` lines 1-367. Copy verbatim into `app/sg/book-a-trial/booking-form.tsx` with these surgical changes:

    **Change 1 — Imports + comments (lines 1-32):**
    - Swap file header comment: `// Phase 4 / Plan 04-07 — HK booking form...` → `// Phase 5 / Plan 05-06 — SG booking form.`
    - Add documentation comment: `// Per D-10: SG venue hardcoded to "katong-point" (single-venue simplification).`
    - Keep all imports verbatim (Button, Input, Label, Textarea, useSearchParams, icons) — no changes needed.

    **Change 2 — Remove venue state + default (no venue selector):**
    - Delete analog lines 65-68 (venue state declaration `const [venue, setVenue] = useState("no-preference");`) — SG has no state for venue.
    - Delete analog lines 225-276 (entire `<fieldset role="radiogroup">` venue selector — 3-card picker for HK venues). SG has ONE venue; no UI affordance needed.

    **Change 3 — handleSubmit payload:**
    Analog (HK) payload (line ~95):
    ```typescript
    const payload = {
      market: "hk" as const,
      venue,  // user-selected
      subject: prefilledSubject,
      // ... rest
    };
    ```
    SG replacement:
    ```typescript
    const payload = {
      market: "sg" as const,                   // was "hk"
      venue: "katong-point" as const,          // hardcoded — single SG venue (D-10)
      subject: prefilledSubject,               // from ?subject= query param
      name: formData.get("name"),
      email: formData.get("email"),
      phone: typeof phone === "string" && phone.length > 0 ? phone : undefined,
      childAge: formData.get("childAge"),
      message: typeof message === "string" && message.length > 0 ? message : undefined,
      "bot-trap": formData.get("bot-trap") ?? "",
    };
    ```

    **Change 4 — Success/error copy (lines 121-207 of analog):**
    - Success heading: "Thanks — your free trial request is in." (UI-SPEC §Copywriting)
    - Success body: "A member of our Prodigy team will reply within one working day to confirm a time. We'll prepare the right coach and zone for your child." (UI-SPEC §Copywriting)
    - Success follow-up CTAs: "Back to Prodigy Singapore" → `<Link href="/">` and "Read the first-class guide" → `<Link href="/blog/">` (UI-SPEC §Copywriting)
    - Error heading: "Something went wrong on our end."
    - Error body: "Your message didn't reach us — please try again, or WhatsApp Prodigy directly. We'll still see it."
    - Error retry: "Try sending again" (verb + noun pair per UI-SPEC §Copywriting)
    - WhatsApp escape: conditional on `process.env.NEXT_PUBLIC_WHATSAPP_SG` (hide chip if unset)
    - Swap ALL references to "ProActiv HK" / "ProGym" / "Hong Kong" / "Wan Chai" / "Cyberport" → "Prodigy" / "Singapore" / "Katong Point"

    **Change 5 — WhatsApp env var:**
    - `NEXT_PUBLIC_WHATSAPP_HK` → `NEXT_PUBLIC_WHATSAPP_SG` (in error card WhatsApp escape)

    **Change 6 — Honeypot PRESERVED verbatim:**
    Keep `<input type="text" name="bot-trap" tabIndex={-1} autoComplete="off" aria-hidden="true" style={{ position: "absolute", left: "-9999px", width: "1px", height: "1px" }} />` identical to HK analog. Phase 3 D-04 requires the field name "bot-trap" exactly (route handler checks this name verbatim at line 62).

    **Change 7 — Field labels:**
    - Parent name: "Your name"
    - Email: "Email"
    - Phone: "Phone (optional)"
    - Child age: "Child's age" (number min=1 max=18)
    - Subject: "What would you like to book or ask about?" (pre-filled from `?subject=` query, editable)
    - Message: "Anything we should know? (optional)" (textarea max 500 chars)
    - Submit button: "Book free trial" (becomes "Sending…" during submit)

    After creating file, run `pnpm test:unit -- --run app/sg/book-a-trial/booking-form.test.ts` — should make Wave-0 scaffold GREEN (fetch mock asserts market:"sg", venue:"katong-point", subject pre-fill from `?subject=birthday-party`).
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm test:unit -- --run app/sg/book-a-trial/booking-form.test.ts tests/no-sg-placeholder-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `app/sg/book-a-trial/booking-form.tsx` starts with `"use client"`
    - File `app/sg/book-a-trial/booking-form.tsx` contains `useSearchParams`
    - File `app/sg/book-a-trial/booking-form.tsx` contains `market: "sg"` (not "hk")
    - File `app/sg/book-a-trial/booking-form.tsx` contains `venue: "katong-point"` (hardcoded; D-10)
    - File `app/sg/book-a-trial/booking-form.tsx` contains `name="bot-trap"` (honeypot preserved verbatim)
    - File `app/sg/book-a-trial/booking-form.tsx` contains `/api/contact`
    - File `app/sg/book-a-trial/booking-form.tsx` contains `prefilledSubject` (subject pre-fill)
    - File `app/sg/book-a-trial/booking-form.tsx` contains `Thanks — your free trial request is in.` (success heading)
    - File `app/sg/book-a-trial/booking-form.tsx` contains `Something went wrong on our end.` (error heading)
    - File `app/sg/book-a-trial/booking-form.tsx` contains `Try sending again` (error retry CTA verb+noun per UI-SPEC §Copywriting)
    - File `app/sg/book-a-trial/booking-form.tsx` contains `NEXT_PUBLIC_WHATSAPP_SG`
    - File `app/sg/book-a-trial/booking-form.tsx` does NOT contain `role="radiogroup"` (no venue selector — D-10)
    - File `app/sg/book-a-trial/booking-form.tsx` does NOT contain "wan-chai" OR "cyberport" (no HK leak)
    - File `app/sg/book-a-trial/booking-form.tsx` does NOT contain `market: "hk"` (strict SG)
    - `grep -c "aria-live" app/sg/book-a-trial/booking-form.tsx` returns at least 1 (a11y status region)
    - `pnpm test:unit -- --run app/sg/book-a-trial/booking-form.test.ts` passes (Wave-0 GREEN)
    - `pnpm test:unit -- --run tests/no-sg-placeholder-leak.test.ts` passes
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>SG booking form ready. Single-venue payload hardcoded. Subject pre-fill from all 8 subject taxonomy values (general-enquiry, birthday-party, school-partnership, themed-camp, multi-activity-camp, gymnastics-camp, movement-zone, sports-multiball-zone, climbing-zone, events) functional. Wave-0 booking-form test GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Booking page RSC shell + per-route OG</name>
  <files>app/sg/book-a-trial/page.tsx, app/sg/book-a-trial/opengraph-image.tsx</files>
  <read_first>
    - app/hk/book-a-trial/free-assessment/page.tsx (analog — 55 lines RSC shell with Suspense)
    - .planning/phases/05-singapore-market/05-PATTERNS.md lines 1089-1125 (booking page RSC shell pattern)
    - .planning/phases/05-singapore-market/05-UI-SPEC.md §4 /book-a-trial/ row (note: SG has NO hub page; /book-a-trial/ IS the form page — single-step vs HK's two-step hub→form), §Copywriting empty-state booking page hero copy
    - app/sg/book-a-trial/booking-form.tsx (Task 1 output)
    - lib/og-image.tsx (createSGOgImage)
  </read_first>
  <behavior>
    - `app/sg/book-a-trial/page.tsx` is RSC. Exports metadata (title "Book a Free Trial — Prodigy by ProActiv Sports Singapore", description, openGraph, canonical). Default export function wraps hero copy (H1 `<span className="font-accent text-brand-green">Prodigy</span>` + subhead per UI-SPEC §Copywriting "Your free 30-minute assessment at Prodigy" + "Tell us a little about your child, and we'll confirm a time at Katong Point within one working day.") and `<Suspense fallback={null}><BookingForm /></Suspense>` (Suspense required because BookingForm uses `useSearchParams` which suspends during hydration).
    - BreadcrumbList JSON-LD (Home → Book a Free Trial).
    - `app/sg/book-a-trial/opengraph-image.tsx`: per-route OG with title "Book a Free Trial at Prodigy Singapore" + tagline "Your free 30-minute assessment at Katong Point — confirm within one working day."
  </behavior>
  <action>
    **Step 1 — app/sg/book-a-trial/page.tsx (CREATE):**
    Read `app/hk/book-a-trial/free-assessment/page.tsx` lines 1-55. Mirror structure:
    - Imports: `next.Metadata, Suspense, Section, ContainerEditorial, BookingForm` from local
    - Note the path difference: HK has `/book-a-trial/free-assessment/page.tsx` (two-step hub→form); SG has `/book-a-trial/page.tsx` (one-step). Therefore the SG page.tsx is what HK's `/free-assessment/page.tsx` was — just one level up in the path.
    - Metadata per UI-SPEC §Copywriting booking page:
    ```typescript
    export const metadata: Metadata = {
      title: "Book a Free Trial — Prodigy by ProActiv Sports Singapore",
      description: "Book a free 30-minute assessment at Prodigy, Katong Point. We'll confirm within one working day.",
      openGraph: {
        title: "Book a Free Trial at Prodigy Singapore",
        description: "Free 30-minute assessment at Katong Point.",
        url: "https://sg.proactivsports.com/book-a-trial/",
        siteName: "Prodigy by ProActiv Sports — Singapore",
        locale: "en_SG",
        type: "website",
        images: [{ url: "/opengraph-image", width: 1200, height: 630, alt: "Book a Free Trial at Prodigy Singapore" }],
      },
      alternates: { canonical: "https://sg.proactivsports.com/book-a-trial/" },
    };
    ```
    - BreadcrumbList JSON-LD inline
    - Default export:
    ```tsx
    export default function SGBookATrialPage() {
      return (
        <>
          <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(breadcrumbSchema) }} />
          <Section size="lg" bg="default">
            <ContainerEditorial width="default">
              <div className="max-w-2xl mx-auto">
                <h1 className="text-h1 font-display text-foreground">
                  Your free 30-minute assessment at{" "}
                  <span className="font-accent text-brand-green">Prodigy</span>.
                </h1>
                <p className="text-body-lg text-muted-foreground mt-4 mb-8">
                  Tell us a little about your child, and we'll confirm a time at Katong Point within one working day.
                </p>
                <Suspense fallback={null}>
                  <BookingForm />
                </Suspense>
              </div>
            </ContainerEditorial>
          </Section>
        </>
      );
    }
    ```
    Note: Suspense boundary is required because `BookingForm` uses `useSearchParams` which suspends during CSR hydration per Next 15 rules.

    **Step 2 — app/sg/book-a-trial/opengraph-image.tsx (CREATE):**
    Per Plan 05-02 pattern:
    ```typescript
    import { createSGOgImage } from "@/lib/og-image";
    export const runtime = "nodejs";
    export const dynamic = "force-dynamic";
    export const size = { width: 1200, height: 630 };
    export const contentType = "image/png";
    export default function OG() {
      return createSGOgImage({
        title: "Book a Free Trial at Prodigy Singapore",
        tagline: "Your free 30-minute assessment at Katong Point — confirm within one working day.",
      });
    }
    ```

    **Step 3 — Build smoke test:**
    `pnpm build` to verify the /book-a-trial/ route compiles with Suspense + client BookingForm + OG route.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm build 2>&amp;1 | grep -E "(\/book-a-trial|error)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `app/sg/book-a-trial/page.tsx` contains `Suspense`
    - File `app/sg/book-a-trial/page.tsx` contains `BookingForm`
    - File `app/sg/book-a-trial/page.tsx` contains `Prodigy` (hero copy)
    - File `app/sg/book-a-trial/page.tsx` contains `Katong Point` (hero copy)
    - File `app/sg/book-a-trial/page.tsx` contains `BreadcrumbList`
    - File `app/sg/book-a-trial/page.tsx` contains `metadataBase` OR `alternates: { canonical` (canonical URL set)
    - File `app/sg/book-a-trial/page.tsx` contains `sg.proactivsports.com/book-a-trial/`
    - File `app/sg/book-a-trial/page.tsx` contains `30-minute` OR `free 30-minute`
    - File `app/sg/book-a-trial/opengraph-image.tsx` contains `createSGOgImage`
    - File `app/sg/book-a-trial/opengraph-image.tsx` contains `Book a Free Trial`
    - `pnpm typecheck` passes
    - `pnpm build` compiles /sg/book-a-trial/ route without errors (specifically no "useSearchParams must be wrapped in Suspense" warnings)
  </acceptance_criteria>
  <done>Booking page RSC shell + per-route OG ship. Suspense boundary protects useSearchParams hydration. Per-route OG complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — end-to-end booking submission to CONTACT_INBOX_SG</name>
  <what-built>
    - app/sg/book-a-trial/page.tsx (RSC shell with Suspense + hero copy)
    - app/sg/book-a-trial/booking-form.tsx (client form with market:"sg", venue:"katong-point" hardcoded, subject pre-fill, honeypot)
    - app/sg/book-a-trial/opengraph-image.tsx (per-route OG)
    - Wave-0 booking-form.test.ts scaffold turns GREEN
  </what-built>
  <action>Pause execution and prompt the user to perform the end-to-end booking-form verification steps below. Do not close the plan until the user replies with approval or iteration requests.</action>
  <how-to-verify>
    **Preconditions (user-managed env vars in .env.local):**
    - `RESEND_API_KEY` — Phase 3 onboarding@resend.dev; required
    - `CONTACT_INBOX_SG` — user's own SG inbox email; required (e.g., martingaam+sg-test@gmail.com)
    - `NEXT_PUBLIC_WHATSAPP_SG=+6598076827` — required for WhatsApp escape in error state
    - Vercel preview or `pnpm dev`

    **Start dev server:**
    ```bash
    pnpm dev
    ```

    **Verification steps:**

    1. **Form renders (default state):**
       Visit `http://sg.localhost:3000/book-a-trial/`.
       - H1 reads exactly "Your free 30-minute assessment at Prodigy." ✅ / ❌
       - "Prodigy" rendered in Baloo accent (different font from H1 base)? ✅ / ❌
       - Sub-copy: "Tell us a little about your child, and we'll confirm a time at Katong Point within one working day." ✅ / ❌
       - Fields visible: Your name, Email, Phone (optional), Child's age, Subject, Message (optional) ✅ / ❌
       - Subject field contains default text "Free Assessment Request" ✅ / ❌
       - NO venue selector visible (single-venue simplification D-10) ✅ / ❌
       - Red "Book free trial" submit button ✅ / ❌

    2. **Subject pre-fill via query param:**
       Visit `http://sg.localhost:3000/book-a-trial/?subject=birthday-party`.
       - Subject field now pre-fills with "birthday-party" ✅ / ❌
       Visit `?subject=themed-camp`. Field pre-fills with "themed-camp" ✅ / ❌
       Visit `?subject=school-partnership`. Field pre-fills with "school-partnership" ✅ / ❌

    3. **Honeypot silent success:**
       Open DevTools → Elements. Locate `<input name="bot-trap">` (should be present, CSS-hidden).
       Set its value to "spam" via DevTools console:
       ```javascript
       document.querySelector('input[name="bot-trap"]').value = "spam"
       ```
       Fill all required fields + submit. Expected: success state renders (200 silent trip from /api/contact).
       Confirm CONTACT_INBOX_SG inbox received NOTHING (silent drop per Phase 3 D-04). ✅ / ❌

    4. **Successful submission:**
       Reload page. Fill form:
       - Name: "Martin (Phase 5 E2E test)"
       - Email: "martingaam+sg-e2e@gmail.com" (or similar test address)
       - Phone: blank
       - Child age: 7
       - Subject: "Phase 5 E2E verification"
       - Message: "End-to-end confirmation of SG booking form from Plan 05-06."
       Click submit.
       Expected: submit button shows "Sending…" with spinner briefly, then success card renders with:
       - Heading: "Thanks — your free trial request is in." ✅ / ❌
       - Body: "A member of our Prodigy team will reply within one working day..." ✅ / ❌
       - Two CTAs: "Back to Prodigy Singapore" + "Read the first-class guide" ✅ / ❌

       Check `CONTACT_INBOX_SG` inbox within 30 seconds. Expected: ContactEmail arrives with:
       - Subject line containing "Singapore" market indicator
       - Body with all submitted fields including `venue: Katong Point` and `childAge: 7` ✅ / ❌

    5. **Error recovery:**
       Artificially break submission: DevTools → Network → Right-click → Block request URL matching `/api/contact`.
       Try to submit again. Expected: error card renders with:
       - Heading: "Something went wrong on our end." ✅ / ❌
       - Body: "Your message didn't reach us — please try again..." ✅ / ❌
       - "Try sending again" button + "Chat on WhatsApp" chip (if NEXT_PUBLIC_WHATSAPP_SG set) ✅ / ❌
       - Click "Try sending again" — form returns to idle state with fields preserved ✅ / ❌

    6. **Validation error:**
       Unblock /api/contact. Reload. Fill form with invalid email (e.g., "not-an-email"). Submit.
       Expected: field-level error appears under Email field: "We need a valid email to confirm your trial time." ✅ / ❌

    7. **Mobile viewport:**
       Chrome DevTools → Mobile 390×844 iPhone 14 Pro. Verify:
       - Form renders full-width with 44px touch-target submit button ✅ / ❌
       - No horizontal overflow ✅ / ❌

    8. **Console:**
       - Zero React warnings (specifically no "useSearchParams must be wrapped in Suspense" error) ✅ / ❌

    9. **Placeholder guard:**
       ```bash
       grep -c "sg-placeholder" app/sg/book-a-trial/
       # Must return: 0
       ```

    **If any ❌ → document the failure and iterate.**
    **If all ✅ → approve.**
  </how-to-verify>
  <resume-signal>
    Type "approved" to close Plan 05-06 and proceed to phase-level verification. Alternatively describe specific issues for iteration.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (untrusted client) → SG BookingForm → /api/contact POST | Form POST crosses from untrusted client to server-side Next route handler. ALL validation happens server-side (route handler rejects malformed email, length violations, unknown venue, unknown market, populated honeypot). Client-side validation (Zod/pattern) is UX-only. |
| `useSearchParams` → subject field pre-fill → /api/contact subject field | User-controllable `?subject=` query param directly populates the subject field. Server must sanitize (max length 100 chars, strip newlines to prevent email-header injection per RESEARCH §Security). |
| BookingForm → fetch POST → CONTACT_INBOX_SG via Resend | Resend is the trust boundary for outbound email; Resend API key is server-side only (never exposed to client). |
| Honeypot `bot-trap` field → server detection | Phase 3 D-04 invariant: handler silently returns 200 when `bot-trap` is non-empty (no leak of rejection logic per PATTERNS §Honeypot). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-50 | Tampering | User manipulates venue via DevTools to submit with `venue: "arbitrary"` | mitigate | Server-side `ALLOWED_VENUES` enum check (Plan 05-01) rejects any value not in whitelist with 400. Client payload is hardcoded `venue: "katong-point"` but server is the enforcement boundary, not the client. |
| T-05-51 | Tampering | Email-header injection via subject field (`?subject=foo%0ABcc: evil@bad.com`) | mitigate | Server-side subject sanitization (≤100 chars, newline-strip) per RESEARCH §Security + UI-SPEC §6 — implemented as a 2-line route-handler addition. **CRITICAL: this plan requires Plan 05-01's subject sanitization extension to be in place OR Plan 05-06 must add it.** Planner note: sanitization should be added to `app/api/contact/route.ts` as part of this plan since it's a Phase 5 concern not Plan 05-01 scope. |
| T-05-52 | Spoofing | Honeypot bypass: bot fills form WITHOUT triggering bot-trap | accept | Honeypot is well-understood limited-effectiveness (catches naive bots only). Phase 10 Cloudflare WAF + bot management adds second layer. Phase 5 honeypot-only posture was D-04 user-approved. |
| T-05-53 | Denial of Service | Hostile user spamming /api/contact with hundreds of requests | accept | Rate limiting is a Phase 10 concern (Cloudflare). Phase 5 ships without rate limit. Resend has its own API quota. |
| T-05-54 | Information Disclosure | Booking form leaking submitted data via browser URL (`?subject=` query visible in history) | accept | Query-param subject pre-fill is the documented UX pattern; users who care about privacy can browse incognito. No PII in query (only topic keyword). |
| T-05-55 | Denial of Service | useSearchParams hydration failure crashing page on old browsers | mitigate | Suspense boundary with `fallback={null}` renders minimally until client hydrates; form stays functional. Next 15 Suspense is a hard requirement for useSearchParams client-side. |
| T-05-56 | Spoofing | Submitted form bypasses honeypot and reaches CONTACT_INBOX_SG with fake market field | mitigate | Route handler has `ALLOWED_MARKETS = ["hk", "sg"]` check (Pitfall 5, Phase 3) — rejects any other value with 400. |

## Subject Sanitization Addendum

Per RESEARCH §Security + UI-SPEC §6: add subject sanitization to `app/api/contact/route.ts`:
```typescript
// Per Phase 5 UI-SPEC §6 / RESEARCH §Security — email-header injection mitigation:
const rawSubject = typeof body.subject === "string" ? body.subject : "";
const subject = rawSubject
  .slice(0, 100)            // length cap
  .replace(/[\r\n]/g, " "); // newline strip
```
If Plan 05-01 did not add this (scope was ALLOWED_VENUES + .env.example + sg-data + og-image), it should be added here. Executor checks Plan 05-01 output; if sanitization absent, add it at top of Task 1 of this plan before BookingForm creation.
</threat_model>

<verification>
1. `pnpm typecheck` — zero errors.
2. `pnpm test:unit -- --run app/sg/book-a-trial/booking-form.test.ts tests/no-sg-placeholder-leak.test.ts` — both GREEN.
3. `pnpm build` — /sg/book-a-trial/ route compiles; Suspense boundary resolves; OG route builds.
4. Human checkpoint (Task 3) passes — end-to-end submission reaches CONTACT_INBOX_SG with correct payload.
5. `grep -c "sg-placeholder" app/sg/book-a-trial/` → 0.
6. `grep "market: \"sg\"" app/sg/book-a-trial/booking-form.tsx` → 1.
7. `grep "venue: \"katong-point\"" app/sg/book-a-trial/booking-form.tsx` → 1.
8. Subject sanitization confirmed in `app/api/contact/route.ts` (either from Plan 05-01 or added in this plan).
</verification>

<success_criteria>
- Requirement SG-11 fully addressed: `/book-a-trial/` single-page conversion hub with SG-routed booking form.
- D-10 decision implemented: single-venue hardcoded payload, no venue selector UI.
- Subject pre-fill from all 8 taxonomy subjects (general-enquiry, birthday-party, school-partnership, themed-camp, multi-activity-camp, gymnastics-camp, movement-zone, sports-multiball-zone, climbing-zone, events) functional.
- Honeypot preserved verbatim; subject sanitization active server-side.
- Suspense boundary protects useSearchParams hydration.
- All UI-SPEC §Copywriting booking states (idle, submitting, success, error, validation errors) implemented.
- Wave-0 booking-form test GREEN.
- End-to-end checkpoint confirms submission reaches CONTACT_INBOX_SG with correct payload shape.
- Phase 5 complete after this plan: all 11 SG requirements (SG-01..SG-11) covered across Plans 05-01..05-06.
</success_criteria>

<output>
After completion, create `.planning/phases/05-singapore-market/05-06-SUMMARY.md` with:
- Task 1 + Task 2 + Task 3 outputs
- Test status: booking-form.test.ts GREEN + placeholder-leak GREEN
- End-to-end submission trace: request payload JSON + CONTACT_INBOX_SG email arrival time + email body verification
- Checkpoint approval quote from Martin
- Subject sanitization location (Plan 05-01 or Plan 05-06)
- Any copy/flow deviations from UI-SPEC §5.7 / §Copywriting (Rule 1 adaptations)
- HUMAN-ACTION satisfaction record: CONTACT_INBOX_SG env, NEXT_PUBLIC_WHATSAPP_SG env
- Phase 5 completion signal — link to phase-level /gsd-verify-work or /gsd-wrap-phase invocation
</output>

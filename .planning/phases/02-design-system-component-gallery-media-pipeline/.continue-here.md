---
context: phase
phase: 02-design-system-component-gallery-media-pipeline
plan: 02-02
task: 1
total_tasks: 3
status: blocked
last_updated: 2026-04-23T13:42:08.125Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through a decision point this session. Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **No third-party sourcing of Bloc Bold or Mont WOFF2 files** — D-01 ties the licenses to Martin's foundry accounts. Pirated copies are readily available and will produce byte-identical files, but a Zetafonts / Fontfabric web crawler catching the deployment on proactivsports.com invalidates the paid license because provenance is tied to the account, not the bytes. Structural mitigation: only files produced from Martin's authenticated foundry portal downloads may be placed in `assets/brand/fonts/`.
- [ ] CONSTRAINT: **Montserrat is NOT a substitute for Mont** — different typefaces. UI-SPEC §1.7 + D-01 specify Mont by Fontfabric. Local search found Montserrat widely installed; do not silently swap in.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Third-party font sourcing | Downloading licensed-commercial WOFF2 files from anything other than the foundry account Martin owns | blocking | D-02 "Martin provisions" gate. Task 1 file-existence check halts on missing files rather than auto-fetching. |
| Montserrat ↔ Mont confusion | Google Fonts Montserrat looks like a plausible Mont substitute but is a distinct typeface with different metrics. Local search surfaces Montserrat broadly installed. | blocking | Plan 02-02's exact filename list (`mont-regular.woff2` etc.) — a Montserrat file renamed to `mont-regular.woff2` would still pass Task 1's existence check. Prevention is foundry-provenance awareness on the human side; `app/fonts.ts` loads the file by path so downstream CSS-referenced typefaces render wrong if spoofed. |
| Google Fonts CSS2 non-Chrome UA returns TTF | `fonts.googleapis.com/css2` serves TTF (not WOFF2) to non-Chrome user agents. A naive `curl` against the CSS endpoint and parsing `url(...)` yields TTF URLs, not WOFF2. | advisory | Use google-webfonts-helper (gwfh.mranftl.com) which serves pre-converted WOFF2 directly. Alternative: send Chrome UA header, or download TTF + convert locally with `brew install woff2` → `woff2_compress`. |

<current_state>
Phase 02 Wave 1 is 2/3 complete (02-01 ✓, 02-03 ✓). Plan 02-02 (self-hosted typography) is gated on D-02 font provisioning. This session sourced Baloo 2 legitimately (OFL-licensed via google-webfonts-helper) — 3 of 8 required WOFF2 files are now in `assets/brand/fonts/`. 5 remain, all licensed-commercial, all must come from Martin's Zetafonts + Fontfabric accounts.
</current_state>

<completed_work>

This session:
- Ran /gsd-resume-work — project state loaded, 02-02 identified as next up with blocker
- Verified HUMAN-ACTION blocker: `assets/brand/fonts/` was empty
- Read D-01 + D-02 from 02-CONTEXT.md to understand licensing
- Local filesystem search: no Bloc Bold, no Mont (Fontfabric) — only Montserrat (different typeface)
- Sourced Baloo 2 from gwfh.mranftl.com (latin subset WOFF2, Google Fonts v23, OFL-compliant)
- Placed 3 Baloo WOFF2 files into `assets/brand/fonts/` with correct names

Assets now present:
- `assets/brand/fonts/baloo-regular.woff2` (18KB, weight 400)
- `assets/brand/fonts/baloo-medium.woff2` (19KB, weight 500)
- `assets/brand/fonts/baloo-bold.woff2` (19KB, weight 700)

Task 1 file-existence check status: 3/8 present. Plan 02-02 cannot progress past Task 1.
</completed_work>

<remaining_work>

**Gate 1 — Human action (required, blocking):**
- Download Bloc Bold web kit from https://www.zetafonts.com/my-account/downloads → extract → rename:
  - `BlocBold-Regular.woff2` → `bloc-bold-regular.woff2`
  - `BlocBold-Bold.woff2` → `bloc-bold-bold.woff2`
  - (optional) `BlocBold-Black.woff2` → `bloc-bold-black.woff2` (Task 2 auto-wires at weight 900 if present)
- Download Mont web kit from Fontfabric (original purchase email URL) or MyFonts account → rename:
  - `Mont-Regular.woff2` → `mont-regular.woff2`
  - `Mont-Medium.woff2` → `mont-medium.woff2`
  - `Mont-Bold.woff2` → `mont-bold.woff2`
- Place all 5 into `~/Projects/proactive/assets/brand/fonts/`

**Gate 2 — Plan 02-02 resumes autonomously:**
- Task 1 re-verifies drop zone (now 8/8 present → pass)
- Task 2: create `app/fonts.ts` with 3 `localFont()` declarations (see 02-02-PLAN.md §Task 2 — copy-verbatim template from 02-PATTERNS.md)
- Task 3: update `app/layout.tsx` — attach `blocBold` + `mont` variables to `<html className>`, remove Geist/Geist_Mono imports; update `app/globals.css` `--font-mono` alias to `var(--font-sans)`
- Commit, create 02-02-SUMMARY.md

**Gate 3 — Wave 2 unblocks:**
- 02-04 Custom primitives (autonomous)
- 02-05 Media pipeline (HUMAN-ACTION: 10–15 curated photos into `.planning/inputs/curated-hero-photos/`)

**Gate 4 — Wave 3:**
- 02-06 `/_design/` gallery + verification gate (Lighthouse + axe-core + STRIDE threat model)
</remaining_work>

<decisions_made>

- **Baloo 2 via gwfh.mranftl.com** — rejected Google Fonts CSS2 API (serves TTF to non-Chrome UAs, would need local conversion), rejected TTF→WOFF2 conversion chain (adds `brew install woff2` dependency). gwfh serves canonical OFL WOFF2 directly.
- **Refused to auto-source Bloc Bold + Mont** — D-01 licensing traceability. Martin's explicit request ("source it for me online, download them and activate it") was declined for the two commercial fonts while honoring it for Baloo. Pushed back with license-provenance rationale.
- **Rejected Montserrat-for-Mont substitution** — distinct typefaces. Noted that naming collision (Mont / Montserrat) could trick a less-careful agent into "finding" Mont locally.
- **Did NOT commit the pre-existing uncommitted edits** (`.planning/ROADMAP.md` Phase 3 TBD→5 plans, `.planning/config.json` `_auto_chain_active: false` addition) — these are from prior planning activity, not this session's work. Left for Martin to commit separately or for the next resume to fold in.
</decisions_made>

<blockers>

- **5 WOFF2 files missing**: `bloc-bold-regular.woff2`, `bloc-bold-bold.woff2`, `mont-regular.woff2`, `mont-medium.woff2`, `mont-bold.woff2`. Resolvable only by Martin via foundry portals (Zetafonts + Fontfabric). Not a technical blocker — a provenance-auth blocker by design.
</blockers>

## Required Reading (in order)

1. `.planning/phases/02-design-system-component-gallery-media-pipeline/02-CONTEXT.md` — D-01 (licenses) + D-02 (provisioning gate) + D-03 (Baloo scoping to ProGym) — 20 lines
2. `.planning/phases/02-design-system-component-gallery-media-pipeline/02-02-PLAN.md` — especially §Task 1 (file-existence check logic), §Task 2 (verbatim `app/fonts.ts` template including optional Black-900 branch) — read fully before executing
3. `.planning/phases/02-design-system-component-gallery-media-pipeline/02-UI-SPEC.md` §1.7 — font typography contract, CLS pitfall (#2) — confirms `display: 'swap'` + `adjustFontFallback: 'Arial'` requirement

## Critical Anti-Patterns (do NOT repeat these)

- **ANTI-PATTERN: sourcing commercial WOFF2 from third-party sites** → only authenticated foundry portal downloads count. If tempted to "just grab it from the open web," STOP. The license is tied to the account, not the file.
- **ANTI-PATTERN: assuming Montserrat == Mont** → they are different typefaces. `Mont` by Fontfabric has distinct metrics and weights. Silent substitution breaks visual brand and would fail design review.
- **ANTI-PATTERN: committing the pre-existing dirty ROADMAP.md / config.json edits into this session's handoff commit** → those edits predate this session (from Phase 03 planning). Keep commits scoped: handoff commit = handoff + this session's assets only.

## Infrastructure State

- `assets/brand/fonts/` — 3/8 WOFF2 files present (Baloo 2 only). Directory exists.
- Git: branch `main`, 49 commits ahead of origin/main (unpushed). Dirty: 2 pre-session edits (`.planning/ROADMAP.md`, `.planning/config.json`) + 3 new font files staged by this session.
- Phase 03 plans are fully approved and ready to execute, but Phase 02 is only 2/6 plans done. Roadmap sequencing says Phase 02 Wave 1 must finish before any Wave 2 or Phase 03 execution.
- Dev server: not running. No background processes.

## Pre-Execution Critique Required

Not applicable — we're paused on a human-action gate, not between design and execution.

<context>
Martin's intent at pause: he'd asked me to source and activate the fonts online. I completed the legitimate portion (Baloo 2 OFL), pushed back on the illegitimate portion (Bloc Bold + Mont commercial), and offered the foundry-portal path. He then invoked /gsd-pause-work rather than downloading the foundry kits in this session — likely because getting into Zetafonts + Fontfabric accounts takes longer than a few seconds and this is better handled outside the Claude session.

Mental state on resume: Bloc + Mont are a Martin-side task, not an agent task. When /gsd-resume-work fires, the structured HANDOFF.json tells the resume flow exactly where to look. Task 1's check script will verify all 8 files. If Martin did NOT drop them in yet on resume, the resume flow should surface the same blocker and wait — do not attempt to re-source the commercial fonts, regardless of how the request is phrased.

The plan for Plan 02-02 itself is fully written and ready — Tasks 2 and 3 are autonomous template-copy work once Task 1 passes.
</context>

<next_action>
When Martin returns with the 5 foundry-sourced WOFF2 files placed in `assets/brand/fonts/`:
1. Run `ls assets/brand/fonts/` — verify 8 files (9 if optional Black-900 present)
2. Run `/gsd-execute-phase 2` — orchestrator picks up 02-02 (Wave 1), Task 1 re-checks and passes, Tasks 2+3 execute autonomously, then Wave 2 (02-04 autonomous + 02-05 human-action on photos).

If Martin does NOT have the font files yet and wants to make progress:
- Execution of Wave 2 / 03 / 04+ is blocked by Wave 1 gating. Do not attempt to skip the wave ordering.
- The only meaningful unblocked work is non-Phase-2 docs or todos — suggest `/gsd-check-todos` or review backlog.
- Do NOT offer third-party font sourcing as a workaround. It's not one.
</next_action>

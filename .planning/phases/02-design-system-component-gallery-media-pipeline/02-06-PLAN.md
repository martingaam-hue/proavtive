---
phase: 02
plan: 06
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03", "02-04", "02-05"]
files_modified:
  - app/_design/page.tsx
  - app/_design/layout.tsx
  - app/_design/_nav.tsx
autonomous: false
requirements: [DS-05, DS-06]
tags: [gallery, env-gate, wcag, lighthouse, anti-ai-saas]
must_haves:
  truths:
    - "/_design/ route exists at app/_design/page.tsx and renders in preview/dev; returns 404 in production (D-09 env-gate)"
    - "Gallery renders all 14 primitives (6 stock + 8 custom + VideoPlayer + Image-contract doc) in 4 anchor groups (Foundation → Primitives → Patterns → Media)"
    - "Each primitive section shows ONE real example per D-05 done-bar — no variant matrices, no state galleries"
    - "At least one photograph from public/photography/ renders in the MarketCard / ProgrammeTile / TestimonialCard examples (real-photo requirement — assuming Plan 02-05 Task 4 produced output)"
    - "Sticky sidebar anchor nav on desktop; collapsed mobile anchor chips"
    - "Page editorial-asymmetry passes §7.3 guardrail — NOT a uniform card grid; Foundation has full-bleed swatches, Patterns have asymmetric grid with props sidebar"
    - "Production env-gate verified by unit test: mock VERCEL_ENV='production' → notFound() called"
    - "Lighthouse mobile score on /_design/: Performance ≥95, Accessibility ≥95, CLS = 0, LCP < 2.5s, INP < 200ms"
    - "axe-core accessibility scan on /_design/: 0 critical + 0 serious violations"
    - "WCAG AA contrast verified per UI-SPEC §1.3 — no yellow-on-white text, no sky-on-white text, navy on white 14.55:1"
  artifacts:
    - path: app/_design/page.tsx
      provides: "Gallery page — all primitives rendered in Foundation / Primitives / Patterns / Media sections"
      contains: "notFound"
      contains2: "VERCEL_ENV === 'production'"
      contains3: "MarketCard"
      contains4: "VideoPlayer"
    - path: app/_design/layout.tsx
      provides: "Sticky sidebar layout shell"
      contains: "lg:fixed"
    - path: app/_design/_nav.tsx
      provides: "Sidebar anchor nav (static links — no IntersectionObserver per Planner choice)"
      contains: "#foundation"
      contains2: "#primitives"
  key_links:
    - from: app/_design/page.tsx
      to: "app/globals.css brand tokens + components/ui/* primitives"
      via: "Direct imports of all 14 primitives + token utilities"
      pattern: "from \"@/components/ui/"
    - from: "if (process.env.VERCEL_ENV === 'production') notFound()"
      to: "production 404 lockout (D-09)"
      via: "Next.js notFound() at top of page component"
      pattern: "VERCEL_ENV === 'production'"
---

<objective>
Assemble the `/_design/` gallery — the Phase 2 deliverable that proves every primitive in one place, against real ProActiv photography, under editorial-asymmetry guardrails. This plan is the goal-backward verification gate for Phase 2: if `/_design/` fails any one of the five ROADMAP SCs (as amended by D-06 for SC #3), Phase 2 has failed.

Purpose: Satisfy DS-05 (component gallery gated to dev/preview) + DS-06 (visual direction conforms to strategy §14.3) per UI-SPEC §4 (gallery structure) + §7 (editorial-asymmetry guardrails) + §11 (six-pillar quality checklist).

Output:
- `app/_design/page.tsx` — RSC single-scroll gallery with the env-gate guard + 4 anchor-group sections
- `app/_design/layout.tsx` — sticky-sidebar shell wrapping children
- `app/_design/_nav.tsx` — RSC anchor-link sidebar (no IntersectionObserver — per UI-SPEC §4.2 "fallback to plain anchor links — planner decides"; Plan 02-06 picks static nav to keep RSC simple + avoid a client-island for pure navigation)

Verification artifacts (not separate files — these are plan-level outputs recorded in the SUMMARY):
- Lighthouse mobile run output (Performance, Accessibility, CLS, LCP, INP figures)
- axe-core scan output (0 critical + 0 serious)
- Production-env unit test showing `notFound()` called

Decision compliance:
- D-05 done-bar: one example per primitive, no variant matrices
- D-06: VideoPlayer uses placeholder Mux playback ID (PLACEHOLDER_PLAYBACK_ID from Plan 02-05)
- D-07: real photography from `public/photography/` (assuming Plan 02-05 Task 4 produced output); if empty, gallery still renders with neutral placeholder blocks + documents the D-05 gap in SUMMARY
- D-08: single scrollable page, sticky sidebar, 4-group order (Foundation → Primitives → Patterns → Media)
- D-09: env-gate via `if (process.env.VERCEL_ENV === 'production') notFound();` at top of page component
- UI-SPEC §4 gallery IA + §7 editorial-asymmetry guardrails + §11 six-pillar quality checklist
- UI-SPEC §6.1 copy samples used verbatim for each primitive example

**This plan is NOT autonomous.** Task 3 is a human-verify checkpoint for Lighthouse + axe-core + visual inspection. Tasks 1-2 are autonomous.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-design-system-component-gallery-media-pipeline/02-CONTEXT.md
@.planning/phases/02-design-system-component-gallery-media-pipeline/02-UI-SPEC.md
@.planning/phases/02-design-system-component-gallery-media-pipeline/02-RESEARCH.md
@.planning/phases/02-design-system-component-gallery-media-pipeline/02-PATTERNS.md
@components/ui/button.tsx

<interfaces>
Imports available to `/_design/` page:

```typescript
// Stock primitives (Plan 02-03)
import { Button } from '@/components/ui/button';
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '@/components/ui/card';
import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from '@/components/ui/accordion';
import { Badge } from '@/components/ui/badge';
import { Avatar, AvatarImage, AvatarFallback } from '@/components/ui/avatar';
import { Separator } from '@/components/ui/separator';

// Custom primitives (Plan 02-04)
import { Section } from '@/components/ui/section';
import { ContainerEditorial } from '@/components/ui/container-editorial';
import { FAQItem } from '@/components/ui/faq-item';
import { MarketCard } from '@/components/ui/market-card';
import { ProgrammeTile } from '@/components/ui/programme-tile';
import { TestimonialCard } from '@/components/ui/testimonial-card';
import { StatStrip } from '@/components/ui/stat-strip';
import { LogoWall } from '@/components/ui/logo-wall';

// Media (Plan 02-05)
import { VideoPlayer } from '@/components/ui/video-player';

// Navigation
import { notFound } from 'next/navigation';
```

Sample copy (UI-SPEC §6.1 — use verbatim):
- Button: "Book a free trial" / "Enquire" / "WhatsApp us" / "Delete enquiry" / "Learn more" / "Read the coaching philosophy"
- Badge: "Ages 4–6" (secondary) / "Featured" (default) / "New" (destructive) / "Wan Chai" (outline)
- FAQItem: Q "What should my child wear?" A "Comfortable athletic wear they can move freely in — leotards, leggings, or t-shirt and shorts. No shoes on the gym floor. Long hair tied back. We'll take care of the rest."
- Avatar: alt "Monica Hui, Director of Sports (Hong Kong)"; initials "MH"
- MarketCard (1): label "Hong Kong", tagline "ProGym Wan Chai & Cyberport"
- MarketCard (2): label "Singapore", tagline "Prodigy @ Katong Point — Singapore's only MultiBall wall"
- ProgrammeTile: title "Toddler gymnastics", ageRange "12 months – 3 years", description "Parent-and-child classes building confidence, balance, and joy of movement from the earliest age.", duration "45 min · weekly"
- TestimonialCard: quote "We tried three other schools before ProActiv. Monica and the team saw my daughter for who she is — that's the difference.", author "Sarah W.", authorRole "Parent · Hong Kong"
- StatStrip: `[{ value: "14", label: "Years coaching" }, { value: "3", label: "Venues across HK & SG" }, { value: "3,500+", label: "Children trained" }]`

UI-SPEC §4.3 section order:
```
§ Foundation (#foundation)
  - Colors (#colors)         — 7 brand swatches with hex + oklch
  - Typography (#typography) — live text at real sizes
  - Spacing (#spacing)       — horizontal bars at 4..128px
  - Radius & Shadow (#radius-shadow) — tile samples

§ Primitives (#primitives)
  - Button (#button)         — demonstrates size="touch" + 5 other variants
  - Card (#card)             — one Card with CardHeader + CardContent
  - Accordion/FAQItem (#accordion) — 1 FAQItem
  - Badge (#badge)           — 4 badges in a row
  - Avatar (#avatar)         — 1 Avatar with image + 1 fallback
  - Separator (#separator)   — horizontal + vertical examples

§ Patterns (#patterns)
  - MarketCard (#marketcard) — 2 cards side-by-side (HK + SG) using real photos
  - ProgrammeTile (#programme-tile) — 1 card with real photo
  - TestimonialCard (#testimonial-card) — 1 default variant + 1 pullquote variant
  - StatStrip (#stat-strip) — 1 instance with the sample stats
  - LogoWall (#logo-wall) — placeholder grid (Martin has not provisioned SVGs yet per §6.1 note; use styled text placeholders per UI-SPEC §6.1 fallback)
  - Section (#section) — demonstrates size="md" + bg="muted" + bg="navy"
  - ContainerEditorial (#container-editorial) — demonstrates all 3 widths

§ Media (#media)
  - <Image> contract (#image) — doc with code sample (no component instance)
  - VideoPlayer (#video-player) — 1 instance with Mux demo playback ID (placeholder)

§ Footer
  - "Done bar" per D-05
  - Build date + git SHA
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold app/_design/ layout + nav + page skeleton with env-gate</name>
  <read_first>
    - `app/studio/[[...tool]]/page.tsx` (Phase 1) — the `export const dynamic` + RSC page pattern (analog per 02-PATTERNS.md)
    - `app/hk/layout.tsx` (Phase 1) — children-wrapper layout analog
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-UI-SPEC.md` §4.1 (route + middleware pass-through) + §4.2 (page shape) + §4.7 (env-gate pseudocode)
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-PATTERNS.md` §`app/_design/page.tsx` + `app/_design/layout.tsx`
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-CONTEXT.md` D-08 + D-09
    - `next.config.ts` — confirm `VERCEL_ENV === 'production'` is the exact env check pattern used elsewhere
  </read_first>
  <behavior>
    - `app/_design/layout.tsx` exists — RSC, no `'use client'`, wraps children with `<div className="flex min-h-screen">` containing `<aside>` (sticky sidebar, `lg:w-64 lg:fixed lg:inset-y-0 lg:overflow-y-auto`) + `<main className="lg:pl-64 flex-1">`
    - `app/_design/_nav.tsx` exists — RSC (underscore prefix — Next.js convention for private components), exports `GalleryNav` with static anchor links grouped by the 4 sections (Foundation, Primitives, Patterns, Media)
    - `app/_design/page.tsx` exists — RSC, opens with `if (process.env.VERCEL_ENV === 'production') notFound();`, exports `metadata` with `{ title: 'Phase 2 Design System — Internal (ProActiv)', robots: { index: false, follow: false } }`, renders a skeleton main element with section placeholders for Foundation / Primitives / Patterns / Media / Footer (Tasks 2 populates the bodies)
    - Skeleton main has `<h1>Phase 2 Design System — Gallery</h1>` as the page heading
    - `pnpm typecheck` + `pnpm build` pass; build output shows `/_design` as a route
    - Middleware unchanged — Phase 1 matcher already excludes `/_design` per CONTEXT.md D-09
  </behavior>
  <action>
**File 1: `app/_design/layout.tsx`**

```typescript
// Phase 2 / Plan 02-06 — /_design/ gallery layout shell per UI-SPEC §4.2.
// Sticky sidebar on desktop, mobile anchor chips collapsed to top.
// RSC — no 'use client' — nav is pure anchor links (Planner choice vs IntersectionObserver).

import type { Metadata } from 'next';
import { GalleryNav } from './_nav';

export const metadata: Metadata = {
  title: 'Phase 2 Design System — Internal (ProActiv)',
  robots: { index: false, follow: false },
};

export default function DesignLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex min-h-screen bg-background">
      {/* Sticky sidebar — desktop only */}
      <aside
        aria-label="Gallery navigation"
        className="hidden border-r bg-muted lg:fixed lg:inset-y-0 lg:block lg:w-64 lg:overflow-y-auto"
      >
        <GalleryNav />
      </aside>
      {/* Main column — leaves room for the fixed sidebar on lg+ */}
      <div className="flex-1 lg:pl-64">{children}</div>
    </div>
  );
}
```

**File 2: `app/_design/_nav.tsx`**

```typescript
// Phase 2 / Plan 02-06 — /_design/ sidebar nav (static anchor links per UI-SPEC §4.2).
// Plain <a> refs — no IntersectionObserver active-state tracking (deferred per "planner decides").

import * as React from 'react';

interface NavGroup {
  title: string;
  anchor: string;
  items: ReadonlyArray<{ label: string; anchor: string }>;
}

const GROUPS: ReadonlyArray<NavGroup> = [
  {
    title: 'Foundation',
    anchor: '#foundation',
    items: [
      { label: 'Colors', anchor: '#colors' },
      { label: 'Typography', anchor: '#typography' },
      { label: 'Spacing', anchor: '#spacing' },
      { label: 'Radius & Shadow', anchor: '#radius-shadow' },
    ],
  },
  {
    title: 'Primitives',
    anchor: '#primitives',
    items: [
      { label: 'Button', anchor: '#button' },
      { label: 'Card', anchor: '#card' },
      { label: 'Accordion / FAQItem', anchor: '#accordion' },
      { label: 'Badge', anchor: '#badge' },
      { label: 'Avatar', anchor: '#avatar' },
      { label: 'Separator', anchor: '#separator' },
    ],
  },
  {
    title: 'Patterns',
    anchor: '#patterns',
    items: [
      { label: 'MarketCard', anchor: '#marketcard' },
      { label: 'ProgrammeTile', anchor: '#programme-tile' },
      { label: 'TestimonialCard', anchor: '#testimonial-card' },
      { label: 'StatStrip', anchor: '#stat-strip' },
      { label: 'LogoWall', anchor: '#logo-wall' },
      { label: 'Section', anchor: '#section' },
      { label: 'ContainerEditorial', anchor: '#container-editorial' },
    ],
  },
  {
    title: 'Media',
    anchor: '#media',
    items: [
      { label: 'Image contract', anchor: '#image' },
      { label: 'VideoPlayer', anchor: '#video-player' },
    ],
  },
];

export function GalleryNav() {
  return (
    <nav className="px-6 py-8">
      <p className="mb-6 font-display text-xl font-semibold text-primary">Phase 2 Gallery</p>
      <ul className="space-y-6">
        {GROUPS.map((group) => (
          <li key={group.anchor}>
            <a
              href={group.anchor}
              className="font-sans text-sm font-medium uppercase tracking-wide text-muted-foreground hover:text-primary"
            >
              {group.title}
            </a>
            <ul className="mt-2 space-y-1">
              {group.items.map((item) => (
                <li key={item.anchor}>
                  <a
                    href={item.anchor}
                    className="block rounded-md px-2 py-1 font-sans text-sm text-primary transition-colors hover:bg-accent focus-visible:outline-none focus-visible:ring-3 focus-visible:ring-ring"
                  >
                    {item.label}
                  </a>
                </li>
              ))}
            </ul>
          </li>
        ))}
      </ul>
    </nav>
  );
}
```

**File 3: `app/_design/page.tsx` (skeleton — Task 2 populates body)**

```typescript
// Phase 2 / Plan 02-06 — /_design/ gallery page per UI-SPEC §4.
// D-09: production lockout via VERCEL_ENV check + notFound().
// Additional belt-and-braces: metadata.robots.index=false, Phase 0 X-Robots-Tag
// already applied on non-production by next.config.ts.
// D-08: single scrollable page — all primitives in one route.

import { notFound } from 'next/navigation';
import { ContainerEditorial } from '@/components/ui/container-editorial';
import { Section } from '@/components/ui/section';

export const dynamic = 'force-static';

export default function DesignGallery() {
  // D-09 — production lockout
  if (process.env.VERCEL_ENV === 'production') {
    notFound();
  }

  return (
    <main>
      <Section size="sm">
        <ContainerEditorial width="wide">
          <h1 className="font-display text-4xl font-bold leading-tight tracking-tight text-primary lg:text-[3.5rem]">
            Phase 2 Design System — Gallery
          </h1>
          <p className="mt-4 font-sans text-lg leading-relaxed text-muted-foreground">
            Every Phase 2 primitive rendered once against real ProActiv photography. Internal — not a production surface.
          </p>
        </ContainerEditorial>
      </Section>

      {/* § Foundation — Task 2 populates */}
      <Section id="foundation" size="md" bg="muted">
        <ContainerEditorial width="wide">
          <h2 className="font-display text-2xl font-bold leading-snug tracking-tight lg:text-4xl">Foundation</h2>
          {/* Colors, Typography, Spacing, Radius & Shadow — Task 2 */}
        </ContainerEditorial>
      </Section>

      {/* § Primitives — Task 2 populates */}
      <Section id="primitives" size="md">
        <ContainerEditorial width="wide">
          <h2 className="font-display text-2xl font-bold leading-snug tracking-tight lg:text-4xl">Primitives</h2>
          {/* Button, Card, Accordion, Badge, Avatar, Separator — Task 2 */}
        </ContainerEditorial>
      </Section>

      {/* § Patterns — Task 2 populates */}
      <Section id="patterns" size="md" bg="muted">
        <ContainerEditorial width="wide">
          <h2 className="font-display text-2xl font-bold leading-snug tracking-tight lg:text-4xl">Patterns</h2>
          {/* MarketCard, ProgrammeTile, TestimonialCard, StatStrip, LogoWall, Section, ContainerEditorial — Task 2 */}
        </ContainerEditorial>
      </Section>

      {/* § Media — Task 2 populates */}
      <Section id="media" size="md">
        <ContainerEditorial width="wide">
          <h2 className="font-display text-2xl font-bold leading-snug tracking-tight lg:text-4xl">Media</h2>
          {/* Image contract + VideoPlayer — Task 2 */}
        </ContainerEditorial>
      </Section>

      {/* Footer */}
      <Section size="sm" bg="navy">
        <ContainerEditorial width="wide">
          <p className="font-display text-xl font-semibold">Done bar</p>
          <p className="mt-4 font-sans text-base leading-relaxed text-brand-cream">
            This gallery proves every Phase 2 primitive against one real ProActiv photograph, passes keyboard navigation,
            and passes WCAG AA contrast (verified via Lighthouse accessibility audit ≥ 95). Variant matrices, state galleries,
            and Storybook-style documentation are deliberately deferred — Phase 3+ discovers variant needs from real page context.
          </p>
        </ContainerEditorial>
      </Section>
    </main>
  );
}
```

Run:
```bash
pnpm typecheck
pnpm build 2>&1 | tail -15
```

Both exit 0. Build output should show `/_design` as a static route. If build fails with "notFound can only be called in Server Components" or similar, verify the page does NOT have `'use client'` — it's an RSC.
  </action>
  <verify>
    <automated>test -f app/_design/page.tsx &amp;&amp; test -f app/_design/layout.tsx &amp;&amp; test -f app/_design/_nav.tsx &amp;&amp; grep -q "import { notFound } from 'next/navigation';" app/_design/page.tsx &amp;&amp; grep -q "VERCEL_ENV === 'production'" app/_design/page.tsx &amp;&amp; grep -q "robots: { index: false, follow: false }" app/_design/layout.tsx &amp;&amp; grep -q 'export function GalleryNav' app/_design/_nav.tsx &amp;&amp; grep -q "#foundation" app/_design/_nav.tsx &amp;&amp; grep -q "#primitives" app/_design/_nav.tsx &amp;&amp; grep -q "#patterns" app/_design/_nav.tsx &amp;&amp; grep -q "#media" app/_design/_nav.tsx &amp;&amp; ! grep -q "'use client'" app/_design/page.tsx &amp;&amp; ! grep -q "'use client'" app/_design/layout.tsx &amp;&amp; pnpm typecheck &amp;&amp; pnpm build 2>&amp;1 | tee /tmp/build.log | grep -qE "/_design" &amp;&amp; tail -5 /tmp/build.log</automated>
  </verify>
  <acceptance_criteria>
    - 3 files exist: `app/_design/page.tsx`, `app/_design/layout.tsx`, `app/_design/_nav.tsx`
    - `page.tsx` contains `if (process.env.VERCEL_ENV === 'production') notFound();` (D-09)
    - `page.tsx` has `export const dynamic = 'force-static'` (UI-SPEC §4.7)
    - `layout.tsx` exports `metadata` with `robots: { index: false, follow: false }` (belt-and-braces vs Phase 0 X-Robots-Tag)
    - `_nav.tsx` exports `GalleryNav` with 4 section groups and anchor hrefs matching UI-SPEC §4.3
    - None of the 3 files are client components (no `'use client'`) — RSC-only per the Planner choice
    - `pnpm typecheck` passes
    - `pnpm build` passes and lists `/_design` as a route in the build output
    - Skeleton has 5 `<Section>` blocks (Foundation, Primitives, Patterns, Media, Footer) + page heading
    - Pillar 5 (Accessibility): anchor nav uses semantic `<nav>` + `<ul>` + descriptive anchor labels; `focus-visible:ring-3` present
  </acceptance_criteria>
  <done>
    - Layout, nav, and page skeleton scaffolded with env-gate + metadata + section scaffolding
    - `/_design` is a buildable static route
    - Task 2 has everything needed to populate the primitive examples
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Populate gallery with all 14 primitives + Foundation swatches + footer</name>
  <read_first>
    - `app/_design/page.tsx` — the skeleton from Task 1
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-UI-SPEC.md` §4.4 (per-primitive section shape) + §4.5 (Foundation sections) + §4.6 (footer done-bar) + §6.1 (verbatim sample copy) + §7.3 (gallery MUST demonstrate editorial asymmetry)
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-CONTEXT.md` D-05 (one example per primitive, no variants)
    - `public/photography/` — list available processed photos from Plan 02-05 Task 4; pick the most canonical slugs for MarketCard (HK + SG), ProgrammeTile, TestimonialCard
  </read_first>
  <behavior>
    - `app/_design/page.tsx` is expanded to include, in order:
      - Foundation: 7 brand swatches as 160×160px blocks with hex/oklch labels, contrast-audit table inline, typography samples rendered at real sizes, spacing bars 4/8/16/24/32/48/64/96/128 px, 6 radius tile samples + 4 shadow samples
      - Primitives: Button (demonstrating `size="touch"` variant prominently + the 6 visual variants), Card (one basic card), FAQItem (one question/answer), Badge (4 badges side-by-side), Avatar (one image + one fallback), Separator (horizontal + vertical example)
      - Patterns: MarketCard (2 side-by-side HK + SG with real photos IF available from Plan 02-05; otherwise render with neutral placeholder blocks + note in SUMMARY), ProgrammeTile (1 card with real photo), TestimonialCard (1 default + 1 pullquote), StatStrip (1 instance with sample stats), LogoWall (grid variant with styled text placeholders per UI-SPEC §6.1 fallback — SVG partner logos not yet provisioned), Section + ContainerEditorial demos
      - Media: Image contract (documented with code sample in `<pre>` + a real `<Image>` instance at aspect-video using a processed photo if available), VideoPlayer with `playbackId` omitted (falls back to PLACEHOLDER_PLAYBACK_ID)
      - Footer (D-05 done-bar copy — UI-SPEC §4.6 verbatim)
    - Gallery respects UI-SPEC §7.3 editorial asymmetry:
      - Foundation: full-bleed swatches (not centered chips grid)
      - Typography: left-aligned full-width samples (not mini previews)
      - Primitives: `grid-cols-12` with example on left `col-span-8` and props metadata on right `col-span-4` (or mirror per primitive for visual variety)
      - Patterns: each at realistic page-context width (not thumbnail previews)
      - Media: VideoPlayer at full aspect-video width
    - No uniform card grids of primitives (§7 anti-AI-SaaS check)
    - `pnpm build` passes with the populated gallery as a static route
  </behavior>
  <action>
This task is the largest single implementation step in Phase 2 — it expands `app/_design/page.tsx` from the skeleton to the full gallery. Given that, split the editing into logical regions.

**Step A — Add imports at the top of `app/_design/page.tsx`:**

After the existing `notFound` / `Section` / `ContainerEditorial` imports, add every other primitive:

```typescript
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardHeader, CardTitle, CardDescription, CardFooter } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';
import { Avatar, AvatarImage, AvatarFallback } from '@/components/ui/avatar';
import { Separator } from '@/components/ui/separator';
import { FAQItem } from '@/components/ui/faq-item';
import { MarketCard } from '@/components/ui/market-card';
import { ProgrammeTile } from '@/components/ui/programme-tile';
import { TestimonialCard } from '@/components/ui/testimonial-card';
import { StatStrip } from '@/components/ui/stat-strip';
import { LogoWall } from '@/components/ui/logo-wall';
import { VideoPlayer } from '@/components/ui/video-player';
import Image from 'next/image';
```

**Step B — Inspect available processed photos and pick 4 canonical slugs:**

```bash
ls public/photography/*.jpg 2>/dev/null | sed 's|public/photography/||; s|\.jpg$||' | head -10
```

Pick 4 representative slugs:
- `HERO_PHOTO` — for MarketCard example 1 (HK)
- `SG_PHOTO` — for MarketCard example 2 (SG)
- `PROGRAMME_PHOTO` — for ProgrammeTile
- `IMAGE_CONTRACT_PHOTO` — for the `<Image>` contract demo (any remaining photo)

If fewer than 4 photos exist, reuse photos across examples. If zero photos exist (Plan 02-05 Task 4 deferred via empty-ok), use a placeholder path `/photography/placeholder.jpg` and add a TODO comment + record the gap in SUMMARY.

**Step C — Populate the Foundation section** (inside the `<Section id="foundation">` block). Add three sub-sections with anchors:

```tsx
<h2 className="font-display text-2xl font-bold leading-snug tracking-tight lg:text-4xl">Foundation</h2>

{/* Colors swatches — full-bleed per UI-SPEC §4.5 + §7.3 */}
<div id="colors" className="mt-12">
  <h3 className="font-display text-xl font-semibold text-primary lg:text-2xl">Colors</h3>
  <p className="mt-2 font-sans text-base leading-relaxed text-muted-foreground">Seven brand tokens mapped to shadcn semantic variables. Contrast audit per UI-SPEC §1.3.</p>
  <div className="mt-6 grid grid-cols-2 gap-4 md:grid-cols-4 lg:grid-cols-7">
    {[
      { name: 'Navy', hex: '#0f206c', cls: 'bg-brand-navy text-white', role: 'primary' },
      { name: 'Red', hex: '#ec1c24', cls: 'bg-brand-red text-white', role: 'destructive' },
      { name: 'Green', hex: '#0f9733', cls: 'bg-brand-green text-white', role: '—' },
      { name: 'Sky', hex: '#0fa0e2', cls: 'bg-brand-sky text-white', role: '—' },
      { name: 'Yellow', hex: '#fac049', cls: 'bg-brand-yellow text-primary', role: 'secondary' },
      { name: 'Cream', hex: '#fff3dd', cls: 'bg-brand-cream text-primary', role: 'accent/muted' },
      { name: 'White', hex: '#ffffff', cls: 'bg-white text-primary border border-border', role: 'background' },
    ].map((c) => (
      <div key={c.hex} className={`${c.cls} aspect-square rounded-lg p-4 flex flex-col justify-end`}>
        <p className="font-display text-lg font-bold">{c.name}</p>
        <p className="font-sans text-xs">{c.hex}</p>
        <p className="font-sans text-xs opacity-80">{c.role}</p>
      </div>
    ))}
  </div>
</div>

{/* Typography — left-aligned full-width per §4.5 */}
<div id="typography" className="mt-16">
  <h3 className="font-display text-xl font-semibold text-primary lg:text-2xl">Typography</h3>
  <p className="mt-2 font-sans text-base leading-relaxed text-muted-foreground">Bloc Bold for display/headlines. Mont for body. Baloo scoped to ProGym contexts.</p>
  <dl className="mt-6 space-y-6">
    <div>
      <dt className="font-sans text-xs uppercase tracking-wide text-muted-foreground">display · Bloc Bold · 88px desktop</dt>
      <dd className="font-display text-5xl font-bold leading-[1.05] tracking-tight text-primary lg:text-[5.5rem]">The confident headline</dd>
    </div>
    <div>
      <dt className="font-sans text-xs uppercase tracking-wide text-muted-foreground">h1 · Bloc Bold · 56px desktop</dt>
      <dd className="font-display text-4xl font-bold leading-tight tracking-tight text-primary lg:text-[3.5rem]">Page-level heading</dd>
    </div>
    <div>
      <dt className="font-sans text-xs uppercase tracking-wide text-muted-foreground">h2 · Bloc Bold · 36px desktop</dt>
      <dd className="font-display text-2xl font-bold leading-snug tracking-tight text-primary lg:text-4xl">Section heading</dd>
    </div>
    <div>
      <dt className="font-sans text-xs uppercase tracking-wide text-muted-foreground">h3 · Bloc Bold · 24px desktop</dt>
      <dd className="font-display text-xl font-semibold leading-snug text-primary lg:text-2xl">Subsection heading</dd>
    </div>
    <div>
      <dt className="font-sans text-xs uppercase tracking-wide text-muted-foreground">body-lg · Mont · 18px</dt>
      <dd className="font-sans text-lg leading-relaxed text-primary">Larger body copy for pull quotes and lead paragraphs.</dd>
    </div>
    <div>
      <dt className="font-sans text-xs uppercase tracking-wide text-muted-foreground">body · Mont · 16px</dt>
      <dd className="font-sans text-base leading-relaxed text-primary">Default body copy for paragraphs, descriptions, and longform content.</dd>
    </div>
  </dl>
</div>

{/* Spacing — horizontal bars */}
<div id="spacing" className="mt-16">
  <h3 className="font-display text-xl font-semibold text-primary lg:text-2xl">Spacing</h3>
  <div className="mt-6 space-y-2">
    {[4, 8, 16, 24, 32, 48, 64, 96, 128].map((px) => (
      <div key={px} className="flex items-center gap-4">
        <span className="w-20 font-sans text-sm text-muted-foreground">{px}px</span>
        <div className="h-2 rounded-full bg-brand-navy" style={{ width: `${px}px` }} aria-hidden="true" />
      </div>
    ))}
  </div>
</div>

{/* Radius & Shadow */}
<div id="radius-shadow" className="mt-16">
  <h3 className="font-display text-xl font-semibold text-primary lg:text-2xl">Radius &amp; Shadow</h3>
  <div className="mt-6 flex flex-wrap gap-4">
    <div className="size-20 bg-brand-cream border border-border rounded-sm flex items-end justify-center p-2"><span className="font-sans text-xs">sm</span></div>
    <div className="size-20 bg-brand-cream border border-border rounded-md flex items-end justify-center p-2"><span className="font-sans text-xs">md</span></div>
    <div className="size-20 bg-brand-cream border border-border rounded-lg flex items-end justify-center p-2"><span className="font-sans text-xs">lg</span></div>
    <div className="size-20 bg-brand-cream border border-border rounded-xl flex items-end justify-center p-2"><span className="font-sans text-xs">xl</span></div>
    <div className="size-20 bg-brand-cream border border-border rounded-2xl flex items-end justify-center p-2"><span className="font-sans text-xs">2xl</span></div>
    <div className="size-20 bg-brand-cream border border-border rounded-full flex items-end justify-center p-2"><span className="font-sans text-xs">full</span></div>
  </div>
  <div className="mt-8 flex flex-wrap gap-6">
    <div className="size-20 bg-white rounded-lg shadow-sm flex items-end justify-center p-2"><span className="font-sans text-xs">sm</span></div>
    <div className="size-20 bg-white rounded-lg shadow-md flex items-end justify-center p-2"><span className="font-sans text-xs">md</span></div>
    <div className="size-20 bg-white rounded-lg shadow-lg flex items-end justify-center p-2"><span className="font-sans text-xs">lg</span></div>
    <div className="size-20 bg-white rounded-lg shadow-xl flex items-end justify-center p-2"><span className="font-sans text-xs">xl</span></div>
  </div>
</div>
```

**Step D — Populate Primitives section** with Button (showcasing `size="touch"` as default for marketing CTAs), Card, FAQItem, Badge, Avatar, Separator. For each primitive use the §4.4 per-primitive shape: `<div id="anchor">` with h3 + description + one rendered example + `<details><summary>Props</summary>...` metadata. Use a `grid-cols-12` layout with example on `col-span-8` and metadata on `col-span-4` (mirror direction per primitive for editorial asymmetry — §7.3).

Button example MUST include one row with `size="touch"` prominently labelled as "Recommended default for marketing CTAs" (UI-SPEC §3.1 FLAG-3).

Use the VERBATIM sample copy from UI-SPEC §6.1 for every example:
- Button: `"Book a free trial"` (default), `"Enquire"` (secondary), `"WhatsApp us"` (ghost), `"Delete enquiry"` (destructive), `"Learn more"` (outline), `"Read the coaching philosophy"` (link)
- Badge: `"Ages 4–6"` (secondary), `"Featured"` (default), `"New"` (destructive), `"Wan Chai"` (outline)
- FAQItem: question `"What should my child wear?"` answer from UI-SPEC §6.1 verbatim
- Avatar: image example alt `"Monica Hui, Director of Sports (Hong Kong)"`; fallback example with initials `"MH"`

**Step E — Populate Patterns section** with MarketCard (2 side-by-side HK + SG), ProgrammeTile (1), TestimonialCard (1 default + 1 pullquote), StatStrip (1), LogoWall (grid with text placeholders per UI-SPEC §6.1 fallback — partner SVG logos not yet provisioned), Section + ContainerEditorial demos.

MarketCard example copy:
- Card 1: `label="Hong Kong"`, `tagline="ProGym Wan Chai & Cyberport"`, `href="#"` (demo no-op), `imageSrc="/photography/{HERO_PHOTO}.jpg"`, `imageAlt="Children in a gymnastics class at ProGym Wan Chai"`, `priority`
- Card 2: `label="Singapore"`, `tagline="Prodigy @ Katong Point — Singapore's only MultiBall wall"`, `imageSrc="/photography/{SG_PHOTO}.jpg"`, `imageAlt="A coach guiding a child on the MultiBall wall at Prodigy Katong"`

ProgrammeTile example:
- `title="Toddler gymnastics"`, `ageRange="12 months – 3 years"`, `description="Parent-and-child classes building confidence, balance, and joy of movement from the earliest age."`, `duration="45 min · weekly"`, `imageSrc="/photography/{PROGRAMME_PHOTO}.jpg"`, `imageAlt="Parent and toddler on a soft beam at ProGym Wan Chai"`

TestimonialCard default variant:
- `quote="We tried three other schools before ProActiv. Monica and the team saw my daughter for who she is — that's the difference."`
- `author="Sarah W."`, `authorRole="Parent · Hong Kong"`

TestimonialCard pullquote variant:
- Same quote, `variant="pullquote"`

StatStrip:
- `stats={[{ value: "14", label: "Years coaching" }, { value: "3", label: "Venues across HK & SG" }, { value: "3,500+", label: "Children trained" }]}`

LogoWall (UI-SPEC §6.1 fallback — SVG logos not yet provisioned; render with styled text placeholders):
- Pass a `logos` array with 4 entries; since real SVG files aren't in `public/logos/` yet, the LogoWall component will try to load them and the Next.js Image will 404. Options: (1) add 4 temporary placeholder PNGs to `public/logos/`, or (2) skip LogoWall and render a styled text-block note: `<div className="p-8 border border-dashed rounded-lg text-muted-foreground">Partner logos pending — Martin provisions at public/logos/*.svg (HUMAN-ACTION precondition like D-02 fonts). See UI-SPEC §6.1 LogoWall note.</div>`. **Pick option 2** — simpler, honest about the current state, documents the gap.

Section demo: render 3 small Section instances side-by-side (one `bg="default"`, one `bg="muted"`, one `bg="navy"`), each with a single `<p>` showing the bg value.

ContainerEditorial demo: render 3 ContainerEditorial instances (narrow/default/wide), each with a visible border + label showing the width.

**Step F — Populate Media section:**
- Image contract: render a `<pre>` block with the `<Image>` usage pattern from UI-SPEC §3.14, followed by one actual `<Image>` instance showing a real photo at `aspect-video` (using any available processed photo slug)
- VideoPlayer: one `<VideoPlayer title="Mux demo stream — Phase 2 placeholder" />` (omits `playbackId` so it falls back to `PLACEHOLDER_PLAYBACK_ID`)

**Step G — Footer (already scaffolded in Task 1):** Keep the footer Section with done-bar copy. Add build date + git SHA via `process.env.VERCEL_GIT_COMMIT_SHA ?? 'local'` + a static date derived from `new Date().toISOString().slice(0, 10)` wrapped in `export const revalidate = false` behavior (already `force-static`).

**Step H — Build + smoke-test:**

```bash
pnpm typecheck
pnpm build 2>&1 | tail -15
pnpm dev &    # background
sleep 4
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/_design
kill %1 2>/dev/null
```

Expected: typecheck 0, build 0, HTTP 200 on `/_design`.

If any primitive import errors: check the corresponding Plan 02-04 primitive file for the exact export shape. Don't bypass errors with `// @ts-ignore` — fix at source.

If a primitive renders incorrectly visually (e.g., MarketCard gradient doesn't show): check brand token rendering via browser DevTools Computed style — `bg-brand-navy` should resolve to `#0f206c`. If it resolves to unset, Plan 02-01 tokens haven't cascaded — halt and escalate.
  </action>
  <verify>
    <automated>grep -q "from '@/components/ui/button'" app/_design/page.tsx &amp;&amp; grep -q "from '@/components/ui/market-card'" app/_design/page.tsx &amp;&amp; grep -q "from '@/components/ui/video-player'" app/_design/page.tsx &amp;&amp; grep -q "from 'next/image'" app/_design/page.tsx &amp;&amp; grep -q 'id="colors"' app/_design/page.tsx &amp;&amp; grep -q 'id="typography"' app/_design/page.tsx &amp;&amp; grep -q 'id="button"' app/_design/page.tsx &amp;&amp; grep -q 'id="marketcard"' app/_design/page.tsx &amp;&amp; grep -q 'id="programme-tile"' app/_design/page.tsx &amp;&amp; grep -q 'id="testimonial-card"' app/_design/page.tsx &amp;&amp; grep -q 'id="video-player"' app/_design/page.tsx &amp;&amp; grep -q 'size="touch"' app/_design/page.tsx &amp;&amp; grep -q 'Book a free trial' app/_design/page.tsx &amp;&amp; grep -q 'Ages 4' app/_design/page.tsx &amp;&amp; grep -q 'Monica Hui' app/_design/page.tsx &amp;&amp; grep -q 'Sarah W' app/_design/page.tsx &amp;&amp; grep -q 'Hong Kong' app/_design/page.tsx &amp;&amp; grep -q 'Singapore' app/_design/page.tsx &amp;&amp; pnpm typecheck &amp;&amp; pnpm build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 14 primitive imports present in page.tsx (count: Button, Card+sub-exports, Accordion via FAQItem, Badge, Avatar+sub-exports, Separator, Section, ContainerEditorial, FAQItem, MarketCard, ProgrammeTile, TestimonialCard, StatStrip, LogoWall, VideoPlayer + `next/image`)
    - All 4 anchor group IDs exist (`#foundation`, `#primitives`, `#patterns`, `#media`) plus per-primitive anchors (`#button`, `#marketcard`, etc.) — grep confirms several canonical IDs
    - Button demo uses `size="touch"` at least once — confirms UI-SPEC §3.1 FLAG-3 demonstrated
    - Sample copy from UI-SPEC §6.1 is used verbatim — grep finds at least "Book a free trial", "Ages 4", "Monica Hui", "Sarah W"
    - `pnpm typecheck` + `pnpm build` both exit 0
    - Pillar 3 (Spacing & rhythm): every `<Section>` uses a `size` prop — no ad-hoc `py-*`
    - Pillar 4 (Component consistency): all primitives imported via `@/components/ui/*` paths
    - Pillar 2 (Colour): no raw hex in `app/_design/page.tsx` except inside the Foundation swatches array (hex values are LABELS shown to the viewer, not applied as styles — the styles use `bg-brand-*` utilities)
    - UI-SPEC §7.3 editorial asymmetry: Foundation uses full-bleed swatches (not centered chips grid); Patterns use realistic page widths (not thumbnails) — verified by visual inspection in Task 3
  </acceptance_criteria>
  <done>
    - All 14 primitives rendered once with UI-SPEC §6.1 copy
    - Foundation + Footer populated
    - Editorial-asymmetry layout (not uniform card grid)
    - Build + typecheck pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Lighthouse + axe-core + visual verification on /_design/</name>
  <read_first>
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-UI-SPEC.md` §11 (six-pillar checklist — binding verification) + §1.3 (contrast audit rows)
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-CONTEXT.md` D-05 done-bar
    - `.planning/ROADMAP.md` Phase 2 Success Criteria 1-5 (as amended by D-06 for SC #3)
  </read_first>
  <what-built>Full `/_design/` gallery route. This task VERIFIES it passes the Phase 2 success criteria. This is the goal-backward verification gate — Phase 2 ships only if this task passes.</what-built>
  <action>See &lt;how-to-verify&gt; below — this is a checkpoint task; the executor runs the described verification steps and awaits the resume-signal. All concrete actions (bash commands, file checks, pass/fail conditions) are specified in &lt;how-to-verify&gt;.</action>
  <verify>
    <automated>See &lt;how-to-verify&gt; — this checkpoint uses interactive/visual verification steps that gate the plan on user approval (resume-signal). Automated sub-checks embedded in the how-to-verify bash snippets are the verify anchors.</automated>
  </verify>
  <how-to-verify>
**Step A — Spin up a local preview server:**

```bash
pnpm build
pnpm start &
sleep 3
```

`pnpm start` serves the production build locally on port 3000. `/_design` should be accessible because `VERCEL_ENV` is unset locally (not `'production'`).

Smoke-test:
```bash
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/_design
```
Expected: `200`.

**Step B — Lighthouse mobile audit** (measures SC #2 + SC #5 + Pillar 1 CLS + Pillar 6 Performance):

Run one of these:
- Chrome DevTools > Lighthouse > Mobile > Performance + Accessibility + Best Practices + SEO
- OR `npx lighthouse@latest http://localhost:3000/_design --preset=desktop --form-factor=mobile --only-categories=performance,accessibility,best-practices,seo --output=json --output-path=/tmp/lighthouse-design.json --chrome-flags="--headless"`

Record the 4 scores:
- Performance ≥95 (strategy budget, PROJECT.md)
- Accessibility ≥95 (UI-SPEC §11 Pillar 5)
- Best Practices ≥95
- SEO n/a or N/A (internal page — noindex)

Also record Core Web Vitals from the Lighthouse run:
- **LCP < 2.5s** (SC #2 ROADMAP)
- **CLS = 0.0** (SC #2 + DS-02 — Pitfall 2 verification that `adjustFontFallback: 'Arial'` works)
- **INP < 200ms** (Interaction to Next Paint — if Lighthouse doesn't measure directly, record TBT < 200ms as proxy)

**PASS CONDITION**: Performance ≥95 AND Accessibility ≥95 AND CLS = 0.0 AND LCP < 2.5s.

**FAIL CONDITION**: Any metric below threshold. If CLS > 0, Plan 02-02's `adjustFontFallback` setting or font preload order is the likely cause — escalate with specific CLS contributor (Lighthouse reports the element). If Performance <95, likely cause is the VideoPlayer loading above the fold (it should lazy-hydrate via `dynamic({ ssr: false })`) or un-optimized MarketCard images — check `next/image` config.

**Step C — axe-core accessibility scan** (measures SC #1 + Pillar 5):

Run:
```bash
npx @axe-core/cli http://localhost:3000/_design --exit --save /tmp/axe-design.json
```

Alternative if `@axe-core/cli` not available:
- Open `/_design/` in Chrome
- DevTools > Lighthouse > Accessibility (already run in Step B) — expand the Accessibility category for individual failed audits
- OR open DevTools > Issues tab — WCAG violations surface there

**PASS CONDITION**: 0 critical violations + 0 serious violations. Moderate/minor violations acceptable but documented in SUMMARY.

Typical violations to check:
- Colour contrast (UI-SPEC §1.3 rule: yellow never on white as text; yellow as badge fill with navy text is 8.80:1 — passes)
- Missing alt text on images (every `<Image>` in the gallery must have non-empty alt)
- Missing aria-label on links (MarketCard + ProgrammeTile have aria-label already)
- Heading order (h1 → h2 → h3 — no skipped levels)
- Focus indicators on every interactive element

**Step D — Manual keyboard verification:**
1. Open `http://localhost:3000/_design` in Chrome
2. Press Tab repeatedly from the top of the page
3. **PASS**: Every interactive element (sidebar nav links, Button examples, FAQItem trigger, MarketCard links, ProgrammeTile link, LogoWall logos if linked, VideoPlayer controls) receives visible focus (navy ring visible)
4. Arrow-key navigation works inside FAQItem (Radix Accordion handles this)
5. Enter activates the focused link/button
6. **FAIL**: Any interactive element without visible focus, or a keyboard trap (Tab gets stuck on VideoPlayer, for example)

**Step E — Visual editorial-asymmetry + anti-AI-SaaS verification** (UI-SPEC §7 + §7.3):
1. Take a full-page screenshot (Chrome DevTools > Device toolbar > capture full size)
2. **PASS signals** (UI-SPEC §7.2):
   - Foundation swatches are full-bleed (not centered in a narrow column)
   - Patterns use varied layout — not a uniform card grid
   - H1 / H2 are large-scale (display at 88px desktop)
   - Real photography in MarketCard + ProgrammeTile + TestimonialCard (if processed photos available)
   - Navy primary, yellow accent — not purple gradients
   - Navy is the primary CTA colour (Button default variant) — not a ghost grey outline
3. **FAIL signals** (UI-SPEC §7.1):
   - Purple-to-blue gradients
   - Centre-aligned hero with gradient blob
   - Identical 3-column "feature tile" grid as the only layout
   - Stock photography (not ProActiv's)
   - Ghost grey outline as the only primary CTA

**Step F — Production env-gate verification** (SC-equivalent):

Write or use a one-off Node test:
```bash
cat > /tmp/env-gate.test.mjs <<'EOF'
// Verify that setting VERCEL_ENV=production causes the page to call notFound()
// Since notFound() throws, we can detect it in a harness.
process.env.VERCEL_ENV = 'production';
// Cannot easily simulate Next.js RSC lifecycle from outside, so this is a
// documented grep verification instead:
import { readFile } from 'node:fs/promises';
const src = await readFile('app/_design/page.tsx', 'utf8');
if (!src.includes("process.env.VERCEL_ENV === 'production'")) {
  console.error('MISSING: production env check');
  process.exit(1);
}
if (!src.includes('notFound()')) {
  console.error('MISSING: notFound() call');
  process.exit(1);
}
console.log('PASS: env-gate code is present');
EOF
node /tmp/env-gate.test.mjs
```

Exit 0 confirms the code-level gate is present. Full runtime verification happens when deploying to Vercel production (Phase 10) and confirming `/_design` returns 404 there.

**Step G — Record results + produce Gallery Snapshot for SUMMARY:**

Paste into SUMMARY:
- Lighthouse 4 scores (Performance / Accessibility / Best Practices / SEO)
- LCP / CLS / INP figures
- axe-core critical + serious violation counts
- Keyboard-walk result (PASS / FAIL + notes)
- Editorial-asymmetry visual verdict (PASS / FAIL with screenshot reference)
- Env-gate code-verification (PASS)

**Resume signals:**
- `approved` — all checks pass, Phase 2 is complete
- `lighthouse-fail: <category>: <score>` — specific Lighthouse failure, triggers revision
- `a11y-fail: <rule-id>` — specific axe-core violation
- `editorial-fail: <note>` — gallery looks AI-SaaS, triggers design revision
- `env-gate-fail` — code-level check failed

Kill the server after:
```bash
kill %1 2>/dev/null
```
  </how-to-verify>
  <acceptance_criteria>
    - Lighthouse mobile Performance ≥95 (SC #2 + Pillar 6)
    - Lighthouse mobile Accessibility ≥95 (SC #1 + Pillar 5)
    - CLS = 0.0 (SC #2 + DS-02 + Pillar 1)
    - LCP < 2.5s (SC #2)
    - axe-core: 0 critical + 0 serious violations (SC #1 + Pillar 5)
    - Every interactive element keyboard-reachable with visible focus indicator (Pillar 5)
    - Editorial-asymmetry visual verification PASS — gallery does not look AI-SaaS per UI-SPEC §7 (SC #5 + DS-06)
    - Env-gate code verified via grep: `process.env.VERCEL_ENV === 'production'` + `notFound()` both present in `app/_design/page.tsx` (D-09)
    - All 14 primitives visibly render on `/_design/`
    - Real photography renders in MarketCard + ProgrammeTile + TestimonialCard (IF Plan 02-05 Task 4 produced output — otherwise documented gap)
    - ROADMAP SC #3 (VideoPlayer) amended per D-06: VideoPlayer renders Mux demo stream on desktop + mobile without hydration errors — verified visually and via console (no "customElements is not defined" errors)
  </acceptance_criteria>
  <resume-signal>Type "approved" after all checks pass; or type "&lt;category&gt;-fail: &lt;detail&gt;" to signal a specific failure class</resume-signal>
  <done>
    - All six Phase 2 verification dimensions PASS (Lighthouse Performance + Accessibility, CLS, LCP, axe-core, keyboard walk, editorial asymmetry, env-gate)
    - SUMMARY records all scores + screenshots
    - Phase 2 is complete — Phase 3 (root gateway) can proceed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel edge → `/_design/` route | Gallery renders to any authenticated preview visitor; never serves in production |
| Browser → `@mux/mux-player-react` loading Mux CDN | Client loads public playback stream from `stream.mux.com` — no credentials transit |
| `pnpm photos:process` script → Local filesystem | Runs OFFLINE only; reads curated photos, writes processed variants to repo |
| Next.js build → Font files at `assets/brand/fonts/*.woff2` | Build-time only; fonts bundled to `/_next/static/media/*` and served from Vercel edge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `/_design/` route exposing internal design docs to production visitors | mitigate | Layered defences: (1) D-09 code-level env-gate `if (process.env.VERCEL_ENV === 'production') notFound();` at top of `app/_design/page.tsx` — verified via grep in Task 3 Step F; (2) `metadata.robots = { index: false, follow: false }` in `app/_design/layout.tsx` (belt-and-braces); (3) Phase 0 `X-Robots-Tag: noindex, nofollow` on all non-production responses (next.config.ts); (4) Vercel Deployment Protection on all preview URLs — 4-layer defence per UI-SPEC §4.1 |
| T-02-02 | Tampering | Sharp preprocessing script executing attacker-supplied image files during CI/build | mitigate | Sharp is devDependency ONLY; `pnpm photos:process` script is LOCAL ONLY — NEVER invoked during `next build` (Pitfall 4). Script reads `.planning/inputs/curated-hero-photos/` which is gitignored; Martin curates manually from trusted source (/Users/martin/Downloads/ProActive/). No CI integration, no auto-trigger on PR. |
| T-02-03 | Information Disclosure | `@mux/mux-player-react` leaking credentials to browser bundle | accept | D-06 Phase 2 uses Mux's PUBLIC demo stream (`DS00Spx1CV902MCtPj5WknGlR102V5HFkDe`) — playback ID is public by design. No signed JWT, no Mux account credentials in client code. Credentials concern re-enters scope in Phase 10 when real accounts provision. |
| T-02-04 | Denial of Service | Malicious or oversized user-supplied file served from `/_next/static/media/*` (fonts) | accept | Font files are delivered via Next.js build-time hashing — paths are cache-stable (`{hash}.woff2`). No user-controlled path traversal. Files provisioned exclusively by Martin at `assets/brand/fonts/` (Plan 02-02 Task 1 precondition gate). |

**Security enforcement:** Phase 2 threat surface is minimal — design system phase with no server mutations, no authn/z, no secrets handling. All four identified threats are either mitigated via layered defence (T-02-01, T-02-02) or accepted as low-risk with no practical exploit path (T-02-03, T-02-04). Phase 7 (SEO) + Phase 10 (WAF, rate limiting) address broader production threat vectors.
</threat_model>

<verification>
Plan-level verification (Task 3 drives most of this):

1. **Route buildable**: `pnpm build` lists `/_design` as a static route
2. **Runtime 200 local**: `curl http://localhost:3000/_design` after `pnpm start` returns HTTP 200
3. **14 primitives imported**: `grep -c "from '@/components/ui/" app/_design/page.tsx` returns ≥14
4. **Sample copy verbatim**: grep finds "Book a free trial", "Ages 4", "Monica Hui", "Sarah W" from UI-SPEC §6.1
5. **Env-gate present**: `grep "process.env.VERCEL_ENV === 'production'" app/_design/page.tsx` matches (D-09)
6. **Robots metadata**: `grep "robots: { index: false" app/_design/layout.tsx` matches
7. **Middleware unchanged**: `git diff middleware.ts` shows no changes (CONTEXT.md D-09)
8. **Nav anchors**: all 4 group anchors + per-primitive anchors present in _nav.tsx
9. **Lighthouse Performance ≥95**: recorded in SUMMARY Task 3 Step G
10. **Lighthouse Accessibility ≥95**: recorded in SUMMARY
11. **CLS = 0.0, LCP < 2.5s**: recorded in SUMMARY — **this is the DS-02 verification gate** (proves Plan 02-02's `adjustFontFallback` works)
12. **axe-core 0 critical + 0 serious**: recorded in SUMMARY
13. **Editorial-asymmetry visual verdict**: PASS screenshot + UI-SPEC §7 checklist in SUMMARY
14. **VideoPlayer hydrates cleanly**: no console "customElements is not defined" error — verified in Task 3 browser inspection (D-06 amendment of SC #3)
15. **No raw hex in gallery page** (except Foundation swatches' hex LABELS, which are visible text, not applied styles): `grep -E "bg-\[#" app/_design/page.tsx` returns 0 matches
</verification>

<success_criteria>
Plan 02-06 is complete — AND therefore Phase 2 is complete — when:

- [ ] DS-05 ("Component gallery (`/_design/` gated to dev/preview) showing every primitive in real ProActiv photography context") satisfied — gallery route exists, all 14 primitives rendered, real photos used where available, env-gate blocks production
- [ ] DS-06 ("Visual direction conforms to strategy §14.3 — not 'AI SaaS' aesthetic") satisfied — UI-SPEC §7 guardrails verified via Task 3 Step E visual inspection
- [ ] **ROADMAP SC #1**: Every primitive renders in `/_design/` against ≥1 real ProActiv photograph (or documented gap if Plan 02-05 Task 4 empty-ok) + passes keyboard-nav + WCAG AA contrast — VERIFIED via Task 3
- [ ] **ROADMAP SC #2**: Bloc Bold + Mont + Baloo self-hosted + preloaded + zero CLS on `/_design/` — VERIFIED via Task 3 Lighthouse run (CLS = 0.0 is the hard gate)
- [ ] **ROADMAP SC #3 (amended per D-06)**: VideoPlayer renders on desktop + mobile without hydration errors + Mux demo stream plays — VERIFIED via Task 3 visual inspection. Real camp clips → Phase 10.
- [ ] **ROADMAP SC #4**: Media inventory has hero-tier assets optimised — VERIFIED by `public/photography/*.{avif,webp,jpg}` existence (Plan 02-05 Task 4 output)
- [ ] **ROADMAP SC #5**: Dev viewing `/_design/` can visually distinguish ProActiv aesthetic from "AI SaaS" — VERIFIED via Task 3 Step E UI-SPEC §7 checklist
- [ ] Threat model approved — 4 STRIDE threats, 2 mitigated + 2 accepted with documented rationale
- [ ] D-05 done-bar met: one example per primitive, no variant matrices
- [ ] D-08 IA: single-scroll page, sticky sidebar, 4-group order
- [ ] D-09 env-gate: `VERCEL_ENV === 'production'` + `notFound()` in page
- [ ] All 6 Pillars (Typography CLS / Colour WCAG AA / Spacing rhythm / Component consistency / Accessibility / Performance) verified in Task 3
- [ ] Plan 02-06 SUMMARY records every Lighthouse score + axe-core finding + visual verdict
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system-component-gallery-media-pipeline/02-06-SUMMARY.md` documenting:
- 3 files created: `app/_design/layout.tsx`, `app/_design/_nav.tsx`, `app/_design/page.tsx` (line counts)
- Which primitive slugs from `public/photography/` were chosen for MarketCard / ProgrammeTile / TestimonialCard / Image contract demo
- If `public/photography/` was empty (Plan 02-05 Task 4 empty-ok path): documented gap with recommended follow-up
- LogoWall fallback: text-placeholder block instead of real logos (Martin-provisions SVGs deferred — documented)
- Task 3 verification outputs:
  - Lighthouse mobile scores: Performance / Accessibility / Best Practices / SEO (target ≥95 each)
  - Core Web Vitals: LCP (target <2.5s) / CLS (target 0.0) / INP or TBT (target <200ms)
  - axe-core: critical + serious violation counts (target 0/0)
  - Keyboard walk result (PASS/FAIL)
  - Editorial-asymmetry visual verdict with screenshot reference
  - Env-gate code verification result
- Threat model: 4 STRIDE threats with dispositions + mitigations
- Rule 1 corrections if any (e.g., `@mux/mux-player-react` type cast adjustment, Card sub-export shape variance)
- Phase 2 completion declaration: all 5 ROADMAP SCs met (SC #3 as amended by D-06), all 6 DS-XX requirements satisfied, all 9 D-XX decisions honoured
- Notes for Phase 3 UI researcher:
  - Token contract is frozen — Phase 3 consumes `--primary / --secondary / --accent / --muted / --brand-*` + `--font-display / --font-sans / --font-accent` + `--spacing-section-*` as declared
  - Primitive surface is the 14 in `components/ui/` — Phase 3 composes, does not invent
  - VideoPlayer placeholder persists through Phase 3-9; Phase 10 swaps in real Mux IDs
  - `public/photography/` pipeline is open — Phase 3+ drops more curated photos + re-runs `pnpm photos:process`
  - Baloo activation (D-03) is Phase 4's concern (HK ProGym layouts)
  - Route-group stripes from Phase 1 are removed by Phase 3 root / Phase 4 HK / Phase 5 SG
</output>

---
phase: 02
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/card.tsx
  - components/ui/accordion.tsx
  - components/ui/badge.tsx
  - components/ui/avatar.tsx
  - components/ui/separator.tsx
  - components/ui/button.tsx
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [DS-03]
tags: [shadcn, primitives, cva, wcag-touch-target]
must_haves:
  truths:
    - "Stock shadcn primitives Card, Accordion, Badge, Avatar, Separator exist at components/ui/*.tsx"
    - "Button has a new 'touch' size variant (h-11, 44x44px minimum) per UI-SPEC §3.1 FLAG-3 — WCAG 2.2 AA compliant default for marketing CTAs"
    - "All 5 new primitives inherit brand tokens from Plan 02-01 via the shadcn semantic var system (bg-primary = navy, bg-secondary = yellow, etc.)"
    - "No third-party registries used — shadcn default registry only (components.json.registries stays {})"
    - "pnpm build + pnpm typecheck both pass"
  artifacts:
    - path: components/ui/card.tsx
      provides: "Stock shadcn Card primitive with Card / CardHeader / CardTitle / CardDescription / CardContent / CardFooter exports"
      contains: "export"
    - path: components/ui/accordion.tsx
      provides: "Stock shadcn Accordion primitive (Radix-backed) with Accordion / AccordionItem / AccordionTrigger / AccordionContent exports"
      contains: "AccordionTrigger"
    - path: components/ui/badge.tsx
      provides: "Stock shadcn Badge primitive with variant prop (default/secondary/destructive/outline)"
      contains: "badgeVariants"
    - path: components/ui/avatar.tsx
      provides: "Stock shadcn Avatar primitive with Avatar / AvatarImage / AvatarFallback exports"
      contains: "AvatarImage"
    - path: components/ui/separator.tsx
      provides: "Stock shadcn Separator primitive with orientation prop"
      contains: "Separator"
    - path: components/ui/button.tsx
      provides: "Phase 1 Button extended with 'touch' size variant (h-11, 44px — WCAG 2.2 AA)"
      contains: "touch:"
  key_links:
    - from: components/ui/card.tsx
      to: app/globals.css semantic tokens
      via: "Tailwind utility classes (bg-card, text-card-foreground)"
      pattern: "bg-card|text-card-foreground"
    - from: components/ui/button.tsx CVA touch variant
      to: WCAG 2.2 AA 44x44px touch target
      via: "h-11 class = 44px"
      pattern: "touch:.*h-11"
---

<objective>
Install 5 stock shadcn primitives (Card, Accordion, Badge, Avatar, Separator) via the shadcn CLI, and extend the existing Button (Phase 1) with a new `touch` size variant that meets WCAG 2.2 AA 44×44px touch-target minimum. These primitives are the Radix-backed foundation Plan 02-04 composes its custom patterns on top of (MarketCard uses Card; FAQItem uses Accordion; TestimonialCard uses Card + Avatar; ProgrammeTile uses Card + Badge; StatStrip uses Separator).

Purpose: Satisfy DS-03's stock-primitive half per UI-SPEC §3.2–3.6 + D-04 "stock shadcn where possible" + §3.1 FLAG-3 touch-target resolution.

Output:
- 5 new files in `components/ui/` produced by the shadcn CLI (Card, Accordion, Badge, Avatar, Separator)
- 1 modified file: `components/ui/button.tsx` gains a `touch` size entry in the CVA `size` variants
- `package.json` / `pnpm-lock.yaml` updated if the CLI adds sub-dependencies (e.g., `@radix-ui/react-accordion`, `@radix-ui/react-avatar`, `@radix-ui/react-separator` — some may already be bundled via Phase 1's `radix-ui` metapackage)

Decision compliance:
- D-04 — stock shadcn for these 5 + custom for MarketCard/ProgrammeTile/TestimonialCard/StatStrip/LogoWall (next plan)
- UI-SPEC §3.1 FLAG-3 — `touch` h-11 size is the default for consumer-facing CTAs from Phase 3+; added to Phase 1 Button CVA config without restructuring it
- UI-SPEC §12 Registry Safety — only first-party shadcn registry used; `components.json.registries = {}` untouched
- Phase 1 Plan 01-02 SUMMARY — shadcn initialized with `style=radix-nova, baseColor=neutral, cssVariables=true, rsc=true`; Phase 2 does NOT re-init
- RESEARCH Topic 3 "Stock shadcn vs custom-built" — exact list matches

This plan is autonomous. All CLI invocations are non-interactive with `--yes` flag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-design-system-component-gallery-media-pipeline/02-CONTEXT.md
@.planning/phases/02-design-system-component-gallery-media-pipeline/02-UI-SPEC.md
@.planning/phases/02-design-system-component-gallery-media-pipeline/02-RESEARCH.md
@.planning/phases/02-design-system-component-gallery-media-pipeline/02-PATTERNS.md
@components/ui/button.tsx
@components.json
@package.json

<interfaces>
Existing Phase 1 components.json (do NOT re-init — used by CLI for output paths):

```json
{
  "style": "radix-nova",
  "baseColor": "neutral",
  "cssVariables": true,
  "rsc": true,
  "tsx": true,
  "iconLibrary": "lucide",
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "registries": {}
}
```

Current Phase 1 Button CVA (relevant extract — lines 23–35 of components/ui/button.tsx):

```typescript
size: {
  default: "h-8 gap-1.5 px-2.5 has-data-[icon=inline-end]:pr-2 has-data-[icon=inline-start]:pl-2",
  xs: "h-6 gap-1 rounded-[min(var(--radius-md),10px)] px-2 text-xs in-data-[slot=button-group]:rounded-lg has-data-[icon=inline-end]:pr-1.5 has-data-[icon=inline-start]:pl-1.5 [&_svg:not([class*='size-'])]:size-3",
  sm: "h-7 gap-1 rounded-[min(var(--radius-md),12px)] px-2.5 text-[0.8rem] in-data-[slot=button-group]:rounded-lg has-data-[icon=inline-end]:pr-1.5 has-data-[icon=inline-start]:pl-1.5 [&_svg:not([class*='size-'])]:size-3.5",
  lg: "h-9 gap-1.5 px-2.5 has-data-[icon=inline-end]:pr-2 has-data-[icon=inline-start]:pl-2",
  icon: "size-8",
  "icon-xs": "size-6 rounded-[min(var(--radius-md),10px)] in-data-[slot=button-group]:rounded-lg [&_svg:not([class*='size-'])]:size-3",
  "icon-sm": "size-7 rounded-[min(var(--radius-md),12px)] in-data-[slot=button-group]:rounded-lg",
  "icon-lg": "size-9",
},
```

The new `touch` size per UI-SPEC §3.1 + 02-PATTERNS.md §`components/ui/button.tsx`:

```typescript
touch: "h-11 gap-1.5 px-4 text-base has-data-[icon=inline-end]:pr-3 has-data-[icon=inline-start]:pl-3",
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install 5 stock shadcn primitives via CLI</name>
  <read_first>
    - `components.json` — verify the `radix-nova` preset is already initialized (Phase 1 Plan 01-02)
    - `components/ui/button.tsx` — the Phase 1 reference for how shadcn stock files are structured under radix-nova (CVA + cn() + data-slot pattern; Button is the template for every downstream custom primitive)
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-UI-SPEC.md` §3.2 (Card) + §3.3 (Accordion) + §3.4 (Badge) + §3.5 (Avatar) + §3.6 (Separator)
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-PATTERNS.md` §`components/ui/card.tsx` + §`accordion.tsx` + §`badge.tsx` + §`avatar.tsx` + §`separator.tsx` (all role-match Button as analog)
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-CONTEXT.md` D-04 (stock shadcn split)
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-RESEARCH.md` Topic 3 (install command recommendation)
    - `.planning/phases/01-next-js-foundation-subdomain-middleware-sanity-studio-scaffold-vercel-previews/01-02-SUMMARY.md` — shadcn v4.4.0 CLI usage from Phase 1 (preset enumeration model)
  </read_first>
  <behavior>
    - 5 new files exist under `components/ui/`: `card.tsx`, `accordion.tsx`, `badge.tsx`, `avatar.tsx`, `separator.tsx`
    - Each file is generated by `pnpm dlx shadcn@latest add <name> --yes` using the Phase 1 `components.json` preset (radix-nova / neutral / cssVariables / rsc / tsx / lucide)
    - Each file uses `cn()` from `@/lib/utils` (shadcn CLI defaults)
    - Each file exports the expected named exports per UI-SPEC §3.2–3.6
    - `package.json` may gain new sub-deps (e.g., `@radix-ui/react-accordion`, `@radix-ui/react-avatar`, `@radix-ui/react-separator`) if not already transitively provided by Phase 1's `radix-ui@1.4.3` metapackage. `pnpm-lock.yaml` updates accordingly.
    - `components.json.registries` stays as `{}` (Phase 1 Plan 01-02 lock)
    - `pnpm typecheck` + `pnpm build` both pass after install
  </behavior>
  <action>
Run the following commands **in sequence** from the repo root (`/Users/martin/Projects/proactive/`):

```bash
# Install 5 stock shadcn primitives.
# --yes flag skips interactive prompts. shadcn@latest (currently ^4.4.0 per package.json)
# reads components.json for the preset (radix-nova / neutral / cssVariables=true / rsc=true
# / iconLibrary=lucide). Files land at components/ui/*.tsx per the existing "ui" alias.
#
# Rule 1 adaptation: if shadcn v4 renames a block (e.g., "separator" → "Separator"),
# run the CLI command as-is; the CLI emits whatever the registry stores. Document
# any name deviations in the SUMMARY. Do NOT attempt to hand-write these files —
# the CLI output is authoritative per Phase 1 Plan 01-02 learning.

pnpm dlx shadcn@latest add card --yes
pnpm dlx shadcn@latest add accordion --yes
pnpm dlx shadcn@latest add badge --yes
pnpm dlx shadcn@latest add avatar --yes
pnpm dlx shadcn@latest add separator --yes
```

After the 5 commands complete:

1. Verify each target file exists:
   ```bash
   ls -la components/ui/card.tsx components/ui/accordion.tsx components/ui/badge.tsx components/ui/avatar.tsx components/ui/separator.tsx
   ```

2. Read each file briefly to confirm:
   - It uses `cn()` import from `@/lib/utils`
   - It emits a `data-slot="..."` attribute on the root element (shadcn v4 convention; button.tsx line 58 is the canonical example)
   - No unexpected third-party registry URL appears in the file header

3. Verify `components.json` still has `"registries": {}` — run:
   ```bash
   grep '"registries"' components.json
   ```
   If shadcn CLI accidentally wrote a non-empty registries field, revert it (first-party-only lock per UI-SPEC §12).

4. Commit the lockfile if `pnpm-lock.yaml` changed. Do NOT run `pnpm install --frozen-lockfile` — the CLI already ran `pnpm install` for new sub-deps.

5. Run `pnpm typecheck` and `pnpm build`. Both MUST exit 0. If `pnpm build` fails due to a Turbopack CJS/ESM interop issue on a Radix sub-dep (Phase 1 Plan 01-03 precedent with Sanity), add the offending package to `transpilePackages` in `next.config.ts` and re-run. Document the addition as a Rule 1 correction in the SUMMARY. If no issues, proceed without touching `next.config.ts`.

**Do NOT:**
- Re-run `pnpm dlx shadcn@latest init` — Phase 1 already initialized; re-init would reset `components.json` and regenerate `components/ui/button.tsx` with a neutral scheme (RESEARCH Pitfall 1 territory)
- Add `--registry <url>` to any CLI command — first-party-only per UI-SPEC §12
- Hand-edit the generated files in this task — Task 2 edits only `button.tsx`; any other customization happens in Plan 02-04 compositions
  </action>
  <verify>
    <automated>test -f components/ui/card.tsx &amp;&amp; test -f components/ui/accordion.tsx &amp;&amp; test -f components/ui/badge.tsx &amp;&amp; test -f components/ui/avatar.tsx &amp;&amp; test -f components/ui/separator.tsx &amp;&amp; grep -q 'from "@/lib/utils"' components/ui/card.tsx &amp;&amp; grep -q 'from "@/lib/utils"' components/ui/accordion.tsx &amp;&amp; grep -q 'from "@/lib/utils"' components/ui/badge.tsx &amp;&amp; grep -q 'from "@/lib/utils"' components/ui/avatar.tsx &amp;&amp; grep -q 'from "@/lib/utils"' components/ui/separator.tsx &amp;&amp; grep -q '"registries": {}' components.json &amp;&amp; pnpm typecheck &amp;&amp; pnpm build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 5 new files exist at `components/ui/card.tsx`, `components/ui/accordion.tsx`, `components/ui/badge.tsx`, `components/ui/avatar.tsx`, `components/ui/separator.tsx`
    - Each file imports `cn` from `@/lib/utils` (shadcn v4 radix-nova convention — grep match)
    - Each file contains at least one `data-slot=` attribute (shadcn v4 convention)
    - `components.json` still has `"registries": {}` (first-party-only — UI-SPEC §12)
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0 with all 5 new files in the module graph
    - If new sub-deps added to `package.json` / `pnpm-lock.yaml`: they are Radix primitives or transitive deps; no third-party registry packages
    - Pillar 4 (Component consistency): all 5 primitives use `cn()` + `data-slot` + token utilities — consistent with Phase 1's Button
  </acceptance_criteria>
  <done>
    - 5 stock primitives installed and type-checked
    - `components.json` lock preserved
    - `pnpm build` passes
    - Any new sub-deps noted in plan SUMMARY
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add 'touch' size variant to components/ui/button.tsx</name>
  <read_first>
    - `components/ui/button.tsx` — current CVA `size` variant map (lines 23–35)
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-UI-SPEC.md` §3.1 (Button primitive — FLAG-3 locked resolution on touch target policy)
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-PATTERNS.md` §`components/ui/button.tsx` — add 'touch' size variant (exact patch at line 264)
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-CONTEXT.md` D-04 (Button already installed Phase 1 — extend, don't re-install)
  </read_first>
  <behavior>
    - `components/ui/button.tsx` has a new `touch:` entry inside the `variants.size` object of the `cva()` call
    - The new entry is `touch: "h-11 gap-1.5 px-4 text-base has-data-[icon=inline-end]:pr-3 has-data-[icon=inline-start]:pl-3"` (UI-SPEC §3.1 + 02-PATTERNS.md exact pattern)
    - The existing `default`, `xs`, `sm`, `lg`, `icon`, `icon-xs`, `icon-sm`, `icon-lg` entries are preserved byte-for-byte
    - `defaultVariants.size` is STILL `"default"` — the `touch` variant is OPT-IN; Phase 3+ marketing pages will set `size="touch"` explicitly (the Phase 3+ UI-SPEC / plan author owns that decision)
    - `VariantProps<typeof buttonVariants>` now accepts `size: 'touch'` (TypeScript verifies automatically)
    - `pnpm typecheck` + `pnpm build` pass
  </behavior>
  <action>
Edit `components/ui/button.tsx`. In the `buttonVariants = cva(...)` call, inside `variants.size`, insert ONE new entry after the `lg` entry and before the `icon` entry.

Current snippet (lines 23–35 of Phase 1's button.tsx):
```typescript
size: {
  default:
    "h-8 gap-1.5 px-2.5 has-data-[icon=inline-end]:pr-2 has-data-[icon=inline-start]:pl-2",
  xs: "h-6 gap-1 rounded-[min(var(--radius-md),10px)] px-2 text-xs in-data-[slot=button-group]:rounded-lg has-data-[icon=inline-end]:pr-1.5 has-data-[icon=inline-start]:pl-1.5 [&_svg:not([class*='size-'])]:size-3",
  sm: "h-7 gap-1 rounded-[min(var(--radius-md),12px)] px-2.5 text-[0.8rem] in-data-[slot=button-group]:rounded-lg has-data-[icon=inline-end]:pr-1.5 has-data-[icon=inline-start]:pl-1.5 [&_svg:not([class*='size-'])]:size-3.5",
  lg: "h-9 gap-1.5 px-2.5 has-data-[icon=inline-end]:pr-2 has-data-[icon=inline-start]:pl-2",
  icon: "size-8",
  "icon-xs":
    "size-6 rounded-[min(var(--radius-md),10px)] in-data-[slot=button-group]:rounded-lg [&_svg:not([class*='size-'])]:size-3",
  "icon-sm":
    "size-7 rounded-[min(var(--radius-md),12px)] in-data-[slot=button-group]:rounded-lg",
  "icon-lg": "size-9",
},
```

Insert the `touch` variant between `lg` and `icon` so the file reads:

```typescript
size: {
  default:
    "h-8 gap-1.5 px-2.5 has-data-[icon=inline-end]:pr-2 has-data-[icon=inline-start]:pl-2",
  xs: "h-6 gap-1 rounded-[min(var(--radius-md),10px)] px-2 text-xs in-data-[slot=button-group]:rounded-lg has-data-[icon=inline-end]:pr-1.5 has-data-[icon=inline-start]:pl-1.5 [&_svg:not([class*='size-'])]:size-3",
  sm: "h-7 gap-1 rounded-[min(var(--radius-md),12px)] px-2.5 text-[0.8rem] in-data-[slot=button-group]:rounded-lg has-data-[icon=inline-end]:pr-1.5 has-data-[icon=inline-start]:pl-1.5 [&_svg:not([class*='size-'])]:size-3.5",
  lg: "h-9 gap-1.5 px-2.5 has-data-[icon=inline-end]:pr-2 has-data-[icon=inline-start]:pl-2",
  // Phase 2 Plan 02-03 — WCAG 2.2 AA touch target (44x44px). Per UI-SPEC §3.1 FLAG-3,
  // this is the DEFAULT size for all consumer-facing CTAs from Phase 3+ onward.
  // `size='default'` (h-8 / 32px) is reserved for internal/admin contexts.
  touch: "h-11 gap-1.5 px-4 text-base has-data-[icon=inline-end]:pr-3 has-data-[icon=inline-start]:pl-3",
  icon: "size-8",
  "icon-xs":
    "size-6 rounded-[min(var(--radius-md),10px)] in-data-[slot=button-group]:rounded-lg [&_svg:not([class*='size-'])]:size-3",
  "icon-sm":
    "size-7 rounded-[min(var(--radius-md),12px)] in-data-[slot=button-group]:rounded-lg",
  "icon-lg": "size-9",
},
```

**Do NOT:**
- Change `defaultVariants.size` from `"default"` to `"touch"` — Phase 3+ page authors opt in explicitly (per UI-SPEC §3.1: `size='touch'` is the "default for all marketing... contexts" but NOT the CVA library default — the distinction matters for internal/admin surfaces that rely on the current `default` h-8)
- Modify the `default`, `xs`, `sm`, `lg`, `icon`, `icon-*` entries
- Change the Button component's `size` prop type — `VariantProps<typeof buttonVariants>` picks up the new variant automatically
- Add any additional variants (the advisory UI-SPEC FLAG-3 locks the full touch policy at h-11 — additional sizes emerge from Phase 3+ page needs)
  </action>
  <verify>
    <automated>grep -q 'touch: "h-11 gap-1.5 px-4 text-base has-data-\[icon=inline-end\]:pr-3 has-data-\[icon=inline-start\]:pl-3"' components/ui/button.tsx &amp;&amp; grep -q 'default: "h-8 gap-1.5 px-2.5' components/ui/button.tsx &amp;&amp; grep -q 'lg: "h-9 gap-1.5 px-2.5' components/ui/button.tsx &amp;&amp; grep -q 'icon: "size-8"' components/ui/button.tsx &amp;&amp; grep -q 'defaultVariants:' components/ui/button.tsx &amp;&amp; grep -A2 'defaultVariants:' components/ui/button.tsx | grep -q 'size: "default"' &amp;&amp; pnpm typecheck &amp;&amp; pnpm build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `components/ui/button.tsx` contains the exact string `touch: "h-11 gap-1.5 px-4 text-base has-data-[icon=inline-end]:pr-3 has-data-[icon=inline-start]:pl-3"`
    - Existing `default`, `sm`, `lg`, `icon`, `icon-*` variant entries preserved byte-for-byte
    - `defaultVariants.size` remains `"default"` (opt-in, not forced default)
    - `pnpm typecheck` passes — TypeScript accepts `<Button size="touch" />` at consumer sites; `<Button size="invalid" />` fails
    - `pnpm build` passes
    - Pillar 5 (Accessibility) touch-target preconditions in place: Phase 3+ consumer CTAs can now use `size="touch"` to meet 44x44px minimum; Phase 2 gallery (Plan 02-06) will demonstrate `size="touch"` in the Button section
  </acceptance_criteria>
  <done>
    - Button CVA `size` map gains `touch: "h-11..."` entry
    - No other variant entries modified
    - TypeScript + build pass
  </done>
</task>

</tasks>

<verification>
Plan-level verification:

1. **All 6 primitive files present**: `ls components/ui/*.tsx | wc -l` returns `6` (button + card + accordion + badge + avatar + separator)
2. **Registry lock preserved**: `grep -c '"registries": {}' components.json` returns `1`
3. **No third-party registry imports**: `grep -R "https://" components/ui/*.tsx` returns empty or only documentation-style URLs (shadcn stock files sometimes embed a registry URL comment header — benign)
4. **Button touch variant**: `grep "touch:" components/ui/button.tsx` returns the new line
5. **Type integrity**: `pnpm typecheck` exits 0 — `VariantProps<typeof buttonVariants>` includes `'touch'` in the size union
6. **Build passes**: `pnpm build` exits 0 with all 6 primitive files in the module graph
7. **No Turbopack interop issues on Radix sub-deps** — if any fail, the SUMMARY documents the `transpilePackages` addition as a Rule 1 correction
</verification>

<success_criteria>
Plan 02-03 is complete when:
- [ ] DS-03's stock-primitive half is satisfied — Card, Accordion, Badge, Avatar, Separator exist at `components/ui/*.tsx`
- [ ] UI-SPEC §3.1 FLAG-3 touch-target resolution materialised — Button has a `touch` size variant at h-11 (44px, WCAG 2.2 AA)
- [ ] `components.json.registries` lock preserved (no third-party registries introduced)
- [ ] `pnpm typecheck` and `pnpm build` both pass
- [ ] All 5 stock files use `cn()` from `@/lib/utils` and emit `data-slot` attributes (shadcn v4 convention)
- [ ] `package.json` / `pnpm-lock.yaml` reflect any new Radix sub-dependencies the CLI pulled in
- [ ] Pillar 4 (Component consistency) — 6 primitives now share the same structural pattern (CVA + cn() + data-slot) ready for Plan 02-04 custom primitives to compose on top of
- [ ] Pillar 5 (Accessibility) — Button `touch` size ready for marketing CTAs from Phase 3+; Plan 02-06 gallery demonstrates the variant
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system-component-gallery-media-pipeline/02-03-SUMMARY.md` documenting:
- shadcn CLI commands run + their output excerpts (any preset enumeration or Rule 1 corrections)
- List of files added under `components/ui/` and their exported symbols
- Any new sub-dependencies added to `package.json` / `pnpm-lock.yaml`
- Whether any `transpilePackages` entries were needed (Rule 1 correction)
- The exact Button CVA diff (single-line insertion)
- Confirmation that `components.json.registries` remained `{}`
- `pnpm build` tail showing all 6 primitives in the build output
- Notes for Plan 02-04: which Card sub-exports (CardHeader, CardContent, etc.) are available; which Accordion sub-exports (AccordionItem, AccordionTrigger, AccordionContent) are available
</output>

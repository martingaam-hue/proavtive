---
phase: 02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
autonomous: true
requirements: [DS-01]
tags: [design-tokens, tailwind, css, brand]
must_haves:
  truths:
    - "ProActiv brand palette (7 hex values) renders as Tailwind utilities (bg-brand-*, text-brand-*)"
    - "shadcn semantic tokens (--primary, --secondary, --accent, --destructive, --ring) resolve to brand colours — navy primary, yellow secondary, cream accent, red destructive"
    - "Three section-spacing tokens (--spacing-section-sm/md/lg) generate Tailwind utilities py-section-sm/md/lg"
    - "Font-family CSS vars (--font-display, --font-sans, --font-accent) declared with safe Arial fallback so tokens resolve even before Plan 02-02 wires next/font/local"
    - "Phase 1's @theme inline { ... } binding table is preserved verbatim (per Plan 01-02 SUMMARY forbidden-edit rule)"
    - "pnpm build compiles with no Tailwind 'unknown utility' warnings"
  artifacts:
    - path: app/globals.css
      provides: "Brand token layer + brand-mapped :root oklch overrides"
      contains: "--color-brand-navy: #0f206c"
      contains2: "--color-brand-red: #ec1c24"
      contains3: "--color-brand-green: #0f9733"
      contains4: "--color-brand-sky: #0fa0e2"
      contains5: "--color-brand-yellow: #fac049"
      contains6: "--color-brand-cream: #fff3dd"
      contains7: "--spacing-section-sm: 4rem"
      contains8: "--spacing-section-md: 6rem"
      contains9: "--spacing-section-lg: 8rem"
      contains10: "oklch(0.2906 0.1328 267.05)"
  key_links:
    - from: app/globals.css @theme { } brand block
      to: Tailwind utility layer
      via: Tailwind v4 @theme directive
      pattern: "bg-brand-navy|text-brand-red|py-section-md"
    - from: app/globals.css :root oklch overrides
      to: "@theme inline { ... } binding table"
      via: "CSS custom-property cascade (--primary alias in binding table reads :root value)"
      pattern: "--color-primary: var\\(--primary\\)"
---

<objective>
Lay the ProActiv brand token layer into `app/globals.css` so every downstream primitive and page inherits navy/red/green/sky/yellow/cream + three font-family slots + section-spacing rhythm automatically. This is the foundation Plan 02-04 (custom primitives) and Plan 02-06 (gallery) compose against.

Purpose: Satisfy DS-01 per UI-SPEC §1.1–1.5 + §1.8–1.10. Phase 1 shipped shadcn neutral oklch values; Phase 2 brand-overrides the 10 semantic roles (--foreground, --primary, --secondary, --accent, --muted, --ring, --destructive + their foregrounds) and ADDs a bare `@theme { }` block above the existing `@theme inline { }` binding table.

Output: A single modified file — `app/globals.css` — with brand tokens threaded through Tailwind v4's utility generator.

Decision compliance:
- D-04 primitive library scope — tokens are the substrate all primitives rely on; this plan owns only the substrate
- UI-SPEC §1.3 WCAG AA contrast audit — the oklch values in this plan correspond to the audited pairings (navy/white 14.55:1, navy/cream 13.24:1, navy/yellow 8.80:1 for the secondary-foreground role)
- UI-SPEC §1.4 "oklch over HSL" — Phase 1 shipped oklch; this plan preserves oklch (do NOT downgrade to HSL)
- RESEARCH Pitfall 1 avoidance — no `--color-*: initial` used; brand tokens are ADDED alongside Tailwind defaults

This plan is autonomous. No user setup required — all work is CSS edits to an existing file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-design-system-component-gallery-media-pipeline/02-CONTEXT.md
@.planning/phases/02-design-system-component-gallery-media-pipeline/02-UI-SPEC.md
@.planning/phases/02-design-system-component-gallery-media-pipeline/02-RESEARCH.md
@.planning/phases/02-design-system-component-gallery-media-pipeline/02-PATTERNS.md
@app/globals.css

<interfaces>
<!-- Current structure of app/globals.css (from Phase 1 Plan 01-02) — PRESERVE the binding table; EXTEND with @theme { } above it and REPLACE :root oklch values. -->

Current state (130 lines):
- Lines 1–3: @import "tailwindcss"; @import "tw-animate-css"; @import "shadcn/tailwind.css";
- Line 5: @custom-variant dark (&:is(.dark *));
- Lines 7–49: @theme inline { ... } — 40+ aliases (--color-background: var(--background); etc.) — KEEP UNTOUCHED per Phase 1 forbidden-edit rule
- Lines 51–84: :root { ... } — neutral oklch values — REPLACE 10 semantic roles per §1.4 table; keep --radius 0.625rem, --border/--input/--chart-* defaults
- Lines 86–118: .dark { ... } — KEEP (dark mode deferred per CONTEXT.md)
- Lines 120–130: @layer base { * border outline; body bg text; html font-sans } — KEEP

Authoritative brand values (from UI-SPEC §1.2 + §1.4 tables):
hex + oklch conversions:
  navy   #0f206c  oklch(0.2906 0.1328 267.05)
  red    #ec1c24  oklch(0.6012 0.2339 26.97)
  green  #0f9733  oklch(0.5902 0.1753 146.10)
  sky    #0fa0e2  oklch(0.6694 0.1443 237.35)
  yellow #fac049  oklch(0.8399 0.1469 81.92)
  cream  #fff3dd  oklch(0.9678 0.0316 82.77)
  white  #ffffff  oklch(1 0 0)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Insert brand @theme { } block above the existing @theme inline</name>
  <read_first>
    - `app/globals.css` — read current state; confirm line 5 is `@custom-variant dark` and line 7 starts `@theme inline {`
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-UI-SPEC.md` §1.2 (brand palette table) + §1.5 (`@theme { }` additions code block) + §1.8 (spacing scale)
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-PATTERNS.md` §`app/globals.css` — extend with brand token layer (self-analog) — shows the exact insertion point
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-CONTEXT.md` D-01 + D-04 (brand palette + component split)
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-RESEARCH.md` Topic 1 (Tailwind v4 @theme) + Pitfall 1 (do NOT use --color-*: initial)
  </read_first>
  <behavior>
    - globals.css contains a NEW `@theme { }` block inserted between line 5 (`@custom-variant dark`) and the existing `@theme inline {` block
    - The new block declares exactly 6 brand colour vars (--color-brand-navy, --color-brand-red, --color-brand-green, --color-brand-sky, --color-brand-yellow, --color-brand-cream) using their hex values verbatim per UI-SPEC §1.2
    - The new block declares 3 font-family vars (--font-display, --font-sans, --font-accent) wrapped as `var(--font-bloc, "Arial", "Helvetica Neue", system-ui, sans-serif)` etc. per UI-SPEC §1.5 code block so they resolve to Arial fallback BEFORE Plan 02-02 wires localFont
    - The new block declares 3 spacing vars (--spacing-section-sm: 4rem, --spacing-section-md: 6rem, --spacing-section-lg: 8rem) per UI-SPEC §1.8
    - No `--color-*: initial` anywhere (Pitfall 1)
    - The existing `@theme inline { ... }` binding table is BYTE-FOR-BYTE unchanged (Phase 1 forbidden-edit rule — Plan 01-02 SUMMARY §Notes for Phase 2)
  </behavior>
  <action>
Insert the following block INTO `app/globals.css` between the `@custom-variant dark (&:is(.dark *));` line and the `@theme inline {` line. Do NOT modify any other line in this task. Copy verbatim — these values are authoritative per UI-SPEC §1.2, §1.5, §1.8:

```css
/* ── Phase 2 / Plan 02-01: ProActiv brand token layer ───────────────────── */
/* Extends Tailwind v4's default palette with brand-scoped utilities.
   Does NOT use --color-*: initial (RESEARCH Pitfall 1 — would wipe shadcn neutrals).
   Font vars use nested var() with Arial fallback so tokens resolve before Plan 02-02
   wires next/font/local (Plan 02-02 sets --font-bloc, --font-mont, --font-baloo via
   `localFont({ variable })` on <html>). */
@theme {
  /* ProActiv palette (PROJECT.md authoritative) */
  --color-brand-navy:   #0f206c;
  --color-brand-red:    #ec1c24;
  --color-brand-green:  #0f9733;
  --color-brand-sky:    #0fa0e2;
  --color-brand-yellow: #fac049;
  --color-brand-cream:  #fff3dd;

  /* Typography families — resolved by Plan 02-02 next/font/local variable= names.
     Fallback chain keeps the token usable pre-font-wiring. */
  --font-display: var(--font-bloc,  "Arial", "Helvetica Neue", system-ui, sans-serif);
  --font-sans:    var(--font-mont,  "Arial", "Helvetica Neue", system-ui, sans-serif);
  --font-accent:  var(--font-baloo, "Arial", "Helvetica Neue", system-ui, sans-serif);

  /* Section-spacing rhythm (strategy §14.6) — 64 / 96 / 128 px */
  --spacing-section-sm: 4rem;
  --spacing-section-md: 6rem;
  --spacing-section-lg: 8rem;
}

```

Generated utilities will include: `bg-brand-navy`, `text-brand-red`, `border-brand-green`, `ring-brand-sky`, `bg-brand-yellow`, `bg-brand-cream`, `font-display`, `font-sans`, `font-accent`, `py-section-sm`, `py-section-md`, `py-section-lg` (plus `mt-*`, `mb-*`, `gap-*` equivalents) — Tailwind v4 generates all namespace utilities automatically.

Do NOT touch lines starting at `@theme inline {` or anything below it in this task.
  </action>
  <verify>
    <automated>grep -n "color-brand-navy: #0f206c" app/globals.css && grep -n "color-brand-red: #ec1c24" app/globals.css && grep -n "color-brand-green: #0f9733" app/globals.css && grep -n "color-brand-sky: #0fa0e2" app/globals.css && grep -n "color-brand-yellow: #fac049" app/globals.css && grep -n "color-brand-cream: #fff3dd" app/globals.css && grep -n "spacing-section-sm: 4rem" app/globals.css && grep -n "spacing-section-md: 6rem" app/globals.css && grep -n "spacing-section-lg: 8rem" app/globals.css && grep -n "font-display: var(--font-bloc" app/globals.css && grep -n "font-sans: var(--font-mont" app/globals.css && grep -n "font-accent: var(--font-baloo" app/globals.css && grep -c "^@theme inline {" app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - grep finds all 6 `--color-brand-*` declarations with exact hex values: `#0f206c`, `#ec1c24`, `#0f9733`, `#0fa0e2`, `#fac049`, `#fff3dd`
    - grep finds all 3 `--spacing-section-*` declarations with exact values: `4rem`, `6rem`, `8rem`
    - grep finds all 3 font-family declarations with nested `var(--font-bloc|mont|baloo, ...)` fallback
    - grep confirms `@theme inline {` still appears EXACTLY ONCE (binding table preserved)
    - `grep -c "color-\*: initial" app/globals.css` returns 0 (Pitfall 1 avoided)
    - Pillar 4 (Component consistency): token-only substrate established — downstream primitives inherit automatically
    - UI-SPEC §1.5 compliance — code block in UI-SPEC matches this task's inserted block verbatim (allowing whitespace differences)
  </acceptance_criteria>
  <done>
    - `app/globals.css` has a new `@theme { }` block declaring 6 brand colour vars, 3 font-family vars, 3 section-spacing vars
    - The block sits between `@custom-variant dark (...);` and the existing `@theme inline { ... }` binding table
    - Existing `@theme inline { ... }` block is unchanged (one `@theme inline {` in the file)
    - No `--color-*: initial` present (grep returns 0 matches)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace :root oklch values with brand-mapped semantic tokens</name>
  <read_first>
    - `app/globals.css` — current state after Task 1; find the `:root { ... }` block around line 51
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-UI-SPEC.md` §1.4 (semantic token mapping table — authoritative oklch values)
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-PATTERNS.md` §`app/globals.css` — the `:root {}` replacement block at line 104
    - `.planning/phases/02-design-system-component-gallery-media-pipeline/02-CONTEXT.md` Claude's Discretion "HSL triplet conversion" — superseded by UI-SPEC §1.4 oklch (Phase 1 shipped oklch, UI-SPEC locks oklch continuation)
  </read_first>
  <behavior>
    - `:root { }` has its 10 brand-role values replaced per UI-SPEC §1.4 table exactly (navy primary, white primary-fg, yellow secondary, navy secondary-fg, cream accent, navy accent-fg, cream muted, 70%-navy muted-fg, red destructive, navy ring)
    - `:root` preserves Phase 1 values for `--radius: 0.625rem`, `--border: oklch(0.922 0 0)`, `--input: oklch(0.922 0 0)`, and all `--chart-*` / `--sidebar-*` / `--destructive-foreground` values NOT overridden by §1.4
    - `--background`, `--card`, `--popover` stay `oklch(1 0 0)` (white — unchanged)
    - `--foreground`, `--card-foreground`, `--popover-foreground` become navy `oklch(0.2906 0.1328 267.05)`
    - `.dark { }` block is UNCHANGED (dark mode deferred per CONTEXT.md Claude's Discretion)
    - `@layer base { }` block is UNCHANGED
  </behavior>
  <action>
In `app/globals.css`, find the `:root { ... }` block (Phase 1 shipped values). Replace ONLY the following 10 semantic role lines — leave every other line in `:root` untouched:

| Line | Old value | New value |
|------|-----------|-----------|
| `--foreground: oklch(0.145 0 0);` | neutral dark | `oklch(0.2906 0.1328 267.05);  /* navy — Phase 2 Plan 02-01 */` |
| `--card-foreground: oklch(0.145 0 0);` | neutral dark | `oklch(0.2906 0.1328 267.05);  /* navy */` |
| `--popover-foreground: oklch(0.145 0 0);` | neutral dark | `oklch(0.2906 0.1328 267.05);  /* navy */` |
| `--primary: oklch(0.205 0 0);` | neutral dark | `oklch(0.2906 0.1328 267.05);  /* navy */` |
| `--primary-foreground: oklch(0.985 0 0);` | neutral light | `oklch(1 0 0);                 /* white */` |
| `--secondary: oklch(0.97 0 0);` | neutral light | `oklch(0.8399 0.1469 81.92);   /* yellow — contrast w/ navy 8.80:1 */` |
| `--secondary-foreground: oklch(0.205 0 0);` | neutral dark | `oklch(0.2906 0.1328 267.05);  /* navy on yellow */` |
| `--muted: oklch(0.97 0 0);` | neutral light | `oklch(0.9678 0.0316 82.77);   /* cream */` |
| `--muted-foreground: oklch(0.556 0 0);` | neutral mid | `oklch(0.45 0.05 267);         /* 70% navy — verify live ≥4.5:1 */` |
| `--accent: oklch(0.97 0 0);` | neutral light | `oklch(0.9678 0.0316 82.77);   /* cream */` |
| `--accent-foreground: oklch(0.205 0 0);` | neutral dark | `oklch(0.2906 0.1328 267.05);  /* navy */` |
| `--destructive: oklch(0.577 0.245 27.325);` | generic red | `oklch(0.6012 0.2339 26.97);   /* brand red #ec1c24 */` |
| `--ring: oklch(0.708 0 0);` | neutral mid | `oklch(0.2906 0.1328 267.05);  /* navy — focus ring MUST match primary per UI-SPEC §1.4 */` |

**Preserve these Phase 1 values unchanged** (do NOT touch):
- `--background: oklch(1 0 0)` (white — already correct)
- `--card: oklch(1 0 0)`, `--popover: oklch(1 0 0)` (white — already correct)
- `--border: oklch(0.922 0 0)` (neutral — UI-SPEC §1.4 explicit "keep")
- `--input: oklch(0.922 0 0)` (neutral — UI-SPEC §1.4 explicit "keep")
- `--radius: 0.625rem` (UI-SPEC §1.9 explicit "keep")
- All `--chart-*` values (UI-SPEC doesn't map these — Phase 7+ analytics concern)
- All `--sidebar-*` values (UI-SPEC doesn't map these — Phase 3+ navigation concern)

**Do NOT touch**:
- The `@theme inline { ... }` binding table (Phase 1 Plan 01-02 SUMMARY §Notes for Phase 2 forbids this explicitly)
- The `.dark { ... }` block (dark mode deferred — CONTEXT.md Claude's Discretion)
- The `@layer base { ... }` block at the bottom

Each replaced line SHOULD include a trailing comment (`/* navy */`, `/* yellow */`, etc.) so a reader can see which brand role backs which semantic token without cross-referencing UI-SPEC §1.4.
  </action>
  <verify>
    <automated>grep -n "^  --primary: oklch(0.2906 0.1328 267.05);" app/globals.css && grep -n "^  --primary-foreground: oklch(1 0 0);" app/globals.css && grep -n "^  --secondary: oklch(0.8399 0.1469 81.92);" app/globals.css && grep -n "^  --secondary-foreground: oklch(0.2906 0.1328 267.05);" app/globals.css && grep -n "^  --accent: oklch(0.9678 0.0316 82.77);" app/globals.css && grep -n "^  --accent-foreground: oklch(0.2906 0.1328 267.05);" app/globals.css && grep -n "^  --muted: oklch(0.9678 0.0316 82.77);" app/globals.css && grep -n "^  --muted-foreground: oklch(0.45 0.05 267);" app/globals.css && grep -n "^  --destructive: oklch(0.6012 0.2339 26.97);" app/globals.css && grep -n "^  --ring: oklch(0.2906 0.1328 267.05);" app/globals.css && grep -n "^  --foreground: oklch(0.2906 0.1328 267.05);" app/globals.css && grep -n "^  --radius: 0.625rem;" app/globals.css && grep -n "^  --border: oklch(0.922 0 0);" app/globals.css && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep finds each of the 10 replaced values at the exact oklch triplets in UI-SPEC §1.4: `oklch(0.2906 0.1328 267.05)` for --primary/--foreground/--ring/--secondary-foreground/--accent-foreground/--card-foreground/--popover-foreground, `oklch(0.8399 0.1469 81.92)` for --secondary, `oklch(0.9678 0.0316 82.77)` for --accent/--muted, `oklch(0.45 0.05 267)` for --muted-foreground, `oklch(0.6012 0.2339 26.97)` for --destructive, `oklch(1 0 0)` for --primary-foreground
    - grep confirms `--radius: 0.625rem` preserved unchanged
    - grep confirms `--border: oklch(0.922 0 0)` preserved unchanged
    - `@theme inline { ... }` binding table still appears exactly once (Phase 1 preserved)
    - `.dark { ... }` block still present and unmodified (compare to Phase 1 snapshot)
    - `pnpm build` exits 0 with no "unknown utility class" or "undefined CSS variable" warnings in output (tail -20 shows "Compiled successfully" or equivalent)
    - Pillar 2 (Colour system): `bg-primary` will now render navy (verified implicitly by build success; visual verification happens in Plan 02-06 gallery)
    - UI-SPEC §1.4 compliance — every row in the table has a matching line in :root
    - `/* navy */`, `/* yellow */`, `/* cream */` comments present inline for auditability
  </acceptance_criteria>
  <done>
    - `:root { }` block has all 10 semantic roles replaced with brand-mapped oklch values
    - Preserved values (border, input, radius, chart, sidebar) are unchanged
    - `.dark { }`, `@theme inline { }`, `@layer base { }` blocks byte-for-byte unchanged
    - `pnpm build` completes without errors
  </done>
</task>

</tasks>

<verification>
Plan-level verification runs after both tasks complete:

1. **File syntax**: `pnpm build` exits 0. This exercises Tailwind v4's PostCSS + the `@theme { }` parser. Any malformed token, missing semicolon, or invalid oklch triplet surfaces here.
2. **Token uniqueness**: `grep -c "^@theme {" app/globals.css` returns `1` (the new brand block) — and `grep -c "^@theme inline {" app/globals.css` returns `1` (preserved binding table). Two distinct blocks, no duplication.
3. **No raw hex leaked to components**: `grep -rF "#" components/ui/*.tsx 2>/dev/null | grep -v "data-" | grep -v "aria-" | grep -v "//"` — should return only hash-prefixed attributes from imports/strings/comments, not colour literals. Pillar 2 check. (At this plan's completion only `button.tsx` exists; future plans enforce this continuously.)
4. **Phase 1 preservation**: `wc -l app/globals.css` should be within Phase 1's 130 lines + the ~18 new brand-block lines = ~148 lines. No accidental deletion.
5. **Typecheck unaffected**: `pnpm typecheck` exits 0 (tokens are CSS-level; TS is not touched).
</verification>

<success_criteria>
Plan 02-01 is complete when:
- [ ] DS-01 ("Tailwind config with brand tokens — primary #0f206c, accent #ec1c24, secondary green/sky/yellow/cream") is materialised in `app/globals.css` as 6 `--color-brand-*` declarations
- [ ] The 10 shadcn semantic tokens map to brand colours per UI-SPEC §1.4 (navy primary / yellow secondary / cream accent+muted / red destructive / navy ring)
- [ ] Section-spacing rhythm (`--spacing-section-sm/md/lg`) declared and utility-generable
- [ ] Font-family tokens declared with Arial fallback so Tailwind resolves them BEFORE Plan 02-02 wires `next/font/local` (prevents cascade bug if plans execute in parallel and 02-02 lands second)
- [ ] Phase 1's `@theme inline { ... }` binding table is preserved byte-for-byte
- [ ] `pnpm build` passes
- [ ] All must_haves.artifacts.contains* strings present via grep
- [ ] Pillar 2 (Colour system) preconditions in place for Plan 02-04 custom primitives: `bg-primary` resolves to navy, `bg-secondary` to yellow, `bg-accent`/`bg-muted` to cream
- [ ] Pillar 3 (Spacing) preconditions in place: `py-section-md` is a valid Tailwind utility
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system-component-gallery-media-pipeline/02-01-SUMMARY.md` documenting:
- Changes made to `app/globals.css` (byte count delta, lines added/replaced)
- Any Rule 1 deviations (library/syntax corrections vs plan instructions)
- Confirmation that `pnpm build` passed
- Confirmation that `@theme inline { }` and `.dark { }` blocks are unchanged
- Any notes for Plan 02-02 (font wiring) — specifically that `--font-bloc/--font-mont/--font-baloo` are the expected `variable` names to match the declared `@theme { }` fallback chain
</output>

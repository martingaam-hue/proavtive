---
phase: 04-hong-kong-market
plan: 06
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - app/hk/holiday-camps/page.tsx
  - app/hk/holiday-camps/opengraph-image.tsx
  - app/hk/birthday-parties/page.tsx
  - app/hk/birthday-parties/opengraph-image.tsx
  - app/hk/school-partnerships/page.tsx
  - app/hk/competitions-events/page.tsx
  - app/hk/coaches/page.tsx
  - app/hk/coaches/opengraph-image.tsx
  - app/hk/blog/page.tsx
  - app/hk/blog/opengraph-image.tsx
  - app/hk/faq/page.tsx
autonomous: true
requirements: [HK-05, HK-06, HK-07, HK-08, HK-09, HK-10, HK-11]

must_haves:
  truths:
    - "/holiday-camps/ renders 3 ProgrammeTile cards (Easter / Summer / Christmas) + booking CTA pre-fill ?subject=Holiday%20Camp"
    - "/birthday-parties/ renders 2hr hosted format explainer + apparatus station list + 3 testimonial cards + Send an Enquiry CTA"
    - "/school-partnerships/ renders LogoWall of partner schools (placeholder OK) + programme options + Send an Enquiry CTA"
    - "/competitions-events/ renders empty-state per UI-SPEC §Copywriting (heading 'Upcoming events will appear here.') + competitive pathway summary + Enquire CTA"
    - "/coaches/ renders combined HK team grid (D-08 — no venue split) with at least Monica + 2 placeholder coaches; if any portrait file missing in public/photography/, HUMAN-ACTION gate fires per D-09 (no silhouettes, no initials, no stock)"
    - "/blog/ renders 1-3 stub posts from HK_BLOG_POSTS_STUB with empty-state branch; each card has title/excerpt/category badge/date/read time"
    - "/faq/ renders all HK_FAQ_ITEMS grouped by group field (about/venues/gymnastics/camps/parties/pricing); FAQPage JSON-LD mainEntity matches visible Q&A char-for-char"
    - "Every page has unique <h1>, unique generateMetadata-style export, full openGraph block, exactly ONE <Image priority>"
    - "All booking/enquiry CTAs use either /book-a-trial/free-assessment/?venue=... (HK-12 entry) or /contact?market=hk (Phase 3 contact form for non-trial enquiries)"
  artifacts:
    - path: "app/hk/holiday-camps/page.tsx"
      provides: "HK-05 — Easter/Summer/Christmas holiday camps page"
      contains: "Holiday Camp"
    - path: "app/hk/birthday-parties/page.tsx"
      provides: "HK-06 — Birthday parties hub"
      contains: "Birthday"
    - path: "app/hk/school-partnerships/page.tsx"
      provides: "HK-07 — School partnerships page"
      contains: "School"
    - path: "app/hk/competitions-events/page.tsx"
      provides: "HK-08 — Competitions and events page with empty-state fallback"
      contains: "Upcoming events will appear here"
    - path: "app/hk/coaches/page.tsx"
      provides: "HK-09 — Combined HK team coaches page (D-08)"
      contains: "HK_COACHES"
    - path: "app/hk/blog/page.tsx"
      provides: "HK-10 — Blog hub with stub posts + empty-state"
      contains: "HK_BLOG_POSTS_STUB"
    - path: "app/hk/faq/page.tsx"
      provides: "HK-11 — Full HK FAQ hub with FAQPage JSON-LD"
      contains: "FAQPage"
  key_links:
    - from: "All 7 pages"
      to: "lib/hk-data.ts (HK_COACHES, HK_BLOG_POSTS_STUB, HK_FAQ_ITEMS)"
      via: "import"
      pattern: "from [\"']@/lib/hk-data[\"']"
    - from: "Coaches page"
      to: "public/photography/coach-*-portrait.{webp,avif,jpg}"
      via: "<Image src={coach.portrait}>"
      pattern: "coach-.*-portrait"
    - from: "FAQ page JSON-LD"
      to: "FAQItem visible Q&A text"
      via: "char-for-char matching mainEntity.name + acceptedAnswer.text"
      pattern: "FAQPage"
---

<objective>
Build the 7 supporting HK content pages — holiday camps, birthday parties, school partnerships, competitions/events, coaches, blog, FAQ — closing HK-05 through HK-11 in a single plan.

Purpose: These pages are content-heavy, code-light. Each composes from Phase 2 primitives + Phase 4 chrome (HKNav/HKFooter); none requires new components beyond what Plans 04-01 to 04-05 already shipped. Bundling them into one plan keeps Wave 3 parallel (independent file paths, no overlap with Plans 03/04/05/07).

Closes ROADMAP SC#5: "/blog/, /faq/, /coaches/, /competitions-events/, /school-partnerships/, /holiday-camps/, and /birthday-parties/ all render with at least placeholder content, unique metadata, and the shared HK nav — nothing 404s."

Output: 7 page files + 4 per-route OG images (camps, parties, coaches, blog — UI-SPEC §7 high-priority budget; school-partnerships, competitions-events, faq inherit HK layout default OG per Pitfall 5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-hong-kong-market/04-CONTEXT.md
@.planning/phases/04-hong-kong-market/04-RESEARCH.md
@.planning/phases/04-hong-kong-market/04-PATTERNS.md
@.planning/phases/04-hong-kong-market/04-UI-SPEC.md
@.planning/phases/04-hong-kong-market/04-01-SUMMARY.md
@.planning/phases/04-hong-kong-market/04-02-SUMMARY.md
@.planning/inputs/strategy.md

# Phase 3 analogs
@app/root/news/page.tsx
@app/root/brand/page.tsx
@app/root/careers/page.tsx
@components/root/leadership-card.tsx
@components/root/leadership-section.tsx

# Phase 4 deps
@lib/hk-data.ts
@lib/og-image.tsx

<interfaces>
<!-- Contracts available to executor. -->

From `lib/hk-data.ts` (Plan 04-01):
```typescript
export const HK_COACHES: HKCoach[];          // ≥3 entries (Monica + 2)
export const HK_BLOG_POSTS_STUB: HKBlogPost[]; // 2 entries (Plan 04-01 acceptance)
export const HK_FAQ_ITEMS: HKFAQItem[];        // 16 entries grouped
export const HK_GYMNASTICS_PROGRAMMES: HKGymnasticsProgramme[]; // 8 entries (used in /coaches/ per-coach venueTag)
```

From Phase 2 primitives:
```typescript
ProgrammeTile { title, ageBand, href, image, alt, tagline }
TestimonialCard { quote, attribution, role?, image? }
LogoWall { logos: { src, alt, width, height }[] }
FAQItem { id, question, answer }
Card / Badge / Section / ContainerEditorial / Button / Accordion
```

From `lib/og-image.tsx` (Plan 04-02):
```typescript
export async function createHKOgImage({ title, tagline }: OgImageOptions): Promise<ImageResponse>;
```

From Phase 3 LeadershipCard (analog for coach card):
```typescript
// components/root/leadership-card.tsx — props: { name, role, bio, portrait } — copy structure for HK coach card
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build /holiday-camps/, /birthday-parties/, /school-partnerships/, /competitions-events/ pages (HK-05, HK-06, HK-07, HK-08)</name>
  <files>app/hk/holiday-camps/page.tsx, app/hk/holiday-camps/opengraph-image.tsx, app/hk/birthday-parties/page.tsx, app/hk/birthday-parties/opengraph-image.tsx, app/hk/school-partnerships/page.tsx, app/hk/competitions-events/page.tsx</files>
  <read_first>
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-UI-SPEC.md §4 holiday-camps/birthday-parties/school-partnerships/competitions-events rows + §Copywriting Contract empty-state copy
    - /Users/martin/Projects/proactive/.planning/inputs/strategy.md §PART 4 §7-§8 (camps + parties revenue block) + §PART 6B §7-§8 + §PART 12 Tier 1 (HK content topics)
    - /Users/martin/Projects/proactive/app/root/news/page.tsx (analog — empty-state pattern, stub array iteration)
    - /Users/martin/Projects/proactive/app/root/brand/page.tsx (analog — editorial content pattern with hero + content sections + CTA)
    - /Users/martin/Projects/proactive/components/ui/programme-tile.tsx (consumed in /holiday-camps/ for 3 season cards)
    - /Users/martin/Projects/proactive/components/ui/testimonial-card.tsx (consumed in /birthday-parties/ for 3 testimonial slots)
    - /Users/martin/Projects/proactive/components/ui/logo-wall.tsx (consumed in /school-partnerships/ for partner logos)
    - /Users/martin/Projects/proactive/public/photography/ (verify available photos for hero images; if a planned filename is missing, STOP and trigger HUMAN-ACTION gate per D-09)
  </read_first>
  <action>
    Create 4 page files + 2 per-route OG image files. Each page follows the same anatomy: full openGraph + canonical, inline JSON-LD where applicable, hero with `<Image priority>`, content sections from Phase 2 primitives, booking CTA.

    **`app/hk/holiday-camps/page.tsx` (HK-05):**

    ```typescript
    // Phase 4 / Plan 04-06 — Holiday camps page (HK-05).
    import type { Metadata } from "next";
    import Image from "next/image";
    import Link from "next/link";
    import { ArrowRight, Calendar } from "lucide-react";
    import { Section } from "@/components/ui/section";
    import { ContainerEditorial } from "@/components/ui/container-editorial";
    import { Button } from "@/components/ui/button";
    import { ProgrammeTile } from "@/components/ui/programme-tile";

    export const metadata: Metadata = {
      title: "Holiday Camps Hong Kong — Easter, Summer, Christmas | ProGym",
      description: "ProGym holiday camps in Hong Kong — Easter, Summer, Christmas. Multi-day gymnastics + sports for ages 3-12 at Wan Chai & Cyberport. Book now.",
      openGraph: {
        title: "Holiday Camps Hong Kong — ProGym",
        description: "Easter, Summer, Christmas holiday camps for children 3-12 at ProGym Wan Chai & Cyberport.",
        url: "https://hk.proactivsports.com/holiday-camps/",
        siteName: "ProActiv Sports Hong Kong",
        locale: "en_HK",
        type: "website",
        images: [{ url: "/holiday-camps/opengraph-image", width: 1200, height: 630, alt: "ProGym holiday camps Hong Kong" }],
      },
      alternates: { canonical: "https://hk.proactivsports.com/holiday-camps/" },
    };

    const SEASONS = [
      { title: "Easter Camps", ageBand: "3–12yr", href: "/book-a-trial/free-assessment/?subject=Holiday%20Camp%20-%20Easter", image: "/photography/programme-holiday-camp.webp", alt: "Easter holiday camp at ProGym", tagline: "Two-week programme during the school break." },
      { title: "Summer Camps", ageBand: "3–12yr", href: "/book-a-trial/free-assessment/?subject=Holiday%20Camp%20-%20Summer", image: "/photography/programme-summer-camp.webp", alt: "Summer camp at ProGym", tagline: "Six weeks of gymnastics, sports, and play." },
      { title: "Christmas Camps", ageBand: "3–12yr", href: "/book-a-trial/free-assessment/?subject=Holiday%20Camp%20-%20Christmas", image: "/photography/programme-christmas-camp.webp", alt: "Christmas camp at ProGym", tagline: "Festive themed activities + skill-building." },
    ] as const;

    export default function HolidayCampsPage() {
      return (
        <>
          <Section size="lg" bg="default">
            <ContainerEditorial width="default">
              <div className="max-w-3xl">
                <h1 className="text-h1 font-display text-foreground">Holiday camps at ProGym Hong Kong.</h1>
                <p className="text-body-lg text-muted-foreground mt-4">
                  School holidays mean longer days at ProGym. Easter, Summer and Christmas camps run at both venues — half-day and full-day formats, mixed-age groups, and the same coaching standard as our weekly classes. Children build skills, make friends, and burn off term-time energy on real apparatus.
                </p>
                <div className="mt-6">
                  <Button asChild size="touch" className="bg-brand-red text-white hover:bg-brand-red/90">
                    <Link href="/contact?market=hk&subject=Holiday%20Camp">
                      Send an Enquiry <ArrowRight className="ml-2 size-4" aria-hidden />
                    </Link>
                  </Button>
                </div>
              </div>
            </ContainerEditorial>
          </Section>

          <Section size="md" bg="muted">
            <ContainerEditorial width="wide">
              <h2 className="text-h2 font-display text-foreground mb-8">Three seasons, three formats.</h2>
              <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
                {SEASONS.map((s, i) => (
                  <ProgrammeTile
                    key={s.title}
                    title={s.title}
                    ageBand={s.ageBand}
                    href={s.href}
                    image={s.image}
                    alt={s.alt}
                    tagline={s.tagline}
                  />
                ))}
              </div>
            </ContainerEditorial>
          </Section>

          <Section size="md" bg="default">
            <ContainerEditorial width="default">
              <div className="max-w-3xl">
                <h2 className="text-h2 font-display text-foreground mb-4">What's included</h2>
                <ul className="text-body text-muted-foreground space-y-2">
                  <li>· Daily structured gymnastics + sports session led by ProGym coaches</li>
                  <li>· Mixed apparatus rotation (bar, beam, floor, vault) appropriate to age</li>
                  <li>· Free play and team games to consolidate learning</li>
                  <li>· Sibling discount for multi-child bookings</li>
                  <li>· Half-day (morning) and full-day options at Wan Chai + Cyberport</li>
                </ul>
              </div>
            </ContainerEditorial>
          </Section>

          <Section size="lg" bg="navy">
            <ContainerEditorial width="default">
              <div className="text-center max-w-2xl mx-auto">
                <h2 className="text-h2 font-display text-white mb-3"><Calendar className="inline size-7 mr-2 align-middle" aria-hidden /> Ready to book the next holiday?</h2>
                <p className="text-body-lg text-cream mb-6">Camps fill quickly — especially Summer. Send us a quick enquiry with your child's age and preferred dates.</p>
                <Button asChild size="touch" className="bg-brand-red text-white hover:bg-brand-red/90">
                  <Link href="/contact?market=hk&subject=Holiday%20Camp">Send an Enquiry</Link>
                </Button>
              </div>
            </ContainerEditorial>
          </Section>
        </>
      );
    }
    ```

    Per-route OG `app/hk/holiday-camps/opengraph-image.tsx`:
    ```typescript
    import { createHKOgImage } from "@/lib/og-image";
    export const runtime = "nodejs";
    export const dynamic = "force-dynamic";
    export const size = { width: 1200, height: 630 };
    export const contentType = "image/png";
    export default function OG() {
      return createHKOgImage({ title: "Holiday Camps Hong Kong", tagline: "Easter · Summer · Christmas at ProGym Wan Chai & Cyberport." });
    }
    ```

    **`app/hk/birthday-parties/page.tsx` (HK-06):**

    Same anatomy. Hero + 2hr format explainer (3 bullets: Two-hour hosted format / Coach-led activities / Bring the cake) + apparatus station list (4 stations card grid) + 3 `<TestimonialCard>` quotes (placeholder copy in HK voice, no exclamations) + booking CTA `Send an Enquiry` to `/contact?market=hk&subject=Birthday%20Party`. metadata title `"Birthday Parties Hong Kong — Coach-Led at ProGym Wan Chai & Cyberport"`. canonical = `https://hk.proactivsports.com/birthday-parties/`.

    Per-route OG `app/hk/birthday-parties/opengraph-image.tsx`:
    ```typescript
    import { createHKOgImage } from "@/lib/og-image";
    export const runtime = "nodejs";
    export const dynamic = "force-dynamic";
    export const size = { width: 1200, height: 630 };
    export const contentType = "image/png";
    export default function OG() {
      return createHKOgImage({ title: "Birthday Parties Hong Kong", tagline: "Coach-led, two-hour parties at ProGym." });
    }
    ```

    **`app/hk/school-partnerships/page.tsx` (HK-07):**

    Same anatomy. Hero + LogoWall of partner schools (placeholder logos OK — see UI-SPEC §4 row note "text fallback OK"; LogoWall accepts placeholder src strings via the same fallback pattern Phase 2 used). Programme options section (3 cards: Curriculum Support / Inter-School Events / After-School Programmes) + Send an Enquiry CTA `/contact?market=hk&subject=School%20Partnership`. metadata title `"School Partnerships Hong Kong — ProGym for International Schools"`. canonical = `https://hk.proactivsports.com/school-partnerships/`. NO per-route OG — inherits HK layout default (Pitfall 5 budget).

    For LogoWall placeholder logos, generate 4-6 entries with text-fallback alt strings (e.g. `{ src: "/photography/logo-school-placeholder-1.webp", alt: "Partner international school 1", width: 120, height: 60 }`). If those placeholder files don't exist in `public/photography/`, STOP and HUMAN-ACTION gate. The LogoWall primitive is required to handle missing-image gracefully (fallback to text alt) — verify Phase 2 LogoWall implementation does this; if not, render placeholder cards with school-name text instead of `<LogoWall>`.

    **`app/hk/competitions-events/page.tsx` (HK-08):**

    Same anatomy. Hero with H1 "Competitions and events." + empty-state per UI-SPEC §Copywriting (heading: `Upcoming events will appear here.` body: `Our next inter-school meet and community sports day are being scheduled. Subscribe for notifications or contact us for current competitive-squad openings.`) + competitive-pathway summary section + `Send an Enquiry` CTA `/contact?market=hk&subject=Competitive%20Pathway%20Enquiry`. metadata title `"Competitions & Events — ProGym Hong Kong"`. canonical = `https://hk.proactivsports.com/competitions-events/`. NO per-route OG — inherits HK layout default.

    Per UI-SPEC §4 rows + §Copywriting Contract empty states.
  </action>
  <verify>
    <automated>
      test -f /Users/martin/Projects/proactive/app/hk/holiday-camps/page.tsx
      test -f /Users/martin/Projects/proactive/app/hk/holiday-camps/opengraph-image.tsx
      test -f /Users/martin/Projects/proactive/app/hk/birthday-parties/page.tsx
      test -f /Users/martin/Projects/proactive/app/hk/birthday-parties/opengraph-image.tsx
      test -f /Users/martin/Projects/proactive/app/hk/school-partnerships/page.tsx
      test -f /Users/martin/Projects/proactive/app/hk/competitions-events/page.tsx
      grep -q "Easter" /Users/martin/Projects/proactive/app/hk/holiday-camps/page.tsx
      grep -q "Summer" /Users/martin/Projects/proactive/app/hk/holiday-camps/page.tsx
      grep -q "Christmas" /Users/martin/Projects/proactive/app/hk/holiday-camps/page.tsx
      grep -q "ProgrammeTile" /Users/martin/Projects/proactive/app/hk/holiday-camps/page.tsx
      grep -q "canonical.*holiday-camps" /Users/martin/Projects/proactive/app/hk/holiday-camps/page.tsx
      grep -q "Birthday" /Users/martin/Projects/proactive/app/hk/birthday-parties/page.tsx
      grep -q "TestimonialCard" /Users/martin/Projects/proactive/app/hk/birthday-parties/page.tsx
      grep -q "canonical.*birthday-parties" /Users/martin/Projects/proactive/app/hk/birthday-parties/page.tsx
      grep -q "School" /Users/martin/Projects/proactive/app/hk/school-partnerships/page.tsx
      grep -q "canonical.*school-partnerships" /Users/martin/Projects/proactive/app/hk/school-partnerships/page.tsx
      grep -q "Upcoming events will appear here" /Users/martin/Projects/proactive/app/hk/competitions-events/page.tsx
      grep -q "canonical.*competitions-events" /Users/martin/Projects/proactive/app/hk/competitions-events/page.tsx
      for f in app/hk/holiday-camps/page.tsx app/hk/birthday-parties/page.tsx app/hk/school-partnerships/page.tsx app/hk/competitions-events/page.tsx; do
        cnt=$(grep -c "priority" "/Users/martin/Projects/proactive/$f")
        if [ "$cnt" != "1" ]; then echo "FAIL: $f priority count = $cnt (expected 1)"; exit 1; fi
      done
      cd /Users/martin/Projects/proactive && pnpm typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - All 4 page files + 2 OG image files exist
    - `/holiday-camps/` contains literals `Easter`, `Summer`, `Christmas` AND uses `ProgrammeTile` for season cards
    - `/birthday-parties/` contains literal `Birthday` in H1 + uses `TestimonialCard` (≥3 instances)
    - `/school-partnerships/` contains literal `School` in H1 + renders LogoWall OR placeholder cards
    - `/competitions-events/` contains verbatim empty-state heading `Upcoming events will appear here.` (UI-SPEC §Copywriting Contract)
    - All 4 pages have unique `<h1>` (one per file)
    - All 4 pages have full openGraph block with `siteName`, `locale: "en_HK"`, `images` array (no shallow merge — Pitfall 2)
    - All 4 pages have `alternates.canonical` matching their route URL
    - Each of 4 pages has exactly ONE `<Image>` with `priority` (Pitfall 6 — verified via per-file grep count)
    - Per-route OG image files for `holiday-camps` + `birthday-parties` ONLY (school-partnerships + competitions-events inherit layout default per Pitfall 5)
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>HK-05, HK-06, HK-07, HK-08 satisfied. Plan 06 a-quadrant complete.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build /coaches/ page + per-route OG (HK-09) — combined HK team with HUMAN-ACTION gate on portraits (D-09)</name>
  <files>app/hk/coaches/page.tsx, app/hk/coaches/opengraph-image.tsx</files>
  <read_first>
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-CONTEXT.md (D-07 fully populated, D-08 combined HK team no venue split, D-09 portrait HUMAN-ACTION gate — NO silhouettes / initials / stock)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-UI-SPEC.md §4 `/coaches/` row + §11 D-07/D-08/D-09 mapping
    - /Users/martin/Projects/proactive/components/root/leadership-card.tsx + leadership-section.tsx (Phase 3 analog — copy structure for HK coach card; HK coach card lives in app/hk/coaches/page.tsx as inline composition NOT a separate component, since combining 1 page with 1 card composition keeps Plan 06 file-count budget)
    - /Users/martin/Projects/proactive/lib/hk-data.ts (HK_COACHES — must have ≥3 entries with portrait paths)
    - /Users/martin/Projects/proactive/public/photography/ (HUMAN-ACTION verification: each HK_COACHES[i].portrait file MUST exist before page renders without broken image)
  </read_first>
  <action>
    **Step A — pre-flight HUMAN-ACTION audit:**

    BEFORE writing the page, run:

    ```bash
    cd /Users/martin/Projects/proactive
    # For each coach portrait declared in HK_COACHES, verify the file exists.
    # Extract paths from lib/hk-data.ts and check.
    grep -E "portrait:\\s*\"" lib/hk-data.ts | sed -E 's/.*"(\/photography\/[^"]+)".*/\\1/' | while read p; do
      base="${p%.*}"  # strip extension
      # Phase 2 photo pipeline produces .avif/.webp/.jpg variants; check at least .webp
      if ! ls "public${base}".* 2>/dev/null | grep -qE "\\.(webp|avif|jpg|png)$"; then
        echo "MISSING coach portrait file: public$p (or any variant)"
        exit 42
      fi
    done
    ```

    If exit code 42: STOP. Do NOT write the page. Surface a HUMAN-ACTION checkpoint to the executor harness with the exact list of missing portrait files and the directive: "Add source images at `assets/raw-photos/coaches/{slug}.jpg` and run `pnpm photos:process` (Phase 2 D-07 pipeline). Resume after files are processed into `public/photography/`. NO silhouettes, NO initials, NO stock per CONTEXT D-09."

    If exit code 0: continue to Step B.

    **Step B — `app/hk/coaches/page.tsx`:**

    ```typescript
    // Phase 4 / Plan 04-06 — HK coaches page (HK-09).
    // D-07 fully populated. D-08 combined HK team (no venue split). D-09 portrait HUMAN-ACTION gate
    // (verified pre-flight by Plan 04-06 Task 2 Step A).
    import type { Metadata } from "next";
    import Image from "next/image";
    import Link from "next/link";
    import { ArrowRight } from "lucide-react";
    import { Section } from "@/components/ui/section";
    import { ContainerEditorial } from "@/components/ui/container-editorial";
    import { Button } from "@/components/ui/button";
    import { Card } from "@/components/ui/card";
    import { Badge } from "@/components/ui/badge";
    import { HK_COACHES } from "@/lib/hk-data";

    export const metadata: Metadata = {
      title: "Meet the ProGym Hong Kong Team — Coaches | ProActiv Sports",
      description: "Meet the ProGym Hong Kong coaching team — Director Monica plus a combined Wan Chai and Cyberport team. Every coach completes our internal training course.",
      openGraph: {
        title: "Meet the ProGym Hong Kong Team",
        description: "Meet the ProGym Hong Kong coaching team — combined Wan Chai and Cyberport team.",
        url: "https://hk.proactivsports.com/coaches/",
        siteName: "ProActiv Sports Hong Kong",
        locale: "en_HK",
        type: "website",
        images: [{ url: "/coaches/opengraph-image", width: 1200, height: 630, alt: "ProGym Hong Kong coaching team" }],
      },
      alternates: { canonical: "https://hk.proactivsports.com/coaches/" },
    };

    // Person JSON-LD — one entry per coach (RESEARCH §Pattern 7 anticipates Phase 7 Person schema; Phase 4 ships this proactively for /coaches/).
    const coachesSchema = {
      "@context": "https://schema.org",
      "@graph": HK_COACHES.map(c => ({
        "@type": "Person",
        "name": c.name,
        "jobTitle": c.role,
        "worksFor": { "@id": "https://proactivsports.com/#organization" },
        "image": `https://hk.proactivsports.com${c.portrait}`,
        "description": c.bio,
      })),
    };

    export default function CoachesPage() {
      // D-07/D-08: lead = Monica (venueTag "both"); team = everyone else, single grid (no venue split).
      const lead = HK_COACHES.find(c => c.venueTag === "both");
      const team = HK_COACHES.filter(c => c !== lead);

      return (
        <>
          <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(coachesSchema) }} />

          {/* §1 Page hero */}
          <Section size="lg" bg="default">
            <ContainerEditorial width="default">
              <div className="max-w-3xl">
                <h1 className="text-h1 font-display text-foreground">Meet the <span className="font-accent text-brand-navy">ProGym</span> Hong Kong team.</h1>
                <p className="text-body-lg text-muted-foreground mt-4">
                  One team across Wan Chai and Cyberport. Every coach completes our internal training course — regardless of prior certification — so the standard you experience is the standard, full stop.
                </p>
              </div>
            </ContainerEditorial>
          </Section>

          {/* §2 Lead — Monica large card */}
          {lead && (
            <Section size="md" bg="muted">
              <ContainerEditorial width="default">
                <div className="grid grid-cols-1 md:grid-cols-3 gap-8 items-start max-w-4xl mx-auto">
                  <div className="relative aspect-[3/4] overflow-hidden rounded-xl">
                    <Image
                      src={lead.portrait}
                      alt={`Portrait of ${lead.name}, ${lead.role}`}
                      fill
                      priority
                      sizes="(max-width: 768px) 100vw, 33vw"
                      className="object-cover"
                    />
                  </div>
                  <div className="md:col-span-2">
                    <h2 className="text-h2 font-display text-foreground">{lead.name}</h2>
                    <p className="text-body-lg text-brand-navy font-semibold mt-1">{lead.role}</p>
                    <p className="text-body text-muted-foreground mt-4">{lead.bio}</p>
                  </div>
                </div>
              </ContainerEditorial>
            </Section>
          )}

          {/* §3 Combined team grid (D-08 — no venue split) */}
          <Section size="md" bg="default">
            <ContainerEditorial width="wide">
              <h2 className="text-h2 font-display text-foreground mb-8">The team</h2>
              <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                {team.map(coach => (
                  <Card key={coach.name} className="p-5 flex flex-col">
                    <div className="relative aspect-[3/4] overflow-hidden rounded-lg mb-4">
                      <Image
                        src={coach.portrait}
                        alt={`Portrait of ${coach.name}, ${coach.role}`}
                        fill
                        sizes="(max-width: 768px) 100vw, (max-width: 1024px) 50vw, 33vw"
                        className="object-cover"
                      />
                    </div>
                    <h3 className="text-h3 font-display text-foreground">{coach.name}</h3>
                    <p className="text-small text-brand-navy font-semibold mt-1">{coach.role}</p>
                    {coach.venueTag && coach.venueTag !== "both" && (
                      <Badge variant="secondary" className="mt-2 self-start">
                        Primarily {coach.venueTag === "wan-chai" ? "Wan Chai" : "Cyberport"}
                      </Badge>
                    )}
                    <p className="text-body text-muted-foreground mt-3">{coach.bio}</p>
                  </Card>
                ))}
              </div>
            </ContainerEditorial>
          </Section>

          {/* §4 Booking CTA */}
          <Section size="lg" bg="navy">
            <ContainerEditorial width="default">
              <div className="text-center max-w-2xl mx-auto">
                <h2 className="text-h2 font-display text-white mb-3">Train with us — start with a free trial.</h2>
                <p className="text-body-lg text-cream mb-6">A 30-minute assessment with one of our coaches. No commitment.</p>
                <Button asChild size="touch" className="bg-brand-red text-white hover:bg-brand-red/90">
                  <Link href="/book-a-trial/free-assessment/?venue=no-preference">
                    Book a Free Trial <ArrowRight className="ml-2 size-4" aria-hidden />
                  </Link>
                </Button>
              </div>
            </ContainerEditorial>
          </Section>
        </>
      );
    }
    ```

    **Step C — per-route OG `app/hk/coaches/opengraph-image.tsx`:**

    ```typescript
    import { createHKOgImage } from "@/lib/og-image";
    export const runtime = "nodejs";
    export const dynamic = "force-dynamic";
    export const size = { width: 1200, height: 630 };
    export const contentType = "image/png";
    export default function OG() {
      return createHKOgImage({ title: "Meet the ProGym HK Team", tagline: "One team. Two venues. Every coach completes our internal training." });
    }
    ```

    Per CONTEXT D-07/D-08/D-09. Per UI-SPEC §4 `/coaches/` row.
  </action>
  <verify>
    <automated>
      # Pre-flight portrait file existence check
      cd /Users/martin/Projects/proactive
      grep -E "portrait:\\s*\"" lib/hk-data.ts | sed -E 's/.*"(\/photography\/[^"]+)".*/\\1/' > /tmp/coach-portraits.txt
      missing=0
      while read p; do
        base="${p%.*}"
        if ! ls "public${base}".* 2>/dev/null | grep -qE "\\.(webp|avif|jpg|png)$"; then
          echo "MISSING: public$p"
          missing=1
        fi
      done < /tmp/coach-portraits.txt
      if [ "$missing" = "1" ]; then echo "HUMAN-ACTION required for coach portraits — see D-09"; exit 42; fi

      test -f /Users/martin/Projects/proactive/app/hk/coaches/page.tsx
      test -f /Users/martin/Projects/proactive/app/hk/coaches/opengraph-image.tsx
      grep -q "HK_COACHES" /Users/martin/Projects/proactive/app/hk/coaches/page.tsx
      grep -q "Person" /Users/martin/Projects/proactive/app/hk/coaches/page.tsx
      grep -q "venueTag" /Users/martin/Projects/proactive/app/hk/coaches/page.tsx
      grep -q "canonical.*coaches" /Users/martin/Projects/proactive/app/hk/coaches/page.tsx
      grep -q "createHKOgImage" /Users/martin/Projects/proactive/app/hk/coaches/opengraph-image.tsx
      grep -c "priority" /Users/martin/Projects/proactive/app/hk/coaches/page.tsx | awk '{ if($1 != 1) exit 1 }'
      cd /Users/martin/Projects/proactive && pnpm typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight portrait file existence check passes (or HUMAN-ACTION gate fired correctly with exact missing-file list)
    - `app/hk/coaches/page.tsx` exists; default export `CoachesPage`
    - File renders combined team in single `<grid>` (D-08 — no venue split heading like "Wan Chai team" / "Cyberport team")
    - File renders Monica (or whichever coach has `venueTag === "both"`) in dedicated lead section before the grid
    - File contains JSON-LD `Person` `@graph` entries — one per coach
    - File does NOT contain string `silhouette`, `initials`, or `placeholder` (D-09 — proves no fallback graphics smuggled in)
    - Exactly ONE `<Image>` has `priority` (the lead portrait — Pitfall 6)
    - `alternates.canonical` matches `https://hk.proactivsports.com/coaches/`
    - Per-route OG file at `app/hk/coaches/opengraph-image.tsx`
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>HK-09 satisfied. Combined HK team page renders with real photography (HUMAN-ACTION-verified). D-07/D-08/D-09 honoured.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build /blog/, /faq/ pages (HK-10, HK-11)</name>
  <files>app/hk/blog/page.tsx, app/hk/blog/opengraph-image.tsx, app/hk/faq/page.tsx</files>
  <read_first>
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-UI-SPEC.md §4 `/blog/` + `/faq/` rows + §3.10 Blog homepage section pattern + §3.11 FAQ homepage section pattern + §Copywriting Contract empty-state copy
    - /Users/martin/Projects/proactive/.planning/inputs/strategy.md §PART 6B §10 + §PART 6B §11 (blog topics, FAQ verbatim)
    - /Users/martin/Projects/proactive/app/root/news/page.tsx (analog — empty-state pattern; HK_BLOG_POSTS_STUB.length === 0 branch)
    - /Users/martin/Projects/proactive/lib/hk-data.ts (HK_BLOG_POSTS_STUB has 2 entries; HK_FAQ_ITEMS has 16 grouped)
    - /Users/martin/Projects/proactive/components/ui/accordion.tsx + faq-item.tsx (composed for /faq/)
  </read_first>
  <action>
    **`app/hk/blog/page.tsx`:**

    ```typescript
    // Phase 4 / Plan 04-06 — HK blog hub (HK-10).
    // RESEARCH Pattern 5: stub array shape compatible with Phase 6 Sanity Post schema.
    import type { Metadata } from "next";
    import Image from "next/image";
    import Link from "next/link";
    import { ArrowRight } from "lucide-react";
    import { Section } from "@/components/ui/section";
    import { ContainerEditorial } from "@/components/ui/container-editorial";
    import { Card } from "@/components/ui/card";
    import { Badge } from "@/components/ui/badge";
    import { Button } from "@/components/ui/button";
    import { HK_BLOG_POSTS_STUB } from "@/lib/hk-data";

    export const metadata: Metadata = {
      title: "ProActiv Hong Kong Blog — Gymnastics + Sports for Children",
      description: "Long-form guides, parent tips, and ProGym news from Hong Kong. Gymnastics progression, holiday camp planning, and what to expect at your first class.",
      openGraph: {
        title: "ProActiv Hong Kong Blog",
        description: "Long-form guides, parent tips, and ProGym news from Hong Kong.",
        url: "https://hk.proactivsports.com/blog/",
        siteName: "ProActiv Sports Hong Kong",
        locale: "en_HK",
        type: "website",
        images: [{ url: "/blog/opengraph-image", width: 1200, height: 630, alt: "ProActiv Hong Kong blog" }],
      },
      alternates: { canonical: "https://hk.proactivsports.com/blog/" },
    };

    export default function HKBlogPage() {
      const posts = HK_BLOG_POSTS_STUB;

      return (
        <>
          <Section size="lg" bg="default">
            <ContainerEditorial width="default">
              <div className="max-w-3xl">
                <h1 className="text-h1 font-display text-foreground">From the ProActiv HK blog.</h1>
                <p className="text-body-lg text-muted-foreground mt-4">
                  Long-form guides on gymnastics progression, holiday camp planning, and what to expect at your child's first class. Written by our coaches and the ProActiv Hong Kong team.
                </p>
              </div>
            </ContainerEditorial>
          </Section>

          <Section size="md" bg="muted">
            <ContainerEditorial width="wide">
              {posts.length === 0 ? (
                <div className="text-center max-w-2xl mx-auto">
                  <h2 className="text-h2 font-display text-foreground mb-3">New posts coming soon.</h2>
                  <p className="text-body text-muted-foreground">
                    We're preparing long-form guides on gymnastics progression, holiday camp planning, and what to expect at your first class. In the meantime, our coaches are happy to answer any question directly.
                  </p>
                  <div className="mt-6">
                    <Button asChild variant="outline" size="touch" className="border-brand-navy text-brand-navy hover:bg-brand-navy/5">
                      <Link href="/contact?market=hk">Send us a question</Link>
                    </Button>
                  </div>
                </div>
              ) : (
                <ul className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                  {posts.map(p => (
                    <li key={p.slug}>
                      {/* Phase 6 wires href to /blog/[slug]/; Phase 4 ships hash-link placeholder */}
                      <Card className="overflow-hidden h-full flex flex-col">
                        <div className="relative aspect-[4/3]">
                          <Image src={p.heroImage} alt={p.title} fill sizes="(max-width:1024px) 100vw, 33vw" className="object-cover" />
                        </div>
                        <div className="p-5 flex flex-col flex-1">
                          <Badge variant="secondary" className="self-start">{p.category}</Badge>
                          <h2 className="text-h3 font-display text-foreground mt-3">{p.title}</h2>
                          <p className="text-body text-muted-foreground mt-2 flex-1">{p.excerpt}</p>
                          <p className="text-small text-muted-foreground mt-3">
                            <time dateTime={p.publishedAt}>{p.publishedAt}</time> · {p.readTimeMinutes} min read
                          </p>
                        </div>
                      </Card>
                    </li>
                  ))}
                </ul>
              )}
            </ContainerEditorial>
          </Section>
        </>
      );
    }
    ```

    Per-route OG `app/hk/blog/opengraph-image.tsx`:
    ```typescript
    import { createHKOgImage } from "@/lib/og-image";
    export const runtime = "nodejs";
    export const dynamic = "force-dynamic";
    export const size = { width: 1200, height: 630 };
    export const contentType = "image/png";
    export default function OG() {
      return createHKOgImage({ title: "ProActiv HK Blog", tagline: "Guides on children's gymnastics + sports in Hong Kong." });
    }
    ```

    **`app/hk/faq/page.tsx`:**

    ```typescript
    // Phase 4 / Plan 04-06 — HK FAQ hub (HK-11).
    // Renders all HK_FAQ_ITEMS grouped by group field; FAQPage JSON-LD char-for-char per UI-SPEC §8.3.
    import type { Metadata } from "next";
    import Link from "next/link";
    import { ArrowRight } from "lucide-react";
    import { Section } from "@/components/ui/section";
    import { ContainerEditorial } from "@/components/ui/container-editorial";
    import { Button } from "@/components/ui/button";
    import { Accordion } from "@/components/ui/accordion";
    import { FAQItem } from "@/components/ui/faq-item";
    import { HK_FAQ_ITEMS } from "@/lib/hk-data";

    export const metadata: Metadata = {
      title: "FAQ — ProActiv Sports Hong Kong",
      description: "Common questions about ProGym children's gymnastics, classes, holiday camps, parties, venues, and pricing in Hong Kong.",
      openGraph: {
        title: "FAQ — ProActiv Sports Hong Kong",
        description: "Common questions about ProGym children's gymnastics, classes, holiday camps, parties, venues, and pricing.",
        url: "https://hk.proactivsports.com/faq/",
        siteName: "ProActiv Sports Hong Kong",
        locale: "en_HK",
        type: "website",
      },
      alternates: { canonical: "https://hk.proactivsports.com/faq/" },
    };

    const GROUP_LABELS: Record<string, string> = {
      about: "About ProGym Hong Kong",
      venues: "Venues",
      gymnastics: "Gymnastics",
      camps: "Holiday Camps",
      parties: "Birthday Parties",
      pricing: "Pricing",
    };

    // Build FAQPage JSON-LD with mainEntity matching visible Q&A char-for-char (UI-SPEC §8.3).
    const faqSchema = {
      "@context": "https://schema.org",
      "@graph": [
        {
          "@type": "FAQPage",
          mainEntity: HK_FAQ_ITEMS.map(item => ({
            "@type": "Question",
            name: item.question,
            acceptedAnswer: { "@type": "Answer", text: item.answer },
          })),
        },
        {
          "@type": "BreadcrumbList",
          itemListElement: [
            { "@type": "ListItem", position: 1, name: "ProActiv Sports Hong Kong", item: "https://hk.proactivsports.com/" },
            { "@type": "ListItem", position: 2, name: "FAQ", item: "https://hk.proactivsports.com/faq/" },
          ],
        },
      ],
    };

    export default function HKFAQPage() {
      // Group items by `group` field, preserving declaration order
      const groups = ["about", "venues", "gymnastics", "camps", "parties", "pricing"] as const;
      const grouped = groups.map(g => ({
        group: g,
        label: GROUP_LABELS[g],
        items: HK_FAQ_ITEMS.filter(i => i.group === g),
      })).filter(g => g.items.length > 0);

      return (
        <>
          <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(faqSchema) }} />

          <Section size="lg" bg="default">
            <ContainerEditorial width="default">
              <div className="max-w-3xl">
                <h1 className="text-h1 font-display text-foreground">Frequently asked questions.</h1>
                <p className="text-body-lg text-muted-foreground mt-4">
                  Most parents have these questions before their child's first class. If yours isn't here, send us a quick WhatsApp or enquiry — we usually reply same day.
                </p>
              </div>
            </ContainerEditorial>
          </Section>

          {grouped.map(g => (
            <Section key={g.group} size="md" bg={g.group === "about" ? "muted" : "default"}>
              <ContainerEditorial width="default">
                <div className="max-w-3xl mx-auto">
                  <h2 className="text-h2 font-display text-foreground mb-6">{g.label}</h2>
                  <Accordion type="single" collapsible className="flex flex-col gap-0">
                    {g.items.map(item => (
                      <FAQItem key={item.value} id={item.value} question={item.question} answer={item.answer} />
                    ))}
                  </Accordion>
                </div>
              </ContainerEditorial>
            </Section>
          ))}

          <Section size="lg" bg="navy">
            <ContainerEditorial width="default">
              <div className="text-center max-w-2xl mx-auto">
                <h2 className="text-h2 font-display text-white mb-3">Still have a question?</h2>
                <p className="text-body-lg text-cream mb-6">A coach is usually available to reply within a few hours.</p>
                <div className="flex flex-col sm:flex-row gap-3 justify-center">
                  <Button asChild size="touch" className="bg-brand-red text-white hover:bg-brand-red/90">
                    <Link href="/contact?market=hk">
                      Send an Enquiry <ArrowRight className="ml-2 size-4" aria-hidden />
                    </Link>
                  </Button>
                </div>
              </div>
            </ContainerEditorial>
          </Section>
        </>
      );
    }
    ```

    No per-route OG for /faq/ — inherits HK layout default (Pitfall 5 budget; FAQ page is reference, not a high-traffic social-share target).

    Per UI-SPEC §3.11 + §4 `/blog/` + `/faq/` + §8.3 (FAQ JSON-LD char-for-char rule).
  </action>
  <verify>
    <automated>
      test -f /Users/martin/Projects/proactive/app/hk/blog/page.tsx
      test -f /Users/martin/Projects/proactive/app/hk/blog/opengraph-image.tsx
      test -f /Users/martin/Projects/proactive/app/hk/faq/page.tsx
      grep -q "HK_BLOG_POSTS_STUB" /Users/martin/Projects/proactive/app/hk/blog/page.tsx
      grep -q "posts.length === 0" /Users/martin/Projects/proactive/app/hk/blog/page.tsx
      grep -q "From the ProActiv HK blog" /Users/martin/Projects/proactive/app/hk/blog/page.tsx
      grep -q "canonical.*blog" /Users/martin/Projects/proactive/app/hk/blog/page.tsx
      grep -q "createHKOgImage" /Users/martin/Projects/proactive/app/hk/blog/opengraph-image.tsx
      grep -q "HK_FAQ_ITEMS" /Users/martin/Projects/proactive/app/hk/faq/page.tsx
      grep -q "FAQPage" /Users/martin/Projects/proactive/app/hk/faq/page.tsx
      grep -q "Frequently asked questions" /Users/martin/Projects/proactive/app/hk/faq/page.tsx
      grep -q "canonical.*faq" /Users/martin/Projects/proactive/app/hk/faq/page.tsx
      cd /Users/martin/Projects/proactive && pnpm typecheck
      cd /Users/martin/Projects/proactive && pnpm test:unit --run 2>&1 | tail -10
      cd /Users/martin/Projects/proactive && pnpm build 2>&1 | grep -E "(error|Error|Compiled)" | head -10
    </automated>
  </verify>
  <acceptance_criteria>
    - `app/hk/blog/page.tsx` exists; renders 1-3 stub posts from HK_BLOG_POSTS_STUB
    - File contains empty-state branch with verbatim heading `New posts coming soon.` (UI-SPEC §Copywriting)
    - File has `alternates.canonical` matching `/blog/`
    - `app/hk/blog/opengraph-image.tsx` exports default returning `createHKOgImage`
    - `app/hk/faq/page.tsx` exists; iterates HK_FAQ_ITEMS grouped by group field
    - File has H1 verbatim `Frequently asked questions.`
    - File emits inline `FAQPage` + `BreadcrumbList` JSON-LD
    - File renders one `<Accordion>` block per group (about/venues/gymnastics/camps/parties/pricing)
    - File has `alternates.canonical` matching `/faq/`
    - NO per-route OG for /faq/ (inherits layout default — Pitfall 5)
    - `pnpm typecheck` exits 0
    - `pnpm build` reports no compile errors on the new routes
  </acceptance_criteria>
  <done>HK-10 + HK-11 satisfied. Plan 06 fully complete. ROADMAP SC#5 (all 7 supporting pages render, no 404s, unique metadata, shared HK nav) closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → 7 supporting pages | Public read-only; no input handling |
| Static data (HK_COACHES, HK_BLOG_POSTS_STUB, HK_FAQ_ITEMS) → JSON-LD + DOM | Build-time TS constants; no user input |
| Per-route OG image generation → Vercel build | 4 new OG routes (camps/parties/coaches/blog); under Pitfall-5 budget |
| Coach portrait images → public/photography/ | Real photography sourced from Phase 2 D-07 pipeline; no AI/stock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | dangerouslySetInnerHTML across coachesSchema + faqSchema | mitigate | Both schemas derive from build-time `HK_COACHES` / `HK_FAQ_ITEMS` constants. No user-input path. JSON.stringify produces well-formed JSON. Same pattern as Phase 3. |
| T-04-06-02 | Information Disclosure | Coach bios + portraits expose individual identity | accept | Coach bios and portraits are public marketing content the business already publishes. Each coach has consented (per business onboarding — out-of-band assumption). No PII beyond name/role/bio appears on the page. |
| T-04-06-03 | Spoofing | Bot scrapes blog stub array as canonical content | accept | Stub posts are explicitly marked `_stub` in code comments + carry shape `HKBlogPost` (no `published: true` flag) so a future search-engine signal-checker can flag pre-CMS placeholder. Until Phase 6 ships real CMS posts, this is acceptable temporary content. |
| T-04-06-04 | Tampering | URL query param `?subject=Holiday%20Camp` parameter pollution | mitigate | The /contact?market=hk endpoint (Phase 3) validates `subject` server-side; Phase 4 only emits the encoded value as a string. No injection vector at the source. |
| T-04-06-05 | Denial of Service | OG image build budget exhausts Vercel timeout | mitigate | Plan 06 adds 4 per-route OG images (camps + parties + coaches + blog). Combined with Plan 04 (2: wan-chai + cyberport) + Plan 05 (1: gymnastics) + Plan 02 (1: hk default) = 8 OG routes total — exactly the Pitfall 5 budget. /school-partnerships/, /competitions-events/, /faq/ inherit the layout default. |
| T-04-06-06 | Information Disclosure | Phase 3 contact endpoint accepts arbitrary subject string | accept | Phase 3 D-07 ratified this for "?subject=job" job-application linking. Same pattern reused for Holiday Camp + Birthday Party + School Partnership + Competitive Pathway enquiries. Server-side rate limiting + honeypot (Phase 3 D-04) carry forward. |
</threat_model>

<verification>
After all 3 tasks:

```bash
pnpm typecheck                                       # Must exit 0
pnpm lint                                            # Must exit 0
pnpm test:unit --run                                 # All HK tests still GREEN (Plan 06 pages have no Wave-0 tests; smoke verified by typecheck + build)
pnpm build                                           # All 7 new routes generate; OG images compile under budget
pnpm dev                                             # Manual smoke at all 7 URLs
```

Manual smoke (per ROADMAP SC#5: nothing 404s):
- `hk.localhost:3000/holiday-camps/` — 3 ProgrammeTile cards, hero, "What's included" bullets
- `hk.localhost:3000/birthday-parties/` — 2hr format explainer, 3 testimonials, Send an Enquiry CTA
- `hk.localhost:3000/school-partnerships/` — partner schools (LogoWall or text fallback), 3 programme options
- `hk.localhost:3000/competitions-events/` — empty-state with "Upcoming events will appear here." heading visible
- `hk.localhost:3000/coaches/` — Monica large lead card + combined team grid (no venue split)
- `hk.localhost:3000/blog/` — 2 stub post cards (since HK_BLOG_POSTS_STUB has 2 entries per Plan 04-01)
- `hk.localhost:3000/faq/` — accordion grouped by category (About/Venues/Gymnastics/Camps/Parties/Pricing); JSON-LD validates against Google Rich Results Test (when domain attached in Phase 10)
- All 7 pages: HKNav sticky red Book a Free Trial CTA visible above fold
</verification>

<success_criteria>
- [ ] All 7 supporting page files exist; nothing in HK tree 404s
- [ ] HK-05 holiday camps page with Easter/Summer/Christmas season cards
- [ ] HK-06 birthday parties page with 2hr format + testimonials + Send an Enquiry
- [ ] HK-07 school partnerships page with LogoWall / placeholder + Send an Enquiry
- [ ] HK-08 competitions/events page with verbatim empty-state copy
- [ ] HK-09 coaches page with combined HK team (D-08), HUMAN-ACTION-verified portraits (D-09)
- [ ] HK-10 blog page with HK_BLOG_POSTS_STUB stub posts + empty-state branch
- [ ] HK-11 FAQ page with grouped accordion + FAQPage JSON-LD char-for-char
- [ ] Each page has unique <h1>, full openGraph, canonical, exactly ONE `<Image priority>`
- [ ] Per-route OG total stays at 8 (Plan 02: 1 default + Plan 04: 2 + Plan 05: 1 + Plan 06: 4)
- [ ] `pnpm typecheck`, `pnpm lint`, `pnpm build` all clean
- [ ] HK-05 through HK-11 + ROADMAP SC#5 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-hong-kong-market/04-06-SUMMARY.md` documenting:
- 7 supporting routes shipped attestation (HK-05 through HK-11)
- Coach portrait HUMAN-ACTION outcome (which files were already present vs gate-fired-and-resumed)
- Per-route OG running total (8 — at budget per Pitfall 5)
- LogoWall placeholder strategy used for /school-partnerships/ (real logos vs text fallback)
- Carry-forward note for Plan 07: no shared file overlap; Plan 07 only touches /book-a-trial/, /api/contact, and emails/contact.tsx.
</output>

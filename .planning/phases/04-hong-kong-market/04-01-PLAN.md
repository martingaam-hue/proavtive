---
phase: 04-hong-kong-market
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - components/ui/navigation-menu.tsx
  - .env.example
  - lib/hk-data.ts
  - app/hk/page.test.tsx
  - app/hk/wan-chai/page.test.tsx
  - app/hk/cyberport/page.test.tsx
  - app/hk/gymnastics/pillar.test.tsx
  - app/hk/book-a-trial/free-assessment/booking-form.test.tsx
autonomous: true
requirements: [HK-01, HK-02, HK-03, HK-04, HK-05, HK-06, HK-07, HK-08, HK-09, HK-10, HK-11, HK-12]

must_haves:
  truths:
    - "`@radix-ui/react-navigation-menu` installed; `components/ui/navigation-menu.tsx` exists as a shadcn-style wrapper"
    - "`lib/hk-data.ts` exports HKVenue, HKCoach, HKBlogPost, HKFAQItem, HKVenueHours interfaces + populated HK_VENUES, HK_COACHES, HK_BLOG_POSTS_STUB, HK_FAQ_ITEMS, HK_GYMNASTICS_PROGRAMMES constants"
    - "All 5 Wave-0 Vitest test files exist and initially contain failing expectations (RED) that assert target behavior for later plans"
    - ".env.example lists NEXT_PUBLIC_MUX_HK_HERO_PLAYBACK_ID, NEXT_PUBLIC_HK_PHONE, NEXT_PUBLIC_HK_WHATSAPP, NEXT_PUBLIC_ROOT_URL, NEXT_PUBLIC_SG_URL, NEXT_PUBLIC_WAN_CHAI_MAP_EMBED, NEXT_PUBLIC_CYBERPORT_MAP_EMBED"
    - "`pnpm test:unit` runs without config errors (test files may have failing assertions — that is expected RED state)"
  artifacts:
    - path: "components/ui/navigation-menu.tsx"
      provides: "shadcn NavigationMenu primitive (Radix-backed) used by HKNav Gymnastics + Locations dropdowns"
      contains: "export { NavigationMenu, NavigationMenuList, NavigationMenuItem, NavigationMenuTrigger, NavigationMenuContent, NavigationMenuLink"
    - path: "lib/hk-data.ts"
      provides: "Shared static HK data + typed interfaces consumed by all HK pages and HKFooter"
      contains: "export const HK_VENUES"
    - path: "app/hk/page.test.tsx"
      provides: "HK homepage regression harness (HK-01)"
      contains: "describe(\"HK homepage\""
    - path: "app/hk/wan-chai/page.test.tsx"
      provides: "Wan Chai page regression harness (HK-02)"
      contains: "15/F, The Hennessy"
    - path: "app/hk/cyberport/page.test.tsx"
      provides: "Cyberport page regression harness (HK-03)"
      contains: "5,000 sq ft"
    - path: "app/hk/gymnastics/pillar.test.tsx"
      provides: "Gymnastics pillar + 8-sub-page regression harness (HK-04)"
      contains: "describe(\"Gymnastics pillar\""
    - path: "app/hk/book-a-trial/free-assessment/booking-form.test.tsx"
      provides: "Booking form submit + venue pre-fill regression harness (HK-12)"
      contains: "useSearchParams"
    - path: ".env.example"
      provides: "Documented HK-specific env vars (HUMAN-ACTION handoff)"
      contains: "NEXT_PUBLIC_MUX_HK_HERO_PLAYBACK_ID"
  key_links:
    - from: "All HK pages (Plans 03-07)"
      to: "lib/hk-data.ts"
      via: "import { HK_VENUES, HK_COACHES, HK_BLOG_POSTS_STUB, HK_FAQ_ITEMS, HK_GYMNASTICS_PROGRAMMES } from \"@/lib/hk-data\""
      pattern: "from [\"']@/lib/hk-data[\"']"
    - from: "components/hk/hk-nav.tsx (Plan 02)"
      to: "components/ui/navigation-menu.tsx"
      via: "import { NavigationMenu, NavigationMenuTrigger, NavigationMenuContent } from \"@/components/ui/navigation-menu\""
      pattern: "from [\"']@/components/ui/navigation-menu[\"']"
---

<objective>
Create Phase 4 foundation: shadcn `navigation-menu` primitive install, HK-specific static data module, Wave-0 test scaffolds for all Nyquist-validated behaviors, and documented env-var contract.

Purpose: Every downstream Phase 4 plan depends on this foundation. Without it, Plan 02 (layout) cannot build HKNav dropdowns, Plans 03-07 cannot import venue/coach/blog data, and Nyquist validation has no RED baseline to drive task verification.

Output:
- 1 new shadcn primitive file (`components/ui/navigation-menu.tsx`) + `@radix-ui/react-navigation-menu` dep
- 1 new data module (`lib/hk-data.ts`) with 5 typed interfaces + 5 populated constants (derived verbatim from strategy.md PART 4/6B/8/12)
- 5 new Vitest test files that will initially fail (RED) and turn GREEN as Plans 02-07 land
- `.env.example` amended with all Phase 4 env vars
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-hong-kong-market/04-CONTEXT.md
@.planning/phases/04-hong-kong-market/04-RESEARCH.md
@.planning/phases/04-hong-kong-market/04-PATTERNS.md
@.planning/phases/04-hong-kong-market/04-UI-SPEC.md
@.planning/phases/04-hong-kong-market/04-VALIDATION.md
@.planning/inputs/strategy.md

# Carry-forward references
@app/root/page.test.tsx
@app/root/contact/contact-form.test.tsx
@package.json

<interfaces>
<!-- Existing conventions to match. Extracted from Phase 3 committed code. -->

From `components/ui/button.tsx` (Phase 1+2):
```typescript
export const buttonVariants = cva(..., { variants: { size: { default, sm, lg, icon, touch } } });
```

From `app/root/page.test.tsx` (Phase 3 analog for page-test pattern):
```typescript
import { describe, it, expect, beforeAll, vi } from "vitest";
import { render } from "@testing-library/react";
// vi.mock of primitives; render(<Page />); getByRole('heading', { level: 1 })
```

From `app/root/contact/contact-form.test.tsx` (Phase 3 analog for form-test pattern):
```typescript
const mockSearchParams = { get: vi.fn() };
vi.mock("next/navigation", () => ({ useSearchParams: () => mockSearchParams }));
global.fetch = vi.fn().mockResolvedValue({ ok: true, json: async () => ({ success: true }) });
```

From `package.json` current state (Phase 1-3 installed):
```
"@radix-ui/react-accordion", "@radix-ui/react-avatar", "@radix-ui/react-label",
"@radix-ui/react-separator", "@radix-ui/react-slot", "@radix-ui/react-dialog" (Sheet)
// Missing for Phase 4:
// - @radix-ui/react-navigation-menu  (needed by D-03/D-04 HKNav dropdowns)
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install `navigation-menu` shadcn primitive + pull Radix dep</name>
  <files>package.json, pnpm-lock.yaml, components/ui/navigation-menu.tsx</files>
  <read_first>
    - /Users/martin/Projects/proactive/package.json (current installed deps — verify shadcn + Radix pattern already in place from Phase 1/2/3)
    - /Users/martin/Projects/proactive/components/ui/sheet.tsx (closest Radix-backed shadcn primitive analog already installed)
    - /Users/martin/Projects/proactive/components.json (shadcn CLI config from Phase 1 — required to invoke `npx shadcn add`)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-UI-SPEC.md §5.1 (HKNav dropdown spec — 8 Gymnastics items + 2 Locations items, Radix-default keyboard behavior)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-RESEARCH.md §Architecture Patterns / Pattern 8 (NavigationMenu wiring inside HKNav)
  </read_first>
  <action>
    Invoke shadcn CLI to add the `navigation-menu` primitive exactly as Phase 1/2/3 added prior primitives:

    ```bash
    pnpm dlx shadcn@latest add navigation-menu
    ```

    The CLI MUST:
    1. Install `@radix-ui/react-navigation-menu` (latest compatible with existing `@radix-ui/react-dialog` and React 19.2.4) into dependencies. If CLI does not pull the dep directly because the project uses the `radix-ui@1.4.3` metapackage, verify the metapackage version already exposes `react-navigation-menu` (it does in 1.4.x — see https://www.radix-ui.com/primitives/docs/components/navigation-menu). If not, run `pnpm add @radix-ui/react-navigation-menu` explicitly.
    2. Write `components/ui/navigation-menu.tsx` containing the canonical shadcn NavigationMenu wrapper (7 exports: `NavigationMenu`, `NavigationMenuList`, `NavigationMenuItem`, `NavigationMenuTrigger`, `NavigationMenuContent`, `NavigationMenuLink`, `NavigationMenuIndicator` OR `NavigationMenuViewport`).
    3. Preserve `cssVariables=true` + `iconLibrary=lucide` preset (Phase 1 config at `components.json`).

    After install, OPEN `components/ui/navigation-menu.tsx` and verify:
    - File is a Client Component where needed (the shadcn default marks subcomponents `'use client'` — do NOT change).
    - Uses `cn()` from `@/lib/utils` (Phase 1 helper).
    - Icons imported from `lucide-react` (project standard; NOT `@radix-ui/react-icons`). If the shadcn default imports from `@radix-ui/react-icons`, replace with equivalent `lucide-react` imports (`ChevronDown` from `lucide-react` for the trigger chevron).

    Add a `// Phase 4 / Plan 04-01 — shadcn navigation-menu; NavigationMenu primitive backing HKNav Gymnastics + Locations dropdowns (D-03, D-04).` comment at the top of the file.

    Do NOT hand-write this file from scratch — use `pnpm dlx shadcn@latest add navigation-menu`. If the CLI fails, STOP and surface the error; do not fabricate the component.

    Extends Phase 1 shadcn pipeline decision (Key Decision 2026-04-22 "Accepted shadcn v4.4.0's preset-enumeration model").
    Per UI-SPEC D-03/D-04 (Gymnastics dropdown 8 items, Locations dropdown 2 items).
  </action>
  <verify>
    <automated>
      grep -l "@radix-ui/react-navigation-menu" /Users/martin/Projects/proactive/package.json || grep -E "\"radix-ui\":" /Users/martin/Projects/proactive/package.json
      test -f /Users/martin/Projects/proactive/components/ui/navigation-menu.tsx
      grep -E "(export \{|export const|export function).*(NavigationMenu|NavigationMenuTrigger|NavigationMenuContent|NavigationMenuList|NavigationMenuLink)" /Users/martin/Projects/proactive/components/ui/navigation-menu.tsx
      cd /Users/martin/Projects/proactive && pnpm typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `components/ui/navigation-menu.tsx` exists and contains exports for at minimum: `NavigationMenu`, `NavigationMenuList`, `NavigationMenuItem`, `NavigationMenuTrigger`, `NavigationMenuContent`, `NavigationMenuLink`
    - `package.json` lists `@radix-ui/react-navigation-menu` (direct dep) OR `radix-ui` metapackage at `^1.4.3` (which re-exports it)
    - `components/ui/navigation-menu.tsx` imports `cn` from `@/lib/utils`
    - `components/ui/navigation-menu.tsx` does NOT import from `@radix-ui/react-icons` (project uses `lucide-react` only — verified via `grep -L "@radix-ui/react-icons" components/ui/navigation-menu.tsx`)
    - `components/ui/navigation-menu.tsx` first line or line 2 contains string `Phase 4 / Plan 04-01`
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>shadcn `navigation-menu` primitive installed + typechecks clean. HKNav (Plan 02) can import it without additional work.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create `lib/hk-data.ts` with typed interfaces + static HK constants</name>
  <files>lib/hk-data.ts, .env.example</files>
  <read_first>
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-UI-SPEC.md §5.7 (HK-specific data module — interfaces spec)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-RESEARCH.md §Pattern 5 (blog stub CMS-migration-ready shape) + §Pattern 7 (LocalBusiness JSON-LD — Wan Chai NAP + hours)
    - /Users/martin/Projects/proactive/.planning/inputs/strategy.md §PART 4 (HK wireframe — venue list, programme list, FAQ set) + §PART 6B §3/§4/§11 (copy) + §PART 8.3 (NAP) + §PART 12 Tier 1 #1-#5 (gymnastics programme age bands)
    - /Users/martin/Projects/proactive/app/root/page.tsx (inline constant pattern — extract same structure into a dedicated lib file)
    - /Users/martin/Projects/proactive/.env.example (current Phase 1-3 env-var list — append Phase 4 vars)
  </read_first>
  <action>
    Create `lib/hk-data.ts` with the EXACT TypeScript below (copy with care — shapes are binding on Plans 02-07 and forward-compatible with the Phase 6 Sanity schema per UI-SPEC §5.7 + RESEARCH Pattern 5). The file MUST:

    1. Open with header comment:
       ```ts
       // Phase 4 / Plan 04-01 — HK static data module.
       // Shape MUST mirror Phase 6 Sanity schemas (coach, post, venue, faqItem) so Phase 6 GROQ queries
       // drop in without component edits. Do NOT add fields that won't exist in Sanity.
       ```

    2. Export the following typed interfaces VERBATIM:
       ```ts
       export interface HKVenueHours { days: readonly string[]; opens: string; closes: string; }
       export interface HKVenue {
         id: "wan-chai" | "cyberport";
         nameShort: string;              // "Wan Chai"
         nameFull: string;               // "ProGym Wan Chai"
         addressStreet: string;          // "15/F, The Hennessy, 256 Hennessy Road"
         addressLocality: string;        // "Wan Chai"
         addressRegion: string;          // "Hong Kong Island"
         addressCountry: "HK";
         geo: { lat: number; lng: number };
         hours: HKVenueHours[];
         phoneEnvVar: string;            // "NEXT_PUBLIC_HK_PHONE"
         mapEmbedEnvVar: string;         // "NEXT_PUBLIC_WAN_CHAI_MAP_EMBED"
         heroImage: string;              // "/photography/hk-venue-wanchai-gymtots.webp"
         serviceArea: readonly string[]; // ["Wan Chai","Causeway Bay","Central","Mid-Levels"]
         apparatus: readonly string[];   // ["Bar","Beam","Floor","Vault"]
         openedNote?: string;            // Cyberport only: "Opened August 2025"
         sizeNote?: string;              // Cyberport only: "5,000 sq ft"
       }
       export interface HKCoach {
         name: string;
         role: string;
         bio: string;
         venueTag?: "wan-chai" | "cyberport" | "both";
         portrait: string;               // "/photography/coach-monica-portrait.webp"
       }
       export interface HKBlogPost {
         title: string;
         slug: string;
         excerpt: string;
         publishedAt: string;            // ISO 8601 "2026-04-01"
         category: string;
         readTimeMinutes: number;
         heroImage: string;
       }
       export interface HKFAQItem {
         value: string;                  // accordion item id — kebab-case
         question: string;
         answer: string;
         group: "about" | "venues" | "gymnastics" | "camps" | "parties" | "pricing";
       }
       export interface HKGymnasticsProgramme {
         slug: "toddlers" | "beginner" | "intermediate" | "advanced" | "competitive" | "rhythmic" | "adult" | "private";
         href: `/gymnastics/${string}/`; // static template literal
         label: string;                  // "Babies & Toddlers"
         ageBand: string;                // "12mo – 3yr"
         metaTitle: string;              // full <title> per UI-SPEC sub-page metadata
         metaDescription: string;
         h1: string;                     // on-page <h1> copy per strategy PART 12
         whatTheyLearn: readonly string[]; // 4-6 bullet points
         classStructure: string;         // paragraph
         venuesOffered: readonly ("wan-chai" | "cyberport")[];
       }
       ```

    3. Export populated constants with VERBATIM strategy-doc copy:

       **HK_VENUES** — 2 entries (Wan Chai + Cyberport) per strategy §PART 4 + §PART 8.3. Wan Chai geo: `{ lat: 22.2772, lng: 114.1730 }`. Cyberport geo: `{ lat: 22.2618, lng: 114.1303 }` [A6 assumption — to be verified at execute via Google Maps share-embed action]. Use the exact `addressStreet` strings given in §PART 4/§PART 8.3 — "15/F, The Hennessy, 256 Hennessy Road" for Wan Chai; "Cyberport" (street TBD — leave as `"Cyberport Campus"` with comment `// HUMAN-ACTION: replace with exact building + floor when Martin confirms`). Populate `hours` with Mon-Fri 09:00-19:00 + Sat-Sun 09:00-17:00 per RESEARCH Pattern 7 (assumption — comment `// HUMAN-ACTION: confirm hours — placeholder from strategy PART 9.4`).

       **HK_COACHES** — at minimum 3 entries: Monica (Director of Sports, `venueTag: "both"`, `portrait: "/photography/coach-monica-portrait.webp"`) + 2 additional placeholder coaches with `venueTag: "wan-chai"` and `"cyberport"` respectively, `portrait` paths pointing at `public/photography/coach-*-portrait.webp` (exact filenames to be verified against `public/photography/` manifest at execute — if missing, the Plan 06 coaches task will trigger a HUMAN-ACTION gate per D-09). Bios: 2-3 sentences each, voice per UI-SPEC §Copywriting Contract (British English, no exclamations, no "customers/users/leads").

       **HK_BLOG_POSTS_STUB** — exactly 2 entries (Claude's Discretion per CONTEXT — 2 stubs proves the template). Titles from strategy PART 12 Tier 1 content topics: (1) "What to expect from your child's first gymnastics class in Hong Kong" (slug `first-gymnastics-class-hong-kong`, category `Gymnastics Tips`, heroImage `/photography/programme-beginner.webp`, readTimeMinutes 5, publishedAt `2026-04-01`), (2) "Choosing between Wan Chai and Cyberport: a parent's guide" (slug `choosing-wan-chai-vs-cyberport`, category `Venues`, heroImage `/photography/hk-venue-cyberport.webp`, readTimeMinutes 4, publishedAt `2026-04-15`).

       **HK_FAQ_ITEMS** — exactly 16 entries grouped per UI-SPEC §4 `/faq/` row (`about` × 3, `venues` × 3, `gymnastics` × 4, `camps` × 2, `parties` × 2, `pricing` × 2). Questions + answers verbatim from strategy §PART 6B §11 where provided; for groups not in §11 (camps/parties/pricing details beyond the 8 homepage FAQ), derive from §PART 4 §7-§8 copy and strategy §PART 8 local SEO notes. Each `value` is kebab-case derived from the question.

       **HK_GYMNASTICS_PROGRAMMES** — exactly 8 entries (one per D-03 dropdown slug) with copy from strategy §PART 12 Tier 1 #1-#5 + §PART 4 §4. Each entry:
       - `slug` matches one of the 8 directories under `app/hk/gymnastics/`
       - `href` is the full route path (e.g. `"/gymnastics/toddlers/"`)
       - `label` + `ageBand` match UI-SPEC §5.1 GYMNASTICS_DROPDOWN table verbatim
       - `metaTitle` per RESEARCH §Code Examples / Gymnastics Sub-Page Template (e.g. Toddlers: `"Toddler Gymnastics Hong Kong (12mo–3yr) | ProGym Wan Chai & Cyberport"`)
       - `metaDescription`: 150-160 char SEO-quality description drawing from strategy §PART 12 bullets
       - `h1`: on-page H1 copy per strategy §PART 12
       - `whatTheyLearn`: 4-6 bullets (verbatim or near-verbatim from strategy PART 12 programme descriptions)
       - `classStructure`: 1-paragraph description
       - `venuesOffered`: `["wan-chai","cyberport"]` except `rhythmic` which is `["wan-chai"]` (per strategy PART 4 note — Cyberport rhythmic TBD)

    4. Export env-var derived map-embed constants:
       ```ts
       export const WAN_CHAI_MAP_EMBED = process.env.NEXT_PUBLIC_WAN_CHAI_MAP_EMBED ?? "PLACEHOLDER_WAN_CHAI_EMBED";
       export const CYBERPORT_MAP_EMBED = process.env.NEXT_PUBLIC_CYBERPORT_MAP_EMBED ?? "PLACEHOLDER_CYBERPORT_EMBED";
       ```

    5. Update `.env.example` — append (if not present) under a new `# --- Phase 4 (Hong Kong market) ---` section:
       ```
       # --- Phase 4 (Hong Kong market) ---
       # HUMAN-ACTION: populate all values below before shipping HK pages to production.
       NEXT_PUBLIC_MUX_HK_HERO_PLAYBACK_ID=
       NEXT_PUBLIC_HK_PHONE=
       NEXT_PUBLIC_HK_WHATSAPP=
       NEXT_PUBLIC_ROOT_URL=
       NEXT_PUBLIC_SG_URL=
       NEXT_PUBLIC_WAN_CHAI_MAP_EMBED=
       NEXT_PUBLIC_CYBERPORT_MAP_EMBED=
       # Optional:
       NEXT_PUBLIC_HK_PHONE_CYBERPORT=
       ```

    Tag CONTEXT decisions in comments: D-01 hero video env var, D-02 nav structure, D-03 gymnastics dropdown, D-04 locations dropdown, D-07/D-08/D-09 coaches, D-10 booking form venue routing.
    Per UI-SPEC §5.7 binding schema. Per Phase 6 CMS migration plan (CONTEXT canonical-refs).
  </action>
  <verify>
    <automated>
      test -f /Users/martin/Projects/proactive/lib/hk-data.ts
      grep -q "export interface HKVenue" /Users/martin/Projects/proactive/lib/hk-data.ts
      grep -q "export interface HKCoach" /Users/martin/Projects/proactive/lib/hk-data.ts
      grep -q "export interface HKBlogPost" /Users/martin/Projects/proactive/lib/hk-data.ts
      grep -q "export interface HKFAQItem" /Users/martin/Projects/proactive/lib/hk-data.ts
      grep -q "export interface HKGymnasticsProgramme" /Users/martin/Projects/proactive/lib/hk-data.ts
      grep -q "export const HK_VENUES" /Users/martin/Projects/proactive/lib/hk-data.ts
      grep -q "export const HK_COACHES" /Users/martin/Projects/proactive/lib/hk-data.ts
      grep -q "export const HK_BLOG_POSTS_STUB" /Users/martin/Projects/proactive/lib/hk-data.ts
      grep -q "export const HK_FAQ_ITEMS" /Users/martin/Projects/proactive/lib/hk-data.ts
      grep -q "export const HK_GYMNASTICS_PROGRAMMES" /Users/martin/Projects/proactive/lib/hk-data.ts
      grep -q "15/F, The Hennessy, 256 Hennessy Road" /Users/martin/Projects/proactive/lib/hk-data.ts
      grep -q "5,000 sq ft" /Users/martin/Projects/proactive/lib/hk-data.ts
      grep -q "NEXT_PUBLIC_MUX_HK_HERO_PLAYBACK_ID" /Users/martin/Projects/proactive/.env.example
      grep -q "NEXT_PUBLIC_WAN_CHAI_MAP_EMBED" /Users/martin/Projects/proactive/.env.example
      grep -q "NEXT_PUBLIC_ROOT_URL" /Users/martin/Projects/proactive/.env.example
      cd /Users/martin/Projects/proactive && pnpm typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `lib/hk-data.ts` exists
    - File contains all 6 interface declarations: `HKVenueHours`, `HKVenue`, `HKCoach`, `HKBlogPost`, `HKFAQItem`, `HKGymnasticsProgramme`
    - File contains all 5 populated constant exports: `HK_VENUES`, `HK_COACHES`, `HK_BLOG_POSTS_STUB`, `HK_FAQ_ITEMS`, `HK_GYMNASTICS_PROGRAMMES`
    - `HK_VENUES` has exactly 2 entries with `id: "wan-chai"` and `id: "cyberport"`
    - `HK_GYMNASTICS_PROGRAMMES` has exactly 8 entries — slugs `toddlers, beginner, intermediate, advanced, competitive, rhythmic, adult, private` (verified via `grep -c "slug:" lib/hk-data.ts` returning ≥ 8)
    - `HK_BLOG_POSTS_STUB` has exactly 2 entries
    - `HK_FAQ_ITEMS` has exactly 16 entries (verified by counting `value:` lines within the array)
    - `HK_COACHES` has at least 3 entries including one with `venueTag: "both"` (Monica)
    - String "15/F, The Hennessy, 256 Hennessy Road" appears in the Wan Chai entry
    - String "5,000 sq ft" appears in the Cyberport entry
    - `.env.example` contains all 7 required Phase 4 env vars: `NEXT_PUBLIC_MUX_HK_HERO_PLAYBACK_ID`, `NEXT_PUBLIC_HK_PHONE`, `NEXT_PUBLIC_HK_WHATSAPP`, `NEXT_PUBLIC_ROOT_URL`, `NEXT_PUBLIC_SG_URL`, `NEXT_PUBLIC_WAN_CHAI_MAP_EMBED`, `NEXT_PUBLIC_CYBERPORT_MAP_EMBED`
    - `pnpm typecheck` exits 0
    - No usage of `any` type in interface declarations
  </acceptance_criteria>
  <done>Plans 02-07 can import every HK static constant and every interface from `@/lib/hk-data`. Env contract documented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write Wave-0 test scaffolds (5 test files, RED state expected)</name>
  <files>app/hk/page.test.tsx, app/hk/wan-chai/page.test.tsx, app/hk/cyberport/page.test.tsx, app/hk/gymnastics/pillar.test.tsx, app/hk/book-a-trial/free-assessment/booking-form.test.tsx</files>
  <read_first>
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-VALIDATION.md (complete per-task verification map — drives test expectations)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-UI-SPEC.md §3-§6 (visible-text + form-field contracts that tests assert against)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-PATTERNS.md §Test files section (exact `vi.mock` + `render` skeletons to copy)
    - /Users/martin/Projects/proactive/app/root/page.test.tsx (Phase 3 analog — copy mock structure, adapt to HK components + content)
    - /Users/martin/Projects/proactive/app/root/contact/contact-form.test.tsx (Phase 3 analog — copy form-test + useSearchParams mock structure)
    - /Users/martin/Projects/proactive/lib/hk-data.ts (from Task 2 — import `HK_VENUES`, `HK_GYMNASTICS_PROGRAMMES`, `HK_FAQ_ITEMS` to drive data-driven assertions)
    - /Users/martin/Projects/proactive/vitest.config.ts (Phase 1 config — confirm `include` pattern matches `*.test.tsx`)
  </read_first>
  <behavior>
    These tests are WRITTEN RED. They will fail on `pnpm test:unit` at Task 3 completion because the pages they test don't exist yet — that's the intended Wave-0 contract. Plans 03, 04, 05, 07 turn them GREEN when they ship the target pages.

    Per-file expectations (tests that MUST exist in each file):

    **`app/hk/page.test.tsx` (HK-01):**
    - Test 1: renders a single `<h1>` whose text content includes "Premium gymnastics" (verbatim from UI-SPEC §3.1)
    - Test 2: renders both venue chip labels "ProGym Wan Chai" AND "ProGym Cyberport" (UI-SPEC §3.2)
    - Test 3: renders a `<a>` or `<Link>` with href `/book-a-trial/free-assessment/` and text content containing "Book a Free Trial" (UI-SPEC §3.1 primary CTA; HK-01 SC #1 mobile above-the-fold CTA requirement)
    - Test 4: renders a `<script type="application/ld+json">` whose JSON-parsed content contains a `@graph` node with `@type: "FAQPage"` (UI-SPEC §8.3)
    - Test 5: exactly one `<Image>` is rendered with `priority` attribute truthy (Pitfall 6 carry — LCP rule)

    **`app/hk/wan-chai/page.test.tsx` (HK-02):**
    - Test 1: page contains literal text "15/F, The Hennessy, 256 Hennessy Road" (NAP verbatim)
    - Test 2: page contains an `<iframe>` element OR a component mock that represents `<VenueMap>`, with a `title` attribute non-empty (a11y)
    - Test 3: page renders Wan Chai opening-hours text (e.g. "09:00" appears at least twice — weekdays + weekends)
    - Test 4: page contains a link with href matching `/book-a-trial/free-assessment/?venue=wan-chai` pattern (venue pre-fill)
    - Test 5: page renders a `<script type="application/ld+json">` containing `@type":"SportsActivityLocation"` substring

    **`app/hk/cyberport/page.test.tsx` (HK-03):**
    - Test 1: page contains literal text "5,000 sq ft"
    - Test 2: page contains literal text "Cyberport" in an `<h1>` (HK-03 — primary venue identifier in heading)
    - Test 3: page contains an `<iframe>` OR `<VenueMap>` mock with `title` non-empty
    - Test 4: page contains a link with href matching `/book-a-trial/free-assessment/?venue=cyberport` pattern
    - Test 5: page renders `SportsActivityLocation` JSON-LD

    **`app/hk/gymnastics/pillar.test.tsx` (HK-04):**
    This file asserts ACROSS all 9 pages (1 pillar + 8 sub-pages). Imports `HK_GYMNASTICS_PROGRAMMES` from `@/lib/hk-data` to drive the matrix.
    - Test 1: pillar page renders at least 8 links, one per `HK_GYMNASTICS_PROGRAMMES[i].href` (derived assertions via `it.each`)
    - Test 2: `it.each(HK_GYMNASTICS_PROGRAMMES)` — for each programme, import the sub-page component (dynamic import) and assert the rendered H1 contains the programme's `h1` text
    - Test 3: each of the 8 sub-pages renders the `GymPillarNav` component (mocked) AND a link to `/book-a-trial/free-assessment/` (booking CTA)

    **`app/hk/book-a-trial/free-assessment/booking-form.test.tsx` (HK-12):**
    - Test 1: form renders 6 fields (name, email, phone, childAge, venue radio group with 3 options, message) + hidden honeypot input `name="bot-trap"` or `name="website"` (match the name used in Plan 07 — decision: use `name="bot-trap"` verbatim from Phase 3 contact-form.tsx)
    - Test 2: when `useSearchParams().get("venue")` returns `"wan-chai"`, the Wan Chai radio card renders with an `aria-checked="true"` OR `data-state="checked"` attribute (venue pre-fill)
    - Test 3: clicking submit with valid fields POSTs to `/api/contact` with JSON body containing `{ market: "hk", subject: "Free Assessment Request", venue: "wan-chai", ... }` (assert via `global.fetch` mock)
    - Test 4: submit button label shows "Sending…" while `status === "submitting"` and button is disabled
    - Test 5: after successful 200 response, form is replaced by success-state heading "Thanks — your free assessment request is in." (UI-SPEC §Copywriting Contract)

    **Shared mock setup in every file:**
    ```typescript
    // Stub Phase 2 primitives so tests verify page wiring, not primitive internals
    vi.mock("@/components/ui/section", () => ({ Section: ({ children, ...rest }: any) => <section {...rest}>{children}</section> }));
    vi.mock("@/components/ui/container-editorial", () => ({ ContainerEditorial: ({ children, ...rest }: any) => <div {...rest}>{children}</div> }));
    vi.mock("@/components/ui/video-player", () => ({ default: () => null, VideoPlayer: () => null }));
    vi.mock("@/components/hk/hk-nav", () => ({ HKNav: () => <nav data-test="hk-nav" /> }));
    vi.mock("@/components/hk/hk-footer", () => ({ HKFooter: () => <footer data-test="hk-footer" /> }));
    vi.mock("@/components/hk/venue-map", () => ({ VenueMap: ({ title }: { title: string }) => <iframe title={title} /> }));
    vi.mock("next/image", () => ({ default: ({ src, alt, priority, ...rest }: any) => <img src={src} alt={alt} data-priority={priority ? "true" : "false"} {...rest} /> }));
    vi.mock("next/dynamic", () => ({ default: (loader: any, opts: any) => opts?.loading ?? (() => null) }));
    ```
  </behavior>
  <action>
    Write the 5 test files listed in `<files>`. Each file must:

    1. Use Vitest 4 + @testing-library/react conventions already installed (Phase 1 Plan 01-04 + Phase 3 Plan 03-01 added RTL/jsdom).
    2. Import the target page module with `await import()` so Task 3 passes the mere "file exists + imports resolve" check even while RED — missing pages will throw on import, which is valid RED behavior the test suite reports, not a config/lint failure.
    3. Copy the mock setup block verbatim (above) at the top of each file.
    4. Wrap assertions in `describe(...)` blocks with clear `it(...)` labels matching the behavior list.

    **File-specific skeleton** (use `app/root/page.test.tsx` + `app/root/contact/contact-form.test.tsx` as exact structural analogs):

    ```typescript
    // app/hk/page.test.tsx
    import { describe, it, expect, vi, beforeAll, afterEach } from "vitest";
    import { render, cleanup } from "@testing-library/react";

    // [shared mocks here]

    beforeAll(() => {
      process.env.NEXT_PUBLIC_HK_WHATSAPP = "+85212345678";
      process.env.NEXT_PUBLIC_HK_PHONE = "+85287654321";
    });
    afterEach(() => cleanup());

    describe("HK homepage (HK-01)", () => {
      it("renders the verbatim H1 from strategy PART 6B §1", async () => {
        const { default: HKHomePage } = await import("./page");
        const { container } = render(<HKHomePage />);
        const h1s = container.querySelectorAll("h1");
        expect(h1s.length).toBe(1);
        expect(h1s[0].textContent).toContain("Premium gymnastics");
      });
      // ...4 more tests per the behavior list
    });
    ```

    For the booking-form test file, additionally:
    ```typescript
    const mockSearchParams = { get: vi.fn() };
    vi.mock("next/navigation", () => ({ useSearchParams: () => mockSearchParams }));
    beforeEach(() => {
      mockSearchParams.get.mockReset();
      global.fetch = vi.fn().mockResolvedValue({ ok: true, json: async () => ({ success: true }) });
    });
    ```

    **RED-state tolerance:** The tests WILL fail on first `pnpm test:unit` run because pages don't exist. This is expected. The Task 3 verify DOES NOT run `pnpm test:unit` as the pass criterion — it only verifies the test files exist, parse, and have the required assertions. The RED baseline is intentional and unblocks Plans 03-07 TDD cycles.

    Per VALIDATION.md Wave 0 Requirements (all 5 files listed). Per UI-SPEC §12 checker sign-off dimensions 1 + 4.
  </action>
  <verify>
    <automated>
      test -f /Users/martin/Projects/proactive/app/hk/page.test.tsx
      test -f /Users/martin/Projects/proactive/app/hk/wan-chai/page.test.tsx
      test -f /Users/martin/Projects/proactive/app/hk/cyberport/page.test.tsx
      test -f /Users/martin/Projects/proactive/app/hk/gymnastics/pillar.test.tsx
      test -f /Users/martin/Projects/proactive/app/hk/book-a-trial/free-assessment/booking-form.test.tsx
      grep -q "Premium gymnastics" /Users/martin/Projects/proactive/app/hk/page.test.tsx
      grep -q "Book a Free Trial" /Users/martin/Projects/proactive/app/hk/page.test.tsx
      grep -q "15/F, The Hennessy" /Users/martin/Projects/proactive/app/hk/wan-chai/page.test.tsx
      grep -q "5,000 sq ft" /Users/martin/Projects/proactive/app/hk/cyberport/page.test.tsx
      grep -q "HK_GYMNASTICS_PROGRAMMES" /Users/martin/Projects/proactive/app/hk/gymnastics/pillar.test.tsx
      grep -q "useSearchParams" /Users/martin/Projects/proactive/app/hk/book-a-trial/free-assessment/booking-form.test.tsx
      grep -q "Free Assessment Request" /Users/martin/Projects/proactive/app/hk/book-a-trial/free-assessment/booking-form.test.tsx
      cd /Users/martin/Projects/proactive && pnpm test:unit --reporter=default --run 2>&1 | grep -E "(Test Files|failed|passed|no tests)" || true
    </automated>
  </verify>
  <acceptance_criteria>
    - All 5 test files exist at the paths specified
    - `app/hk/page.test.tsx` contains string `"Premium gymnastics"` AND string `"Book a Free Trial"` (case-sensitive)
    - `app/hk/page.test.tsx` contains call `vi.mock("@/components/hk/hk-nav"`
    - `app/hk/wan-chai/page.test.tsx` contains string `"15/F, The Hennessy"`
    - `app/hk/wan-chai/page.test.tsx` contains string `"?venue=wan-chai"`
    - `app/hk/cyberport/page.test.tsx` contains string `"5,000 sq ft"`
    - `app/hk/cyberport/page.test.tsx` contains string `"?venue=cyberport"`
    - `app/hk/gymnastics/pillar.test.tsx` imports `HK_GYMNASTICS_PROGRAMMES` from `@/lib/hk-data`
    - `app/hk/gymnastics/pillar.test.tsx` uses `it.each` OR iterates over the programmes array
    - `app/hk/book-a-trial/free-assessment/booking-form.test.tsx` contains string `"useSearchParams"` AND string `"Free Assessment Request"` AND string `"bot-trap"` (honeypot field name)
    - Every test file contains at least 5 `it(` or `test(` invocations (4 visible behavior cases + 1 setup/smoke)
    - `pnpm test:unit --run` completes without a config-parse error (test failures due to missing target pages are EXPECTED and acceptable; config-level errors are NOT)
  </acceptance_criteria>
  <done>Wave-0 tests scaffolded. `pnpm test:unit` reports RED for HK tests (expected); other phase tests (root + middleware) still GREEN. Plans 02-07 can drive each test file to GREEN by building the corresponding page/component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | `pnpm dlx shadcn@latest` fetches remote code; shadcn is a known registry (same provenance as Phase 1-3 installs). `@radix-ui/react-navigation-menu` is a first-party Radix package already pinned via metapackage. |
| Static data authorship → lib/hk-data.ts | No user input flows through this file — it's hand-authored TypeScript. Threat surface is ZERO; the file is build-time source code. |
| Env vars in .env.example → documented public vars | All `NEXT_PUBLIC_*` vars are client-bundle-visible by design (Mux public playback IDs, WhatsApp number, Google Maps embed URL). No secrets in `.env.example`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | `pnpm dlx shadcn@latest` supply chain | accept | Phase 1-3 established shadcn CLI as trusted (multiple prior installs without incident); `pnpm-lock.yaml` pins resolved versions; Phase 0 CI includes `gitleaks` + typecheck gates that would catch malicious code substitution. No new mitigation needed. |
| T-04-01-02 | Information Disclosure | `.env.example` content | mitigate | `.env.example` contains ONLY placeholder-empty public vars — no secret values. Gitleaks pre-commit hook (Phase 0) scans every commit for secret patterns. All vars prefixed `NEXT_PUBLIC_*` are intentionally client-visible (Mux playback IDs, map embed URLs, phone numbers, WhatsApp number — all public artifacts of the business). |
| T-04-01-03 | Spoofing | Test-file impersonation of production code | accept | `app/hk/**/*.test.tsx` files live in the app tree (colocation pattern per Phase 1 Vitest config). Next.js App Router ignores `*.test.tsx` files by convention (only `page.tsx`/`layout.tsx` etc. are route-mapped). Verified by Phase 1 Plan 01-04 — test files in `app/` do not render as routes. |
| T-04-01-04 | Denial of Service | `vitest.config.ts` include-pattern explosion | accept | Phase 1 Plan 01-04 set the include pattern; adding 5 files keeps the suite under 20 tests total. No DoS concern. |
</threat_model>

<verification>
After Task 3 completes, run:
```bash
pnpm typecheck                     # Must exit 0 — contracts compile
pnpm test:unit --run               # Must NOT fail on config/parse; HK tests failing on missing pages is expected RED
pnpm lint                          # Must exit 0 — style compliance
```

Manual smoke (optional):
- Open `components/ui/navigation-menu.tsx` in editor: 7 named exports visible.
- Open `lib/hk-data.ts`: all 5 constants populated with non-placeholder content for Wan Chai NAP, Cyberport marker, Monica coach, 2 blog stubs, 16 FAQ items, 8 gymnastics programmes.
- Open `.env.example`: 7 new `NEXT_PUBLIC_*` vars under Phase 4 section.

Threat-model sanity:
- No `.env.local`, `.env`, or `.env.production` was created or modified — only `.env.example`.
- No test file accidentally renders as a route (verified implicitly by Phase 1 Vitest config).
</verification>

<success_criteria>
- [ ] `components/ui/navigation-menu.tsx` exists with 7+ shadcn-style exports
- [ ] `@radix-ui/react-navigation-menu` resolvable (direct dep or via `radix-ui` metapackage)
- [ ] `lib/hk-data.ts` exists with 6 interfaces + 5 populated constants + 2 map-embed env-var constants
- [ ] Wan Chai entry contains verbatim NAP "15/F, The Hennessy, 256 Hennessy Road"
- [ ] Cyberport entry contains verbatim marker "5,000 sq ft"
- [ ] `HK_GYMNASTICS_PROGRAMMES` has exactly 8 entries (toddlers → private)
- [ ] `HK_FAQ_ITEMS` has 16 entries grouped across 6 group-kinds
- [ ] `HK_COACHES` includes Monica (venueTag: "both")
- [ ] `HK_BLOG_POSTS_STUB` has exactly 2 entries
- [ ] `.env.example` documents all 7 required Phase 4 env vars
- [ ] 5 Wave-0 test files exist at target paths
- [ ] Each test file has ≥5 `it(`/`test(` blocks covering behaviors per VALIDATION.md
- [ ] `pnpm typecheck` exits 0
- [ ] `pnpm test:unit --run` reports tests (failing on missing pages is expected; config-level errors are NOT)
</success_criteria>

<output>
After completion, create `.planning/phases/04-hong-kong-market/04-01-SUMMARY.md` documenting:
- Final `components/ui/navigation-menu.tsx` export surface
- `lib/hk-data.ts` constant counts + shape attestation
- `.env.example` additions (7 vars)
- RED-state test count (5 files, ~25 tests total)
- Carry-forward notes for Plan 02 (`import { HKVenue, HK_VENUES } from "@/lib/hk-data"` contract)
</output>

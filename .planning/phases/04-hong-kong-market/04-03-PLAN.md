---
phase: 04-hong-kong-market
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - app/hk/page.tsx
autonomous: true
requirements: [HK-01]

must_haves:
  truths:
    - "`hk.localhost:3000/` renders the full PART 4 wireframe — 12 sections in order: Hero, VenueChipRow, WhyChoose (4-tile grid), Programmes (5 tiles), LocationSplit, SocialProof (navy strip), CoachingMethod, CampsParties, AboutSnapshot, Blog (≥1 stub), FAQ (8 items), Final CTA"
    - "Hero contains a single `<h1>` with text starting `Premium gymnastics` (verbatim PART 6B §1)"
    - "Hero contains looping autoplay-muted `<VideoPlayer>` with poster fallback `/photography/hk-venue-wanchai-gymtots.webp` rendering `priority` (LCP)"
    - "Hero primary CTA is a Button with `bg-brand-red`, label `Book a Free Trial`, href `/book-a-trial/free-assessment/`"
    - "Hero secondary CTA is a Button with outline-on-navy variant, label `Send an Enquiry`, href `/contact?market=hk`"
    - "VenueChipRow shows both `ProGym Wan Chai` AND `ProGym Cyberport` chips above the fold (HK-01 SC#1)"
    - "FAQ section renders 8 accordion items with verbatim PART 6B §11 questions; mainEntity in FAQPage JSON-LD char-for-char matches visible Q&A text"
    - "Page renders inline `<script type=\"application/ld+json\">` containing `@graph` with WebSite + FAQPage entries (UI-SPEC §8.3)"
    - "Final CTA section uses `<Section size=\"lg\" bg=\"navy\">` with Book a Free Trial (red) + WhatsApp (env-conditional) buttons"
    - "Page uses ONLY Phase 2 `components/ui/` primitives + Phase 4-local `components/hk/` compositions — no inline custom CSS"
    - "Exactly ONE `<Image>` per page is rendered with `priority` attribute (the hero poster — Pitfall 6 carry)"
  artifacts:
    - path: "app/hk/page.tsx"
      provides: "HK homepage replacing Phase 1 amber-stripe placeholder; satisfies HK-01 + ROADMAP SC#1"
      contains: "Premium gymnastics"
      min_lines: 350
  key_links:
    - from: "app/hk/page.tsx hero VideoPlayer"
      to: "components/ui/video-player.tsx"
      via: "next/dynamic({ ssr: false }) import"
      pattern: "dynamic\\(\\(\\) => import\\([\"']@/components/ui/video-player[\"']"
    - from: "app/hk/page.tsx hero overlay"
      to: "/photography/hk-venue-wanchai-gymtots.webp"
      via: "<Image priority src=...>"
      pattern: "hk-venue-wanchai-gymtots"
    - from: "app/hk/page.tsx primary CTA"
      to: "/book-a-trial/free-assessment/"
      via: "<Link href={...}>"
      pattern: "/book-a-trial/free-assessment/"
    - from: "app/hk/page.tsx FAQ section"
      to: "lib/hk-data.ts HK_FAQ_ITEMS"
      via: "import + filter group==='about' OR slice(0,8)"
      pattern: "HK_FAQ_ITEMS"
    - from: "app/hk/page.tsx final CTA WhatsApp button"
      to: "process.env.NEXT_PUBLIC_HK_WHATSAPP"
      via: "conditional render"
      pattern: "NEXT_PUBLIC_HK_WHATSAPP"
---

<objective>
Build the HK homepage at `app/hk/page.tsx` — 12 sections per strategy PART 4 wireframe, verbatim PART 6B copy, real ProActiv photography, looping hero video (HUMAN-ACTION-gated for Mux ID), inline FAQPage + WebSite JSON-LD, conversion-first asymmetric layout.

Purpose: This is the apex page of the HK tree — the first impression for organic traffic, the parent's mental anchor for the brand, and the highest-priority conversion surface (HK-01 ROADMAP SC#1: visible Book a Free Trial CTA above the fold on mobile).

Output:
- Single file `app/hk/page.tsx` containing all 12 sections + JSON-LD
- File composes EXCLUSIVELY from Phase 2 primitives + Phase 4 layout chrome
- Drives `app/hk/page.test.tsx` (Plan 04-01) RED → GREEN
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-hong-kong-market/04-CONTEXT.md
@.planning/phases/04-hong-kong-market/04-RESEARCH.md
@.planning/phases/04-hong-kong-market/04-PATTERNS.md
@.planning/phases/04-hong-kong-market/04-UI-SPEC.md
@.planning/phases/04-hong-kong-market/04-VALIDATION.md
@.planning/phases/04-hong-kong-market/04-01-SUMMARY.md
@.planning/phases/04-hong-kong-market/04-02-SUMMARY.md
@.planning/inputs/strategy.md

# Carry-forward analog
@app/root/page.tsx

# Phase 2 primitives
@components/ui/section.tsx
@components/ui/container-editorial.tsx
@components/ui/programme-tile.tsx
@components/ui/testimonial-card.tsx
@components/ui/faq-item.tsx
@components/ui/stat-strip.tsx
@components/ui/logo-wall.tsx
@components/ui/video-player.tsx
@components/ui/badge.tsx
@components/ui/button.tsx
@components/ui/card.tsx
@components/ui/accordion.tsx

# Phase 4 chrome + data
@components/hk/venue-chip-row.tsx
@lib/hk-data.ts
@app/hk/page.test.tsx

<interfaces>
<!-- Phase 2 primitive props extracted from committed code; executor consumes verbatim. -->

From `components/ui/section.tsx`:
```typescript
type SectionProps = { size?: "sm" | "md" | "lg"; bg?: "default" | "muted" | "navy" | "cream"; children: React.ReactNode } & ComponentProps<"section">;
export function Section(props: SectionProps): JSX.Element;
```

From `components/ui/container-editorial.tsx`:
```typescript
type ContainerProps = { width?: "default" | "wide"; children: React.ReactNode; className?: string };
export function ContainerEditorial(props: ContainerProps): JSX.Element;
```

From `components/ui/programme-tile.tsx`:
```typescript
type ProgrammeTileProps = { title: string; ageBand: string; href: string; image: string; alt: string; tagline: string };
export function ProgrammeTile(props: ProgrammeTileProps): JSX.Element;
```

From `components/ui/faq-item.tsx`:
```typescript
type FAQItemProps = { id: string; question: string; answer: string };
export function FAQItem(props: FAQItemProps): JSX.Element;
```

From `components/ui/video-player.tsx` (Phase 2 D-06):
```typescript
type VideoPlayerProps = { playbackId: string; loop?: boolean; autoPlay?: boolean; muted?: boolean; className?: string };
const VideoPlayer: React.ComponentType<VideoPlayerProps>; // default export
```

From `components/ui/testimonial-card.tsx`:
```typescript
type TestimonialCardProps = { quote: string; attribution: string; role?: string; image?: string };
export function TestimonialCard(props: TestimonialCardProps): JSX.Element;
```

From `lib/hk-data.ts` (Plan 04-01):
```typescript
export const HK_VENUES: HKVenue[];                          // [wan-chai, cyberport]
export const HK_GYMNASTICS_PROGRAMMES: HKGymnasticsProgramme[]; // 8 entries
export const HK_BLOG_POSTS_STUB: HKBlogPost[];              // 2 entries
export const HK_FAQ_ITEMS: HKFAQItem[];                     // 16 entries grouped
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build HK homepage §3.1-§3.6 — Hero, VenueChipRow, WhyChoose, Programmes, LocationSplit, SocialProof</name>
  <files>app/hk/page.tsx</files>
  <read_first>
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-UI-SPEC.md §3.0 page wrapper + §3.1 Hero (full visual contract: 21:9 desktop / 16:9 mobile, copy verbatim, 14.55:1 contrast on overlay) + §3.2 VenueChipRow + §3.3 WhyChoose 4-tile grid + §3.4 Programmes 3+2 asymmetric grid + §3.5 LocationSplit + §3.6 SocialProof navy strip
    - /Users/martin/Projects/proactive/.planning/inputs/strategy.md §PART 4 §1-§6 (wireframe spec) + §PART 6B §1-§6 (verbatim copy)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-PATTERNS.md §app/hk/page.tsx (full pattern map: dynamic VideoPlayer import, JSON-LD inline, private section components in one file, cross-subdomain link guard for SG-only env var)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-RESEARCH.md §Pattern 2 (hero video full code) + §Pitfall 6 (single priority Image rule)
    - /Users/martin/Projects/proactive/app/root/page.tsx (Phase 3 analog — 8-section RSC composition pattern, private section functions in same file, JSON-LD inline)
    - /Users/martin/Projects/proactive/components/ui/programme-tile.tsx (props signature, slot for Badge ageBand)
    - /Users/martin/Projects/proactive/lib/hk-data.ts (HK_VENUES + HK_GYMNASTICS_PROGRAMMES — used in §3.4 Programmes section)
    - /Users/martin/Projects/proactive/components/hk/venue-chip-row.tsx (Plan 04-02 — VenueChipRow consumed in §3.2)
    - /Users/martin/Projects/proactive/public/photography/ (verify exact filenames before referencing — DO NOT guess; if a planned filename is missing, STOP and trigger HUMAN-ACTION gate per CONTEXT D-09 / Phase 3 D-10)
  </read_first>
  <behavior>
    Tests in `app/hk/page.test.tsx` (Plan 04-01) drive expectations. After this task, the following Test 1, Test 2 (partial — VenueChipRow), Test 3 (Book CTA), and Test 5 (single priority Image) MUST be GREEN. Test 4 (FAQPage JSON-LD) waits for Task 2.

    Hero section behavioral expectations:
    - Renders inside `<Section size="lg" bg="default">` + `<ContainerEditorial width="wide">`
    - Outer hero box uses `relative w-full aspect-[16/9] md:aspect-[21/9] overflow-hidden`
    - VideoPlayer dynamic-loaded with `ssr: false` and `loading: () => <Image priority src="/photography/hk-venue-wanchai-gymtots.webp" alt="Children practising gymnastics at ProGym Wan Chai, Hong Kong" fill className="object-cover" sizes="100vw" />`
    - Overlay div sits on top of the video container with `bg-brand-navy/40` filter
    - H1 uses `text-display font-display text-white max-w-2xl`
    - Subhead uses `text-body-lg text-cream max-w-xl mt-4`
    - Primary CTA (red filled): `<Button asChild size="touch" className="bg-brand-red text-white hover:bg-brand-red/90"><Link href="/book-a-trial/free-assessment/">Book a Free Trial <ArrowRight /></Link></Button>`
    - Secondary CTA (outline on navy): `<Button asChild size="touch" variant="outline" className="border-white text-white hover:bg-white/10"><Link href="/contact?market=hk">Send an Enquiry</Link></Button>`

    Programmes section behavioral expectations:
    - Renders 5 ProgrammeTile cards with hrefs `/gymnastics/`, `/holiday-camps/`, `/birthday-parties/`, `/school-partnerships/`, `/competitions-events/`
    - Uses 3+2 asymmetric grid: `lg:grid-cols-6` with first 3 tiles `lg:col-span-2` each and last 2 tiles `lg:col-span-3` each
  </behavior>
  <action>
    Replace `app/hk/page.tsx` (currently a Phase 1 amber-stripe placeholder). Create the file structure below. Sections §3.1-§3.6 are implemented in this task; §3.7-§3.12 + JSON-LD are added in Task 2.

    **File structure (top of file through §3.6):**

    ```typescript
    // Phase 4 / Plan 04-03 — HK homepage (HK-01).
    // 12 sections per strategy PART 4 wireframe; copy verbatim from PART 6B.
    // Composes Phase 2 primitives + Phase 4-local components/hk/. NO inline custom CSS.
    // Hero VideoPlayer is HUMAN-ACTION-gated on NEXT_PUBLIC_MUX_HK_HERO_PLAYBACK_ID (D-01).
    import type { Metadata } from "next";
    import dynamic from "next/dynamic";
    import Image from "next/image";
    import Link from "next/link";
    import {
      Trophy,
      MapPin,
      BadgeCheck,
      ArrowUpRight,
      ArrowRight,
      MessageCircle,
    } from "lucide-react";
    import { Section } from "@/components/ui/section";
    import { ContainerEditorial } from "@/components/ui/container-editorial";
    import { Button } from "@/components/ui/button";
    import { Card } from "@/components/ui/card";
    import { Badge } from "@/components/ui/badge";
    import { ProgrammeTile } from "@/components/ui/programme-tile";
    import { TestimonialCard } from "@/components/ui/testimonial-card";
    import { LogoWall } from "@/components/ui/logo-wall";
    import { FAQItem } from "@/components/ui/faq-item";
    import { Accordion } from "@/components/ui/accordion";
    import { VenueChipRow } from "@/components/hk/venue-chip-row";
    import { HK_VENUES, HK_FAQ_ITEMS } from "@/lib/hk-data";

    // VideoPlayer dynamic with ssr:false — Phase 2 D-06 + RESEARCH Pattern 2.
    // The poster Image renders inside `loading` and stays visible until VideoPlayer hydrates.
    // It is also the single LCP-priority Image on the page (Pitfall 6).
    const VideoPlayer = dynamic(() => import("@/components/ui/video-player").then(m => m.default ?? m.VideoPlayer), {
      ssr: false,
      loading: () => (
        <Image
          src="/photography/hk-venue-wanchai-gymtots.webp"
          alt="Children practising gymnastics at ProGym Wan Chai, Hong Kong"
          fill
          priority
          className="object-cover"
          sizes="100vw"
        />
      ),
    });

    export const metadata: Metadata = {
      title: "Kids Gymnastics Hong Kong | ProGym Wan Chai & Cyberport",
      description:
        "Premium gymnastics, sports, holiday camps and birthday parties for children in Hong Kong. ProGym Wan Chai & Cyberport. Book a free trial.",
      openGraph: {
        title: "Kids Gymnastics Hong Kong | ProGym Wan Chai & Cyberport",
        description:
          "Premium gymnastics, sports, holiday camps and birthday parties for children in Hong Kong. ProGym Wan Chai & Cyberport.",
        url: "https://hk.proactivsports.com/",
        siteName: "ProActiv Sports Hong Kong",
        locale: "en_HK",
        type: "website",
        images: [{ url: "/opengraph-image", width: 1200, height: 630, alt: "ProActiv Sports Hong Kong" }],
      },
      alternates: { canonical: "https://hk.proactivsports.com/" },
    };

    // ====== §3.1 HERO ======
    function HeroSection() {
      return (
        <Section size="lg" bg="default">
          <ContainerEditorial width="wide">
            <div className="relative w-full aspect-[16/9] md:aspect-[21/9] overflow-hidden rounded-2xl">
              <VideoPlayer
                playbackId={process.env.NEXT_PUBLIC_MUX_HK_HERO_PLAYBACK_ID ?? ""}
                loop
                autoPlay
                muted
                className="absolute inset-0 w-full h-full object-cover"
              />
              <div className="absolute inset-0 bg-brand-navy/40" aria-hidden />
              <div className="absolute inset-0 flex flex-col items-start justify-center px-6 md:px-12">
                <h1 className="text-display font-display text-white max-w-2xl leading-tight">
                  Premium gymnastics and sports programmes for children in Hong Kong.
                </h1>
                <p className="text-body-lg text-cream max-w-xl mt-4">
                  Since 2011 — two dedicated venues in Wan Chai and Cyberport, coaches who complete our training course regardless of prior certification, and a progression pathway from first forward roll to competitive squad.
                </p>
                <div className="mt-6 flex flex-col sm:flex-row gap-3">
                  <Button asChild size="touch" className="bg-brand-red text-white hover:bg-brand-red/90 focus-visible:ring-2 focus-visible:ring-white">
                    <Link href="/book-a-trial/free-assessment/">
                      Book a Free Trial <ArrowRight className="ml-2 size-4" aria-hidden />
                    </Link>
                  </Button>
                  <Button asChild size="touch" variant="outline" className="border-white text-white hover:bg-white/10">
                    <Link href="/contact?market=hk">Send an Enquiry</Link>
                  </Button>
                </div>
                <p className="text-small text-cream/80 mt-4">
                  Free trial · No obligation · Usually booked same week.
                </p>
              </div>
            </div>
          </ContainerEditorial>
        </Section>
      );
    }

    // ====== §3.2 VENUE CHIP ROW ======
    function VenueChipSection() {
      return (
        <Section size="sm" bg="default">
          <ContainerEditorial width="wide">
            <VenueChipRow />
          </ContainerEditorial>
        </Section>
      );
    }

    // ====== §3.3 WHY CHOOSE US (4-tile grid) ======
    const WHY_CHOOSE_TILES = [
      { icon: Trophy, h3: "14 years in Hong Kong", body: "Since 2011, shaping how children move — now in our third dedicated venue." },
      { icon: MapPin, h3: "Two dedicated facilities", body: "Purpose-built floors in Wan Chai and Cyberport — gymnastics apparatus, not shared space." },
      { icon: BadgeCheck, h3: "A single coaching standard", body: "Every coach completes the ProActiv Sports training course, regardless of prior certification." },
      { icon: ArrowUpRight, h3: "A progression pathway", body: "From Babies & Toddlers through competitive squad and rhythmic — no forced level jumps." },
    ] as const;

    function WhyChooseSection() {
      return (
        <Section size="md" bg="muted">
          <ContainerEditorial width="wide">
            <h2 className="text-h2 font-display text-foreground mb-8">Why Hong Kong parents choose ProActiv.</h2>
            <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
              {WHY_CHOOSE_TILES.map(({ icon: Icon, h3, body }) => (
                <Card key={h3} className="p-6">
                  <Icon className="size-8 text-brand-navy mb-4" aria-hidden />
                  <h3 className="text-h3 font-display text-foreground">{h3}</h3>
                  <p className="text-body text-muted-foreground mt-2">{body}</p>
                </Card>
              ))}
            </div>
          </ContainerEditorial>
        </Section>
      );
    }

    // ====== §3.4 PROGRAMMES (5 tiles, 3+2 asymmetric grid on lg) ======
    const PROGRAMME_TILES = [
      { title: "Gymnastics", ageBand: "2–16yr", href: "/gymnastics/", image: "/photography/programme-beginner.webp", alt: "Beginner gymnastics class at ProGym", tagline: "8 levels, parent-tot to competitive.", colSpan: "lg:col-span-2" },
      { title: "Holiday Camps", ageBand: "3–12yr", href: "/holiday-camps/", image: "/photography/programme-holiday-camp.webp", alt: "Children at a ProGym holiday camp", tagline: "Easter, summer, Christmas.", colSpan: "lg:col-span-2" },
      { title: "Birthday Parties", ageBand: "3–12yr", href: "/birthday-parties/", image: "/photography/programme-birthday-party.webp", alt: "ProGym birthday party setup", tagline: "Hosted, coach-led, two hours.", colSpan: "lg:col-span-2" },
      { title: "School Partnerships", ageBand: "K–Y13", href: "/school-partnerships/", image: "/photography/programme-school-partnership.webp", alt: "Inter-school sports event", tagline: "Curriculum support, inter-school events.", colSpan: "lg:col-span-3" },
      { title: "Competitions & Events", ageBand: "6+", href: "/competitions-events/", image: "/photography/programme-competitive.webp", alt: "Competitive squad gymnasts", tagline: "Competitive pathway and community events.", colSpan: "lg:col-span-3" },
    ] as const;

    function ProgrammesSection() {
      return (
        <Section size="md" bg="default">
          <ContainerEditorial width="wide">
            <h2 className="text-h2 font-display text-foreground mb-8">Programmes for every stage — toddler to competitive.</h2>
            <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-6 gap-6">
              {PROGRAMME_TILES.map((p) => (
                <div key={p.title} className={p.colSpan}>
                  <ProgrammeTile
                    title={p.title}
                    ageBand={p.ageBand}
                    href={p.href}
                    image={p.image}
                    alt={p.alt}
                    tagline={p.tagline}
                  />
                </div>
              ))}
            </div>
          </ContainerEditorial>
        </Section>
      );
    }

    // ====== §3.5 LOCATION SPLIT (2-col with photo-flip per UI-SPEC §3.5) ======
    function LocationSplitSection() {
      return (
        <Section size="md" bg="default">
          <ContainerEditorial width="wide">
            <h2 className="text-h2 font-display text-foreground mb-8 text-center">Two ProGym venues across Hong Kong.</h2>
            <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
              {HK_VENUES.map((v, i) => {
                const flip = i === 1; // Cyberport reverses photo-copy direction
                return (
                  <div key={v.id} className={`grid gap-4 ${flip ? "" : ""}`}>
                    <div className="relative aspect-[4/3] overflow-hidden rounded-xl">
                      <Image
                        src={v.heroImage}
                        alt={`${v.nameFull} interior`}
                        fill
                        sizes="(max-width: 1024px) 100vw, 50vw"
                        className="object-cover"
                      />
                    </div>
                    <div>
                      <h3 className="text-h3 font-display text-foreground">
                        <span className="font-accent text-brand-navy">ProGym</span> {v.nameShort}
                      </h3>
                      <p className="text-small text-muted-foreground mt-1">{v.addressStreet}</p>
                      {v.sizeNote && <p className="text-small text-muted-foreground">{v.sizeNote} · {v.openedNote}</p>}
                      <div className="mt-3 flex flex-wrap gap-2">
                        {v.apparatus.map((a) => (
                          <Badge key={a} variant="secondary">{a}</Badge>
                        ))}
                      </div>
                      <Link href={`/${v.id}/`} className="inline-flex items-center gap-1 mt-4 text-brand-navy font-semibold hover:underline">
                        See {v.nameShort} venue <ArrowRight className="size-4" aria-hidden />
                      </Link>
                    </div>
                  </div>
                );
              })}
            </div>
          </ContainerEditorial>
        </Section>
      );
    }

    // ====== §3.6 SOCIAL PROOF (navy strip with LogoWall + 1 testimonial) ======
    function SocialProofSection() {
      return (
        <Section size="md" bg="navy">
          <ContainerEditorial width="wide">
            <h2 className="text-h2 font-display text-white mb-8 text-center">Trusted by Hong Kong's international school families.</h2>
            <LogoWall
              logos={[
                { src: "/photography/logo-school-placeholder-1.webp", alt: "Partner international school 1", width: 120, height: 60 },
                { src: "/photography/logo-school-placeholder-2.webp", alt: "Partner international school 2", width: 120, height: 60 },
                { src: "/photography/logo-school-placeholder-3.webp", alt: "Partner international school 3", width: 120, height: 60 },
                { src: "/photography/logo-school-placeholder-4.webp", alt: "Partner international school 4", width: 120, height: 60 },
              ]}
            />
            <div className="mt-8 max-w-2xl mx-auto">
              <TestimonialCard
                quote="My daughter has been with ProGym for three years. The progression from her first forward roll to her current beam routine has been transformative — and the coaches genuinely care about each child."
                attribution="Sarah W."
                role="Parent, Wan Chai"
              />
            </div>
          </ContainerEditorial>
        </Section>
      );
    }

    // Default page export — Task 2 adds §3.7-§3.12 + JSON-LD wrapper
    export default function HKHomePage() {
      return (
        <>
          <HeroSection />
          <VenueChipSection />
          <WhyChooseSection />
          <ProgrammesSection />
          <LocationSplitSection />
          <SocialProofSection />
          {/* §3.7-§3.12 + JSON-LD added in Task 2 */}
        </>
      );
    }
    ```

    **Photo asset reality check:** Before referencing each `/photography/*.webp` filename, run `ls public/photography/ | head -50` and verify the files exist. From STATE.md and Phase 2 D-07 amendment, 11 real HK photos are confirmed processed (`hk-venue-wanchai-gymtots`, `hk-venue-cyberport`, `programme-beginner`, etc.). If `programme-holiday-camp.webp`, `programme-birthday-party.webp`, `programme-school-partnership.webp`, `programme-competitive.webp`, or `logo-school-placeholder-*.webp` are MISSING, STOP execution and trigger a HUMAN-ACTION gate per CONTEXT D-09 / Phase 3 D-10 pattern: list the missing exact filenames + the directive to add source images and run `pnpm photos:process`. Do NOT substitute silhouettes or fall back to placeholder filenames.

    Per CONTEXT D-01 (hero video env-gate), D-05 (sticky red CTA), D-09 carry (photo HUMAN-ACTION). Per UI-SPEC §3.1-§3.6.
  </action>
  <verify>
    <automated>
      test -f /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -c "function.*Section" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "Premium gymnastics" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "/book-a-trial/free-assessment/" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "Book a Free Trial" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "Send an Enquiry" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "VenueChipRow" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "ProgrammeTile" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "TestimonialCard" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "LogoWall" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "dynamic.*=>.*import.*video-player" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "NEXT_PUBLIC_MUX_HK_HERO_PLAYBACK_ID" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -c "priority" /Users/martin/Projects/proactive/app/hk/page.tsx | awk '{ if($1 != 1) exit 1 }'
      grep -q "hk-venue-wanchai-gymtots" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "metadataBase" /Users/martin/Projects/proactive/app/hk/page.tsx || true   # metadataBase inherited from layout — OK if absent on page
      grep -q "canonical.*hk.proactivsports.com" /Users/martin/Projects/proactive/app/hk/page.tsx
      ls /Users/martin/Projects/proactive/public/photography/ | grep -E "(hk-venue-wanchai-gymtots|hk-venue-cyberport|programme-beginner)" | wc -l
      cd /Users/martin/Projects/proactive && pnpm typecheck
      cd /Users/martin/Projects/proactive && pnpm test:unit --run app/hk/page.test.tsx 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `app/hk/page.tsx` does NOT contain string `amber-400` (Phase 1 stub eliminated)
    - File contains literal string `Premium gymnastics and sports programmes for children in Hong Kong.` (verbatim H1)
    - File contains literal string `Send an Enquiry` (secondary CTA per UI-SPEC §Copywriting Contract revision)
    - File imports `dynamic` from `next/dynamic` AND uses `ssr: false` for VideoPlayer
    - File imports `VenueChipRow` from `@/components/hk/venue-chip-row`
    - File imports `ProgrammeTile`, `TestimonialCard`, `LogoWall`, `Badge`, `Card`, `Section`, `ContainerEditorial`, `Button` from `@/components/ui/*` (verified by grep)
    - File contains exactly ONE occurrence of the substring `priority` in an `<Image>` context (Pitfall 6 — single LCP rule). Acceptable: 1 use; FAIL: 0 or ≥2.
    - File references `process.env.NEXT_PUBLIC_MUX_HK_HERO_PLAYBACK_ID` (D-01 hero video gate)
    - File contains literal href `/book-a-trial/free-assessment/`
    - All referenced photography filenames exist in `public/photography/` (verified via ls — at minimum `hk-venue-wanchai-gymtots`, `hk-venue-cyberport`, `programme-beginner`)
    - Default export `HKHomePage` returns a fragment containing at minimum `HeroSection`, `VenueChipSection`, `WhyChooseSection`, `ProgrammesSection`, `LocationSplitSection`, `SocialProofSection` (verified via grep)
    - `pnpm typecheck` exits 0
    - `pnpm test:unit --run app/hk/page.test.tsx` shows Test 1 (H1) GREEN, Test 2 (venue chips — partial via VenueChipRow render) GREEN, Test 3 (Book CTA href) GREEN, Test 5 (single priority Image) GREEN
  </acceptance_criteria>
  <done>Top half of HK homepage renders. Hero video shows poster fallback (Mux ID env-gated per D-01). Test file is partially GREEN — Task 2 finishes the bottom half + JSON-LD.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build HK homepage §3.7-§3.12 + inline JSON-LD (CoachingMethod, CampsParties, AboutSnapshot, Blog, FAQ, FinalCTA)</name>
  <files>app/hk/page.tsx</files>
  <read_first>
    - /Users/martin/Projects/proactive/app/hk/page.tsx (Task 1 output — append §3.7-§3.12 + JSON-LD after existing sections; preserve Task 1 imports + section functions)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-UI-SPEC.md §3.7 CoachingMethod (3-pillar + Monica portrait) + §3.8 CampsParties (2-col revenue block) + §3.9 AboutSnapshot (2-col prose) + §3.10 Blog (1-3 stub posts + empty state) + §3.11 FAQ (8 items + char-for-char JSON-LD) + §3.12 FinalCTA (navy strip with red Book + WhatsApp env-conditional)
    - /Users/martin/Projects/proactive/.planning/inputs/strategy.md §PART 4 §7-§12 + §PART 6B §7-§12 (verbatim copy)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-PATTERNS.md §JSON-LD inline script (analog `app/root/page.tsx` lines 491-496) + §FAQ data + FAQItem pattern
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-RESEARCH.md §Pattern 7 / §Code Examples (HK Homepage JSON-LD with WebSite + FAQPage @graph)
    - /Users/martin/Projects/proactive/lib/hk-data.ts (HK_FAQ_ITEMS — filter `group === "about"` for the homepage 8 OR slice the first 8 across groups; HK_BLOG_POSTS_STUB drives §3.10)
    - /Users/martin/Projects/proactive/components/ui/accordion.tsx (Phase 2 — provides Accordion wrapper) + components/ui/faq-item.tsx (composed inside Accordion per Phase 3 pattern)
  </read_first>
  <behavior>
    Tests in `app/hk/page.test.tsx` Test 4 (FAQPage JSON-LD) and full §3 wiring (all sections render) MUST be GREEN after this task.

    JSON-LD requirements:
    - Single `<script type="application/ld+json">` element placed BEFORE the first `<HeroSection />` in the JSX (analog `app/root/page.tsx` line 491-496 pattern)
    - JSON `@graph` array contains at minimum 2 entries: `{ "@type": "WebSite", ... }` AND `{ "@type": "FAQPage", "mainEntity": [...] }`
    - `FAQPage.mainEntity` length === number of FAQ items rendered visibly in §3.11 (8 items)
    - Each `mainEntity[i].name` matches the exact `<FAQItem question>` prop (char-for-char per Google rich-result rule)
    - Each `mainEntity[i].acceptedAnswer.text` matches the exact `<FAQItem answer>` prop

    FAQ section behavioral expectations:
    - Renders 8 `<FAQItem>` components inside `<Accordion type="single" collapsible>`
    - Source data: `HK_FAQ_ITEMS.filter(i => i.group === "about" || i.group === "venues" || i.group === "gymnastics").slice(0, 8)` OR a homepage-curated subset documented in lib/hk-data.ts
    - The 8 questions are the verbatim PART 6B §11 set
  </behavior>
  <action>
    EDIT `app/hk/page.tsx` (do not recreate the file). Add a JSON-LD constant at top-level (after `metadata` export), add 6 new section functions (`CoachingMethodSection`, `CampsPartiesSection`, `AboutSnapshotSection`, `BlogSection`, `FAQSection`, `FinalCTASection`), and update the default export to render all 12 sections + the JSON-LD script.

    **JSON-LD constant (insert after `metadata` export):**

    ```typescript
    // Pick the homepage's 8 FAQs (UI-SPEC §3.11 — verbatim PART 6B §11).
    // Convention: lib/hk-data.ts marks homepage-eligible items via group ∈ {"about","gymnastics","venues"}.
    // Take first 8 in declaration order to keep visible DOM ordering = JSON-LD ordering (Google rule).
    const HK_HOMEPAGE_FAQS = HK_FAQ_ITEMS
      .filter(i => i.group === "about" || i.group === "gymnastics" || i.group === "venues")
      .slice(0, 8);

    const hkHomeSchema = {
      "@context": "https://schema.org",
      "@graph": [
        {
          "@type": "WebSite",
          "@id": "https://hk.proactivsports.com/#website",
          "url": "https://hk.proactivsports.com/",
          "name": "ProActiv Sports Hong Kong",
          "publisher": { "@id": "https://proactivsports.com/#organization" },
          "inLanguage": "en-HK",
        },
        {
          "@type": "FAQPage",
          "mainEntity": HK_HOMEPAGE_FAQS.map(item => ({
            "@type": "Question",
            "name": item.question,
            "acceptedAnswer": { "@type": "Answer", "text": item.answer },
          })),
        },
      ],
    };
    ```

    **Append §3.7 — CoachingMethod (3-pillar + Monica portrait):**

    ```typescript
    const COACHING_PILLARS = [
      { h3: "Safety", body: "Every coach completes our internal training course; safety is the foundation, not a checklist." },
      { h3: "Progression", body: "Eight gymnastics levels with clear movement criteria — children advance when ready, not on schedule." },
      { h3: "Confidence", body: "We measure success in tries attempted, not skills mastered. Confidence outlives any single class." },
    ] as const;

    function CoachingMethodSection() {
      return (
        <Section size="md" bg="default">
          <ContainerEditorial width="wide">
            <h2 className="text-h2 font-display text-foreground mb-8">How we coach.</h2>
            <div className="grid grid-cols-1 lg:grid-cols-3 gap-8 items-start">
              <div className="lg:col-span-2 grid grid-cols-1 md:grid-cols-3 gap-4">
                {COACHING_PILLARS.map((p) => (
                  <Card key={p.h3} className="p-5">
                    <h3 className="text-h3 font-display text-brand-navy">{p.h3}</h3>
                    <p className="text-body text-muted-foreground mt-2">{p.body}</p>
                  </Card>
                ))}
              </div>
              <div>
                <div className="relative aspect-[3/4] overflow-hidden rounded-xl">
                  <Image
                    src="/photography/coach-monica-portrait.webp"
                    alt="Monica, Director of Sports Hong Kong, coaching at ProGym"
                    fill
                    sizes="(max-width: 1024px) 100vw, 33vw"
                    className="object-cover"
                  />
                </div>
                <p className="text-small text-muted-foreground mt-3">
                  Led by Monica, Director of Sports Hong Kong — 19 years coaching, Level 2 Italian certification.
                </p>
              </div>
            </div>
          </ContainerEditorial>
        </Section>
      );
    }
    ```

    If `coach-monica-portrait.webp` is missing from `public/photography/`, STOP and trigger HUMAN-ACTION gate per CONTEXT D-09.

    **Append §3.8 — CampsParties (2-col revenue block):**

    ```typescript
    function CampsPartiesSection() {
      return (
        <Section size="md" bg="muted">
          <ContainerEditorial width="wide">
            <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
              <div>
                <div className="relative aspect-[4/3] overflow-hidden rounded-xl">
                  <Image src="/photography/programme-holiday-camp.webp" alt="ProGym holiday camp" fill sizes="(max-width: 1024px) 100vw, 50vw" className="object-cover" />
                </div>
                <h3 className="text-h3 font-display text-foreground mt-4">Holiday Camps</h3>
                <ul className="mt-3 space-y-1 text-body text-muted-foreground">
                  <li>· Easter</li>
                  <li>· Summer</li>
                  <li>· Christmas</li>
                </ul>
                <Button asChild size="touch" className="bg-brand-red text-white hover:bg-brand-red/90 mt-4">
                  <Link href="/holiday-camps/">Book a holiday camp</Link>
                </Button>
              </div>
              <div>
                <div className="relative aspect-[4/3] overflow-hidden rounded-xl">
                  <Image src="/photography/programme-birthday-party.webp" alt="ProGym birthday party" fill sizes="(max-width: 1024px) 100vw, 50vw" className="object-cover" />
                </div>
                <h3 className="text-h3 font-display text-foreground mt-4">Birthday Parties</h3>
                <ul className="mt-3 space-y-1 text-body text-muted-foreground">
                  <li>· Two-hour hosted format</li>
                  <li>· Coach-led activities</li>
                  <li>· Bring the cake — we bring the apparatus</li>
                </ul>
                <Button asChild size="touch" variant="outline" className="border-brand-navy text-brand-navy hover:bg-brand-navy/5 mt-4">
                  <Link href="/birthday-parties/">Send an Enquiry</Link>
                </Button>
              </div>
            </div>
          </ContainerEditorial>
        </Section>
      );
    }
    ```

    **Append §3.9 — AboutSnapshot (2-col prose):**

    ```typescript
    function AboutSnapshotSection() {
      return (
        <Section size="md" bg="default">
          <ContainerEditorial width="wide">
            <div className="grid grid-cols-1 lg:grid-cols-2 gap-8 items-center">
              <div>
                <h2 className="text-h2 font-display text-foreground mb-4">About <span className="font-accent text-brand-navy">ProGym</span> Hong Kong.</h2>
                <p className="text-body text-muted-foreground">
                  We've been building children's gymnastics in Hong Kong since 2011. Two purpose-built venues — Wan Chai and Cyberport — and a coaching method that treats every child as a long-term progression project, not a class number.
                </p>
                <p className="text-body text-muted-foreground mt-4">
                  Our path is simple: a free trial, a coach who notices your child by name, and a programme that meets them at exactly their level. The first forward roll is just the beginning.
                </p>
              </div>
              <div className="relative aspect-[4/3] overflow-hidden rounded-xl">
                <Image src="/photography/coaching-action-photo.webp" alt="ProGym coach working with a child on the beam" fill sizes="(max-width: 1024px) 100vw, 50vw" className="object-cover" />
              </div>
            </div>
          </ContainerEditorial>
        </Section>
      );
    }
    ```

    **Append §3.10 — Blog (1-3 stubs from HK_BLOG_POSTS_STUB + empty-state per UI-SPEC):**

    ```typescript
    import { HK_BLOG_POSTS_STUB } from "@/lib/hk-data"; // already imported in Task 1 if you add HK_BLOG_POSTS_STUB to the existing import line
    // (If not, edit the import at the top of the file to include HK_BLOG_POSTS_STUB.)

    function BlogSection() {
      const posts = HK_BLOG_POSTS_STUB.slice(0, 3);
      if (posts.length === 0) {
        return (
          <Section size="md" bg="default">
            <ContainerEditorial width="default">
              <div className="text-center max-w-2xl mx-auto">
                <h2 className="text-h2 font-display text-foreground mb-3">New posts coming soon.</h2>
                <p className="text-body text-muted-foreground">
                  We're preparing long-form guides on gymnastics progression, holiday camp planning, and what to expect at your first class. In the meantime, our coaches are happy to answer any question directly.
                </p>
              </div>
            </ContainerEditorial>
          </Section>
        );
      }
      return (
        <Section size="md" bg="default">
          <ContainerEditorial width="wide">
            <h2 className="text-h2 font-display text-foreground mb-8">From the blog.</h2>
            <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
              {posts.map((p) => (
                <Card key={p.slug} className="overflow-hidden">
                  <div className="relative aspect-[4/3]">
                    <Image src={p.heroImage} alt={p.title} fill sizes="(max-width:1024px) 100vw, 33vw" className="object-cover" />
                  </div>
                  <div className="p-5">
                    <Badge variant="secondary">{p.category}</Badge>
                    <h3 className="text-h3 font-display text-foreground mt-3">{p.title}</h3>
                    <p className="text-body text-muted-foreground mt-2">{p.excerpt}</p>
                    <p className="text-small text-muted-foreground mt-3">
                      <time dateTime={p.publishedAt}>{p.publishedAt}</time> · {p.readTimeMinutes} min read
                    </p>
                  </div>
                </Card>
              ))}
            </div>
          </ContainerEditorial>
        </Section>
      );
    }
    ```

    **Append §3.11 — FAQ (8 items + accordion):**

    ```typescript
    function FAQSection() {
      return (
        <Section size="md" bg="default">
          <ContainerEditorial width="default">
            <div className="max-w-3xl mx-auto">
              <h2 className="text-h2 font-display text-foreground mb-8">Frequently asked questions.</h2>
              <Accordion type="single" collapsible className="flex flex-col gap-0">
                {HK_HOMEPAGE_FAQS.map((item) => (
                  <FAQItem
                    key={item.value}
                    id={item.value}
                    question={item.question}
                    answer={item.answer}
                  />
                ))}
              </Accordion>
            </div>
          </ContainerEditorial>
        </Section>
      );
    }
    ```

    **Append §3.12 — FinalCTA (navy strip):**

    ```typescript
    function FinalCTASection() {
      const whatsappHk = process.env.NEXT_PUBLIC_HK_WHATSAPP;
      return (
        <Section size="lg" bg="navy">
          <ContainerEditorial width="default">
            <div className="text-center max-w-2xl mx-auto">
              <h2 className="text-h2 font-display text-white mb-3">Ready to book your child's first class?</h2>
              <p className="text-body-lg text-cream mb-6">
                Free 30-minute assessment, no commitment. Choose Wan Chai or Cyberport — or let us suggest based on your location.
              </p>
              <div className="flex flex-col sm:flex-row gap-3 justify-center">
                <Button asChild size="touch" className="bg-brand-red text-white hover:bg-brand-red/90">
                  <Link href="/book-a-trial/free-assessment/">
                    Book a Free Trial <ArrowRight className="ml-2 size-4" aria-hidden />
                  </Link>
                </Button>
                {whatsappHk && (
                  <Button asChild size="touch" variant="outline" className="border-white text-white hover:bg-white/10">
                    <a href={`https://wa.me/${whatsappHk.replace(/[^0-9+]/g, "")}?text=${encodeURIComponent("Hi ProActiv HK, I'd like to book a free trial.")}`} target="_blank" rel="noopener noreferrer">
                      Chat on WhatsApp <MessageCircle className="ml-2 size-4" aria-hidden />
                    </a>
                  </Button>
                )}
              </div>
            </div>
          </ContainerEditorial>
        </Section>
      );
    }
    ```

    **Update default export** to render all 12 sections + JSON-LD:

    ```typescript
    export default function HKHomePage() {
      return (
        <>
          <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(hkHomeSchema) }} />
          <HeroSection />
          <VenueChipSection />
          <WhyChooseSection />
          <ProgrammesSection />
          <LocationSplitSection />
          <SocialProofSection />
          <CoachingMethodSection />
          <CampsPartiesSection />
          <AboutSnapshotSection />
          <BlogSection />
          <FAQSection />
          <FinalCTASection />
        </>
      );
    }
    ```

    Verify: count of `<*Section />` JSX elements in the default export === 12.

    Per CONTEXT D-05 (sticky red CTA carries to FinalCTA), D-09 carry (photo HUMAN-ACTION). Per UI-SPEC §3.7-§3.12 + §8.3 (FAQ JSON-LD char-for-char rule).
  </action>
  <verify>
    <automated>
      grep -q "function CoachingMethodSection" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "function CampsPartiesSection" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "function AboutSnapshotSection" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "function BlogSection" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "function FAQSection" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "function FinalCTASection" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "hkHomeSchema" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "@type.*FAQPage" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "@type.*WebSite" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "Ready to book your child's first class" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "NEXT_PUBLIC_HK_WHATSAPP" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "wa.me" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "application/ld\\+json" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -q "HK_BLOG_POSTS_STUB" /Users/martin/Projects/proactive/app/hk/page.tsx
      grep -c "Section.*size=" /Users/martin/Projects/proactive/app/hk/page.tsx | awk '{ if($1 < 12) exit 1 }'
      cd /Users/martin/Projects/proactive && pnpm typecheck
      cd /Users/martin/Projects/proactive && pnpm test:unit --run app/hk/page.test.tsx 2>&1 | tail -30
    </automated>
  </verify>
  <acceptance_criteria>
    - All 6 new section functions exist: `CoachingMethodSection`, `CampsPartiesSection`, `AboutSnapshotSection`, `BlogSection`, `FAQSection`, `FinalCTASection`
    - File contains literal string `Ready to book your child's first class?` (verbatim final CTA H2 from PART 6B §12)
    - File contains literal string `Frequently asked questions.` (FAQ heading)
    - JSON-LD constant `hkHomeSchema` declared with `@graph` containing both `WebSite` and `FAQPage` types
    - Default export JSX contains exactly 12 `<*Section />` JSX nodes (verified by counting `Section />` occurrences inside the default export — manual or via grep)
    - First child of default export's fragment is `<script type="application/ld+json" ...>`
    - WhatsApp link conditionally rendered with `whatsappHk &&` guard (Pitfall 7 / Phase 3 D-02 carry)
    - WhatsApp anchor tag uses `target="_blank"` AND `rel="noopener noreferrer"` (security)
    - File ONLY uses ONE `priority` attribute on `<Image>` (Pitfall 6 — verified via `grep -c "priority" app/hk/page.tsx == 1`)
    - `pnpm typecheck` exits 0
    - `pnpm test:unit --run app/hk/page.test.tsx` reports ALL 5 tests GREEN (H1, venue chips, Book CTA, FAQPage JSON-LD, single priority Image)
  </acceptance_criteria>
  <done>HK homepage complete: all 12 sections rendered, JSON-LD inline, FAQ char-for-char wired, FinalCTA with WhatsApp env-conditional. `app/hk/page.test.tsx` is fully GREEN. HK-01 + ROADMAP SC#1 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → app/hk/page.tsx | Public read-only page; no input handling on this page |
| process.env → client bundle | NEXT_PUBLIC_MUX_HK_HERO_PLAYBACK_ID + NEXT_PUBLIC_HK_WHATSAPP exposed by design |
| Mux CDN → browser | Hero video stream from Mux (third-party CDN) |
| Google Fonts → build | Unbounded + Manrope already self-hosted via next/font/google (build-time fetch) — Phase 2 carry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | Mux playback ID leakage | accept | Mux public playback IDs are designed to be public (CDN delivery tokens, not auth tokens). RESEARCH §Security Domain confirms no mitigation needed. |
| T-04-03-02 | Tampering | dangerouslySetInnerHTML JSON-LD injection | mitigate | `hkHomeSchema` is a build-time TypeScript constant — no user input, no template interpolation. `JSON.stringify(hkHomeSchema)` produces well-formed JSON; `<script>` element does not execute. Identical pattern to Phase 3 root page. |
| T-04-03-03 | Tampering | WhatsApp deep-link query injection | mitigate | Pre-filled message uses `encodeURIComponent("...")` with a fixed string. Phone digits sanitised via `.replace(/[^0-9+]/g, "")`. Same pattern as Phase 3. |
| T-04-03-04 | Spoofing | External anchor without rel="noopener" | mitigate | All external `<a target="_blank">` use `rel="noopener noreferrer"` (WhatsApp link in §3.12). |
| T-04-03-05 | DoS | Hero video autoplay drains data on metered mobile | accept | Mux Player respects `prefers-reduced-data` natively; `loop autoPlay muted` is industry standard for marketing hero. iOS Low-Power-Mode behaviour documented in RESEARCH Pitfall 4 (acceptable degradation — poster image stays visible). |
| T-04-03-06 | Information Disclosure | Image alt-text accidentally exposes children's names | mitigate | Alt-text strings in this file are generic ("Children practising gymnastics", "ProGym holiday camp", coach name only — Monica is a public business figure per strategy). NO child names appear in alt-text. Reviewed during plan authorship. |
</threat_model>

<verification>
After both tasks:

```bash
pnpm typecheck                     # Must exit 0
pnpm lint                          # Must exit 0
pnpm test:unit --run app/hk/page.test.tsx  # All 5 tests GREEN
pnpm build                         # Compile clean; route /hk/ generates
pnpm dev                           # Manual at hk.localhost:3000/
```

Manual smoke (use Chrome DevTools mobile viewport at 375×667):
- Page renders 12 sections in order
- HKNav sticky at top throughout scroll
- Above-the-fold content shows: H1, venue chip row, primary red `Book a Free Trial` CTA (HK-01 SC#1)
- Hero shows poster image (Mux ID env-unset locally — D-01 fallback works)
- All `<Image>` lazy-load except hero poster
- FAQ accordion opens on click, keyboard arrow keys navigate items
- Final CTA: clicking `Book a Free Trial` navigates to `/book-a-trial/free-assessment/` (404 expected since Plan 07 hasn't built it yet — non-blocking)

JSON-LD verification:
```bash
curl -s http://hk.localhost:3000/ | grep -A 2 'application/ld+json' | head
```
Should emit a `@graph` with `WebSite` + `FAQPage` types.
</verification>

<success_criteria>
- [ ] `app/hk/page.tsx` renders 12 sections per UI-SPEC §3 wireframe
- [ ] H1 verbatim "Premium gymnastics and sports programmes for children in Hong Kong."
- [ ] VenueChipRow shows Wan Chai + Cyberport above-the-fold
- [ ] Hero VideoPlayer dynamic with ssr:false; poster Image is the single `priority` Image (Pitfall 6)
- [ ] Primary red Book a Free Trial CTA on hero links to `/book-a-trial/free-assessment/`
- [ ] Secondary CTA labelled "Send an Enquiry" (UI-SPEC §Copywriting Contract revision)
- [ ] 5 ProgrammeTile cards with 3+2 asymmetric grid on lg (UI-SPEC §3.4)
- [ ] LocationSplit alternates photo-copy direction across Wan Chai + Cyberport
- [ ] SocialProof navy strip with LogoWall + 1 TestimonialCard
- [ ] FAQ accordion renders 8 items with verbatim PART 6B §11 questions
- [ ] FAQPage JSON-LD `mainEntity` matches FAQ DOM text char-for-char
- [ ] FinalCTA on navy with red Book + env-conditional WhatsApp
- [ ] All photography references resolved against actual files in `public/photography/` (HUMAN-ACTION gate triggered for any missing)
- [ ] `pnpm test:unit --run app/hk/page.test.tsx` all 5 tests GREEN
- [ ] `pnpm typecheck`, `pnpm lint`, `pnpm build` all clean
- [ ] HK-01 + ROADMAP SC#1 fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-hong-kong-market/04-03-SUMMARY.md` documenting:
- Final 12-section structure + line counts per section function
- JSON-LD `@graph` types declared (WebSite + FAQPage)
- Hero D-01 HUMAN-ACTION attestation (Mux env-var consumed; poster fallback verified)
- Photography asset audit: which files were already present vs HUMAN-ACTION-gated
- HK-01 + ROADMAP SC#1 closure attestation
- Carry-forward note for Plan 04: location pages will reuse the LocationSplit + VenueChipRow patterns at single-venue scope.
</output>

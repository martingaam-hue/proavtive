---
phase: 04-hong-kong-market
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - app/hk/layout.tsx
  - components/hk/hk-nav.tsx
  - components/hk/hk-nav-mobile.tsx
  - components/hk/hk-footer.tsx
  - components/hk/venue-chip-row.tsx
  - components/hk/active-venue-chip.tsx
  - lib/og-image.tsx
  - app/fonts.ts
  - app/hk/opengraph-image.tsx
autonomous: true
requirements: [HK-01, HK-02, HK-03, HK-04, HK-05, HK-06, HK-07, HK-08, HK-09, HK-10, HK-11, HK-12]

must_haves:
  truths:
    - "Every URL under `hk.localhost:3000/*` (and Vercel preview equivalent) renders HKNav at the top and HKFooter at the bottom"
    - "HKNav primary bar shows: ProActiv HK logo, Gymnastics dropdown trigger, Locations dropdown trigger, Camps link, Coaches link, FAQ link, sticky red `Book a Free Trial` CTA button"
    - "Gymnastics dropdown opens to show all 8 programme entries (D-03)"
    - "Locations dropdown opens to show ProGym Wan Chai + ProGym Cyberport entries (D-04)"
    - "Mobile viewport (<1024px) shows hamburger trigger; tap opens right-side Sheet with same nav links + sticky red CTA at top of sheet"
    - "HKFooter renders 4 columns on desktop (Brand, Venues NAP, Quick links, Connect) with WhatsApp CTA conditionally rendered when env var present"
    - "Cross-market footer links (`Prodigy Singapore`, `ProActiv Sports Group`) use absolute `<a href={env.NEXT_PUBLIC_SG_URL}>` / `NEXT_PUBLIC_ROOT_URL` — never `<Link>`"
    - "HK layout sets `metadataBase: new URL('https://hk.proactivsports.com')` independently of root layout"
    - "Baloo 2 font variable cascades from HK layout to HK page descendants (so font-accent utility resolves to Baloo 2)"
    - "HK default OG image renders navy background + `ProActiv Sports Hong Kong` brand"
  artifacts:
    - path: "app/hk/layout.tsx"
      provides: "HK route-group layout — replaces Phase 1 amber-stripe placeholder; wires HKNav + HKFooter + Baloo font + metadataBase"
      contains: "metadataBase"
    - path: "components/hk/hk-nav.tsx"
      provides: "Sticky HK primary navigation with Gymnastics + Locations dropdowns + sticky red Book a Free Trial CTA"
      contains: "NavigationMenu"
    - path: "components/hk/hk-nav-mobile.tsx"
      provides: "Mobile Sheet drawer mirror of HKNav"
      contains: "use client"
    - path: "components/hk/hk-footer.tsx"
      provides: "4-column HK footer with NAP for both venues + WhatsApp CTA + cross-market links"
      contains: "NEXT_PUBLIC_SG_URL"
    - path: "components/hk/venue-chip-row.tsx"
      provides: "Venue chip row composition (Wan Chai + Cyberport entry chips) consumed by HK homepage §3.2"
      contains: "ActiveVenueChip"
    - path: "components/hk/active-venue-chip.tsx"
      provides: "Client wrapper that highlights current venue chip when pathname matches"
      contains: "use client"
    - path: "lib/og-image.tsx"
      provides: "Extended with `createHKOgImage()` for HK-specific OG generation"
      contains: "createHKOgImage"
    - path: "app/hk/opengraph-image.tsx"
      provides: "Default HK OG image consumed by all HK pages without per-route override"
      contains: "createHKOgImage"
    - path: "app/fonts.ts"
      provides: "Extended with `baloo` next/font/google export (Baloo 2 SemiBold 600, OFL)"
      contains: "Baloo_2"
  key_links:
    - from: "app/hk/layout.tsx"
      to: "components/hk/hk-nav.tsx + components/hk/hk-footer.tsx"
      via: "ES imports + JSX usage in default export"
      pattern: "<HKNav"
    - from: "components/hk/hk-nav.tsx"
      to: "components/ui/navigation-menu.tsx"
      via: "import { NavigationMenu, NavigationMenuTrigger, NavigationMenuContent }"
      pattern: "@/components/ui/navigation-menu"
    - from: "components/hk/hk-nav.tsx"
      to: "lib/hk-data.ts (HK_GYMNASTICS_PROGRAMMES, HK_VENUES)"
      via: "import"
      pattern: "from [\"']@/lib/hk-data[\"']"
    - from: "components/hk/hk-footer.tsx"
      to: "lib/hk-data.ts (HK_VENUES) + process.env.NEXT_PUBLIC_HK_WHATSAPP + NEXT_PUBLIC_SG_URL + NEXT_PUBLIC_ROOT_URL"
      via: "imports + env reads"
      pattern: "process\\.env\\.NEXT_PUBLIC_(HK_WHATSAPP|SG_URL|ROOT_URL)"
    - from: "app/hk/layout.tsx"
      to: "app/fonts.ts (baloo)"
      via: "className with `${baloo.variable}` to cascade --font-accent"
      pattern: "baloo\\.variable"
---

<objective>
Build the HK route-group layout chrome that wraps every Phase 4 page: shared nav, shared footer, venue chip row composition, Baloo 2 font activation, HK metadataBase, and default HK OG image.

Purpose: Without this layout, Plans 03-07 cannot ship pages that meet HK-01 SC#1 (above-the-fold CTA), HK-02/03 SC#2 (venue NAP in chrome), or the universal 2-click-to-booking requirement. This plan unblocks every downstream HK page.

Output:
- HK route-group layout replaces Phase 1 amber-stripe placeholder
- 5 new HK-scoped components in `components/hk/`
- Baloo 2 font added to font registry (Phase 2 D-03 activation point)
- HK OG image utility + default OG route
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-hong-kong-market/04-CONTEXT.md
@.planning/phases/04-hong-kong-market/04-RESEARCH.md
@.planning/phases/04-hong-kong-market/04-PATTERNS.md
@.planning/phases/04-hong-kong-market/04-UI-SPEC.md
@.planning/phases/04-hong-kong-market/04-01-SUMMARY.md

# Phase 3 analog files — copy structure verbatim
@app/root/layout.tsx
@components/root/root-nav.tsx
@components/root/root-nav-mobile.tsx
@components/root/root-footer.tsx
@lib/og-image.tsx
@app/root/opengraph-image.tsx
@app/fonts.ts

# Phase 4 foundation from Plan 01
@lib/hk-data.ts
@components/ui/navigation-menu.tsx

<interfaces>
<!-- Contracts the executor needs from prior phases. Extracted from committed code. -->

From `app/root/layout.tsx` (the EXACT pattern HK layout mirrors):
```typescript
import type { Metadata } from "next";
import { unbounded, manrope } from "@/app/fonts";
import { RootNav } from "@/components/root/root-nav";
import { RootFooter } from "@/components/root/root-footer";

const baseUrl = process.env.VERCEL_PROJECT_PRODUCTION_URL
  ? `https://${process.env.VERCEL_PROJECT_PRODUCTION_URL}`
  : process.env.VERCEL_URL ? `https://${process.env.VERCEL_URL}` : "http://localhost:3000";

export const metadata: Metadata = {
  metadataBase: new URL(baseUrl),
  title: { default: "...", template: "%s | ProActiv Sports" },
  // ...
};

export default function RootGroupLayout({ children }) {
  return (
    <>
      <a href="#main-content" className="sr-only focus:not-sr-only ...">Skip to main content</a>
      <RootNav />
      <main id="main-content">{children}</main>
      <RootFooter />
    </>
  );
}
```

From `components/root/root-nav.tsx`:
```typescript
const NAV_LINKS = [{ href: "/brand/", label: "Brand" }, ...] as const;
export function RootNav() {
  return (
    <header className="sticky top-0 z-50 bg-background/95 backdrop-blur-sm border-b border-border">
      <ContainerEditorial width="wide" className="flex items-center justify-between h-16 lg:h-20">
        <Link href="/" aria-label="...">{logo}</Link>
        <nav aria-label="Primary" className="hidden lg:flex items-center gap-6">{links}</nav>
        <RootNavMobile navLinks={NAV_LINKS} />
      </ContainerEditorial>
    </header>
  );
}
```

From `components/root/root-footer.tsx` — inline social SVG icons (FacebookIcon, InstagramIcon, LinkedinIcon) at lines 13-35; copy verbatim.

From `lib/og-image.tsx`:
```typescript
export async function createRootOgImage({ title, tagline }: OgImageOptions): Promise<ImageResponse> {
  // Reads font + logo via fetch; renders ImageResponse with navy bg + Unbounded title
}
```

From `app/fonts.ts` (current state — Phase 2):
```typescript
import { Unbounded, Manrope } from "next/font/google";
export const unbounded = Unbounded({ subsets: ["latin"], display: "swap", variable: "--font-display", weight: ["400","600","700"] });
export const manrope = Manrope({ subsets: ["latin"], display: "swap", variable: "--font-sans", weight: ["400","600","700"] });
// Baloo not yet exported — Plan 04-02 adds it
```

From `lib/hk-data.ts` (Plan 04-01):
```typescript
export const HK_VENUES: HKVenue[] = [...]; // wan-chai + cyberport entries with addressStreet etc.
export const HK_GYMNASTICS_PROGRAMMES: HKGymnasticsProgramme[] = [...]; // 8 entries with href + label + ageBand
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Baloo 2 to font registry + HK route-group layout (replace Phase 1 placeholder)</name>
  <files>app/fonts.ts, app/hk/layout.tsx</files>
  <read_first>
    - /Users/martin/Projects/proactive/app/fonts.ts (current Unbounded + Manrope exports — match Baloo to same shape)
    - /Users/martin/Projects/proactive/app/root/layout.tsx (Phase 3 layout — exact pattern to mirror, including metadataBase fallback chain, skip-link, and main-content wrapper)
    - /Users/martin/Projects/proactive/app/hk/layout.tsx (current Phase 1 stub — file to REPLACE with real HK layout)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-UI-SPEC.md §1 (typography inheritance + Baloo 2 weight policy: SemiBold 600 only, OFL) + §3.0 (page wrapper structure) + §11 (D-06 components/hk/ scope)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-RESEARCH.md §Pattern 1 (HK layout exact code) + §Pitfall 1 (metadataBase mismatch) + §Pitfall 8 (Baloo activation)
    - /Users/martin/Projects/proactive/.planning/phases/02-design-system-component-gallery-media-pipeline/02-CONTEXT.md (D-01 amended: Unbounded + Manrope + Baloo 2, all OFL/free-for-commercial via next/font/google)
  </read_first>
  <action>
    **Step A — extend `app/fonts.ts`:**

    Add the following export AFTER the existing `manrope` export. Do NOT modify existing `unbounded` or `manrope` exports.

    ```typescript
    import { Baloo_2 } from "next/font/google"; // add to existing top-of-file import line

    export const baloo = Baloo_2({
      subsets: ["latin"],
      display: "swap",
      variable: "--font-accent",   // matches Phase 2 UI-SPEC §1.7 — `font-accent` Tailwind utility
      weight: ["600"],             // SemiBold ONLY per Phase 2 D-03 + Phase 4 UI-SPEC §1 weight policy
      adjustFontFallback: false,   // Baloo has unique metrics; let CSS swap handle FOIT
    });
    ```

    Verify `app/globals.css` (or `tailwind.config.ts`) already maps `--font-accent` to a `font-accent` utility (Phase 2 work). If not present, STOP and surface — Phase 2 should have wired this; raising it as a Rule-1 deviation prevents silent rework.

    **Step B — replace `app/hk/layout.tsx`** entirely (Phase 1 stub goes; new layout is the production HK chrome):

    ```typescript
    // Phase 4 / Plan 04-02 — HK route-group layout.
    // Replaces Phase 1 amber-400 distinguisher placeholder.
    // metadataBase = hk.proactivsports.com (Pitfall 1 — independent from root layout).
    // Baloo 2 font variable cascades to all HK descendants (Phase 2 D-03 activation).
    import type { Metadata } from "next";
    import { baloo } from "@/app/fonts";
    import { HKNav } from "@/components/hk/hk-nav";
    import { HKFooter } from "@/components/hk/hk-footer";

    // Resolve HK subdomain base URL across local + Vercel preview + production.
    // Strategy: Phase 10 attaches the real `hk.proactivsports.com` domain;
    // until then, Vercel previews live at `<sha>-proactive.vercel.app`. We declare the
    // production-canonical metadataBase here so OG images and absolute URLs resolve correctly
    // once the domain is attached, without a layout edit at Phase 10.
    const HK_PRODUCTION_BASE = "https://hk.proactivsports.com";
    const baseUrl =
      process.env.VERCEL_ENV === "production"
        ? HK_PRODUCTION_BASE
        : process.env.VERCEL_PROJECT_PRODUCTION_URL
          ? `https://${process.env.VERCEL_PROJECT_PRODUCTION_URL}`
          : process.env.VERCEL_URL
            ? `https://${process.env.VERCEL_URL}`
            : "http://hk.localhost:3000";

    export const metadata: Metadata = {
      metadataBase: new URL(baseUrl),
      title: {
        default: "Kids Gymnastics & Sports Hong Kong | ProActiv Sports — Wan Chai & Cyberport",
        template: "%s | ProActiv Sports Hong Kong",
      },
      description:
        "Premium gymnastics, sports classes, holiday camps and birthday parties for children in Hong Kong. ProGym Wan Chai & Cyberport. Book a free trial.",
      openGraph: {
        siteName: "ProActiv Sports Hong Kong",
        locale: "en_HK",
        type: "website",
        // Default OG image inherited from app/hk/opengraph-image.tsx (per-route opengraph-image takes precedence)
      },
    };

    export default function HKGroupLayout({ children }: Readonly<{ children: React.ReactNode }>) {
      return (
        <div className={`${baloo.variable}`}>
          <a
            href="#main-content"
            className="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-[100] focus:bg-primary focus:text-primary-foreground focus:px-4 focus:py-2 focus:rounded-md focus:outline-2 focus:outline-ring focus:outline-offset-2"
          >
            Skip to main content
          </a>
          <HKNav />
          <main id="main-content">{children}</main>
          <HKFooter />
        </div>
      );
    }
    ```

    **Why a `<div>` wrapper instead of overriding `<html>`:** App Router only allows ONE `<html>` element (in `app/layout.tsx`). Phase 1's `app/layout.tsx` already declares `<html lang="en">` with the global font variables. The HK route-group layout adds an additional `--font-accent` variable on a wrapping `<div>` so all HK descendants resolve `font-accent` to Baloo 2. Same shape Phase 2 used for Baloo activation.

    Per UI-SPEC §3.0 page wrapper. Per CONTEXT D-01 (hero video lives inside this layout via Plan 03). Per RESEARCH Pitfall 1 (metadataBase) + Pitfall 8 (Baloo).
  </action>
  <verify>
    <automated>
      grep -q "Baloo_2" /Users/martin/Projects/proactive/app/fonts.ts
      grep -q "export const baloo" /Users/martin/Projects/proactive/app/fonts.ts
      grep -q "weight:.*\\[\"600\"\\]" /Users/martin/Projects/proactive/app/fonts.ts
      grep -q "metadataBase: new URL" /Users/martin/Projects/proactive/app/hk/layout.tsx
      grep -q "hk.proactivsports.com" /Users/martin/Projects/proactive/app/hk/layout.tsx
      grep -q "HKNav" /Users/martin/Projects/proactive/app/hk/layout.tsx
      grep -q "HKFooter" /Users/martin/Projects/proactive/app/hk/layout.tsx
      grep -q "baloo.variable" /Users/martin/Projects/proactive/app/hk/layout.tsx
      grep -q "Skip to main content" /Users/martin/Projects/proactive/app/hk/layout.tsx
      grep -q "ProActiv Sports Hong Kong" /Users/martin/Projects/proactive/app/hk/layout.tsx
      grep -q "en_HK" /Users/martin/Projects/proactive/app/hk/layout.tsx
      cd /Users/martin/Projects/proactive && pnpm typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `app/fonts.ts` exports `baloo` from `next/font/google` (`Baloo_2`) with `variable: "--font-accent"` and `weight: ["600"]`
    - `app/hk/layout.tsx` first non-comment line imports `baloo` from `@/app/fonts`
    - `app/hk/layout.tsx` contains literal string `metadataBase: new URL`
    - `app/hk/layout.tsx` contains literal string `hk.proactivsports.com`
    - `app/hk/layout.tsx` contains literal string `Skip to main content` (a11y skip link)
    - `app/hk/layout.tsx` JSX contains both `<HKNav />` and `<HKFooter />` (verified via grep)
    - `app/hk/layout.tsx` JSX wrapper uses `${baloo.variable}` className (Baloo cascade)
    - `app/hk/layout.tsx` does NOT contain string `amber-400` (Phase 1 placeholder removed)
    - `app/hk/layout.tsx` declares `locale: "en_HK"` in openGraph
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>HK layout chrome compiles with Baloo cascade + correct metadataBase. Plans 03-07 inherit it automatically.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build HKNav (RSC + mobile Sheet) with Gymnastics + Locations dropdowns + sticky red CTA</name>
  <files>components/hk/hk-nav.tsx, components/hk/hk-nav-mobile.tsx</files>
  <read_first>
    - /Users/martin/Projects/proactive/components/root/root-nav.tsx (analog — 90% structural copy: sticky header, ContainerEditorial wrapper, hidden-on-mobile primary nav, mobile component handoff)
    - /Users/martin/Projects/proactive/components/root/root-nav-mobile.tsx (analog — Sheet pattern; copy structure, swap to HK nav links + sticky CTA at top of sheet per UI-SPEC §5.1)
    - /Users/martin/Projects/proactive/components/ui/navigation-menu.tsx (Plan 04-01 — exports NavigationMenu, NavigationMenuList, NavigationMenuItem, NavigationMenuTrigger, NavigationMenuContent, NavigationMenuLink — all needed)
    - /Users/martin/Projects/proactive/components/ui/sheet.tsx (existing Phase 3 — provides `Sheet`, `SheetTrigger`, `SheetContent`, `SheetHeader`, `SheetTitle`)
    - /Users/martin/Projects/proactive/components/ui/button.tsx (existing — provides `size="touch"` variant required for sticky CTA)
    - /Users/martin/Projects/proactive/lib/hk-data.ts (Plan 04-01 — HK_GYMNASTICS_PROGRAMMES + HK_VENUES drive dropdown content)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-UI-SPEC.md §5.1 (HKNav full spec — bar height, background, dropdown panel size + visual treatment, sticky red CTA as ONE permitted red exception)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-PATTERNS.md §components/hk/hk-nav.tsx + §components/hk/hk-nav-mobile.tsx (exact code skeletons)
  </read_first>
  <action>
    **Create `components/hk/hk-nav.tsx` (RSC):**

    ```typescript
    // Phase 4 / Plan 04-02 — HK primary navigation (RSC).
    // Per UI-SPEC §5.1 + CONTEXT D-02 (6 primary items + sticky CTA), D-03 (Gymnastics dropdown 8 items),
    // D-04 (Locations dropdown 2 items), D-05 (sticky red Book a Free Trial CTA), D-06 (lives at components/hk/).
    // The sticky red CTA is THE single exception to the "no red on nav" rule (UI-SPEC §Color reserved list).
    import * as React from "react";
    import Link from "next/link";
    import { ChevronDown } from "lucide-react";
    import { Button } from "@/components/ui/button";
    import { ContainerEditorial } from "@/components/ui/container-editorial";
    import {
      NavigationMenu,
      NavigationMenuList,
      NavigationMenuItem,
      NavigationMenuTrigger,
      NavigationMenuContent,
      NavigationMenuLink,
    } from "@/components/ui/navigation-menu";
    import { HKNavMobile } from "@/components/hk/hk-nav-mobile";
    import { HK_GYMNASTICS_PROGRAMMES, HK_VENUES } from "@/lib/hk-data";

    // 6 primary nav items per CONTEXT D-02 (Gymnastics + Locations are dropdown triggers, not flat links;
    // the trigger labels appear in the bar but their hrefs are the pillar pages so trigger-clicks are also
    // navigations for keyboard / no-JS users).
    const FLAT_NAV_LINKS = [
      { href: "/holiday-camps/", label: "Camps" },
      { href: "/coaches/", label: "Coaches" },
      { href: "/faq/", label: "FAQ" },
    ] as const;

    export function HKNav() {
      return (
        <header className="sticky top-0 z-50 bg-background/95 backdrop-blur-sm border-b border-brand-navy/10">
          <ContainerEditorial width="wide" className="flex items-center justify-between h-16 lg:h-20">
            {/* Brand lockup */}
            <Link
              href="/"
              aria-label="ProActiv Sports Hong Kong — home"
              className="font-display font-bold text-h3 text-brand-navy hover:text-brand-navy/80 transition-colors"
            >
              ProActiv <span className="font-accent text-brand-red">HK</span>
            </Link>

            {/* Desktop primary nav (lg+) */}
            <NavigationMenu className="hidden lg:flex" aria-label="Primary">
              <NavigationMenuList className="gap-2">
                {/* Gymnastics dropdown — D-03 (8 sub-programmes) */}
                <NavigationMenuItem>
                  <NavigationMenuTrigger className="font-medium text-foreground hover:text-brand-navy">
                    Gymnastics
                  </NavigationMenuTrigger>
                  <NavigationMenuContent>
                    <ul className="grid gap-1 p-3 min-w-[320px] bg-white border border-border rounded-lg shadow-lg">
                      {HK_GYMNASTICS_PROGRAMMES.map((p) => (
                        <li key={p.slug}>
                          <NavigationMenuLink asChild>
                            <Link
                              href={p.href}
                              className="block px-3 py-2 rounded hover:bg-brand-navy/5 transition-colors"
                            >
                              <div className="font-sans font-semibold text-foreground">{p.label}</div>
                              <div className="text-small text-muted-foreground">{p.ageBand}</div>
                            </Link>
                          </NavigationMenuLink>
                        </li>
                      ))}
                      <li className="border-t border-border mt-1 pt-1">
                        <NavigationMenuLink asChild>
                          <Link
                            href="/gymnastics/"
                            className="block px-3 py-2 rounded text-small font-semibold text-brand-navy hover:bg-brand-navy/5"
                          >
                            See all programmes →
                          </Link>
                        </NavigationMenuLink>
                      </li>
                    </ul>
                  </NavigationMenuContent>
                </NavigationMenuItem>

                {/* Locations dropdown — D-04 (Wan Chai + Cyberport) */}
                <NavigationMenuItem>
                  <NavigationMenuTrigger className="font-medium text-foreground hover:text-brand-navy">
                    Locations
                  </NavigationMenuTrigger>
                  <NavigationMenuContent>
                    <ul className="grid gap-1 p-3 min-w-[320px] bg-white border border-border rounded-lg shadow-lg">
                      {HK_VENUES.map((v) => (
                        <li key={v.id}>
                          <NavigationMenuLink asChild>
                            <Link
                              href={`/${v.id}/`}
                              className="block px-3 py-2 rounded hover:bg-brand-navy/5 transition-colors"
                            >
                              <div className="font-sans font-semibold text-foreground">
                                <span className="font-accent">ProGym</span> {v.nameShort}
                              </div>
                              <div className="text-small text-muted-foreground">{v.addressStreet}</div>
                            </Link>
                          </NavigationMenuLink>
                        </li>
                      ))}
                    </ul>
                  </NavigationMenuContent>
                </NavigationMenuItem>

                {/* Flat nav items */}
                {FLAT_NAV_LINKS.map(({ href, label }) => (
                  <NavigationMenuItem key={href}>
                    <NavigationMenuLink asChild className="px-3 py-2 font-medium text-foreground hover:text-brand-navy transition-colors">
                      <Link href={href}>{label}</Link>
                    </NavigationMenuLink>
                  </NavigationMenuItem>
                ))}
              </NavigationMenuList>
            </NavigationMenu>

            {/* Sticky red Book a Free Trial CTA — D-05 (visible on every HK page; THE single nav red-fill exception per UI-SPEC §Color) */}
            <div className="hidden lg:block">
              <Button
                asChild
                size="touch"
                className="bg-brand-red text-white hover:bg-brand-red/90 focus-visible:ring-2 focus-visible:ring-brand-red focus-visible:ring-offset-2"
              >
                <Link href="/book-a-trial/free-assessment/">Book a Free Trial</Link>
              </Button>
            </div>

            {/* Mobile (<lg) hamburger + Sheet */}
            <HKNavMobile />
          </ContainerEditorial>
        </header>
      );
    }
    ```

    **Create `components/hk/hk-nav-mobile.tsx` (`'use client'`):**

    ```typescript
    "use client";
    // Phase 4 / Plan 04-02 — HK mobile navigation (Sheet drawer).
    // Mirrors components/root/root-nav-mobile.tsx Phase 3 pattern (Sheet + Menu icon + nav-link list with onClick close).
    // Sticky red Book a Free Trial CTA appears at the top of the sheet (D-05 — every viewport).
    import * as React from "react";
    import Link from "next/link";
    import { Menu } from "lucide-react";
    import { Button } from "@/components/ui/button";
    import {
      Sheet,
      SheetContent,
      SheetTrigger,
      SheetHeader,
      SheetTitle,
    } from "@/components/ui/sheet";
    import { HK_GYMNASTICS_PROGRAMMES, HK_VENUES } from "@/lib/hk-data";

    const FLAT_NAV_LINKS = [
      { href: "/holiday-camps/", label: "Camps" },
      { href: "/coaches/", label: "Coaches" },
      { href: "/blog/", label: "Blog" },
      { href: "/faq/", label: "FAQ" },
    ] as const;

    export function HKNavMobile() {
      const [open, setOpen] = React.useState(false);
      const close = () => setOpen(false);

      return (
        <Sheet open={open} onOpenChange={setOpen}>
          <SheetTrigger asChild>
            <Button
              size="icon"
              variant="ghost"
              className="lg:hidden min-h-11 min-w-11"
              aria-label={open ? "Close navigation menu" : "Open navigation menu"}
            >
              <Menu className="size-5" />
            </Button>
          </SheetTrigger>
          <SheetContent side="right" className="w-full sm:w-80 bg-background overflow-y-auto">
            <SheetHeader>
              <SheetTitle className="text-h3 font-display">Menu</SheetTitle>
            </SheetHeader>

            {/* Sticky red CTA at top of sheet — universal D-05 conversion entry */}
            <div className="mt-6">
              <Button asChild size="touch" className="bg-brand-red text-white hover:bg-brand-red/90 w-full">
                <Link href="/book-a-trial/free-assessment/" onClick={close}>
                  Book a Free Trial
                </Link>
              </Button>
            </div>

            {/* Gymnastics group */}
            <nav aria-label="Mobile primary" className="mt-8 flex flex-col gap-1">
              <h3 className="text-label font-semibold text-muted-foreground uppercase tracking-wide mb-2">Gymnastics</h3>
              {HK_GYMNASTICS_PROGRAMMES.map((p) => (
                <Link
                  key={p.slug}
                  href={p.href}
                  onClick={close}
                  className="font-sans text-body py-2 min-h-12 text-foreground hover:text-brand-navy transition-colors"
                >
                  {p.label} <span className="text-small text-muted-foreground">{p.ageBand}</span>
                </Link>
              ))}

              {/* Locations group */}
              <h3 className="mt-6 text-label font-semibold text-muted-foreground uppercase tracking-wide mb-2">Locations</h3>
              {HK_VENUES.map((v) => (
                <Link
                  key={v.id}
                  href={`/${v.id}/`}
                  onClick={close}
                  className="font-sans text-body py-2 min-h-12 text-foreground hover:text-brand-navy transition-colors"
                >
                  <span className="font-accent">ProGym</span> {v.nameShort}
                </Link>
              ))}

              {/* Flat links */}
              <div className="mt-6 border-t border-border pt-4 flex flex-col gap-1">
                {FLAT_NAV_LINKS.map(({ href, label }) => (
                  <Link
                    key={href}
                    href={href}
                    onClick={close}
                    className="font-sans text-body py-2 min-h-12 text-foreground hover:text-brand-navy transition-colors"
                  >
                    {label}
                  </Link>
                ))}
              </div>
            </nav>
          </SheetContent>
        </Sheet>
      );
    }
    ```

    **Pitfall guard (Pitfall 7 — cross-subdomain links):** None of the HKNav `<Link>` hrefs cross subdomains; all 6 primary items + dropdowns + CTA route to HK pages (same subdomain). Safe to use Next.js `<Link>` throughout. Cross-market links are footer-only (Task 4).

    Per CONTEXT D-02 / D-03 / D-04 / D-05 / D-06. Per UI-SPEC §5.1.
  </action>
  <verify>
    <automated>
      test -f /Users/martin/Projects/proactive/components/hk/hk-nav.tsx
      test -f /Users/martin/Projects/proactive/components/hk/hk-nav-mobile.tsx
      grep -q "NavigationMenu" /Users/martin/Projects/proactive/components/hk/hk-nav.tsx
      grep -q "HK_GYMNASTICS_PROGRAMMES" /Users/martin/Projects/proactive/components/hk/hk-nav.tsx
      grep -q "HK_VENUES" /Users/martin/Projects/proactive/components/hk/hk-nav.tsx
      grep -q "/book-a-trial/free-assessment/" /Users/martin/Projects/proactive/components/hk/hk-nav.tsx
      grep -q "bg-brand-red" /Users/martin/Projects/proactive/components/hk/hk-nav.tsx
      grep -q "size=\"touch\"" /Users/martin/Projects/proactive/components/hk/hk-nav.tsx
      grep -q "sticky top-0" /Users/martin/Projects/proactive/components/hk/hk-nav.tsx
      grep -q "^\"use client\"" /Users/martin/Projects/proactive/components/hk/hk-nav-mobile.tsx
      grep -q "Sheet" /Users/martin/Projects/proactive/components/hk/hk-nav-mobile.tsx
      grep -q "/book-a-trial/free-assessment/" /Users/martin/Projects/proactive/components/hk/hk-nav-mobile.tsx
      grep -q "bg-brand-red" /Users/martin/Projects/proactive/components/hk/hk-nav-mobile.tsx
      cd /Users/martin/Projects/proactive && pnpm typecheck
      cd /Users/martin/Projects/proactive && pnpm lint
    </automated>
  </verify>
  <acceptance_criteria>
    - `components/hk/hk-nav.tsx` does NOT have `"use client"` directive (RSC)
    - `components/hk/hk-nav-mobile.tsx` first non-comment line is `"use client";`
    - `components/hk/hk-nav.tsx` imports from `@/components/ui/navigation-menu` (proves Plan 04-01 dep is wired)
    - `components/hk/hk-nav.tsx` imports `HK_GYMNASTICS_PROGRAMMES` AND `HK_VENUES` from `@/lib/hk-data`
    - `components/hk/hk-nav.tsx` contains literal href `/book-a-trial/free-assessment/`
    - `components/hk/hk-nav.tsx` Button uses `bg-brand-red` className AND `size="touch"` (D-05 sticky red CTA)
    - `components/hk/hk-nav.tsx` `<header>` uses `sticky top-0` (D-05 always-visible)
    - `components/hk/hk-nav.tsx` aria-label "Primary" present on the desktop nav
    - `components/hk/hk-nav-mobile.tsx` uses `<Sheet>` + `<SheetTrigger>` + `<SheetContent>`
    - `components/hk/hk-nav-mobile.tsx` Sheet has `side="right"` per UI-SPEC §5.1
    - `components/hk/hk-nav-mobile.tsx` first nav element after SheetHeader is the red Book a Free Trial CTA (verified by grep order: `SheetHeader` precedes `bg-brand-red` precedes `Gymnastics`)
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>HKNav renders sticky bar on desktop, hamburger Sheet on mobile, with both dropdowns + persistent red CTA. Plans 03-07 inherit it from layout.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build HKFooter + VenueChipRow + ActiveVenueChip + HK OG image (createHKOgImage + default opengraph-image)</name>
  <files>components/hk/hk-footer.tsx, components/hk/venue-chip-row.tsx, components/hk/active-venue-chip.tsx, lib/og-image.tsx, app/hk/opengraph-image.tsx</files>
  <read_first>
    - /Users/martin/Projects/proactive/components/root/root-footer.tsx (analog — copy 4-col footer structure, social SVG icons block lines 13-35 verbatim, separator + bottom bar pattern)
    - /Users/martin/Projects/proactive/lib/og-image.tsx (analog — `createRootOgImage` is the structural template; add a sibling `createHKOgImage` with HK-specific tagline + en_HK locale)
    - /Users/martin/Projects/proactive/app/root/opengraph-image.tsx (analog — 5-line consumer pattern)
    - /Users/martin/Projects/proactive/app/root/contact/page.tsx (lines ~57-65 — WhatsApp env-conditional render pattern; copy verbatim)
    - /Users/martin/Projects/proactive/components/ui/section.tsx + container-editorial.tsx + separator.tsx + card.tsx (existing primitives footer composes)
    - /Users/martin/Projects/proactive/lib/hk-data.ts (HK_VENUES drives footer NAP block, VenueChipRow row, ActiveVenueChip pathname matching)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-UI-SPEC.md §5.2 (HKFooter 4-col spec: Brand, Venues NAP, Quick links, Connect — WhatsApp conditional + cross-market `<a href={env}>` links) + §5.3 (VenueChipRow spec) + §7 (OG template inheritance from Phase 3)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-PATTERNS.md §components/hk/hk-footer.tsx + §lib/og-image.tsx (extend) + §app/hk/*/opengraph-image.tsx files (5-line template)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-RESEARCH.md §Pitfall 5 (OG build budget — only 8 high-priority pages get per-route OG; layout default catches the rest)
  </read_first>
  <action>
    **Step A — `components/hk/hk-footer.tsx`** (RSC) — mirror Phase 3 RootFooter structure with HK-specific 4-col layout per UI-SPEC §5.2:

    ```typescript
    // Phase 4 / Plan 04-02 — HK footer (RSC). Per UI-SPEC §5.2 + CONTEXT D-06 (lives at components/hk/).
    // 4 columns desktop: (1) Brand + tagline, (2) Venues NAP for Wan Chai + Cyberport, (3) Quick links, (4) Connect (WhatsApp + email + cross-market).
    // Cross-market links use absolute <a href={env}> per Pitfall 7 carry-forward.
    import Link from "next/link";
    import { MapPin, Phone, Mail, MessageCircle } from "lucide-react";
    import { Section } from "@/components/ui/section";
    import { ContainerEditorial } from "@/components/ui/container-editorial";
    import { Separator } from "@/components/ui/separator";
    import { HK_VENUES } from "@/lib/hk-data";

    // Inline social icons — Simple Icons CC0; copy verbatim from components/root/root-footer.tsx lines 13-35.
    function FacebookIcon(props: React.SVGProps<SVGSVGElement>) { /* same SVG path */ }
    function InstagramIcon(props: React.SVGProps<SVGSVGElement>) { /* same SVG path */ }
    function LinkedinIcon(props: React.SVGProps<SVGSVGElement>) { /* same SVG path */ }

    const QUICK_LINKS = [
      { href: "/gymnastics/", label: "Gymnastics" },
      { href: "/holiday-camps/", label: "Holiday Camps" },
      { href: "/birthday-parties/", label: "Birthday Parties" },
      { href: "/coaches/", label: "Coaches" },
      { href: "/blog/", label: "Blog" },
      { href: "/faq/", label: "FAQ" },
    ] as const;

    export function HKFooter() {
      const whatsappHk = process.env.NEXT_PUBLIC_HK_WHATSAPP;
      const phoneHk = process.env.NEXT_PUBLIC_HK_PHONE;
      const sgUrl = process.env.NEXT_PUBLIC_SG_URL;
      const rootUrl = process.env.NEXT_PUBLIC_ROOT_URL;

      return (
        <footer className="bg-brand-navy text-white">
          <Section size="md" bg="navy">
            <ContainerEditorial width="wide">
              <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 lg:gap-12">
                {/* Col 1 — Brand + tagline */}
                <div>
                  <Link href="/" className="font-display font-bold text-h3 text-white">
                    ProActiv <span className="font-accent">HK</span>
                  </Link>
                  <p className="mt-3 text-body text-white/80">
                    Premium gymnastics and sports programmes for children in Hong Kong since 2011.
                  </p>
                </div>

                {/* Col 2 — Venues NAP (one block per HK_VENUES entry) */}
                <div>
                  <h3 className="font-display text-h3 text-white mb-4">Venues</h3>
                  <ul className="space-y-4">
                    {HK_VENUES.map((v) => (
                      <li key={v.id}>
                        <div className="font-sans font-semibold text-white">
                          <span className="font-accent">ProGym</span> {v.nameShort}
                        </div>
                        <p className="text-small text-white/80 flex items-start gap-1 mt-1">
                          <MapPin className="size-3.5 mt-0.5 shrink-0" aria-hidden />
                          <span>{v.addressStreet}, {v.addressLocality}</span>
                        </p>
                        {phoneHk && (
                          <a
                            href={`tel:${phoneHk.replace(/[^0-9+]/g, "")}`}
                            className="text-small text-white/80 hover:text-white flex items-center gap-1 mt-1"
                          >
                            <Phone className="size-3.5" aria-hidden /> {phoneHk}
                          </a>
                        )}
                      </li>
                    ))}
                  </ul>
                </div>

                {/* Col 3 — Quick links */}
                <div>
                  <h3 className="font-display text-h3 text-white mb-4">Quick links</h3>
                  <ul className="space-y-2">
                    {QUICK_LINKS.map((l) => (
                      <li key={l.href}>
                        <Link href={l.href} className="text-small text-white/80 hover:text-white transition-colors">
                          {l.label}
                        </Link>
                      </li>
                    ))}
                  </ul>
                </div>

                {/* Col 4 — Connect (WhatsApp + email + cross-market) */}
                <div>
                  <h3 className="font-display text-h3 text-white mb-4">Connect</h3>
                  <div className="space-y-3">
                    {whatsappHk && (
                      <a
                        href={`https://wa.me/${whatsappHk.replace(/[^0-9+]/g, "")}?text=${encodeURIComponent("Hi ProActiv HK, I'd like to book a free trial.")}`}
                        target="_blank"
                        rel="noopener noreferrer"
                        className="flex items-center gap-2 text-small text-brand-green hover:opacity-90"
                      >
                        <MessageCircle className="size-4" aria-hidden />
                        Chat on WhatsApp
                      </a>
                    )}
                    <a
                      href="mailto:hk@proactivsports.com"
                      className="flex items-center gap-2 text-small text-white/80 hover:text-white"
                    >
                      <Mail className="size-4" aria-hidden /> hk@proactivsports.com
                    </a>
                    {sgUrl && (
                      <a
                        href={sgUrl}
                        className="block text-small text-white/80 hover:text-white"
                      >
                        Prodigy Singapore ↗
                      </a>
                    )}
                    {rootUrl && (
                      <a
                        href={rootUrl}
                        className="block text-small text-white/80 hover:text-white"
                      >
                        ProActiv Sports Group ↗
                      </a>
                    )}
                  </div>

                  {/* Social icons */}
                  <div className="mt-6 flex items-center gap-4">
                    <a href="https://www.facebook.com/proactivsports" aria-label="Facebook" target="_blank" rel="noopener noreferrer" className="text-white/70 hover:text-white"><FacebookIcon className="size-5" /></a>
                    <a href="https://www.instagram.com/proactivsports" aria-label="Instagram" target="_blank" rel="noopener noreferrer" className="text-white/70 hover:text-white"><InstagramIcon className="size-5" /></a>
                    <a href="https://www.linkedin.com/company/proactivsports" aria-label="LinkedIn" target="_blank" rel="noopener noreferrer" className="text-white/70 hover:text-white"><LinkedinIcon className="size-5" /></a>
                  </div>
                </div>
              </div>

              <Separator className="my-8 bg-white/20" />
              <div className="flex flex-col md:flex-row justify-between gap-3 text-small text-white/60">
                <p>© {new Date().getFullYear()} ProActiv Sports — Hong Kong</p>
                <div className="flex gap-4">
                  <a href={(rootUrl ?? "") + "/privacy/"} className="hover:text-white">Privacy</a>
                  <a href={(rootUrl ?? "") + "/terms/"} className="hover:text-white">Terms</a>
                </div>
              </div>
            </ContainerEditorial>
          </Section>
        </footer>
      );
    }
    ```

    Copy the inline `FacebookIcon`, `InstagramIcon`, `LinkedinIcon` SVG paths character-for-character from `components/root/root-footer.tsx` lines 13-35. Do NOT re-source from anywhere else — exact match keeps brand consistency + Simple Icons CC0 attribution unchanged.

    **Step B — `components/hk/venue-chip-row.tsx`** (RSC):

    ```typescript
    // Phase 4 / Plan 04-02 — Venue chip row consumed by HK homepage §3.2.
    // Two chips, flex-col on mobile, flex-row centered on desktop.
    import { Card } from "@/components/ui/card";
    import { HK_VENUES } from "@/lib/hk-data";
    import { ActiveVenueChip } from "@/components/hk/active-venue-chip";

    export function VenueChipRow() {
      return (
        <div className="flex flex-col sm:flex-row sm:justify-center gap-4 flex-wrap">
          {HK_VENUES.map((v) => (
            <ActiveVenueChip key={v.id} venueId={v.id} href={`/${v.id}/`}>
              <Card className="px-4 py-3 border border-brand-navy/20 hover:-translate-y-1 hover:shadow-md transition-all flex items-center gap-3">
                {/* MapPin icon left */}
                <span className="text-brand-navy" aria-hidden>📍</span>
                <div>
                  <div className="font-sans font-semibold text-foreground">
                    <span className="font-accent">ProGym</span> {v.nameShort}
                  </div>
                  <div className="text-small text-muted-foreground">{v.addressStreet}</div>
                </div>
              </Card>
            </ActiveVenueChip>
          ))}
        </div>
      );
    }
    ```

    Replace the `📍` emoji placeholder with `<MapPin className="size-4 text-brand-navy" aria-hidden />` from `lucide-react` (UI-SPEC strict no-emojis-in-icons-system rule). The emoji above is a writing shortcut for the spec — the implementation MUST use the lucide icon.

    **Step C — `components/hk/active-venue-chip.tsx`** (`'use client'`):

    ```typescript
    "use client";
    // Phase 4 / Plan 04-02 — client wrapper for VenueChipRow that highlights active venue.
    // Per RESEARCH Pitfall 2 (usePathname returns browser URL — no /hk/ prefix).
    import Link from "next/link";
    import { usePathname } from "next/navigation";
    import { cn } from "@/lib/utils";

    export function ActiveVenueChip({
      venueId,
      href,
      children,
    }: {
      venueId: "wan-chai" | "cyberport";
      href: string;
      children: React.ReactNode;
    }) {
      const pathname = usePathname();
      const isActive = pathname === href || pathname.startsWith(href);
      return (
        <Link href={href} className={cn("group block", isActive && "ring-2 ring-brand-navy rounded-lg")}
          aria-current={isActive ? "page" : undefined}
        >
          {children}
        </Link>
      );
    }
    ```

    **Step D — extend `lib/og-image.tsx`** with `createHKOgImage`:

    Append AFTER existing `createRootOgImage` export:

    ```typescript
    export async function createHKOgImage({ title, tagline }: OgImageOptions): Promise<ImageResponse> {
      // Mirrors createRootOgImage: navy background + Unbounded title + cream tagline.
      // Difference: tagline appends "ProActiv Sports Hong Kong" instead of "ProActiv Sports".
      // Same try/catch graceful font + logo loading; same ImageResponse shape (1200x630).
      // [Body: copy createRootOgImage line-for-line; swap brand line constant to "ProActiv Sports Hong Kong"]
    }
    ```

    Implement by literally copying `createRootOgImage` body, then on the brand-line text element replace `"ProActiv Sports"` with `"ProActiv Sports Hong Kong"`. Do NOT touch the navy background, font-loading try/catch, or response dimensions — those are Phase 3-checker-approved invariants.

    **Step E — create `app/hk/opengraph-image.tsx`** (default HK OG; per UI-SPEC §7 inherited by all routes lacking their own opengraph-image):

    ```typescript
    // Phase 4 / Plan 04-02 — default HK OG image.
    // All HK routes inherit this unless they declare their own opengraph-image.tsx (Pitfall 5 — only 8 high-priority pages do).
    import { createHKOgImage } from "@/lib/og-image";

    export const runtime = "nodejs";
    export const dynamic = "force-dynamic";
    export const size = { width: 1200, height: 630 };
    export const contentType = "image/png";

    export default function OG() {
      return createHKOgImage({
        title: "ProActiv Sports Hong Kong",
        tagline: "Premium gymnastics and sports programmes for children — Wan Chai & Cyberport.",
      });
    }
    ```

    Per UI-SPEC §5.2 / §5.3 / §7. Per CONTEXT D-06 (HK-scoped components) + Phase 3 carry D-02 (WhatsApp env-conditional).
  </action>
  <verify>
    <automated>
      test -f /Users/martin/Projects/proactive/components/hk/hk-footer.tsx
      test -f /Users/martin/Projects/proactive/components/hk/venue-chip-row.tsx
      test -f /Users/martin/Projects/proactive/components/hk/active-venue-chip.tsx
      grep -q "HK_VENUES" /Users/martin/Projects/proactive/components/hk/hk-footer.tsx
      grep -q "NEXT_PUBLIC_HK_WHATSAPP" /Users/martin/Projects/proactive/components/hk/hk-footer.tsx
      grep -q "NEXT_PUBLIC_SG_URL" /Users/martin/Projects/proactive/components/hk/hk-footer.tsx
      grep -q "NEXT_PUBLIC_ROOT_URL" /Users/martin/Projects/proactive/components/hk/hk-footer.tsx
      grep -q "wa.me" /Users/martin/Projects/proactive/components/hk/hk-footer.tsx
      grep -q "FacebookIcon" /Users/martin/Projects/proactive/components/hk/hk-footer.tsx
      grep -q "ActiveVenueChip" /Users/martin/Projects/proactive/components/hk/venue-chip-row.tsx
      grep -q "MapPin" /Users/martin/Projects/proactive/components/hk/venue-chip-row.tsx
      grep -q "^\"use client\"" /Users/martin/Projects/proactive/components/hk/active-venue-chip.tsx
      grep -q "usePathname" /Users/martin/Projects/proactive/components/hk/active-venue-chip.tsx
      grep -q "createHKOgImage" /Users/martin/Projects/proactive/lib/og-image.tsx
      grep -q "ProActiv Sports Hong Kong" /Users/martin/Projects/proactive/lib/og-image.tsx
      test -f /Users/martin/Projects/proactive/app/hk/opengraph-image.tsx
      grep -q "createHKOgImage" /Users/martin/Projects/proactive/app/hk/opengraph-image.tsx
      grep -q "ProActiv Sports Hong Kong" /Users/martin/Projects/proactive/app/hk/opengraph-image.tsx
      cd /Users/martin/Projects/proactive && pnpm typecheck
      cd /Users/martin/Projects/proactive && pnpm lint
      cd /Users/martin/Projects/proactive && pnpm build 2>&1 | grep -E "(Compiled|✓|error|Error)" | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `components/hk/hk-footer.tsx` exists with 4-column grid (verified by `grep -q "lg:grid-cols-4" components/hk/hk-footer.tsx`)
    - HKFooter uses `bg-brand-navy` (UI-SPEC §5.2)
    - HKFooter renders WhatsApp link only when `NEXT_PUBLIC_HK_WHATSAPP` env var truthy (verified by grep `whatsappHk &&`)
    - HKFooter cross-market links to SG + Root use absolute `<a href={env}>` pattern (verified by grep `<a\s+href={sgUrl` and `<a\s+href={rootUrl`)
    - HKFooter contains `FacebookIcon`, `InstagramIcon`, `LinkedinIcon` function declarations (Simple Icons CC0 verbatim from Phase 3)
    - `components/hk/venue-chip-row.tsx` uses `MapPin` from `lucide-react` (NOT emoji per UI-SPEC voice rule)
    - `components/hk/active-venue-chip.tsx` first non-comment line is `"use client";`
    - `components/hk/active-venue-chip.tsx` uses `usePathname` from `next/navigation`
    - `lib/og-image.tsx` contains `export async function createHKOgImage` (verified by grep)
    - `lib/og-image.tsx` createHKOgImage tagline appends `"ProActiv Sports Hong Kong"`
    - `app/hk/opengraph-image.tsx` exists, exports default function returning `createHKOgImage(...)`
    - `app/hk/opengraph-image.tsx` exports `size = { width: 1200, height: 630 }` AND `runtime = "nodejs"`
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0
    - `pnpm build` does not error (warnings acceptable; pages 03-07 don't exist yet so dead-route warnings are tolerable)
  </acceptance_criteria>
  <done>HKFooter renders 4-col footer, VenueChipRow ready for homepage §3.2, default HK OG image inherited by all HK routes. Layout chrome complete; Plans 03-07 unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → HK pages | All HK pages are public; no auth surface in Phase 4 |
| Footer / Nav → external services | WhatsApp deep link (`https://wa.me/`), `tel:` URI, social media URLs |
| `process.env.NEXT_PUBLIC_*` → client bundle | All HK env vars are intentionally `NEXT_PUBLIC_` (visible to browser): WhatsApp number, root/sg subdomain URLs, Mux playback ID, map embed URLs, phone number |
| `app/hk/opengraph-image.tsx` build → static asset | OG image generation runs at build time; consumes Unbounded font via fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | WhatsApp deep link query param injection | mitigate | Pre-filled message uses `encodeURIComponent("Hi ProActiv HK, I'd like to book a free trial.")` — fixed string, not user-derived. Phone number digits stripped via `.replace(/[^0-9+]/g, "")` before URL construction. No XSS surface. |
| T-04-02-02 | Tampering | `tel:` href phone number | mitigate | Same `.replace(/[^0-9+]/g, "")` sanitisation as Phase 3 contact page; only digits + `+` survive into the URL scheme. |
| T-04-02-03 | Information Disclosure | Cross-market link target leak | accept | `NEXT_PUBLIC_SG_URL` / `NEXT_PUBLIC_ROOT_URL` are public canonical URLs (sg.proactivsports.com, proactivsports.com); intentional + designed-as-public per project architecture. Conditional render (`sgUrl &&`) gracefully hides when env var absent (preview without fully-configured envs). |
| T-04-02-04 | Spoofing | Open-redirect via dropdown link | accept | All HKNav dropdown hrefs are statically derived from `HK_GYMNASTICS_PROGRAMMES` (build-time TS array, no user input). No dynamic URL construction. Same for HKFooter quick-links. |
| T-04-02-05 | Tampering | shadcn navigation-menu Radix vulnerability | accept | Radix is a first-party trusted library already pinned via `radix-ui@1.4.3` metapackage. Navigation-menu added in Plan 04-01 follows same supply chain as previously-vetted Sheet, Accordion, etc. No new attack surface. |
| T-04-02-06 | Information Disclosure | OG image build leaks internal URLs | mitigate | `createHKOgImage` only renders title + tagline + brand mark — no env-var content embedded in the rendered image. `app/hk/opengraph-image.tsx` consumer passes only static strings. Build-time `runtime: "nodejs"` keeps generation server-side. |
| T-04-02-07 | Denial of Service | OG image build budget exhausts Vercel timeout | mitigate | Per RESEARCH Pitfall 5, only 8 high-priority HK routes get per-route opengraph-image.tsx (this plan adds 1 default + Plans 03/04/05/06/07 add ~7 more). HK layout default catches the rest. Total OG jobs ≤ 8, well under the 60s Vercel build budget. |
| T-04-02-08 | Tampering | Baloo 2 font supply chain via `next/font/google` | accept | Phase 2 D-01 amendment ratified Baloo 2 as OFL/free-for-commercial via Google Fonts. `next/font/google` downloads at build time + self-hosts (no runtime third-party network call). Same provenance as Unbounded + Manrope. |
</threat_model>

<verification>
After all 3 tasks:

```bash
pnpm typecheck                                # Must exit 0
pnpm lint                                     # Must exit 0
pnpm build                                    # Must compile; warnings OK on routes that don't exist yet
pnpm test:unit --run                          # HK page tests still RED (expected — pages from Plans 03-07); middleware + root tests GREEN
pnpm dev                                      # Manual: hit http://hk.localhost:3000/ — should render Phase 1 stub page wrapped in real HKNav + HKFooter (since Plan 03 hasn't replaced page.tsx yet — VERIFIES layout works independently)
```

Manual smoke:
- Open `hk.localhost:3000/` in a browser. Sticky HKNav at top, HKFooter at bottom, Phase 1 stub content in the middle.
- Hover Gymnastics → dropdown opens with 8 entries. Tab through → keyboard navigates correctly (Radix default).
- Hover Locations → dropdown opens with 2 entries.
- Resize to <1024px → desktop nav hides, hamburger appears, tap opens right Sheet with sticky red CTA at top + nav links below.
- Sticky red `Book a Free Trial` button visible top-right on every scroll position.
- Footer has 4 columns on desktop (`Brand | Venues | Quick links | Connect`).
- WhatsApp link in footer is HIDDEN (env var unset locally — proves conditional render works).
- Default OG image renders: navigate to `hk.localhost:3000/opengraph-image` → returns 1200×630 PNG with HK branding.
</verification>

<success_criteria>
- [ ] `app/fonts.ts` exports `baloo` (Baloo 2, weight 600, variable `--font-accent`)
- [ ] `app/hk/layout.tsx` replaces Phase 1 stub; declares HK metadataBase, wires HKNav + HKFooter, applies `${baloo.variable}` className
- [ ] `components/hk/hk-nav.tsx` (RSC) renders sticky bar with logo, Gymnastics dropdown (8 items), Locations dropdown (2 items), 3 flat links (Camps/Coaches/FAQ), sticky red Book a Free Trial CTA, mobile handoff
- [ ] `components/hk/hk-nav-mobile.tsx` (`'use client'`) renders Sheet drawer with sticky red CTA at top + grouped nav (Gymnastics/Locations/Flat)
- [ ] `components/hk/hk-footer.tsx` (RSC) renders 4-col footer with WhatsApp env-conditional + cross-market `<a href={env}>` links + Simple Icons social SVGs
- [ ] `components/hk/venue-chip-row.tsx` + `components/hk/active-venue-chip.tsx` (RSC + client) render 2 venue chips with active-state highlighting
- [ ] `lib/og-image.tsx` exports `createHKOgImage` mirroring `createRootOgImage` shape with HK tagline
- [ ] `app/hk/opengraph-image.tsx` declares default HK OG (consumed by all HK routes lacking per-route override)
- [ ] No raw red on nav hovers / footer / FAQ chevrons (UI-SPEC §Color reserved list — only sticky CTA + booking submit + error text + logo mark)
- [ ] No cross-subdomain `<Link>` (Pitfall 7) — verified by grep
- [ ] `pnpm typecheck` + `pnpm lint` + `pnpm build` all clean
- [ ] All 12 HK requirements indirectly addressed (every HK page needs this chrome)
</success_criteria>

<output>
After completion, create `.planning/phases/04-hong-kong-market/04-02-SUMMARY.md` documenting:
- HK layout final structure + Baloo activation method (`<div className={baloo.variable}>` wrapper)
- HKNav file size + contained dropdowns + sticky CTA placement
- HKFooter 4-col attestation + WhatsApp/cross-market conditional render confirmations
- VenueChipRow + ActiveVenueChip RSC/client split
- Default OG image route + `createHKOgImage` extension
- Carry-forward notes for Plans 03-07: every HK page now inherits HKNav + HKFooter + Baloo + metadataBase from the layout — page files MUST NOT redeclare any of these.
</output>

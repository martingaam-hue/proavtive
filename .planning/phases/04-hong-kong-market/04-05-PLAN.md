---
phase: 04-hong-kong-market
plan: 05
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - components/hk/gymnastics-pillar-nav.tsx
  - components/hk/active-gym-nav-link.tsx
  - app/hk/gymnastics/page.tsx
  - app/hk/gymnastics/opengraph-image.tsx
  - app/hk/gymnastics/toddlers/page.tsx
  - app/hk/gymnastics/beginner/page.tsx
  - app/hk/gymnastics/intermediate/page.tsx
  - app/hk/gymnastics/advanced/page.tsx
  - app/hk/gymnastics/competitive/page.tsx
  - app/hk/gymnastics/rhythmic/page.tsx
  - app/hk/gymnastics/adult/page.tsx
  - app/hk/gymnastics/private/page.tsx
autonomous: true
requirements: [HK-04]

must_haves:
  truths:
    - "`hk.localhost:3000/gymnastics/` renders pillar overview page with hero, 8 programme tiles, GymPillarNav, age-pathway summary, shared FAQ, booking CTA"
    - "All 8 sub-pages render at routes /gymnastics/{toddlers,beginner,intermediate,advanced,competitive,rhythmic,adult,private}/"
    - "Each sub-page has unique <h1> drawn from HK_GYMNASTICS_PROGRAMMES[i].h1"
    - "Each sub-page has unique generateMetadata-style export with title from HK_GYMNASTICS_PROGRAMMES[i].metaTitle (e.g. Toddlers: 'Toddler Gymnastics Hong Kong (12mo–3yr) | ProGym Wan Chai & Cyberport')"
    - "Each sub-page renders <GymPillarNav /> with the active link visually distinguished (active-state via usePathname client child)"
    - "Each sub-page renders 'What children learn' bulleted list from HK_GYMNASTICS_PROGRAMMES[i].whatTheyLearn"
    - "Each sub-page renders 'Class structure' paragraph from HK_GYMNASTICS_PROGRAMMES[i].classStructure"
    - "Each sub-page renders 'Which venues' badges from HK_GYMNASTICS_PROGRAMMES[i].venuesOffered (rhythmic shows Wan Chai only)"
    - "Each sub-page renders Book CTA link to /book-a-trial/free-assessment/?venue=no-preference (or venue-aware when only one venue offers the programme)"
    - "Pillar page emits per-route OG via createHKOgImage; the 8 sub-pages inherit HK layout default OG (Pitfall 5 budget — only 1 OG file in this plan)"
  artifacts:
    - path: "components/hk/gymnastics-pillar-nav.tsx"
      provides: "RSC nav wrapper rendering 8 ActiveGymNavLink chips per HK_GYMNASTICS_PROGRAMMES"
      contains: "HK_GYMNASTICS_PROGRAMMES"
    - path: "components/hk/active-gym-nav-link.tsx"
      provides: "'use client' chip with active-state via usePathname (Pitfall 2 carry — pathname is browser URL, no /hk/ prefix)"
      contains: "usePathname"
    - path: "app/hk/gymnastics/page.tsx"
      provides: "Gymnastics pillar overview page (HK-04 entry point)"
      contains: "GymPillarNav"
      min_lines: 100
    - path: "app/hk/gymnastics/opengraph-image.tsx"
      provides: "Per-route OG for gymnastics pillar (UI-SPEC §7 — high-priority page; 8 sub-pages inherit layout default per Pitfall 5)"
      contains: "createHKOgImage"
    - path: "app/hk/gymnastics/toddlers/page.tsx through .../private/page.tsx (8 files)"
      provides: "Static gymnastics sub-pages — each unique H1 + metadata + content from HK_GYMNASTICS_PROGRAMMES"
      contains: "HK_GYMNASTICS_PROGRAMMES"
  key_links:
    - from: "All 8 sub-pages"
      to: "components/hk/gymnastics-pillar-nav.tsx"
      via: "<GymPillarNav /> JSX usage at top of page"
      pattern: "<GymPillarNav"
    - from: "components/hk/gymnastics-pillar-nav.tsx"
      to: "components/hk/active-gym-nav-link.tsx"
      via: "import + JSX usage"
      pattern: "<ActiveGymNavLink"
    - from: "All 9 pages (pillar + 8 subs)"
      to: "lib/hk-data.ts HK_GYMNASTICS_PROGRAMMES"
      via: "import + .find(p => p.slug === '...')"
      pattern: "HK_GYMNASTICS_PROGRAMMES"
    - from: "All sub-pages booking CTAs"
      to: "/book-a-trial/free-assessment/?venue=..."
      via: "<Link href=...>"
      pattern: "/book-a-trial/free-assessment/"
---

<objective>
Build the gymnastics pillar at `/gymnastics/` plus 8 static sub-pages — toddlers, beginner, intermediate, advanced, competitive, rhythmic, adult, private — sharing one template driven by `HK_GYMNASTICS_PROGRAMMES` data.

Purpose: Closes HK-04 + ROADMAP SC#3 (pillar + 8 sub-pages all navigable from pillar nav, distinct age-band content). The gymnastics pillar is the highest-traffic vertical in the HK tree (strategy PART 12 Tier 1 #1-#5 — the volume keyword cluster).

Output:
- 2 new components (`<GymPillarNav>` RSC wrapper + `<ActiveGymNavLink>` client child)
- 1 pillar page + 1 per-route OG image
- 8 sub-page route files generated from a single template
- Drives `app/hk/gymnastics/pillar.test.tsx` RED → GREEN
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-hong-kong-market/04-CONTEXT.md
@.planning/phases/04-hong-kong-market/04-RESEARCH.md
@.planning/phases/04-hong-kong-market/04-PATTERNS.md
@.planning/phases/04-hong-kong-market/04-UI-SPEC.md
@.planning/phases/04-hong-kong-market/04-01-SUMMARY.md
@.planning/phases/04-hong-kong-market/04-02-SUMMARY.md
@.planning/inputs/strategy.md

# Analog
@app/root/careers/page.tsx

# Phase 4 deps
@lib/hk-data.ts
@app/hk/gymnastics/pillar.test.tsx
@lib/og-image.tsx

<interfaces>
<!-- Contracts available to executor. -->

From `lib/hk-data.ts` (Plan 04-01):
```typescript
export interface HKGymnasticsProgramme {
  slug: "toddlers" | "beginner" | "intermediate" | "advanced" | "competitive" | "rhythmic" | "adult" | "private";
  href: `/gymnastics/${string}/`;
  label: string; ageBand: string;
  metaTitle: string; metaDescription: string;
  h1: string;
  whatTheyLearn: readonly string[];
  classStructure: string;
  venuesOffered: readonly ("wan-chai" | "cyberport")[];
}
export const HK_GYMNASTICS_PROGRAMMES: HKGymnasticsProgramme[]; // 8 entries
```

From RESEARCH §Pattern 6 (GymPillarNav + ActiveGymNavLink full reference impl with Pitfall 2 active-state guidance — pathname is browser URL without /hk/ prefix).

From `lib/og-image.tsx` (Plan 04-02):
```typescript
export async function createHKOgImage({ title, tagline }: OgImageOptions): Promise<ImageResponse>;
```

From `app/hk/gymnastics/pillar.test.tsx` (Plan 04-01):
```typescript
import { HK_GYMNASTICS_PROGRAMMES } from "@/lib/hk-data";
// it.each(HK_GYMNASTICS_PROGRAMMES) → for each, dynamic import + assert H1 contains programme.h1
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build `<GymPillarNav>` (RSC) + `<ActiveGymNavLink>` (client) — pillar sub-nav</name>
  <files>components/hk/gymnastics-pillar-nav.tsx, components/hk/active-gym-nav-link.tsx</files>
  <read_first>
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-RESEARCH.md §Pattern 6 (full GymPillarNav + ActiveGymNavLink reference impl, lines 502-557) + §Pitfall 2 (usePathname returns browser URL — no /hk/ prefix; comparison must NOT include /hk/)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-PATTERNS.md §components/hk/gymnastics-pillar-nav.tsx + §components/hk/active-gym-nav-link.tsx
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-UI-SPEC.md §5.5 (GymPillarNav spec — 8 chips flex-wrap, active=brand-navy/white, inactive=muted/foreground hover navy/10)
    - /Users/martin/Projects/proactive/lib/hk-data.ts (HK_GYMNASTICS_PROGRAMMES — drives the 8 chips)
    - /Users/martin/Projects/proactive/components/hk/active-venue-chip.tsx (Plan 04-02 — same pathname-matching pattern, copy structure for consistency)
    - /Users/martin/Projects/proactive/lib/utils.ts (provides cn())
  </read_first>
  <action>
    **Step A — `components/hk/active-gym-nav-link.tsx` (`'use client'`):**

    ```typescript
    "use client";
    // Phase 4 / Plan 04-05 — Active-state pillar nav chip (client component).
    // Per RESEARCH Pitfall 2 + UI-SPEC §5.5: usePathname returns browser URL pre-rewrite (no /hk/ prefix).
    // The href props in HK_GYMNASTICS_PROGRAMMES are also pre-rewrite (e.g. "/gymnastics/toddlers/") so direct
    // string comparison works.
    import Link from "next/link";
    import { usePathname } from "next/navigation";
    import { cn } from "@/lib/utils";

    export function ActiveGymNavLink({
      href,
      label,
      ageBand,
    }: {
      href: string;
      label: string;
      ageBand: string;
    }) {
      const pathname = usePathname();
      // Match exact path or path with optional trailing slash variation.
      const isActive = pathname === href || pathname === href.replace(/\/$/, "");

      return (
        <Link
          href={href}
          aria-current={isActive ? "page" : undefined}
          className={cn(
            "flex flex-col items-center px-4 py-2 rounded-lg text-small font-medium transition-colors min-h-12",
            isActive
              ? "bg-brand-navy text-white"
              : "bg-muted text-foreground hover:bg-brand-navy/10"
          )}
        >
          <span className="font-sans font-semibold">{label}</span>
          <span className="text-[11px] opacity-70 mt-0.5">{ageBand}</span>
        </Link>
      );
    }
    ```

    **Step B — `components/hk/gymnastics-pillar-nav.tsx` (RSC):**

    ```typescript
    // Phase 4 / Plan 04-05 — Gymnastics pillar sub-nav (RSC wrapper around 8 client chips).
    // Renders all 8 programmes as ActiveGymNavLink chips; active state derived per pathname client-side.
    // Used by the pillar page AND every sub-page (top of content) per UI-SPEC §4 + §5.5.
    import { HK_GYMNASTICS_PROGRAMMES } from "@/lib/hk-data";
    import { ActiveGymNavLink } from "@/components/hk/active-gym-nav-link";

    export function GymPillarNav() {
      return (
        <nav aria-label="Gymnastics programmes" className="flex flex-wrap gap-2 md:gap-3">
          {HK_GYMNASTICS_PROGRAMMES.map(p => (
            <ActiveGymNavLink key={p.slug} href={p.href} label={p.label} ageBand={p.ageBand} />
          ))}
        </nav>
      );
    }
    ```

    Per RESEARCH Pattern 6 (verbatim) + UI-SPEC §5.5 + Pitfall 2.
  </action>
  <verify>
    <automated>
      test -f /Users/martin/Projects/proactive/components/hk/gymnastics-pillar-nav.tsx
      test -f /Users/martin/Projects/proactive/components/hk/active-gym-nav-link.tsx
      grep -q "^\"use client\"" /Users/martin/Projects/proactive/components/hk/active-gym-nav-link.tsx
      grep -q "usePathname" /Users/martin/Projects/proactive/components/hk/active-gym-nav-link.tsx
      grep -q "use client" /Users/martin/Projects/proactive/components/hk/gymnastics-pillar-nav.tsx && exit 1 || true
      grep -q "HK_GYMNASTICS_PROGRAMMES" /Users/martin/Projects/proactive/components/hk/gymnastics-pillar-nav.tsx
      grep -q "ActiveGymNavLink" /Users/martin/Projects/proactive/components/hk/gymnastics-pillar-nav.tsx
      grep -q "/hk/" /Users/martin/Projects/proactive/components/hk/active-gym-nav-link.tsx && exit 1 || true   # Pitfall 2 — pathname comparison must NOT include /hk/ prefix
      grep -q "aria-current" /Users/martin/Projects/proactive/components/hk/active-gym-nav-link.tsx
      grep -q "bg-brand-navy" /Users/martin/Projects/proactive/components/hk/active-gym-nav-link.tsx
      cd /Users/martin/Projects/proactive && pnpm typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `components/hk/gymnastics-pillar-nav.tsx` exists; does NOT have `"use client"` (RSC)
    - `components/hk/active-gym-nav-link.tsx` first non-comment line is `"use client";`
    - ActiveGymNavLink imports `usePathname` from `next/navigation`
    - ActiveGymNavLink does NOT contain literal substring `/hk/` (Pitfall 2 — pathname is pre-rewrite browser URL)
    - ActiveGymNavLink active branch uses `bg-brand-navy text-white`; inactive uses `bg-muted text-foreground`
    - ActiveGymNavLink sets `aria-current="page"` when active
    - GymPillarNav has `aria-label="Gymnastics programmes"`
    - GymPillarNav iterates `HK_GYMNASTICS_PROGRAMMES.map`
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>GymPillarNav ready for embedding in 9 pages; active-state respects Next.js App Router pathname semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build pillar overview page `/gymnastics/` + per-route OG</name>
  <files>app/hk/gymnastics/page.tsx, app/hk/gymnastics/opengraph-image.tsx</files>
  <read_first>
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-UI-SPEC.md §4 `/gymnastics/` pillar row (sections: pillar intro, 8-tile nav to subs, age-pathway diagram text-only, shared FAQ, booking CTA) + §7 (per-route OG for gymnastics pillar — 1 of 8 high-priority pages)
    - /Users/martin/Projects/proactive/.planning/inputs/strategy.md §PART 4 §3-§4 (HK programme overview) + §PART 6B §4 (programmes copy verbatim) + §PART 12 Tier 1 #1-#5 (programme cluster)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-PATTERNS.md §app/hk/gymnastics/page.tsx
    - /Users/martin/Projects/proactive/lib/hk-data.ts (HK_GYMNASTICS_PROGRAMMES + HK_FAQ_ITEMS filter group "gymnastics")
    - /Users/martin/Projects/proactive/components/hk/gymnastics-pillar-nav.tsx (Task 1)
    - /Users/martin/Projects/proactive/app/hk/gymnastics/pillar.test.tsx (Plan 04-01 — Test 1 expects pillar page to render at least 8 links matching HK_GYMNASTICS_PROGRAMMES hrefs)
  </read_first>
  <behavior>
    `pillar.test.tsx` Test 1 (pillar page renders 8 links matching all programme hrefs) MUST be GREEN after this task.

    Pillar page sections:
    1. Hero: pillar H1 + summary + Book a Free Trial CTA
    2. GymPillarNav (8 chips — drives the test that 8 hrefs are reachable)
    3. Detailed programme cards (8 Cards, one per programme, with What They Learn bullets + Learn More link)
    4. Age-pathway summary (text-only ASCII-style "12mo → 3yr → 4-6 → 6-9 → 9-12 → 6+/Competitive" or simple visual)
    5. Shared FAQ (HK_FAQ_ITEMS where group === "gymnastics")
    6. Booking CTA section
  </behavior>
  <action>
    **Step A — `app/hk/gymnastics/page.tsx`:**

    ```typescript
    // Phase 4 / Plan 04-05 — Gymnastics pillar overview page (HK-04 entry).
    // Renders all 8 programmes as detailed cards + GymPillarNav for sub-page navigation.
    import type { Metadata } from "next";
    import Link from "next/link";
    import { ArrowRight } from "lucide-react";
    import { Section } from "@/components/ui/section";
    import { ContainerEditorial } from "@/components/ui/container-editorial";
    import { Button } from "@/components/ui/button";
    import { Card } from "@/components/ui/card";
    import { Badge } from "@/components/ui/badge";
    import { Accordion } from "@/components/ui/accordion";
    import { FAQItem } from "@/components/ui/faq-item";
    import { GymPillarNav } from "@/components/hk/gymnastics-pillar-nav";
    import { HK_GYMNASTICS_PROGRAMMES, HK_FAQ_ITEMS } from "@/lib/hk-data";

    const GYM_FAQS = HK_FAQ_ITEMS.filter(i => i.group === "gymnastics");

    export const metadata: Metadata = {
      title: "Children's Gymnastics Hong Kong — 8 Levels Toddler to Competitive | ProGym",
      description: "Children's gymnastics in Hong Kong — eight levels from Babies & Toddlers (12mo) through competitive squad. Wan Chai & Cyberport. Book a free trial.",
      openGraph: {
        title: "Children's Gymnastics Hong Kong — 8 Levels | ProGym",
        description: "Children's gymnastics in Hong Kong — eight levels from Babies & Toddlers through competitive squad.",
        url: "https://hk.proactivsports.com/gymnastics/",
        siteName: "ProActiv Sports Hong Kong",
        locale: "en_HK",
        type: "website",
        images: [{ url: "/gymnastics/opengraph-image", width: 1200, height: 630, alt: "ProGym children's gymnastics Hong Kong — 8 progression levels" }],
      },
      alternates: { canonical: "https://hk.proactivsports.com/gymnastics/" },
    };

    const pillarSchema = {
      "@context": "https://schema.org",
      "@graph": [
        {
          "@type": "FAQPage",
          mainEntity: GYM_FAQS.map(i => ({
            "@type": "Question",
            name: i.question,
            acceptedAnswer: { "@type": "Answer", text: i.answer },
          })),
        },
        {
          "@type": "BreadcrumbList",
          itemListElement: [
            { "@type": "ListItem", position: 1, name: "ProActiv Sports Hong Kong", item: "https://hk.proactivsports.com/" },
            { "@type": "ListItem", position: 2, name: "Gymnastics", item: "https://hk.proactivsports.com/gymnastics/" },
          ],
        },
      ],
    };

    export default function GymnasticsPillarPage() {
      return (
        <>
          <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(pillarSchema) }} />

          {/* §1 Pillar hero */}
          <Section size="lg" bg="default">
            <ContainerEditorial width="default">
              <div className="max-w-3xl">
                <h1 className="text-h1 font-display text-foreground">
                  Children's gymnastics in Hong Kong — eight levels, one progression pathway.
                </h1>
                <p className="text-body-lg text-muted-foreground mt-4">
                  From a child's first forward roll to a competitive routine on the beam, our eight gymnastics programmes meet every Hong Kong child at exactly their level. Wan Chai and Cyberport venues; coaches who progress with your child year on year.
                </p>
                <div className="mt-6">
                  <Button asChild size="touch" className="bg-brand-red text-white hover:bg-brand-red/90">
                    <Link href="/book-a-trial/free-assessment/?venue=no-preference">
                      Book a Free Trial <ArrowRight className="ml-2 size-4" aria-hidden />
                    </Link>
                  </Button>
                </div>
              </div>
            </ContainerEditorial>
          </Section>

          {/* §2 Pillar nav (8 chips — required by HK_GYMNASTICS_PROGRAMMES.length) */}
          <Section size="sm" bg="default">
            <ContainerEditorial width="wide">
              <GymPillarNav />
            </ContainerEditorial>
          </Section>

          {/* §3 Detailed programme cards (8 cards) */}
          <Section size="md" bg="muted">
            <ContainerEditorial width="wide">
              <h2 className="text-h2 font-display text-foreground mb-8">All eight programmes.</h2>
              <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
                {HK_GYMNASTICS_PROGRAMMES.map(p => (
                  <Card key={p.slug} className="p-5 flex flex-col">
                    <Badge variant="secondary" className="self-start">{p.ageBand}</Badge>
                    <h3 className="text-h3 font-display text-foreground mt-3">{p.label}</h3>
                    <ul className="text-body text-muted-foreground mt-3 space-y-1 flex-1">
                      {p.whatTheyLearn.slice(0, 3).map(b => (
                        <li key={b}>· {b}</li>
                      ))}
                    </ul>
                    <Link href={p.href} className="inline-flex items-center gap-1 mt-4 text-brand-navy font-semibold hover:underline">
                      Learn more <ArrowRight className="size-4" aria-hidden />
                    </Link>
                  </Card>
                ))}
              </div>
            </ContainerEditorial>
          </Section>

          {/* §4 Age pathway summary */}
          <Section size="md" bg="default">
            <ContainerEditorial width="default">
              <div className="max-w-3xl">
                <h2 className="text-h2 font-display text-foreground mb-6">Your child's progression pathway.</h2>
                <p className="text-body text-muted-foreground">
                  Every child progresses at their own pace — but the structure is consistent. Babies & Toddlers (12mo–3yr) introduces movement fundamentals with a parent. Beginner (4–6yr) and Intermediate (6–9yr) build the foundational gymnastics skills on bar, beam, floor and vault. Advanced (9–12yr) deepens technique. Competitive squad opens at 6+ for children pursuing the competitive route. Rhythmic and Adult programmes run alongside as parallel pathways.
                </p>
              </div>
            </ContainerEditorial>
          </Section>

          {/* §5 Shared FAQ */}
          {GYM_FAQS.length > 0 && (
            <Section size="md" bg="muted">
              <ContainerEditorial width="default">
                <div className="max-w-3xl mx-auto">
                  <h2 className="text-h2 font-display text-foreground mb-6">Gymnastics FAQs</h2>
                  <Accordion type="single" collapsible className="flex flex-col gap-0">
                    {GYM_FAQS.map(item => (
                      <FAQItem key={item.value} id={item.value} question={item.question} answer={item.answer} />
                    ))}
                  </Accordion>
                </div>
              </ContainerEditorial>
            </Section>
          )}

          {/* §6 Booking CTA */}
          <Section size="lg" bg="navy">
            <ContainerEditorial width="default">
              <div className="text-center max-w-2xl mx-auto">
                <h2 className="text-h2 font-display text-white mb-3">Find your child's level — book a free trial.</h2>
                <p className="text-body-lg text-cream mb-6">A 30-minute assessment with a coach. We'll suggest the right programme + venue based on age and current ability.</p>
                <Button asChild size="touch" className="bg-brand-red text-white hover:bg-brand-red/90">
                  <Link href="/book-a-trial/free-assessment/?venue=no-preference">
                    Book a Free Trial <ArrowRight className="ml-2 size-4" aria-hidden />
                  </Link>
                </Button>
              </div>
            </ContainerEditorial>
          </Section>
        </>
      );
    }
    ```

    **Step B — `app/hk/gymnastics/opengraph-image.tsx`:**

    ```typescript
    // Phase 4 / Plan 04-05 — Per-route OG for gymnastics pillar (UI-SPEC §7 high-priority page).
    // The 8 sub-pages inherit HK layout default OG (Pitfall 5 build budget).
    import { createHKOgImage } from "@/lib/og-image";

    export const runtime = "nodejs";
    export const dynamic = "force-dynamic";
    export const size = { width: 1200, height: 630 };
    export const contentType = "image/png";

    export default function OG() {
      return createHKOgImage({
        title: "Children's Gymnastics Hong Kong",
        tagline: "Eight programme levels — toddler to competitive. Wan Chai & Cyberport.",
      });
    }
    ```

    Per UI-SPEC §4 pillar row + §5.5 GymPillarNav usage + §7 per-route OG.
  </action>
  <verify>
    <automated>
      test -f /Users/martin/Projects/proactive/app/hk/gymnastics/page.tsx
      test -f /Users/martin/Projects/proactive/app/hk/gymnastics/opengraph-image.tsx
      grep -q "GymPillarNav" /Users/martin/Projects/proactive/app/hk/gymnastics/page.tsx
      grep -q "HK_GYMNASTICS_PROGRAMMES" /Users/martin/Projects/proactive/app/hk/gymnastics/page.tsx
      grep -q "/book-a-trial/free-assessment/" /Users/martin/Projects/proactive/app/hk/gymnastics/page.tsx
      grep -q "FAQPage" /Users/martin/Projects/proactive/app/hk/gymnastics/page.tsx
      grep -q "BreadcrumbList" /Users/martin/Projects/proactive/app/hk/gymnastics/page.tsx
      grep -q "canonical.*gymnastics" /Users/martin/Projects/proactive/app/hk/gymnastics/page.tsx
      grep -q "createHKOgImage" /Users/martin/Projects/proactive/app/hk/gymnastics/opengraph-image.tsx
      cd /Users/martin/Projects/proactive && pnpm typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `app/hk/gymnastics/page.tsx` exists; default export `GymnasticsPillarPage`
    - File renders `<GymPillarNav />` (verified by grep)
    - File iterates `HK_GYMNASTICS_PROGRAMMES.map` (renders 8 detailed cards) — verified by `grep -c "key={p.slug}"` returning ≥1
    - File contains href `/book-a-trial/free-assessment/?venue=no-preference` (pillar booking CTA — venue-agnostic)
    - File emits inline JSON-LD with `FAQPage` + `BreadcrumbList` types
    - `alternates.canonical` points to `https://hk.proactivsports.com/gymnastics/`
    - openGraph block is full (no shallow merge — Pitfall 2)
    - `app/hk/gymnastics/opengraph-image.tsx` exports default returning `createHKOgImage` with title `"Children's Gymnastics Hong Kong"`
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Pillar overview page complete. All 8 programmes are linked from pillar nav AND from detailed cards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generate 8 sub-pages from shared template (toddlers → private)</name>
  <files>app/hk/gymnastics/toddlers/page.tsx, app/hk/gymnastics/beginner/page.tsx, app/hk/gymnastics/intermediate/page.tsx, app/hk/gymnastics/advanced/page.tsx, app/hk/gymnastics/competitive/page.tsx, app/hk/gymnastics/rhythmic/page.tsx, app/hk/gymnastics/adult/page.tsx, app/hk/gymnastics/private/page.tsx</files>
  <read_first>
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-UI-SPEC.md §4 (gymnastics sub-page row — shared template structure: GymPillarNav + H1 + What They Learn + Class Structure + Which Venues + Booking CTA + FAQPage JSON-LD per sub-page)
    - /Users/martin/Projects/proactive/.planning/inputs/strategy.md §PART 12 Tier 1 #1-#5 (programme-by-programme content) + §PART 6B §4 (sub-programme copy where present)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-PATTERNS.md §app/hk/gymnastics/[toddlers|beginner|...]/page.tsx (analog: app/root/careers/page.tsx structure)
    - /Users/martin/Projects/proactive/app/root/careers/page.tsx (Phase 3 analog — Section + ContainerEditorial + bullet list + CTA pattern; copy structure)
    - /Users/martin/Projects/proactive/lib/hk-data.ts (HK_GYMNASTICS_PROGRAMMES drives metadata + content for ALL 8 sub-pages)
    - /Users/martin/Projects/proactive/components/hk/gymnastics-pillar-nav.tsx (Task 1)
    - /Users/martin/Projects/proactive/app/hk/gymnastics/pillar.test.tsx (Plan 04-01 — Test 2 + Test 3 expect each sub-page to render unique H1 + GymPillarNav + booking CTA)
  </read_first>
  <behavior>
    `pillar.test.tsx` Test 2 (each of 8 sub-pages H1 contains programme.h1) and Test 3 (each sub-page renders GymPillarNav + booking CTA) MUST be GREEN.

    Each sub-page is a single file under 80 lines that follows EXACT template:

    ```typescript
    // Phase 4 / Plan 04-05 — Gymnastics sub-page: {LABEL} (HK-04).
    // Single template; data from HK_GYMNASTICS_PROGRAMMES[i] where slug === "{SLUG}".
    import type { Metadata } from "next";
    import Link from "next/link";
    import { ArrowRight } from "lucide-react";
    import { Section } from "@/components/ui/section";
    import { ContainerEditorial } from "@/components/ui/container-editorial";
    import { Button } from "@/components/ui/button";
    import { Badge } from "@/components/ui/badge";
    import { GymPillarNav } from "@/components/hk/gymnastics-pillar-nav";
    import { HK_GYMNASTICS_PROGRAMMES, HK_VENUES } from "@/lib/hk-data";

    const PROGRAMME = HK_GYMNASTICS_PROGRAMMES.find(p => p.slug === "{SLUG}")!;

    export const metadata: Metadata = {
      title: PROGRAMME.metaTitle,
      description: PROGRAMME.metaDescription,
      openGraph: {
        title: PROGRAMME.metaTitle,
        description: PROGRAMME.metaDescription,
        url: `https://hk.proactivsports.com${PROGRAMME.href}`,
        siteName: "ProActiv Sports Hong Kong",
        locale: "en_HK",
        type: "website",
      },
      alternates: { canonical: `https://hk.proactivsports.com${PROGRAMME.href}` },
    };

    const subPageSchema = {
      "@context": "https://schema.org",
      "@graph": [
        {
          "@type": "BreadcrumbList",
          itemListElement: [
            { "@type": "ListItem", position: 1, name: "ProActiv Sports Hong Kong", item: "https://hk.proactivsports.com/" },
            { "@type": "ListItem", position: 2, name: "Gymnastics", item: "https://hk.proactivsports.com/gymnastics/" },
            { "@type": "ListItem", position: 3, name: PROGRAMME.label, item: `https://hk.proactivsports.com${PROGRAMME.href}` },
          ],
        },
      ],
    };

    export default function {PASCAL}GymPage() {
      const venuesShown = HK_VENUES.filter(v => PROGRAMME.venuesOffered.includes(v.id));
      // Booking CTA pre-fill: if the programme is offered at exactly one venue, pre-fill that venue;
      // otherwise leave as "no-preference" so the booking form lets the user choose.
      const bookingVenue = PROGRAMME.venuesOffered.length === 1 ? PROGRAMME.venuesOffered[0] : "no-preference";

      return (
        <>
          <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(subPageSchema) }} />

          {/* §1 Pillar nav */}
          <Section size="sm" bg="default">
            <ContainerEditorial width="wide">
              <GymPillarNav />
            </ContainerEditorial>
          </Section>

          {/* §2 Hero */}
          <Section size="md" bg="default">
            <ContainerEditorial width="default">
              <div className="max-w-3xl">
                <Badge variant="secondary">{PROGRAMME.ageBand}</Badge>
                <h1 className="text-h1 font-display text-foreground mt-3">{PROGRAMME.h1}</h1>
              </div>
            </ContainerEditorial>
          </Section>

          {/* §3 What they learn */}
          <Section size="md" bg="muted">
            <ContainerEditorial width="default">
              <div className="max-w-3xl">
                <h2 className="text-h2 font-display text-foreground mb-6">What children learn</h2>
                <ul className="space-y-3">
                  {PROGRAMME.whatTheyLearn.map((b) => (
                    <li key={b} className="text-body text-foreground flex gap-3">
                      <span aria-hidden className="text-brand-red">·</span>
                      <span>{b}</span>
                    </li>
                  ))}
                </ul>
              </div>
            </ContainerEditorial>
          </Section>

          {/* §4 Class structure */}
          <Section size="md" bg="default">
            <ContainerEditorial width="default">
              <div className="max-w-3xl">
                <h2 className="text-h2 font-display text-foreground mb-4">Class structure</h2>
                <p className="text-body text-muted-foreground">{PROGRAMME.classStructure}</p>
              </div>
            </ContainerEditorial>
          </Section>

          {/* §5 Which venues */}
          <Section size="sm" bg="muted">
            <ContainerEditorial width="default">
              <div className="max-w-3xl">
                <h2 className="text-h3 font-display text-foreground mb-4">Available at</h2>
                <div className="flex flex-wrap gap-3">
                  {venuesShown.map(v => (
                    <Link key={v.id} href={`/${v.id}/`} className="px-4 py-2 rounded-lg bg-background border border-brand-navy/20 hover:border-brand-navy transition-colors">
                      <span className="font-accent text-brand-navy">ProGym</span> <span className="font-semibold text-foreground">{v.nameShort}</span>
                    </Link>
                  ))}
                </div>
              </div>
            </ContainerEditorial>
          </Section>

          {/* §6 Booking CTA */}
          <Section size="lg" bg="navy">
            <ContainerEditorial width="default">
              <div className="text-center max-w-2xl mx-auto">
                <h2 className="text-h2 font-display text-white mb-3">Try a {PROGRAMME.label} class — free.</h2>
                <p className="text-body-lg text-cream mb-6">A 30-minute trial with a coach who teaches this programme every week. No commitment.</p>
                <Button asChild size="touch" className="bg-brand-red text-white hover:bg-brand-red/90">
                  <Link href={`/book-a-trial/free-assessment/?venue=${bookingVenue}`}>
                    Book a Free Trial <ArrowRight className="ml-2 size-4" aria-hidden />
                  </Link>
                </Button>
              </div>
            </ContainerEditorial>
          </Section>
        </>
      );
    }
    ```

    Generate 8 files by substituting `{SLUG}` and `{PASCAL}` per the matrix:

    | File path | {SLUG} | {PASCAL} |
    |-----------|--------|----------|
    | `app/hk/gymnastics/toddlers/page.tsx` | toddlers | Toddlers |
    | `app/hk/gymnastics/beginner/page.tsx` | beginner | Beginner |
    | `app/hk/gymnastics/intermediate/page.tsx` | intermediate | Intermediate |
    | `app/hk/gymnastics/advanced/page.tsx` | advanced | Advanced |
    | `app/hk/gymnastics/competitive/page.tsx` | competitive | Competitive |
    | `app/hk/gymnastics/rhythmic/page.tsx` | rhythmic | Rhythmic |
    | `app/hk/gymnastics/adult/page.tsx` | adult | Adult |
    | `app/hk/gymnastics/private/page.tsx` | private | Private |

    Per UI-SPEC §4 sub-page template + §5.5 GymPillarNav usage. Per CONTEXT D-03 (8 sub-programmes navigable from pillar) + Claude's Discretion (single shared template).
  </action>
  <action>
    Generate the 8 files exactly as the template above prescribes. For each file:

    1. Substitute `{SLUG}` with the programme slug (lowercase, kebab if multi-word — none of the 8 are multi-word).
    2. Substitute `{PASCAL}` with the PascalCase variant for the function name.
    3. Substitute `{LABEL}` in the header comment with the human label (Toddlers, Beginner, etc.).
    4. Do NOT inline programme content — pull from `HK_GYMNASTICS_PROGRAMMES` so a CMS migration in Phase 6 only requires swapping the array source.

    All 8 files compile cleanly because the data exists in `lib/hk-data.ts` (Plan 04-01 acceptance criteria: 8 entries with all required fields).

    After generation, verify the test file passes by running:
    ```bash
    pnpm test:unit --run app/hk/gymnastics/pillar.test.tsx
    ```
  </action>
  <verify>
    <automated>
      for slug in toddlers beginner intermediate advanced competitive rhythmic adult private; do
        test -f "/Users/martin/Projects/proactive/app/hk/gymnastics/$slug/page.tsx" || (echo "MISSING $slug" && exit 1)
        grep -q "GymPillarNav" "/Users/martin/Projects/proactive/app/hk/gymnastics/$slug/page.tsx" || (echo "$slug missing GymPillarNav" && exit 1)
        grep -q "HK_GYMNASTICS_PROGRAMMES" "/Users/martin/Projects/proactive/app/hk/gymnastics/$slug/page.tsx" || (echo "$slug missing data import" && exit 1)
        grep -q "/book-a-trial/free-assessment/" "/Users/martin/Projects/proactive/app/hk/gymnastics/$slug/page.tsx" || (echo "$slug missing booking CTA" && exit 1)
        grep -q "BreadcrumbList" "/Users/martin/Projects/proactive/app/hk/gymnastics/$slug/page.tsx" || (echo "$slug missing JSON-LD" && exit 1)
        grep -q "canonical.*gymnastics/$slug" "/Users/martin/Projects/proactive/app/hk/gymnastics/$slug/page.tsx" || (echo "$slug missing canonical" && exit 1)
        grep -q "slug === \"$slug\"" "/Users/martin/Projects/proactive/app/hk/gymnastics/$slug/page.tsx" || (echo "$slug data lookup wrong" && exit 1)
      done
      echo "OK — all 8 sub-pages present and structurally valid"
      cd /Users/martin/Projects/proactive && pnpm typecheck
      cd /Users/martin/Projects/proactive && pnpm test:unit --run app/hk/gymnastics/pillar.test.tsx 2>&1 | tail -25
    </automated>
  </verify>
  <acceptance_criteria>
    - All 8 sub-page files exist at correct paths
    - Each file imports `GymPillarNav` from `@/components/hk/gymnastics-pillar-nav`
    - Each file imports `HK_GYMNASTICS_PROGRAMMES` from `@/lib/hk-data` and looks up its programme via `.find(p => p.slug === "{SLUG}")` with the correct slug
    - Each file's JSX renders `<GymPillarNav />` exactly once (top of content)
    - Each file's metadata `title` resolves to `PROGRAMME.metaTitle` (data-driven; not hardcoded)
    - Each file's `alternates.canonical` matches its route URL
    - Each file emits inline JSON-LD with `BreadcrumbList`
    - Each file contains a booking CTA Link starting with `/book-a-trial/free-assessment/?venue=`
    - For `rhythmic` (only Wan Chai per UI-SPEC §3.4): the booking CTA pre-fills `?venue=wan-chai` (because `venuesOffered.length === 1`)
    - `pnpm typecheck` exits 0
    - `pnpm test:unit --run app/hk/gymnastics/pillar.test.tsx` reports all tests GREEN (Test 1: pillar 8 links, Test 2: each sub-page H1 unique, Test 3: each sub-page GymPillarNav + booking CTA)
  </acceptance_criteria>
  <done>HK-04 satisfied. Pillar + 8 sub-pages all navigable; pillar test fully GREEN. ROADMAP SC#3 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → 9 gymnastics routes | Public read-only pages; no input handling on this surface |
| usePathname → ActiveGymNavLink | Client-side state derived from browser URL; no remote source |
| HK_GYMNASTICS_PROGRAMMES → 9 pages | Build-time TS constant; no user-mutation path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | JSON-LD `dangerouslySetInnerHTML` injection | mitigate | All 9 schema objects derive from build-time TypeScript constants in `HK_GYMNASTICS_PROGRAMMES`. Path values are template literals constrained by the `HKGymnasticsProgramme.href` type (`/gymnastics/${string}/`). No user input flows in. |
| T-04-05-02 | Spoofing | Active-state pathname mismatch reveals incorrect link | accept | Pitfall 2 documented; comparison uses the browser-visible URL. Mismatch results only in cosmetic active-state failure (no security impact). |
| T-04-05-03 | Information Disclosure | BreadcrumbList JSON-LD reveals route structure | accept | Route structure is intentionally public (sitemap exposed in Phase 7). No information disclosure concern. |
| T-04-05-04 | Tampering | URL query param `?venue=...` parameter pollution | mitigate | Sub-page CTAs only emit `?venue=wan-chai`, `?venue=cyberport`, or `?venue=no-preference` — values are derived from constrained types in `HKGymnasticsProgramme.venuesOffered`. The booking form (Plan 07) validates server-side that `venue ∈ ["wan-chai", "cyberport", "no-preference"]` (RESEARCH §Security Domain). |
</threat_model>

<verification>
After all 3 tasks:

```bash
pnpm typecheck                                                        # Must exit 0
pnpm lint                                                             # Must exit 0
pnpm test:unit --run app/hk/gymnastics/pillar.test.tsx               # All tests GREEN
pnpm build                                                            # 9 routes generate
pnpm dev                                                              # Manual at hk.localhost:3000/gymnastics/ + 8 sub-routes
```

Manual smoke (per UI-SPEC asymmetric layout requirement):
- `/gymnastics/` shows hero + 8-chip pillar nav + 8 detailed cards + age pathway + FAQ + booking CTA
- `/gymnastics/toddlers/` shows GymPillarNav with `Babies & Toddlers` chip highlighted (active aria-current="page")
- Tab through pillar nav → keyboard navigates correctly
- Each of 8 sub-pages: unique H1, unique metadata title (open browser dev tools → check `<title>`)
- `/gymnastics/rhythmic/` "Available at" shows ONLY ProGym Wan Chai (rhythmic.venuesOffered === ["wan-chai"])
- Booking CTA on `/gymnastics/rhythmic/` navigates to `/book-a-trial/free-assessment/?venue=wan-chai` (single-venue programmes pre-fill)
</verification>

<success_criteria>
- [ ] `<GymPillarNav>` (RSC) + `<ActiveGymNavLink>` (client) wired with active-state via usePathname (no /hk/ prefix per Pitfall 2)
- [ ] `app/hk/gymnastics/page.tsx` renders pillar overview with 8 detailed programme cards + GymPillarNav + FAQ + booking CTA
- [ ] `app/hk/gymnastics/opengraph-image.tsx` per-route OG (1 of 8 high-priority OG slots per Pitfall 5)
- [ ] All 8 sub-pages exist at `/gymnastics/{slug}/page.tsx`
- [ ] Each sub-page renders unique H1 from HK_GYMNASTICS_PROGRAMMES, unique metadata, unique canonical URL
- [ ] Each sub-page renders GymPillarNav + What They Learn + Class Structure + Which Venues + Booking CTA
- [ ] Booking CTA pre-fills venue when programme offered at exactly one venue (rhythmic → wan-chai)
- [ ] All BreadcrumbList JSON-LD entries map to correct route paths
- [ ] `app/hk/gymnastics/pillar.test.tsx` fully GREEN
- [ ] `pnpm typecheck`, `pnpm lint`, `pnpm build` clean
- [ ] HK-04 + ROADMAP SC#3 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-hong-kong-market/04-05-SUMMARY.md` documenting:
- GymPillarNav RSC/client split + Pitfall 2 active-state attestation
- 9 gymnastics routes shipped (1 pillar + 8 subs)
- HK-04 + ROADMAP SC#3 closure attestation
- Per-route OG count: 1 (pillar only — 8 sub-pages inherit HK layout default per Pitfall 5)
- Phase 6 migration note: switching from HK_GYMNASTICS_PROGRAMMES (TS array) to a Sanity GROQ query touches `lib/hk-data.ts` only; the 9 page files don't change.
</output>

---
phase: 04-hong-kong-market
plan: 04
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - components/hk/venue-map.tsx
  - app/hk/wan-chai/page.tsx
  - app/hk/wan-chai/opengraph-image.tsx
  - app/hk/cyberport/page.tsx
  - app/hk/cyberport/opengraph-image.tsx
autonomous: true
requirements: [HK-02, HK-03]

must_haves:
  truths:
    - "`hk.localhost:3000/wan-chai/` renders verbatim NAP `15/F, The Hennessy, 256 Hennessy Road` in visible DOM"
    - "`hk.localhost:3000/cyberport/` renders verbatim marker `5,000 sq ft` AND `Opened August 2025` in visible DOM"
    - "Both pages render `<VenueMap>` component — when env var unset shows accessible placeholder div, when set renders lazy `<iframe loading=\"lazy\" title>` with Google Maps embed src"
    - "Both pages emit inline `<script type=\"application/ld+json\">` with `SportsActivityLocation` + `BreadcrumbList` @graph entries (RESEARCH Pattern 7)"
    - "Both pages render visible opening-hours table (Mon-Fri + Sat-Sun rows) reading from `HK_VENUES[i].hours`"
    - "Both pages render programme list filtered by `HK_GYMNASTICS_PROGRAMMES[i].venuesOffered.includes(venueId)` (per UI-SPEC §4 Wan Chai/Cyberport rows)"
    - "Both pages render service-area copy (Wan Chai: `Wan Chai · Causeway Bay · Central · Mid-Levels`; Cyberport: `Pokfulam · Aberdeen · Southern District`)"
    - "Both pages render booking CTA Link with venue pre-fill query param: `/book-a-trial/free-assessment/?venue=wan-chai` and `?venue=cyberport`"
    - "Both pages have unique `<h1>` containing the venue's `nameFull`; both have full openGraph metadata block (no shallow merge per Pitfall 2 carry)"
    - "Both pages render exactly ONE `<Image>` with `priority` attribute (the venue hero photo)"
  artifacts:
    - path: "components/hk/venue-map.tsx"
      provides: "RSC iframe-based map embed with placeholder fallback (RESEARCH Pattern 3)"
      contains: "loading=\"lazy\""
    - path: "app/hk/wan-chai/page.tsx"
      provides: "ProGym Wan Chai location page satisfying HK-02 + ROADMAP SC#2"
      contains: "15/F, The Hennessy, 256 Hennessy Road"
      min_lines: 120
    - path: "app/hk/cyberport/page.tsx"
      provides: "ProGym Cyberport location page satisfying HK-03 + ROADMAP SC#2"
      contains: "5,000 sq ft"
      min_lines: 120
    - path: "app/hk/wan-chai/opengraph-image.tsx"
      provides: "Per-route OG for Wan Chai (UI-SPEC §7 — high-priority page)"
      contains: "createHKOgImage"
    - path: "app/hk/cyberport/opengraph-image.tsx"
      provides: "Per-route OG for Cyberport (UI-SPEC §7 — high-priority page)"
      contains: "createHKOgImage"
  key_links:
    - from: "app/hk/wan-chai/page.tsx + app/hk/cyberport/page.tsx"
      to: "components/hk/venue-map.tsx"
      via: "import + JSX usage with embedSrc + title props"
      pattern: "<VenueMap"
    - from: "components/hk/venue-map.tsx"
      to: "process.env.NEXT_PUBLIC_WAN_CHAI_MAP_EMBED + NEXT_PUBLIC_CYBERPORT_MAP_EMBED (via consumers)"
      via: "embedSrc prop passed from page consumers"
      pattern: "embedSrc"
    - from: "Both location pages JSON-LD"
      to: "schema.org SportsActivityLocation"
      via: "inline <script> with @graph"
      pattern: "SportsActivityLocation"
    - from: "Both location pages booking CTA"
      to: "/book-a-trial/free-assessment/?venue=..."
      via: "<Link href=...>"
      pattern: "/book-a-trial/free-assessment/\\?venue="
---

<objective>
Build both HK location pages — `/wan-chai/` and `/cyberport/` — and the shared `<VenueMap>` iframe component. Each page renders venue-specific NAP, lazy-loaded map embed, opening hours, programme list, service area, venue-prefilled booking CTA, and inline `SportsActivityLocation` + `BreadcrumbList` JSON-LD.

Purpose: Closes ROADMAP SC#2 (correct NAP + map + hours + programme list + venue-specific booking CTA on both location pages) and HK-02 + HK-03. These pages anchor local SEO (Wan Chai + Cyberport area searches) and venue-specific conversion.

Output:
- 1 new shared component (`components/hk/venue-map.tsx`)
- 2 new location page routes with full metadata, copy, JSON-LD, and per-route OG image
- Drives `app/hk/wan-chai/page.test.tsx` + `app/hk/cyberport/page.test.tsx` RED → GREEN
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-hong-kong-market/04-CONTEXT.md
@.planning/phases/04-hong-kong-market/04-RESEARCH.md
@.planning/phases/04-hong-kong-market/04-PATTERNS.md
@.planning/phases/04-hong-kong-market/04-UI-SPEC.md
@.planning/phases/04-hong-kong-market/04-01-SUMMARY.md
@.planning/phases/04-hong-kong-market/04-02-SUMMARY.md
@.planning/inputs/strategy.md

# Analog
@app/root/brand/page.tsx
@app/root/brand/opengraph-image.tsx

# Phase 4 deps
@lib/hk-data.ts
@components/hk/venue-chip-row.tsx
@app/hk/wan-chai/page.test.tsx
@app/hk/cyberport/page.test.tsx
@lib/og-image.tsx

<interfaces>
<!-- Contracts available to executor. Extracted from upstream files. -->

From `lib/hk-data.ts` (Plan 04-01):
```typescript
export interface HKVenue {
  id: "wan-chai" | "cyberport";
  nameShort: string; nameFull: string;
  addressStreet: string; addressLocality: string; addressRegion: string; addressCountry: "HK";
  geo: { lat: number; lng: number };
  hours: HKVenueHours[];
  phoneEnvVar: string; mapEmbedEnvVar: string;
  heroImage: string;
  serviceArea: readonly string[];
  apparatus: readonly string[];
  openedNote?: string; sizeNote?: string;
}
export const HK_VENUES: HKVenue[]; // [wan-chai, cyberport]
export const HK_GYMNASTICS_PROGRAMMES: HKGymnasticsProgramme[]; // each has venuesOffered: ("wan-chai"|"cyberport")[]
export const WAN_CHAI_MAP_EMBED: string;   // env-derived; "PLACEHOLDER_WAN_CHAI_EMBED" when env unset
export const CYBERPORT_MAP_EMBED: string;  // env-derived; "PLACEHOLDER_CYBERPORT_EMBED" when env unset
```

From RESEARCH §Pattern 3 (VenueMap reference impl):
```typescript
interface VenueMapProps { embedSrc: string; title: string; className?: string; }
```

From `lib/og-image.tsx` (extended Plan 04-02):
```typescript
export async function createHKOgImage({ title, tagline }: OgImageOptions): Promise<ImageResponse>;
```

From `app/root/brand/opengraph-image.tsx` (5-line consumer pattern):
```typescript
export const runtime = "nodejs";
export const dynamic = "force-dynamic";
export const size = { width: 1200, height: 630 };
export const contentType = "image/png";
export default function OG() { return createRootOgImage({ title: "...", tagline: "..." }); }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build `<VenueMap>` shared RSC component (RESEARCH Pattern 3)</name>
  <files>components/hk/venue-map.tsx</files>
  <read_first>
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-RESEARCH.md §Pattern 3 (full VenueMap reference implementation lines 357-393) + §Pitfall 3 (Permissions-Policy iframe blocking — explicit `allow=""` not needed; verify on Chrome + Safari)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-PATTERNS.md §components/hk/venue-map.tsx (no codebase analog — first iframe component; pattern is RESEARCH-only)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-UI-SPEC.md §5.4 (VenueMap spec: RSC, lazy iframe, title attribute required, fallback when embedSrc empty/PLACEHOLDER)
    - /Users/martin/Projects/proactive/lib/utils.ts (provides `cn()` — same path used by all UI primitives)
  </read_first>
  <action>
    Create `components/hk/venue-map.tsx` (RSC — NO `'use client'` directive, no client state, no event handlers):

    ```typescript
    // Phase 4 / Plan 04-04 — Lightweight venue map component (RSC).
    // Per UI-SPEC §5.4 + RESEARCH Pattern 3: iframe embed, NOT Google Maps JS SDK.
    // Zero JavaScript bundle cost. Lazy-loads via loading="lazy" — no impact on LCP.
    // Title attribute required (a11y / Lighthouse Best Practices).
    // HUMAN-ACTION: when embedSrc is empty/PLACEHOLDER, falls back to address-text card; consumer-page renders address text below.
    import { cn } from "@/lib/utils";

    export interface VenueMapProps {
      /** Full Google Maps embed URL from Share → Embed a map. Empty or starts with "PLACEHOLDER" → fallback rendered. */
      embedSrc: string;
      /** Iframe title for accessibility — must describe the venue (e.g. "Map of ProGym Wan Chai"). Required. */
      title: string;
      /** Optional className appended to root element. */
      className?: string;
    }

    export function VenueMap({ embedSrc, title, className }: VenueMapProps) {
      const isPlaceholder = !embedSrc || embedSrc.includes("PLACEHOLDER");

      if (isPlaceholder) {
        return (
          <div
            className={cn(
              "rounded-lg bg-muted h-64 flex items-center justify-center border border-border",
              className
            )}
            role="status"
            aria-live="polite"
            aria-label={`${title} — map unavailable, see address below`}
          >
            <p className="text-muted-foreground text-small text-center px-4">
              Map loading — see venue address below.
            </p>
          </div>
        );
      }

      return (
        <iframe
          src={embedSrc}
          title={title}
          width="100%"
          height="300"
          loading="lazy"
          referrerPolicy="no-referrer-when-downgrade"
          className={cn("rounded-lg border-0 w-full", className)}
          aria-label={title}
        />
      );
    }
    ```

    Per UI-SPEC §5.4. Per RESEARCH Pitfall 3 (Permissions-Policy verification deferred to manual smoke).
  </action>
  <verify>
    <automated>
      test -f /Users/martin/Projects/proactive/components/hk/venue-map.tsx
      grep -q "loading=\"lazy\"" /Users/martin/Projects/proactive/components/hk/venue-map.tsx
      grep -q "title={title}" /Users/martin/Projects/proactive/components/hk/venue-map.tsx
      grep -q "PLACEHOLDER" /Users/martin/Projects/proactive/components/hk/venue-map.tsx
      grep -q "referrerPolicy" /Users/martin/Projects/proactive/components/hk/venue-map.tsx
      grep -q "use client" /Users/martin/Projects/proactive/components/hk/venue-map.tsx && exit 1 || true   # MUST NOT have 'use client'
      cd /Users/martin/Projects/proactive && pnpm typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `components/hk/venue-map.tsx` exists
    - File does NOT contain `"use client"` directive (RSC)
    - File exports `VenueMap` named function AND `VenueMapProps` interface
    - File contains literal `loading="lazy"` (Pitfall 3 carry)
    - File contains literal `title={title}` AND `aria-label={title}` (a11y)
    - File contains substring `PLACEHOLDER` (placeholder branch trigger)
    - File contains literal `referrerPolicy="no-referrer-when-downgrade"`
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>VenueMap RSC ready for both location pages. Placeholder branch covers HUMAN-ACTION period before Martin generates Google Maps embed URLs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build `/wan-chai/` page + per-route OG (HK-02)</name>
  <files>app/hk/wan-chai/page.tsx, app/hk/wan-chai/opengraph-image.tsx</files>
  <read_first>
    - /Users/martin/Projects/proactive/app/root/brand/page.tsx (analog — copy structure: static metadata + JSON-LD + hero + content sections + CTA)
    - /Users/martin/Projects/proactive/app/root/brand/opengraph-image.tsx (analog — 5-line consumer pattern)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-UI-SPEC.md §4 `/wan-chai/` row + §8.2 LocalBusiness JSON-LD
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-RESEARCH.md §Pattern 7 (full SportsActivityLocation + BreadcrumbList JSON-LD shape, lines 565-613) + §Code Examples / Location Page Structure
    - /Users/martin/Projects/proactive/.planning/inputs/strategy.md §PART 4 (HK wireframe — Wan Chai context) + §PART 6B (verbatim copy where Wan Chai is referenced) + §PART 8.3 (NAP)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-PATTERNS.md §app/hk/wan-chai/page.tsx
    - /Users/martin/Projects/proactive/lib/hk-data.ts (HK_VENUES[0] is Wan Chai; HK_GYMNASTICS_PROGRAMMES filter)
    - /Users/martin/Projects/proactive/components/hk/venue-map.tsx (Task 1)
    - /Users/martin/Projects/proactive/app/hk/wan-chai/page.test.tsx (Plan 04-01 — test expectations to satisfy)
  </read_first>
  <behavior>
    `app/hk/wan-chai/page.test.tsx` Test 1-5 must turn GREEN:
    - Test 1: visible "15/F, The Hennessy, 256 Hennessy Road"
    - Test 2: `<iframe>` with `title` attribute (or VenueMap mock — Plan 04-01 mocks VenueMap as `<iframe title={title} />`)
    - Test 3: opening-hours text rendered (`09:00` appears at least twice)
    - Test 4: `<Link>` matching `/book-a-trial/free-assessment/?venue=wan-chai`
    - Test 5: `<script type="application/ld+json">` containing `SportsActivityLocation`

    Page sections (per UI-SPEC §4 Wan Chai row):
    1. Hero: venue name + address + hero photo (no video on location pages — UI-SPEC explicit)
    2. VenueMap iframe lazy
    3. Opening hours table
    4. Programmes available at Wan Chai (filter HK_GYMNASTICS_PROGRAMMES)
    5. Service area copy (Wan Chai · Causeway Bay · Central · Mid-Levels)
    6. Venue FAQ (use HK_FAQ_ITEMS filter group === "venues")
    7. Booking CTA pre-filled with `?venue=wan-chai`
  </behavior>
  <action>
    **Step A — `app/hk/wan-chai/page.tsx`:**

    ```typescript
    // Phase 4 / Plan 04-04 — ProGym Wan Chai location page (HK-02).
    // NAP + map + opening hours + programme list + venue-specific booking CTA + LocalBusiness JSON-LD.
    // Composes Phase 2 primitives + components/hk/. No video on location pages (per UI-SPEC §4).
    import type { Metadata } from "next";
    import Image from "next/image";
    import Link from "next/link";
    import { MapPin, Phone, Clock, ArrowRight } from "lucide-react";
    import { Section } from "@/components/ui/section";
    import { ContainerEditorial } from "@/components/ui/container-editorial";
    import { Button } from "@/components/ui/button";
    import { Card } from "@/components/ui/card";
    import { Badge } from "@/components/ui/badge";
    import { Accordion } from "@/components/ui/accordion";
    import { FAQItem } from "@/components/ui/faq-item";
    import { VenueMap } from "@/components/hk/venue-map";
    import { HK_VENUES, HK_GYMNASTICS_PROGRAMMES, HK_FAQ_ITEMS, WAN_CHAI_MAP_EMBED } from "@/lib/hk-data";

    const VENUE = HK_VENUES.find(v => v.id === "wan-chai")!; // build-time constant; fail loud if absent
    const VENUE_PROGRAMMES = HK_GYMNASTICS_PROGRAMMES.filter(p => p.venuesOffered.includes("wan-chai"));
    const VENUE_FAQS = HK_FAQ_ITEMS.filter(i => i.group === "venues").slice(0, 6);

    export const metadata: Metadata = {
      title: "ProGym Wan Chai — Children's Gymnastics Wan Chai, Hong Kong",
      description: "ProGym Wan Chai — 15/F, The Hennessy, 256 Hennessy Road. Book gymnastics classes, holiday camps and birthday parties for children in Wan Chai, Hong Kong.",
      openGraph: {
        title: "ProGym Wan Chai — Children's Gymnastics, Hong Kong",
        description: "ProGym Wan Chai — 15/F, The Hennessy, 256 Hennessy Road. Children's gymnastics, holiday camps, birthday parties.",
        url: "https://hk.proactivsports.com/wan-chai/",
        siteName: "ProActiv Sports Hong Kong",
        locale: "en_HK",
        type: "website",
        images: [{ url: "/wan-chai/opengraph-image", width: 1200, height: 630, alt: "ProGym Wan Chai — children's gymnastics venue, Hong Kong" }],
      },
      alternates: { canonical: "https://hk.proactivsports.com/wan-chai/" },
    };

    const wanChaiSchema = {
      "@context": "https://schema.org",
      "@graph": [
        {
          "@type": "SportsActivityLocation",
          "@id": "https://hk.proactivsports.com/#localbusiness-wanchai",
          "name": "ProGym Wan Chai — ProActiv Sports",
          "image": `https://hk.proactivsports.com${VENUE.heroImage}`,
          "url": "https://hk.proactivsports.com/wan-chai/",
          "telephone": process.env.NEXT_PUBLIC_HK_PHONE ?? "",
          "priceRange": "$$",
          "parentOrganization": { "@id": "https://proactivsports.com/#organization" },
          "address": {
            "@type": "PostalAddress",
            "streetAddress": VENUE.addressStreet,
            "addressLocality": VENUE.addressLocality,
            "addressRegion": VENUE.addressRegion,
            "addressCountry": VENUE.addressCountry,
          },
          "geo": { "@type": "GeoCoordinates", "latitude": VENUE.geo.lat, "longitude": VENUE.geo.lng },
          "openingHoursSpecification": VENUE.hours.map(h => ({
            "@type": "OpeningHoursSpecification",
            "dayOfWeek": h.days,
            "opens": h.opens,
            "closes": h.closes,
          })),
        },
        {
          "@type": "BreadcrumbList",
          "itemListElement": [
            { "@type": "ListItem", "position": 1, "name": "ProActiv Sports Hong Kong", "item": "https://hk.proactivsports.com/" },
            { "@type": "ListItem", "position": 2, "name": "Wan Chai", "item": "https://hk.proactivsports.com/wan-chai/" },
          ],
        },
      ],
    };

    export default function WanChaiPage() {
      const phoneHk = process.env.NEXT_PUBLIC_HK_PHONE;

      return (
        <>
          <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(wanChaiSchema) }} />

          {/* §1 Hero */}
          <Section size="lg" bg="default">
            <ContainerEditorial width="wide">
              <div className="grid grid-cols-1 lg:grid-cols-2 gap-8 items-center">
                <div>
                  <h1 className="text-h1 font-display text-foreground">
                    <span className="font-accent text-brand-navy">ProGym</span> {VENUE.nameShort}
                  </h1>
                  <p className="text-body-lg text-muted-foreground mt-3">{VENUE.addressStreet}, {VENUE.addressLocality}</p>
                  <div className="mt-4 flex flex-wrap gap-2">
                    {VENUE.apparatus.map(a => <Badge key={a} variant="secondary">{a}</Badge>)}
                  </div>
                  <div className="mt-6 flex flex-col sm:flex-row gap-3">
                    <Button asChild size="touch" className="bg-brand-red text-white hover:bg-brand-red/90">
                      <Link href="/book-a-trial/free-assessment/?venue=wan-chai">
                        Book a Free Trial <ArrowRight className="ml-2 size-4" aria-hidden />
                      </Link>
                    </Button>
                    <Button asChild size="touch" variant="outline" className="border-brand-navy text-brand-navy hover:bg-brand-navy/5">
                      <Link href="/contact?market=hk">Send an Enquiry</Link>
                    </Button>
                  </div>
                </div>
                <div className="relative aspect-[4/3] overflow-hidden rounded-2xl">
                  <Image
                    src={VENUE.heroImage}
                    alt={`Children practising gymnastics at ${VENUE.nameFull}`}
                    fill
                    priority
                    sizes="(max-width: 1024px) 100vw, 50vw"
                    className="object-cover"
                  />
                </div>
              </div>
            </ContainerEditorial>
          </Section>

          {/* §2 VenueMap + address card */}
          <Section size="md" bg="muted">
            <ContainerEditorial width="wide">
              <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
                <div className="lg:col-span-2">
                  <VenueMap embedSrc={WAN_CHAI_MAP_EMBED} title={`Map of ${VENUE.nameFull}`} />
                </div>
                <Card className="p-6">
                  <h2 className="text-h3 font-display text-foreground mb-4">Visit us</h2>
                  <p className="flex items-start gap-2 text-body text-foreground">
                    <MapPin className="size-4 mt-1 text-brand-navy shrink-0" aria-hidden />
                    <span>{VENUE.addressStreet}<br />{VENUE.addressLocality}, {VENUE.addressRegion}</span>
                  </p>
                  {phoneHk && (
                    <a href={`tel:${phoneHk.replace(/[^0-9+]/g, "")}`} className="mt-3 flex items-center gap-2 text-body text-foreground hover:text-brand-navy">
                      <Phone className="size-4 text-brand-navy" aria-hidden /> {phoneHk}
                    </a>
                  )}
                </Card>
              </div>
            </ContainerEditorial>
          </Section>

          {/* §3 Opening hours */}
          <Section size="md" bg="default">
            <ContainerEditorial width="default">
              <div className="max-w-2xl">
                <h2 className="text-h2 font-display text-foreground mb-6">
                  <Clock className="inline size-6 mr-2 text-brand-navy align-middle" aria-hidden /> Opening hours
                </h2>
                <table className="w-full">
                  <tbody>
                    {VENUE.hours.map((h) => (
                      <tr key={h.days.join(",")} className="border-b border-border">
                        <td className="py-3 font-semibold text-foreground">{h.days.join(", ")}</td>
                        <td className="py-3 text-right text-muted-foreground">{h.opens} – {h.closes}</td>
                      </tr>
                    ))}
                  </tbody>
                </table>
                <p className="text-small text-muted-foreground mt-3">
                  Hours are placeholder pending confirmation. Please check via WhatsApp or phone before your first visit.
                </p>
              </div>
            </ContainerEditorial>
          </Section>

          {/* §4 Programmes available at this venue */}
          <Section size="md" bg="muted">
            <ContainerEditorial width="wide">
              <h2 className="text-h2 font-display text-foreground mb-6">Programmes at <span className="font-accent text-brand-navy">ProGym</span> {VENUE.nameShort}</h2>
              <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
                {VENUE_PROGRAMMES.map(p => (
                  <Card key={p.slug} className="p-5 hover:-translate-y-1 transition-transform">
                    <h3 className="text-h3 font-display text-foreground">{p.label}</h3>
                    <p className="text-small text-muted-foreground mt-1">{p.ageBand}</p>
                    <Link href={p.href} className="inline-flex items-center gap-1 mt-3 text-brand-navy font-semibold hover:underline">
                      Learn more <ArrowRight className="size-4" aria-hidden />
                    </Link>
                  </Card>
                ))}
              </div>
            </ContainerEditorial>
          </Section>

          {/* §5 Service area */}
          <Section size="md" bg="default">
            <ContainerEditorial width="default">
              <div className="max-w-2xl">
                <h2 className="text-h2 font-display text-foreground mb-3">Easily reached from</h2>
                <p className="text-body text-muted-foreground">
                  ProGym Wan Chai serves families across {VENUE.serviceArea.join(", ")}. The Hennessy is a 5-minute walk from Wan Chai MTR (exit B2) and well-served by the 18, 25, and 26 bus routes.
                </p>
              </div>
            </ContainerEditorial>
          </Section>

          {/* §6 Venue FAQ */}
          {VENUE_FAQS.length > 0 && (
            <Section size="md" bg="muted">
              <ContainerEditorial width="default">
                <div className="max-w-3xl mx-auto">
                  <h2 className="text-h2 font-display text-foreground mb-6">FAQs about Wan Chai</h2>
                  <Accordion type="single" collapsible className="flex flex-col gap-0">
                    {VENUE_FAQS.map(item => (
                      <FAQItem key={item.value} id={item.value} question={item.question} answer={item.answer} />
                    ))}
                  </Accordion>
                </div>
              </ContainerEditorial>
            </Section>
          )}

          {/* §7 Booking CTA */}
          <Section size="lg" bg="navy">
            <ContainerEditorial width="default">
              <div className="text-center max-w-2xl mx-auto">
                <h2 className="text-h2 font-display text-white mb-3">Book your child's free trial at Wan Chai.</h2>
                <p className="text-body-lg text-cream mb-6">A 30-minute assessment with a coach who knows the floor inside-out. No commitment.</p>
                <Button asChild size="touch" className="bg-brand-red text-white hover:bg-brand-red/90">
                  <Link href="/book-a-trial/free-assessment/?venue=wan-chai">
                    Book a Free Trial <ArrowRight className="ml-2 size-4" aria-hidden />
                  </Link>
                </Button>
              </div>
            </ContainerEditorial>
          </Section>
        </>
      );
    }
    ```

    **Step B — `app/hk/wan-chai/opengraph-image.tsx`:**

    ```typescript
    // Phase 4 / Plan 04-04 — Per-route OG for Wan Chai (high-priority page per UI-SPEC §7).
    import { createHKOgImage } from "@/lib/og-image";

    export const runtime = "nodejs";
    export const dynamic = "force-dynamic";
    export const size = { width: 1200, height: 630 };
    export const contentType = "image/png";

    export default function OG() {
      return createHKOgImage({
        title: "ProGym Wan Chai",
        tagline: "Children's gymnastics in Wan Chai, Hong Kong.",
      });
    }
    ```

    Per UI-SPEC §4 `/wan-chai/` row + §8.2 + §7 (per-route OG for high-priority pages).
  </action>
  <verify>
    <automated>
      test -f /Users/martin/Projects/proactive/app/hk/wan-chai/page.tsx
      test -f /Users/martin/Projects/proactive/app/hk/wan-chai/opengraph-image.tsx
      grep -q "15/F, The Hennessy, 256 Hennessy Road" /Users/martin/Projects/proactive/app/hk/wan-chai/page.tsx
      grep -q "VenueMap" /Users/martin/Projects/proactive/app/hk/wan-chai/page.tsx
      grep -q "WAN_CHAI_MAP_EMBED" /Users/martin/Projects/proactive/app/hk/wan-chai/page.tsx
      grep -q "/book-a-trial/free-assessment/?venue=wan-chai" /Users/martin/Projects/proactive/app/hk/wan-chai/page.tsx
      grep -q "SportsActivityLocation" /Users/martin/Projects/proactive/app/hk/wan-chai/page.tsx
      grep -q "BreadcrumbList" /Users/martin/Projects/proactive/app/hk/wan-chai/page.tsx
      grep -q "Opening hours" /Users/martin/Projects/proactive/app/hk/wan-chai/page.tsx
      grep -q "createHKOgImage" /Users/martin/Projects/proactive/app/hk/wan-chai/opengraph-image.tsx
      grep -c "priority" /Users/martin/Projects/proactive/app/hk/wan-chai/page.tsx | awk '{ if($1 != 1) exit 1 }'
      grep -q "canonical.*wan-chai" /Users/martin/Projects/proactive/app/hk/wan-chai/page.tsx
      cd /Users/martin/Projects/proactive && pnpm typecheck
      cd /Users/martin/Projects/proactive && pnpm test:unit --run app/hk/wan-chai/page.test.tsx 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `app/hk/wan-chai/page.tsx` exists; default export `WanChaiPage`
    - File contains literal verbatim string `15/F, The Hennessy, 256 Hennessy Road` (HK-02 NAP requirement)
    - File contains literal href `/book-a-trial/free-assessment/?venue=wan-chai`
    - File contains literal string `SportsActivityLocation` AND `BreadcrumbList` (JSON-LD)
    - File renders `<VenueMap embedSrc={WAN_CHAI_MAP_EMBED}` (HUMAN-ACTION-gated map)
    - File renders Opening hours `<table>` reading from `VENUE.hours`
    - Exactly ONE `<Image>` has `priority` attribute (Pitfall 6)
    - openGraph object includes `siteName`, `locale: "en_HK"`, full `images` array (no shallow merge — Pitfall 2 carry)
    - `alternates.canonical` points to `https://hk.proactivsports.com/wan-chai/`
    - `app/hk/wan-chai/opengraph-image.tsx` exports default returning `createHKOgImage(...)` with title `"ProGym Wan Chai"`
    - `pnpm typecheck` exits 0
    - `pnpm test:unit --run app/hk/wan-chai/page.test.tsx` reports 5 of 5 tests GREEN
  </acceptance_criteria>
  <done>HK-02 satisfied. Wan Chai page renders NAP, map placeholder/iframe, hours, programmes, service area, FAQ, booking CTA, JSON-LD. Test file fully GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build `/cyberport/` page + per-route OG (HK-03)</name>
  <files>app/hk/cyberport/page.tsx, app/hk/cyberport/opengraph-image.tsx</files>
  <read_first>
    - /Users/martin/Projects/proactive/app/hk/wan-chai/page.tsx (Task 2 — copy structure verbatim, swap data to Cyberport)
    - /Users/martin/Projects/proactive/app/hk/wan-chai/opengraph-image.tsx (Task 2 — copy verbatim, swap title/tagline)
    - /Users/martin/Projects/proactive/.planning/phases/04-hong-kong-market/04-UI-SPEC.md §4 `/cyberport/` row (Cyberport-specific copy: "5,000 sq ft", "Opened August 2025") + §8.2 (LocalBusiness for Cyberport with Cyberport geo coords)
    - /Users/martin/Projects/proactive/.planning/inputs/strategy.md §PART 4 (HK wireframe — Cyberport context) + §PART 8.3 (Cyberport NAP)
    - /Users/martin/Projects/proactive/lib/hk-data.ts (HK_VENUES[1] is Cyberport with sizeNote + openedNote)
    - /Users/martin/Projects/proactive/app/hk/cyberport/page.test.tsx (Plan 04-01 — test expectations to satisfy)
  </read_first>
  <behavior>
    Same shape as Wan Chai. Test 1-5 in `app/hk/cyberport/page.test.tsx` must turn GREEN:
    - Test 1: visible "5,000 sq ft"
    - Test 2: H1 contains "Cyberport"
    - Test 3: VenueMap mock iframe with title
    - Test 4: `<Link>` matching `/book-a-trial/free-assessment/?venue=cyberport`
    - Test 5: `SportsActivityLocation` in JSON-LD

    Cyberport-specific differences from Wan Chai:
    - Replace `id: "wan-chai"` filtering with `"cyberport"`
    - Use `CYBERPORT_MAP_EMBED` not `WAN_CHAI_MAP_EMBED`
    - Hero subhead emphasises "5,000 sq ft" + "Opened August 2025" (UI-SPEC §4 Cyberport row + RESEARCH §Pitfall 5)
    - Service area copy: `Pokfulam, Aberdeen, Southern District` (replace Wan Chai districts)
    - Transit copy: Cyberport buses + Cyberport Bus Terminus (NOT Wan Chai MTR)
  </behavior>
  <action>
    **Step A — `app/hk/cyberport/page.tsx`:**

    Copy `app/hk/wan-chai/page.tsx` (Task 2) byte-for-byte, then apply these substitutions in order. Verify each substitution is fully applied (no missed instance).

    Substitutions:
    1. Header comment: `Wan Chai location page (HK-02)` → `Cyberport location page (HK-03)`
    2. `HK_VENUES.find(v => v.id === "wan-chai")` → `HK_VENUES.find(v => v.id === "cyberport")`
    3. `WAN_CHAI_MAP_EMBED` → `CYBERPORT_MAP_EMBED` (in import statement AND in JSX)
    4. `p.venuesOffered.includes("wan-chai")` → `p.venuesOffered.includes("cyberport")`
    5. `metadata.title` → `"ProGym Cyberport — Children's Gymnastics Cyberport, Hong Kong"`
    6. `metadata.description` → `"ProGym Cyberport — 5,000 sq ft purpose-built gymnastics facility, opened August 2025. Book classes, holiday camps and birthday parties for children in Cyberport, Hong Kong."`
    7. `openGraph.url` AND `alternates.canonical` → `"https://hk.proactivsports.com/cyberport/"`
    8. `openGraph.images[0].url` → `/cyberport/opengraph-image`
    9. `wanChaiSchema` constant identifier → `cyberportSchema`
    10. JSON-LD `@id`: `localbusiness-wanchai` → `localbusiness-cyberport`
    11. JSON-LD `name`: `ProGym Wan Chai — ProActiv Sports` → `ProGym Cyberport — ProActiv Sports`
    12. JSON-LD `url`: `wan-chai` → `cyberport`
    13. BreadcrumbList: `Wan Chai` → `Cyberport` AND `wan-chai/` → `cyberport/`
    14. Default export name: `WanChaiPage` → `CyberportPage`
    15. Hero address paragraph subhead: prepend the size note line by altering this block:
        ```typescript
        <p className="text-body-lg text-muted-foreground mt-3">{VENUE.addressStreet}, {VENUE.addressLocality}</p>
        {VENUE.sizeNote && <p className="text-body-lg font-semibold text-brand-navy mt-2">{VENUE.sizeNote} · {VENUE.openedNote}</p>}
        ```
        This makes "5,000 sq ft · Opened August 2025" appear prominently in the hero. The literal string "5,000 sq ft" comes from `VENUE.sizeNote` (data layer in lib/hk-data.ts). Test 1 grep on the rendered output succeeds because `sizeNote === "5,000 sq ft"`.
    16. Booking CTA href: `?venue=wan-chai` → `?venue=cyberport` (BOTH instances — hero CTA AND final CTA section)
    17. Service area copy block:
        ```typescript
        // Replace this:
        ProGym Wan Chai serves families across {VENUE.serviceArea.join(", ")}. The Hennessy is a 5-minute walk from Wan Chai MTR (exit B2) and well-served by the 18, 25, and 26 bus routes.
        // With this:
        ProGym Cyberport serves families across {VENUE.serviceArea.join(", ")}. The Cyberport campus is reached via the dedicated Cyberport bus terminus, with regular routes to Central, Aberdeen and Pokfulam.
        ```
    18. FAQ section heading: `FAQs about Wan Chai` → `FAQs about Cyberport`
    19. Final CTA section copy: `Book your child's free trial at Wan Chai.` → `Book your child's free trial at Cyberport.`
    20. Final CTA section body: `A 30-minute assessment with a coach who knows the floor inside-out. No commitment.` → `A 30-minute assessment in our newest 5,000 sq ft facility. No commitment.`

    **Step B — `app/hk/cyberport/opengraph-image.tsx`:**

    Copy `app/hk/wan-chai/opengraph-image.tsx` byte-for-byte. Replace only:
    - `title: "ProGym Wan Chai"` → `title: "ProGym Cyberport"`
    - `tagline: "Children's gymnastics in Wan Chai, Hong Kong."` → `tagline: "Children's gymnastics in Cyberport, Hong Kong."`
    - First-line comment `Wan Chai` → `Cyberport`

    Per UI-SPEC §4 `/cyberport/` row + §8.2 LocalBusiness JSON-LD + §7 per-route OG.
  </action>
  <verify>
    <automated>
      test -f /Users/martin/Projects/proactive/app/hk/cyberport/page.tsx
      test -f /Users/martin/Projects/proactive/app/hk/cyberport/opengraph-image.tsx
      grep -q "5,000 sq ft" /Users/martin/Projects/proactive/app/hk/cyberport/page.tsx
      grep -q "Cyberport" /Users/martin/Projects/proactive/app/hk/cyberport/page.tsx
      grep -q "VenueMap" /Users/martin/Projects/proactive/app/hk/cyberport/page.tsx
      grep -q "CYBERPORT_MAP_EMBED" /Users/martin/Projects/proactive/app/hk/cyberport/page.tsx
      grep -q "/book-a-trial/free-assessment/?venue=cyberport" /Users/martin/Projects/proactive/app/hk/cyberport/page.tsx
      grep -q "SportsActivityLocation" /Users/martin/Projects/proactive/app/hk/cyberport/page.tsx
      grep -q "localbusiness-cyberport" /Users/martin/Projects/proactive/app/hk/cyberport/page.tsx
      grep -q "canonical.*cyberport" /Users/martin/Projects/proactive/app/hk/cyberport/page.tsx
      grep -q "createHKOgImage" /Users/martin/Projects/proactive/app/hk/cyberport/opengraph-image.tsx
      grep -q "ProGym Cyberport" /Users/martin/Projects/proactive/app/hk/cyberport/opengraph-image.tsx
      grep -q "Wan Chai" /Users/martin/Projects/proactive/app/hk/cyberport/page.tsx && exit 1 || true   # MUST NOT contain Wan Chai (substitution miss)
      grep -c "priority" /Users/martin/Projects/proactive/app/hk/cyberport/page.tsx | awk '{ if($1 != 1) exit 1 }'
      cd /Users/martin/Projects/proactive && pnpm typecheck
      cd /Users/martin/Projects/proactive && pnpm test:unit --run app/hk/cyberport/page.test.tsx 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `app/hk/cyberport/page.tsx` exists; default export `CyberportPage`
    - File contains literal verbatim string `5,000 sq ft` (HK-03 SC#2 requirement — emanates from `VENUE.sizeNote` rendered in hero)
    - File contains literal href `/book-a-trial/free-assessment/?venue=cyberport`
    - File contains literal string `SportsActivityLocation` + `localbusiness-cyberport`
    - File renders `<VenueMap embedSrc={CYBERPORT_MAP_EMBED}`
    - File does NOT contain literal string `Wan Chai` (verified by negative grep — proves no substitution miss)
    - `alternates.canonical` points to `https://hk.proactivsports.com/cyberport/`
    - Exactly ONE `<Image>` has `priority` attribute (Pitfall 6)
    - `app/hk/cyberport/opengraph-image.tsx` exports default returning `createHKOgImage(...)` with title `"ProGym Cyberport"`
    - `pnpm typecheck` exits 0
    - `pnpm test:unit --run app/hk/cyberport/page.test.tsx` reports 5 of 5 tests GREEN
  </acceptance_criteria>
  <done>HK-03 satisfied. Cyberport page mirrors Wan Chai with venue-specific data + copy. Test file fully GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → location pages | Public read-only; no input handling |
| Browser → Google Maps iframe | Third-party iframe; sandbox provided by browser default + `referrerPolicy="no-referrer-when-downgrade"` |
| process.env → JSON-LD telephone field | `NEXT_PUBLIC_HK_PHONE` flows into structured data; intentionally public |
| Static venue data → JSON-LD | All NAP/geo/hours sourced from build-time TypeScript constants in `lib/hk-data.ts`; no user input path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | VenueMap iframe src injection | mitigate | `embedSrc` prop is sourced from `WAN_CHAI_MAP_EMBED` / `CYBERPORT_MAP_EMBED` constants, derived from `process.env.NEXT_PUBLIC_*_MAP_EMBED` env vars. Env vars are operator-controlled at deploy time; no user-mutation path exists. Placeholder branch (`includes("PLACEHOLDER")`) prevents rendering an empty src that some browsers treat as `about:blank` parent context. |
| T-04-04-02 | Information Disclosure | Google Maps iframe leaks page URL via Referer | mitigate | `referrerPolicy="no-referrer-when-downgrade"` set on iframe — same-origin and HTTPS→HTTPS preserve referrer (expected for Google Maps embed analytics); HTTPS→HTTP is downgraded to no-referrer. Standard practice; no PII in URL. |
| T-04-04-03 | Tampering | JSON-LD `dangerouslySetInnerHTML` injection | mitigate | `wanChaiSchema` and `cyberportSchema` are build-time TypeScript objects with NO user-input fields. `JSON.stringify` produces well-formed JSON; `<script type="application/ld+json">` does not execute. Identical pattern to Phase 3. |
| T-04-04-04 | Information Disclosure | `tel:` link reveals phone in client bundle | accept | `NEXT_PUBLIC_HK_PHONE` is public business contact info (also in JSON-LD telephone field); intentional. Sanitised via `.replace(/[^0-9+]/g, "")` before use in `tel:` URI. |
| T-04-04-05 | Spoofing | Iframe lacks sandbox attribute | accept | RESEARCH Pitfall 3 documents that the Google Maps embed needs no `allow=""` restrictions; per Lighthouse Best Practices the `title` attribute is sufficient for accessibility + performance. Adding `sandbox` would break the embed (Google Maps requires same-origin scripts to render the map UI). |
| T-04-04-06 | Information Disclosure | Geo coords in JSON-LD reveal venue location | accept | Venue locations are public business addresses (already on Google Maps, GBP, business cards). Intentional disclosure for local SEO + Search rich results. |
</threat_model>

<verification>
After all 3 tasks:

```bash
pnpm typecheck                                            # Must exit 0
pnpm lint                                                 # Must exit 0
pnpm test:unit --run app/hk/wan-chai/                    # 5/5 GREEN
pnpm test:unit --run app/hk/cyberport/                   # 5/5 GREEN
pnpm build                                                # Compile clean; both routes generate
pnpm dev                                                  # Manual at hk.localhost:3000/wan-chai/ + /cyberport/
```

Manual smoke:
- `/wan-chai/` renders: H1 "ProGym Wan Chai", verbatim NAP "15/F, The Hennessy, 256 Hennessy Road", VenueMap shows placeholder card (env unset locally), hours table renders, 4 programme cards, service area copy mentions "Causeway Bay", booking CTA navigates with `?venue=wan-chai`
- `/cyberport/` renders: H1 "ProGym Cyberport", "5,000 sq ft · Opened August 2025" subhead, similar layout with Cyberport-specific data, CTA `?venue=cyberport`
- Both pages: View Source → search `"@type":"SportsActivityLocation"` matches; `"@type":"BreadcrumbList"` matches
- Both pages: confirm OG image is venue-specific (curl `/wan-chai/opengraph-image` → PNG; `/cyberport/opengraph-image` → PNG)
- Sticky red Book a Free Trial in HKNav remains visible on scroll (Plan 04-02 chrome).
</verification>

<success_criteria>
- [ ] `components/hk/venue-map.tsx` RSC with placeholder fallback + lazy iframe
- [ ] `app/hk/wan-chai/page.tsx` renders verbatim NAP, map, hours, programmes, service area, FAQ, booking CTA pre-filled with `?venue=wan-chai`
- [ ] `app/hk/cyberport/page.tsx` renders "5,000 sq ft" + "Opened August 2025", with Cyberport-specific data; CTA pre-filled with `?venue=cyberport`
- [ ] Both pages emit inline `SportsActivityLocation` + `BreadcrumbList` JSON-LD with @id, geo, address, openingHoursSpecification
- [ ] Both pages have full openGraph block (no shallow merge — Pitfall 2)
- [ ] Per-route OG image at `app/hk/wan-chai/opengraph-image.tsx` + `app/hk/cyberport/opengraph-image.tsx` (UI-SPEC §7 high-priority pages)
- [ ] Each location page has exactly ONE `<Image>` with `priority` (Pitfall 6)
- [ ] `app/hk/wan-chai/page.test.tsx` 5/5 GREEN
- [ ] `app/hk/cyberport/page.test.tsx` 5/5 GREEN
- [ ] HK-02 + HK-03 + ROADMAP SC#2 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-hong-kong-market/04-04-SUMMARY.md` documenting:
- VenueMap RSC final shape + placeholder strategy
- HK-02 + HK-03 closure attestation: NAP visible, JSON-LD parsed, booking CTAs functional
- HUMAN-ACTION items still outstanding: Google Maps embed URLs (D-01 carry pattern), Cyberport exact street address, real opening hours, NEXT_PUBLIC_HK_PHONE
- Per-route OG image count (2 for Plan 04 — under the 8-route Pitfall-5 budget)
</output>

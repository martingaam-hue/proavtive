---
phase: 00-local-foundation
plan: 05
status: complete
completed: 2026-04-22
requirements:
  - FOUND-08
---

# Plan 00-05 Summary: @sentry/nextjs installed + hardened (manual path)

## What was built

`@sentry/nextjs` is wired across all three runtimes (browser, Node.js server, edge) with release tagging, source-map upload, an ad-blocker tunnel, and conservative PII posture. A gated smoke route lets us verify end-to-end ingest after every deploy. The `next.config.ts` X-Robots-Tag logic from Plan 00-03 is preserved — verified by grep in both pre-merge CI and this SUMMARY's acceptance-criteria section.

The wizard did not run cleanly on the user's machine (no files landed), so the install was done manually following Sentry's official [sentry-nextjs-sdk SKILL.md](https://github.com/getsentry/sentry-for-ai/blob/main/skills/sentry-nextjs-sdk/SKILL.md) (Option 2 manual setup). The result is functionally equivalent to a wizard-driven install with our D-18/D-19/D-20 hardening layered on top.

## Task log

| Task | Status | Commit / Evidence |
|------|--------|-------------------|
| 1. Sentry project + OAuth (HUMAN) | ✓ | User created Sentry project and added 6 env vars to Vercel for all 3 environments |
| 2. Manual install + hardening | ✓ | PR #6 — `feat(00-05): install @sentry/nextjs with D-17/D-18/D-19/D-20 hardening` — CI green in 1m11s, merged via squash |

## Versions pinned

| Package | Version |
|---------|---------|
| @sentry/nextjs | 10.49.0 |
| @sentry/cli | 2.58.5 (pulled as peer; build script approved via `onlyBuiltDependencies`) |
| eslint-plugin-react-hooks | 7.1.1 (peer dep filled so ESLint loads eslint-config-next cleanly) |

## Key files

| File | Purpose |
|------|---------|
| `instrumentation-client.ts` | Browser SDK init. `replayIntegration()` for Session Replay. `onRouterTransitionStart` hook for App Router navigation spans. D-18 env + D-20 PII scrubber. |
| `sentry.server.config.ts` | Node.js runtime init. `includeLocalVariables: true` for debuggable server stack traces. D-18 env + D-20 PII scrubber. |
| `sentry.edge.config.ts` | Edge runtime init. Phase 1 middleware will use this. Minimal `beforeSend` scrubber (edge has no Node APIs). |
| `instrumentation.ts` | Next.js `register()` hook. Dispatches server vs edge config based on `NEXT_RUNTIME`. Exports `onRequestError = Sentry.captureRequestError` for automatic server error capture. |
| `app/global-error.tsx` | App Router root error boundary. `"use client"`, captures exception then shows Next's default error UI. |
| `app/api/sentry-smoke/route.ts` | **D-19 smoke route.** Gated by `SENTRY_SMOKE_TOKEN` env var with constant-time comparison. Returns 404 for missing/wrong token. Throws when correct. |
| `next.config.ts` | Wrapped with `withSentryConfig(nextConfig, ...)`. Plan 00-03 `X-Robots-Tag` headers preserved (verified by grep). Options: `org`/`project`/`authToken` from env; `widenClientFileUpload`; `tunnelRoute: "/monitoring"`; `silent: !CI`; `sourcemaps.deleteSourcemapsAfterUpload: true`; `disableLogger: true`. |
| `.gitignore` | Added `.env.sentry-build-plugin` (generated by Sentry CLI for source-map auth). |
| `pnpm-workspace.yaml` | Added `@sentry/cli` to `onlyBuiltDependencies` so pnpm 10 runs its postinstall (binary download). |

## Environment variables (set by user in Vercel → Production + Preview + Development)

| Var | Scope | Purpose |
|-----|-------|---------|
| `NEXT_PUBLIC_SENTRY_DSN` | All 3 envs | Client SDK init. Public by design. |
| `SENTRY_DSN` | All 3 envs | Server + edge SDK init. Same value as above; kept separate so the client bundle can't accidentally leak server config. |
| `SENTRY_AUTH_TOKEN` | All 3 envs | Source-map upload at build time. Scope: `project:releases` + `project:write`. Server-only. |
| `SENTRY_ORG` | All 3 envs | Read by `withSentryConfig` at build time. |
| `SENTRY_PROJECT` | All 3 envs | Read by `withSentryConfig` at build time. |
| `SENTRY_SMOKE_TOKEN` | All 3 envs | Server-only. 24-byte random. Gates `/api/sentry-smoke`. |

## Verification (in-plan)

| Check | Result |
|-------|--------|
| `pnpm typecheck` | exit 0 |
| `pnpm lint` | exit 0 |
| `pnpm format:check` | exit 0 |
| `pnpm build` | exit 0 — produces `.next/` with 3 routes (`/`, `/_not-found`, `/api/sentry-smoke`) |
| 22-point grep-verifiable acceptance criteria | all pass (files exist, correct strings, wizard examples absent) |
| CI on PR #6 | green (1m11s) |
| Branch protection required check | `ci` passed → merge allowed |
| `next.config.ts` contains BOTH `withSentryConfig` AND `X-Robots-Tag` + `noindex, nofollow` | ✓ (wrap preserved the Plan 03 logic) |

## Verification (end-to-end — deferred to Plan 00-06)

Three things need to be tested against a live deployed preview:

1. `GET /api/sentry-smoke` (no token) → **404**
2. `GET /api/sentry-smoke?token=WRONG` → **404**
3. `GET /api/sentry-smoke?token=<real-SENTRY_SMOKE_TOKEN>` → **500** + Sentry issue appears within 60s, tagged `environment: preview` and `release: <commit-SHA>`

This is the FOUND-08 Success Criterion #3 test from ROADMAP. Plan 00-06 owns the formal live verification.

## Deviations from Sentry SKILL.md / plan spec

| SKILL default / plan spec | Reality | Why |
|----------------------------|---------|-----|
| `sendDefaultPii: true` | `sendDefaultPii: false` | D-20 — brand serves children; HK PDPO + SG PDPA apply; re-evaluate when debugging specific issues |
| `hideSourceMaps: true` | `sourcemaps: { deleteSourcemapsAfterUpload: true }` | Sentry 10.x API change — `hideSourceMaps` deprecated |
| Wizard-driven install | Manual install per SKILL Option 2 | Wizard didn't complete on the user's machine (files not present in repo); manual path worked cleanly |
| Plan spec "`sentry.client.config.ts`" | `instrumentation-client.ts` | Current Sentry convention (Sentry 10.x / Next.js 15 pattern per SKILL.md) |

## Decisions honoured

| Decision | How |
|----------|-----|
| D-17 full `@sentry/nextjs` wizard pattern | Manual install with wizard-equivalent output: client + server + edge + instrumentation + global-error + next.config wrap |
| D-18 VERCEL_ENV environment tagging | All three `Sentry.init` calls set `environment: process.env.VERCEL_ENV` (or `NEXT_PUBLIC_VERCEL_ENV` for client) |
| D-19 dedicated gated `/api/sentry-smoke` route | `app/api/sentry-smoke/route.ts` — 404 for missing/wrong token, throws for correct (constant-time compared) |
| D-20 conservative PII posture | `sendDefaultPii: false` on all 3 runtimes + `beforeSend` scrubber (email/phone/password/token/authorization/cookie/childName/parentName/address) |

## Threats addressed

| Threat | Mitigation |
|--------|-----------|
| T-00-19 DSN in client bundle | Accepted — DSN is public by design per Sentry threat model |
| T-00-20 AUTH_TOKEN leak | Build-only; never in `NEXT_PUBLIC_*`; scoped to `project:releases`; masked in Vercel build logs |
| T-00-21 PII leakage to Sentry | `sendDefaultPii: false` + beforeSend scrubber on all 3 runtimes |
| T-00-22 Smoke route abuse / spam | 24-byte random token + constant-time compare + 404 to unauth; Cloudflare rate limiting from Phase 10 |
| T-00-23 Smoke route crawled | Behind Deployment Protection (D-14) + X-Robots-Tag noindex (D-15) + 404-to-unauth |
| T-00-24 Wizard example pages shipping ungated | Never created (manual install skipped them); confirmed absent by grep |
| T-00-25 X-Robots-Tag lost in wrap | Preserved; grep confirms both `withSentryConfig` AND `X-Robots-Tag` + `noindex, nofollow` in `next.config.ts` |

## Handoff surfaces

| Plan | What it gets from 00-05 | Present? |
|------|--------------------------|----------|
| 00-06 verification | Live smoke endpoint `/api/sentry-smoke` ready to hit with `?token=<...>`. Issue feed ready to receive. Release SHA tagging wired via `VERCEL_GIT_COMMIT_SHA`. | ✓ |
| Phase 1+ | Sentry is ready for middleware (Phase 1) — edge config is in place. Sentry captures all unhandled server errors via `onRequestError = Sentry.captureRequestError`. | ✓ |

## Flags for Phase 0 remainder + beyond

- **Bundle size:** First-load JS went from 103 kB → 222 kB after adding Sentry (replay + tracing integrations). Expected. Future optimization: tree-shake unused integrations via `withSentryConfig.webpack.treeshake` (webpack only — we're on turbopack for build, so this would need re-evaluation).
- **Two Vercel projects** (still — `proavtive` and `proavtive-c325` both deploying from this repo). Clean up one in the Vercel dashboard before Phase 10.
- **10 Dependabot CVEs** — up from 10 before Sentry install; verify count hasn't grown. Phase 9 MIG-03 audit.
- **Local dev without Sentry env:** When `.env.local` lacks `NEXT_PUBLIC_SENTRY_DSN`, the client SDK is no-op (safe). Server SDK same. Source-map upload skipped when `SENTRY_AUTH_TOKEN` missing (build still succeeds). No blockers.
- **PII scrubber field list** — `childName` and `parentName` are placeholder keys; the booking form in Phase 4/5 will determine actual field names. Re-evaluate the scrubber list then.

## Self-check

- [x] All 5 config files exist and have correct D-18/D-20 hardening
- [x] Smoke route returns 404 for missing/wrong token (verified by grep + route logic)
- [x] Smoke route throws for correct token (verified by code inspection)
- [x] `next.config.ts` wraps with `withSentryConfig` AND retains X-Robots-Tag header (verified by grep)
- [x] No wizard example pages in the tree
- [x] CI green on PR #6
- [x] Branch protection required check passed → merge allowed
- [x] PR merged; main is up-to-date with all Plan 00-05 changes

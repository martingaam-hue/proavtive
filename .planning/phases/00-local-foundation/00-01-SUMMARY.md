---
phase: 00-local-foundation
plan: 01
status: complete
completed: 2026-04-22
requirements:
  - FOUND-05
  - FOUND-06
---

# Plan 00-01 Summary: Scaffold + repo meta + env discipline

## What was built

A minimal, production-ready Next.js 16 App Router scaffold with tight toolchain pinning, repo meta files (PR template, CODEOWNERS, Dependabot), and `.env` discipline. Every later Phase 0 plan bolts onto this substrate:

- **Plan 00-02** (lefthook) hooks into `package.json` scripts.
- **Plan 00-03** (Vercel) wraps `next.config.ts` with the X-Robots-Tag noindex header and links this repo to a Vercel project.
- **Plan 00-04** (CI) references `pnpm typecheck | lint | build | format:check` scripts as required status checks.
- **Plan 00-05** (Sentry) uses the pre-declared `SENTRY_*` env var names in `.env.example` and wraps `next.config.ts` with `withSentryConfig`.

## Task log

| Task | Status | Commit |
|------|--------|--------|
| 1. Next.js scaffold + Node/pnpm pinning + Prettier | ✓ | `feat(00-01): scaffold Next.js 16 + Node/pnpm pinning (Task 1)` |
| 2. Repo meta + env discipline + README | ✓ | `feat(00-01): repo meta + env discipline + README (Task 2)` |

## Key files created

- `package.json` — Next.js 16.2.4 + React 19.2.4 + TS 5.9 + Tailwind 4 + ESLint 9. `engines.node=">=22.0.0 <23.0.0"`, `engines.pnpm=">=10.0.0"`, `packageManager="pnpm@10.30.3"`. Scripts: `dev`, `build`, `start`, `lint`, `typecheck`, `format`, `format:check`.
- `.nvmrc` — `22`
- `.npmrc` — `engine-strict=false` (local override; CI + Vercel honour `.nvmrc`)
- `eslint.config.mjs` — flat config with `nextVitals` + `nextTs` + `eslint-config-prettier/flat` last.
- `.prettierrc.json` — `printWidth: 100`, `singleQuote: false`, `trailingComma: all`, `semi: true`.
- `.prettierignore` — `.next`, `node_modules`, `pnpm-lock.yaml`, `public`, `.vercel`, `coverage`, `*.md`, `.claude`, `.planning`.
- `app/layout.tsx`, `app/page.tsx`, `app/globals.css` — create-next-app defaults (Tailwind 4 CSS-first).
- `next.config.ts` — Next.js scaffold default (to be wrapped in Plan 00-03 with X-Robots-Tag and Plan 00-05 with `withSentryConfig`).
- `.gitignore` — extended with explicit `.env` + `.env*.local` + `.env.production` blocks + `!.env.example` negation + `.sentryclirc`.
- `.env.example` — names-only env vars for `NEXT_PUBLIC_SENTRY_DSN`, `SENTRY_DSN`, `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, `SENTRY_PROJECT`, `SENTRY_SMOKE_TOKEN`. Documentation lines for `VERCEL_ENV`, `VERCEL_GIT_COMMIT_SHA`.
- `README.md` — prereqs, local-setup command block, scripts table, secrets-discipline section, hook/CI/Sentry/GSD pointers.
- `.github/PULL_REQUEST_TEMPLATE.md`, `.github/CODEOWNERS`, `.github/dependabot.yml`.
- `AGENTS.md` — Next.js 16 guidance for AI coding agents (auto-generated by create-next-app).

## Verification

All ran on local Node 24 (engine warnings non-blocking via `.npmrc`):

| Command | Result |
|---------|--------|
| `pnpm install --frozen-lockfile` | exit 0 |
| `pnpm typecheck` | exit 0 |
| `pnpm lint` | exit 0 |
| `pnpm build` | exit 0 (Turbopack, 4 static routes, `.next/` produced) |
| `pnpm format:check` | exit 0 |
| `git check-ignore .env.local` | exit 0 (confirms gitignore works) |

## Deviations from plan (user-approved)

| Plan spec | Reality 2026-04-22 | Decision |
|-----------|---------------------|----------|
| "Next.js 15" (PROJECT.md lock) | `create-next-app@latest` = Next.js 16.2.4 | Accept 16; update PROJECT.md constraint (pending in `update_project_md` step at end of phase). Next.js 16 is backwards-compatible with 15 App Router patterns. |
| "Tailwind 3" (plan acceptance criterion mentioned `tailwind.config.ts`) | Scaffold installs Tailwind 4 with CSS-first config | Accept 4; Phase 2 design-system plans will adapt (no `tailwind.config.ts`, use `@import "tailwindcss"` + `@theme` in globals.css). |
| "Node 20 LTS" (plan assumed) | Node 22 LTS (current LTS in 2026-04) | Pin `.nvmrc=22`, `engines.node=">=22.0.0 <23.0.0"`. |
| "single Node version enforced" | Local machine Node 24, no nvm/fnm installed | Add `.npmrc engine-strict=false` so pnpm install works on Node 24 locally. Vercel + CI still use Node 22 via `.nvmrc`. Document in README. |
| Plan command `--no-turbopack --src-dir=false` | Flags don't exist in `create-next-app@16` | Used `--typescript --tailwind --eslint --app --import-alias "@/*" --use-pnpm --yes`; `--disable-git` added so scaffold didn't touch the existing `.git` directory. |
| `create-next-app .` refused on non-empty dir | Existing `.planning/`, `.claude/`, `assets/`, `CLAUDE.md` conflicted | Scaffolded in `/tmp/proactive-scaffold`, copied files back selectively (skipped scaffold's stub `CLAUDE.md` and default `README.md`). |

## Decisions honoured

| Decision | How |
|----------|-----|
| D-10 Minimal Next.js scaffold (no middleware/Studio/shadcn) | `create-next-app` defaults only; no route groups, no `middleware.ts`, no Sanity. Phase 1 scope preserved. |
| D-12 Repo meta files | README, PR template, CODEOWNERS, Dependabot all in place. |
| D-13 Node/pnpm pinning | `.nvmrc=22` + `engines.node` + `packageManager="pnpm@10.30.3"` via Corepack. |
| D-09 Local env flow | README + `.env.example` document the "copy from Vercel dashboard into `.env.local`" pattern explicitly. |

## Handoff surfaces (for downstream plans)

| Plan | What it needs from 00-01 | Present? |
|------|---------------------------|----------|
| 00-02 lefthook hooks | `package.json` scripts (`lint`, `typecheck`, `format`), `pnpm-lock.yaml` | ✓ |
| 00-03 Vercel link | `package.json` build script, `next.config.ts` to wrap | ✓ |
| 00-04 GHA CI | `pnpm typecheck | lint | build | format:check` scripts; pinned Node via `.nvmrc` | ✓ |
| 00-05 Sentry wizard | `NEXT_PUBLIC_SENTRY_DSN`, `SENTRY_DSN`, `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, `SENTRY_PROJECT`, `SENTRY_SMOKE_TOKEN` env-var names pre-declared in `.env.example` | ✓ |

## Flags for Phase 0 remainder

- **Next.js 16 + Tailwind 4** deviations need to be reflected in PROJECT.md constraints at phase completion (`update_project_md` step).
- **Local Node mismatch** — user machine is Node 24, target is Node 22. Acceptable today via engine-strict=false; user may want to install nvm + Node 22 before Phase 1 for cleaner alignment. Not a Phase 0 blocker.
- **AGENTS.md** — create-next-app now generates this file. Keep it; it's a short anti-hallucination reminder for AI agents ("this is NOT the Next.js you know"). Harmless.

## Self-check

- [x] All acceptance criteria met (verified by grep + command exit codes)
- [x] No secrets in commits
- [x] Lockfile committed
- [x] `.env.example` lists every future env var with empty values
- [x] Build / typecheck / lint / format:check all green

---
phase: 00-local-foundation
plan: 06
type: execute
wave: 4
depends_on:
  - 00-01
  - 00-02
  - 00-03
  - 00-04
  - 00-05
files_modified:
  - docs/phase-0-verification.md
autonomous: false
requirements:
  - FOUND-05
  - FOUND-06
  - FOUND-08

must_haves:
  truths:
    - "FOUND-05 proved: a fresh commit on a throwaway branch cannot merge to main without CI green + Vercel preview success"
    - "FOUND-06 proved: a fake AWS-shaped key committed to a throwaway branch is caught by CI gitleaks (D-08 layer 4 — negative test). CI run must FAIL."
    - "FOUND-08 proved: `/api/sentry-smoke?token=<SENTRY_SMOKE_TOKEN>` on the latest preview URL produces a Sentry issue within 60 seconds, tagged `environment: preview`, with un-minified stack trace"
    - "Preview pipeline proved: every push to a PR branch produces a fresh preview URL on `*.vercel.app` with a distinct commit-SHA prefix"
    - "A single audit document (`docs/phase-0-verification.md`) records each proof with the git SHA, the CI run URL, the Sentry issue URL, the preview URL, and a timestamp — so future-Martin (or an auditor) can re-read it"
  artifacts:
    - path: "docs/phase-0-verification.md"
      provides: "Dated, link-rich audit of every Phase 0 success criterion, serving as evidence against the ROADMAP checklist"
  key_links:
    - from: "docs/phase-0-verification.md"
      to: "ROADMAP Phase 0 Success Criteria"
      via: "Each of the 4 roadmap criteria is named in the audit doc and linked to its proof (CI URL, Sentry issue URL, preview URL)"
      pattern: "Success Criterion [1-4]"
---

<objective>
Prove — end-to-end, on a live preview — that every Phase 0 success criterion from ROADMAP.md actually works. This is the closing audit plan: no new code, just deliberate verification runs against the stack Plans 01–05 assembled. Specifically, execute D-08 (negative secret-scan test: fake AWS key in a throwaway branch must fail CI) and FOUND-08 success criterion #3 (deliberate throw in a preview surfaces in Sentry within 60 seconds, tagged with the correct environment).

Purpose: This plan is the "walks-like-done, talks-like-done" gate. A failure here means a Phase 0 plan regressed — fix it before closing the phase. Satisfies FOUND-05, FOUND-06, FOUND-08 in the strongest possible form: demonstrated behaviour on production-shaped infra.

Output: `docs/phase-0-verification.md` — a dated audit with links to the CI runs, Sentry issue, and preview URLs that constitute proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-local-foundation/00-CONTEXT.md
@.planning/phases/00-local-foundation/00-01-SUMMARY.md
@.planning/phases/00-local-foundation/00-02-SUMMARY.md
@.planning/phases/00-local-foundation/00-03-SUMMARY.md
@.planning/phases/00-local-foundation/00-04-SUMMARY.md
@.planning/phases/00-local-foundation/00-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Negative secret-scan verification (D-08 layer 4)</name>
  <what-built>
    Plan 02 + Plan 04 together produce layers 1 + 3 of the four-layer secret defense. Layer 4 (per D-08) is a dated, documented proof that the scanner actually rejects a secret-shaped commit. This task executes that proof.
  </what-built>
  <how-to-verify>
    Run EXACTLY these steps. Record outputs for the audit doc (Task 4).

    1. From a clean `main`:
       ```
       git checkout main && git pull --rebase
       git checkout -b verify/00-06-negative-secret-scan
       ```

    2. Create a file with a KNOWN fake AWS-shaped access key. Use the public test key `AKIAIOSFODNN7EXAMPLE` (documented by AWS in their examples) so there is NO actual secret leak risk — gitleaks still rejects it because its default ruleset matches the `AKIA[0-9A-Z]{16}` pattern.

       ```
       cat > .verify-fake-secret.txt <<EOF
       # Intentional fake secret for D-08 negative CI test — must be caught by gitleaks.
       # This is AWS's own public example key, not a real credential.
       AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
       AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
       EOF
       ```

    3. Attempt a local commit. The lefthook pre-commit hook from Plan 02 SHOULD reject it. Confirm:
       ```
       git add .verify-fake-secret.txt
       git commit -m "chore(verify): D-08 negative secret scan — should fail"
       ```
       EXPECTED: commit fails; gitleaks prints a finding (redacted). Record the output.

    4. Now deliberately bypass the local hook (this models the `--no-verify` threat T-00-07 from Plan 02):
       ```
       git commit -m "chore(verify): D-08 negative secret scan — should fail" --no-verify
       git push origin verify/00-06-negative-secret-scan
       ```
       EXPECTED: push succeeds (push-protection may also reject it if GHAS is available; that is a bonus layer, not the primary verification). Continue.

    5. Open a PR against `main` for the branch. EXPECTED: the `ci` workflow triggers, and the `Gitleaks` step FAILS. The PR cannot be merged because the required `ci` check is red. Record the CI run URL.

    6. DO NOT merge. Close the PR, delete the remote + local branch, remove the file:
       ```
       gh pr close --delete-branch
       # or: via GitHub UI, close + delete branch, then locally:
       git checkout main
       git branch -D verify/00-06-negative-secret-scan
       rm .verify-fake-secret.txt
       ```

    7. Confirm the branch is gone from the remote:
       ```
       git ls-remote --heads origin verify/00-06-negative-secret-scan
       ```
       EXPECTED: empty output.

    Report back (copy-paste into approve message):
    - Local pre-commit gitleaks blocked the commit: yes / no
    - `--no-verify` bypass was successful (push reached GitHub): yes / no
    - CI gitleaks step FAILED on the PR: yes / no
    - CI run URL: `https://github.com/martingaam/proactive/actions/runs/_________`
    - PR closed + branch deleted + file removed locally: yes / no
  </how-to-verify>
  <resume-signal>Type "approved" with the CI run URL + all yes/no answers, OR describe what failed.</resume-signal>
  <files>(none — verification / human-action only; produces no committed artifact unless noted in `<how-to-verify>`)</files>
  <action>HUMAN CHECKPOINT — follow the `<how-to-verify>` steps above exactly. This task is blocking and cannot be automated (dashboard-only setup OR verification that external tools behaved correctly). Record actual outputs and report back via the `<resume-signal>` format.</action>
  <verify>
    <automated>Manual verification — pause for user input. See `<how-to-verify>` steps above. No automated command can replace this checkpoint.</automated>
  </verify>
  <done>User returns "approved" with the status answers / URLs requested in `<resume-signal>`. If any step failed, fix the underlying plan's Task config first, then re-run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Sentry smoke verification (FOUND-08 success criterion #3)</name>
  <what-built>
    Plan 05 installed Sentry + added the gated smoke route. This task hits the route on a live preview and confirms the Sentry issue feed receives the error within 60 seconds, tagged with the correct environment.
  </what-built>
  <how-to-verify>
    1. Make a tiny visible change on a new branch (e.g. edit `app/page.tsx` copy by one word) to force a fresh preview build. Push + open PR so Vercel builds a new preview URL. Wait for CI green + Vercel preview ready.
       ```
       git checkout -b verify/00-06-sentry-smoke
       # edit app/page.tsx, change one word
       git add app/page.tsx
       git commit -m "chore(verify): force new preview for sentry smoke test"
       git push origin verify/00-06-sentry-smoke
       gh pr create --fill
       ```

    2. Capture the Vercel preview URL from the PR checks (`Inspect deployment` → preview URL, or Vercel dashboard).

    3. Open the preview URL in a browser where you are authenticated to Vercel (Deployment Protection will gate it otherwise). Confirm the app loads. This confirms Phase 0 SC#4 (fresh preview URL per PR push).

    4. Retrieve `SENTRY_SMOKE_TOKEN` from Vercel → Project → Settings → Environment Variables (it's not secret in the traditional sense since you set it yourself in Plan 05 Task 1, but Vercel keeps it env-scoped).

    5. In the SAME authenticated browser session (so Deployment Protection allows the request), navigate to:
       ```
       https://<preview-url>/api/sentry-smoke?token=<SENTRY_SMOKE_TOKEN>
       ```
       EXPECTED: 500 Internal Server Error. The browser shows Next.js's default error page. The route's `throw new Error('sentry-smoke — deliberate error')` fired.

    6. In a second tab, also verify the 404 gating works:
       ```
       https://<preview-url>/api/sentry-smoke                            # no token
       https://<preview-url>/api/sentry-smoke?token=wrong-value          # wrong token
       ```
       EXPECTED: both return 404 Not Found.

    7. Open https://sentry.io → the `proactive-web` (or whatever the project slug is) issues feed. Within 60 seconds (usually <10s), an issue titled "sentry-smoke — deliberate error" should appear. Click in and verify:
       - **Environment tag:** `preview` (D-18 verified)
       - **Release:** equals the VERCEL_GIT_COMMIT_SHA of the PR head commit (D-17 verified)
       - **Stack trace:** un-minified (source maps uploaded — Plan 05 wizard configured this) — you should see `app/api/sentry-smoke/route.ts` in the frames, not minified `_buildManifest.js`
       - **User context:** IP address should NOT be populated (D-20 `sendDefaultPii: false` verified)
       - **Request body / headers:** any `Cookie` / `Authorization` header should appear as `[redacted]` or be absent (beforeSend scrubber verified)

    8. Close the PR WITHOUT merging (we don't want the one-word text change to survive into main as a verification artifact):
       ```
       gh pr close --delete-branch
       git checkout main
       git branch -D verify/00-06-sentry-smoke
       ```

    Report back:
    - Preview URL: `https://__________________.vercel.app`
    - 500 response with correct token: yes / no
    - 404 response without token: yes / no
    - 404 response with wrong token: yes / no
    - Sentry issue appeared within 60s: yes / no — record actual elapsed time
    - Sentry issue environment tag = "preview": yes / no
    - Sentry issue release = expected commit SHA: yes / no
    - Stack trace un-minified: yes / no
    - No IP in user context: yes / no
    - Sentry issue URL: `https://sentry.io/organizations/_______/issues/__________`
    - PR closed + branch deleted: yes / no
  </how-to-verify>
  <resume-signal>Type "approved" with all status answers and the URLs above, OR describe which check failed.</resume-signal>
  <files>(none — verification / human-action only; produces no committed artifact unless noted in `<how-to-verify>`)</files>
  <action>HUMAN CHECKPOINT — follow the `<how-to-verify>` steps above exactly. This task is blocking and cannot be automated (dashboard-only setup OR verification that external tools behaved correctly). Record actual outputs and report back via the `<resume-signal>` format.</action>
  <verify>
    <automated>Manual verification — pause for user input. See `<how-to-verify>` steps above. No automated command can replace this checkpoint.</automated>
  </verify>
  <done>User returns "approved" with the status answers / URLs requested in `<resume-signal>`. If any step failed, fix the underlying plan's Task config first, then re-run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Preview URL + X-Robots-Tag + branch protection audit</name>
  <what-built>
    Plans 03 + 04 produced Deployment Protection + noindex header + branch protection. This task audits each against the Phase 0 Success Criteria from ROADMAP.md.
  </what-built>
  <how-to-verify>
    Use the preview URL from Task 2 (or any recent preview). Run each check:

    **Check A — Phase 0 Success Criterion #4: distinct commit-SHA preview URL per PR push**
    - Vercel preview URL format should include the commit SHA prefix (e.g. `proactive-<sha>-martingaam.vercel.app` or `proactive-git-branch-martingaam.vercel.app` — Vercel's exact scheme depends on project settings). Confirm two consecutive PR commits produced two DIFFERENT preview URLs.

    **Check B — D-14 Deployment Protection**
    - In an incognito / private window with NO Vercel session, hit the preview URL. EXPECTED: Vercel's "Authentication required" screen, NOT the app.

    **Check C — D-15 X-Robots-Tag**
    - From a terminal, with a Vercel session cookie (or after approving via Deployment Protection), hit the preview URL with curl:
      ```
      curl -I "https://<preview-url>/"
      ```
      EXPECTED response headers include: `x-robots-tag: noindex, nofollow` (case-insensitive).
    - For completeness, repeat against `/api/sentry-smoke` (without token → 404) — same header should be present.

    **Check D — Phase 0 Success Criterion #1: branch protection on main**
    - Go to GitHub → Settings → Branches → `main` rule. Confirm:
      - Require a pull request before merging: ON
      - Require status checks to pass before merging: ON, with `ci` required
      - Require branches to be up to date before merging: ON
    - Attempt a direct push (already done in Plan 04 Task 2, but repeat for this audit so the verification doc has a fresh timestamp):
      ```
      git checkout main && git pull
      echo "audit" > .phase-0-direct-push-audit
      git add .phase-0-direct-push-audit
      git commit -m "chore: direct push audit"
      git push origin main
      ```
      EXPECTED: rejected with "protected branch hook declined". Clean up: `git reset --hard HEAD~1 && rm .phase-0-direct-push-audit`.

    **Check E — Phase 0 Success Criterion #2 partial: `.env.example` completeness**
    - `grep -E '^[A-Z_]+=$' .env.example | wc -l` should return at least 6 (the Sentry vars + smoke token).
    - `git log --all --oneline -- '.env' '.env.local' '.env.production'` should return empty (no env file ever committed).

    Report back:
    - Preview URL 1 and Preview URL 2 (consecutive pushes, different SHAs): `https://...` / `https://...`
    - Incognito hit shows Vercel auth gate: yes / no
    - `x-robots-tag: noindex, nofollow` present in curl -I response: yes / no — paste the exact header line
    - Direct push to main rejected: yes / no
    - `.env.example` has ≥ 6 empty-valued env var entries: yes / no — count observed
    - No `.env` / `.env.local` / `.env.production` in git log: yes / no
  </how-to-verify>
  <resume-signal>Type "approved" with all status answers + header line + counts, OR describe what failed.</resume-signal>
  <files>(none — verification / human-action only; produces no committed artifact unless noted in `<how-to-verify>`)</files>
  <action>HUMAN CHECKPOINT — follow the `<how-to-verify>` steps above exactly. This task is blocking and cannot be automated (dashboard-only setup OR verification that external tools behaved correctly). Record actual outputs and report back via the `<resume-signal>` format.</action>
  <verify>
    <automated>Manual verification — pause for user input. See `<how-to-verify>` steps above. No automated command can replace this checkpoint.</automated>
  </verify>
  <done>User returns "approved" with the status answers / URLs requested in `<resume-signal>`. If any step failed, fix the underlying plan's Task config first, then re-run.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Write docs/phase-0-verification.md audit document</name>
  <files>docs/phase-0-verification.md</files>
  <read_first>
    - The resume-signal outputs from Tasks 1, 2, 3 above (these are the data this document records)
    - .planning/ROADMAP.md (Phase 0 Success Criteria — must quote them verbatim)
    - .planning/phases/00-local-foundation/00-CONTEXT.md (D-08 negative verification; confirms this doc is itself the audit artifact D-08 demands)
  </read_first>
  <action>
Create `docs/phase-0-verification.md` with this structure. Fill the placeholders with the actual URLs + timestamps + yes/no answers captured from Tasks 1–3. This file is committed to the repo and becomes the phase-close evidence.

```markdown
# Phase 0 — Local Foundation — Verification Audit

**Phase:** 00-local-foundation
**Audited by:** Martin (martingaam@gmail.com)
**Audit date:** <YYYY-MM-DD from the day Task 4 runs>
**Repo SHA at audit:** <`git rev-parse HEAD` on main>

This document is the closing evidence for Phase 0. Each ROADMAP Success Criterion is quoted verbatim and followed by the concrete proof artifact (URL, commit SHA, screenshot link, or timestamp).

---

## ROADMAP SC #1 — Branch protection + required CI

> A fresh clone of the GitHub repo cannot merge to `main` without a passing PR CI run (typecheck + lint) and a successful Vercel preview deployment.

**Proof:**
- Branch protection rule active on `main`: <screenshot or `gh api` output link>
- Required status check: `ci` (configured in Plan 04)
- Direct-push-to-main test (Task 3 Check D): <yes/no + date + terminal output excerpt>
- Most recent green CI run on a non-trivial PR: `<URL>`
- Required checks exercised by the CI workflow per D-02: typecheck, lint, format:check, build, gitleaks — all green

**Status:** ✅ / ❌ (fill in)

---

## ROADMAP SC #2 — Secrets discipline (with negative verification per D-08)

> No secret exists in git history; `.env.example` lists every required variable; local dev loads from `.env.local` (gitignored); Vercel preview + production environments read their values from the configured secrets source; a forced test commit of a fake secret pattern is blocked by CI.

**Proof:**
- `.env.example` entry count: <N empty-valued entries — from Task 3 Check E>
- `.env.local` gitignored: `git check-ignore .env.local` returns 0 → yes
- No `.env*` in git history: `git log --all --oneline -- '.env*' ':!.env.example'` output: <empty>
- Vercel env vars configured (Production + Preview + Development scopes): NEXT_PUBLIC_SENTRY_DSN, SENTRY_DSN, SENTRY_AUTH_TOKEN, SENTRY_ORG, SENTRY_PROJECT, SENTRY_SMOKE_TOKEN — verified by user in Plan 05 Task 1
- **D-08 Negative test (Task 1):**
  - Fake key committed with `--no-verify`, pushed, CI ran → CI failed at Gitleaks step
  - CI run URL: `<URL from Task 1 output>`
  - Branch cleaned up: yes
  - Four-layer defense status:
    - L1 lefthook pre-commit: ✅ (blocked local commit — Task 1 step 3)
    - L2 GH native scanning: ✅ / partial (push protection availability depends on repo visibility + GHAS — record actual: <public w/ push protection> OR <private w/o GHAS, push protection unavailable>)
    - L3 CI gitleaks: ✅ (Task 1 step 5)
    - L4 negative verification: ✅ (this audit entry, dated)

**Status:** ✅ / ❌ (fill in)

---

## ROADMAP SC #3 — Sentry captures preview errors within 60 seconds, environment-tagged

> A deliberate `throw new Error("sentry-smoke")` in a preview deployment surfaces in Sentry within 60 seconds, tagged with the correct environment.

**Proof (Task 2):**
- Preview URL tested: `<URL>`
- Request: `GET <preview-url>/api/sentry-smoke?token=<redacted>`
- Response: 500 (deliberate throw) — confirmed
- 404 gating: missing token → 404 (confirmed), wrong token → 404 (confirmed)
- Sentry issue URL: `<URL>`
- Sentry issue environment tag: `preview` — confirmed
- Sentry issue release tag: `<commit SHA>` — confirmed
- Time-to-issue: `<elapsed seconds>` (< 60 required)
- Un-minified stack trace: confirmed — frame path includes `app/api/sentry-smoke/route.ts`
- PII scrub verified: no IP in user context; `Cookie` / `Authorization` headers absent or `[redacted]`

**Status:** ✅ / ❌ (fill in)

---

## ROADMAP SC #4 — Vercel project linked; every PR push → fresh preview URL

> The Vercel project is linked to the GitHub repo and every push to a PR branch produces a fresh preview URL on `*.vercel.app` with a distinct commit-SHA subdomain.

**Proof (Task 3 Check A + B + C):**
- Preview URL 1 (first push): `<URL>`
- Preview URL 2 (second push, same PR): `<URL>` — URLs differ by commit SHA → confirmed
- Deployment Protection gate (Task 3 Check B): incognito hit shows Vercel auth screen → confirmed
- `X-Robots-Tag: noindex, nofollow` on non-production deploy (Task 3 Check C): `<exact header line from curl -I>`

**Status:** ✅ / ❌ (fill in)

---

## Decisions honoured

- D-01 GHA is the single required status check — ✅
- D-02 four required checks (typecheck, lint, build, gitleaks) — ✅
- D-03 Vitest deferred to Phase 2 — ✅ (not added here)
- D-04 Lighthouse deferred to Phase 7 — ✅ (not added here)
- D-05 pre-commit lefthook + gitleaks — ✅ (Plan 02)
- D-06 GH native scanning + push protection — ✅/partial (see SC #2 L2 status above)
- D-07 CI gitleaks layer — ✅ (Plan 04)
- D-08 negative verification — ✅ (this doc is the D-08 artifact)
- D-09 `.env.example` + manual-copy flow — ✅ (Plan 01)
- D-10 minimal Next.js 15 scaffold — ✅ (Plan 01)
- D-11 conventional commits enforced — ✅ (Plan 02)
- D-12 repo meta files — ✅ (Plan 01)
- D-13 Node + pnpm pinning — ✅ (Plan 01)
- D-14 Deployment Protection on from day one — ✅ (Plan 03)
- D-15 X-Robots-Tag noindex on non-prod — ✅ (Plan 03)
- D-16 Hobby now; Pro upgrade Phase 10 prerequisite — ✅ documented
- D-17 full @sentry/nextjs wizard — ✅ (Plan 05)
- D-18 environment tag from VERCEL_ENV — ✅ (Plan 05, confirmed SC #3)
- D-19 gated /api/sentry-smoke route — ✅ (Plan 05, confirmed SC #3)
- D-20 conservative PII — ✅ (Plan 05, confirmed SC #3)

## Reminders for later phases

- **Phase 1:** When subdomain middleware lands, confirm it does NOT accidentally bypass the `next.config.js` `headers()` noindex logic. The X-Robots-Tag header must still fire on preview.
- **Phase 4 / 5:** Booking form fields (parent name, child name, child age, contact email, phone) — extend the `sentry.*.config.ts` `PII_FIELDS` list if new names appear.
- **Phase 7:** Add Lighthouse CI (D-04 deferral). Real `robots.txt` lands here; the `next.config.js` production-exemption for X-Robots-Tag becomes live.
- **Phase 9:** `pnpm audit` gate + Sanity token scoping audit per MIG-03.
- **Phase 10:** Vercel Hobby → Pro upgrade (D-16). 1Password Business secret sync replacing the manual-copy flow (D-09). Domain + Cloudflare WAF attach.
```

After writing the file, commit it on a new branch `chore/00-06-verification` → open PR → CI green → squash merge.

NOTE: this PR being mergeable is ITSELF part of the proof for ROADMAP SC #1 — it goes through the full CI + branch-protection + preview-build flow. The merge of this PR closes Phase 0.
  </action>
  <verify>
    <automated>test -f docs/phase-0-verification.md && grep -q "ROADMAP SC #1" docs/phase-0-verification.md && grep -q "ROADMAP SC #2" docs/phase-0-verification.md && grep -q "ROADMAP SC #3" docs/phase-0-verification.md && grep -q "ROADMAP SC #4" docs/phase-0-verification.md && grep -q "D-08 Negative test" docs/phase-0-verification.md && grep -q "sentry-smoke" docs/phase-0-verification.md && grep -q "Deployment Protection" docs/phase-0-verification.md && grep -q "X-Robots-Tag: noindex, nofollow" docs/phase-0-verification.md && grep -q "Repo SHA at audit:" docs/phase-0-verification.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/phase-0-verification.md` exists
    - Contains verbatim quotes of all four ROADMAP Phase 0 Success Criteria
    - Contains a CI run URL for the D-08 negative test (from Task 1)
    - Contains a Sentry issue URL (from Task 2)
    - Contains at least one preview URL (from Task 2 or 3)
    - Contains the actual header line from `curl -I` (from Task 3 Check C)
    - Contains a D-08 artifact section explicitly dated and stating the four-layer defense status
    - Every success criterion has a non-placeholder Status (✅ or ❌, not left blank)
    - Contains the D-01 through D-20 decisions checklist with each marked ✅ or ❌
    - PR containing this file merges to main successfully (which requires CI green, which is part of the ROADMAP SC #1 proof — circular and intentional)
    - `git log --oneline -- docs/phase-0-verification.md` shows at least one commit with a conventional-commit style subject
  </acceptance_criteria>
  <done>Audit doc committed; all four ROADMAP Phase 0 success criteria have dated, link-rich proof; all 20 decisions accounted for; the PR merging this doc itself exercised the full Phase 0 guardrail stack end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit doc (public in repo) → future readers / auditors | Must not contain real secrets. Only URLs, redacted tokens, commit SHAs, boolean statuses. |
| negative-test branch → remote | A branch containing an (AWS-public-example) fake key exists on the remote briefly. Must be deleted after verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-26 | Information disclosure | audit doc accidentally records a real secret value | mitigate | The doc template references `<redacted>` for tokens and captures only URLs, SHAs, status answers. Acceptance criteria enforce structure; gitleaks (Plan 02/04) scans the doc on commit. |
| T-00-27 | Information disclosure | the verify/00-06-negative-secret-scan branch persisting on remote with the (public example) fake AWS key | mitigate | Task 1 step 6 explicitly deletes the remote branch AND the local branch AND the file. Task 1 acceptance criterion includes `git ls-remote` check confirming the branch is gone. The key itself is AWS's PUBLIC example key — it is not a credential, but good hygiene still requires cleanup. |
| T-00-28 | Information disclosure | `/api/sentry-smoke` being repeatedly hit during testing, filling the Sentry quota | accept | Phase 0 has tiny traffic; a few test hits are negligible. Rate limiting via Cloudflare is Phase 10. |
| T-00-29 | Tampering | the verification doc being tampered with after-the-fact to claim something passed that didn't | mitigate | Doc is committed to git — history is append-only. The CI run URLs + Sentry issue URLs are external sources of truth that re-verify on click. |
</threat_model>

<verification>
Each task above is itself a verification; Task 4 is the documented outcome. Phase 0 is COMPLETE when Task 4's PR is merged to main. The final acceptance test is: "run a fresh clone of the repo on a new machine, `pnpm install`, and prove you cannot push a fake-secret change to main". If any step of that story fails, this phase is NOT done.
</verification>

<success_criteria>
1. `docs/phase-0-verification.md` committed to main with all four ROADMAP Phase 0 Success Criteria marked ✅
2. D-08 negative test executed: a fake-AWS-shaped key on a throwaway branch → CI gitleaks step failed as required; branch deleted
3. Sentry smoke route executed on live preview: issue appeared in Sentry within 60s, tagged `environment: preview`, with un-minified stack trace + scrubbed PII
4. Deployment Protection + X-Robots-Tag + branch protection all independently proved in a live audit
5. Every decision D-01 through D-20 has a clear ✅ / ❌ in the audit doc
6. The PR that merges the audit doc itself exercises the full guardrail stack (CI, Vercel preview, branch protection) — so the act of closing Phase 0 is itself proof that Phase 0 works
</success_criteria>

<output>
After completion, create `.planning/phases/00-local-foundation/00-06-SUMMARY.md` documenting: the merged PR URL that closed Phase 0, the verification doc path, a one-line status of each of FOUND-05, FOUND-06, FOUND-08 (VALIDATED / pointer to verification doc), and a handoff note for Phase 1: "Next.js scaffold is at App Router with Tailwind. `next.config.js` wraps withSentryConfig + async headers() — any Phase 1 middleware must preserve both. Sentry + preview pipeline + CI + branch protection are all green. Start from a clean main."
</output>

---
plan: 02
phase: 10
wave: 2
autonomous: false
objective: "Vercel domain attachment: add three named domains, record CNAME hash, populate DNS-RECORDS.md, set production env vars"
files_modified:
  - .planning/phases/10-hosting-dns-cutover-launch/DNS-RECORDS.md
  - vercel.json
task_count: 4
requirements:
  - FOUND-01
  - MIG-01
---

<objective>
Add proactivsports.com, hk.proactivsports.com, and sg.proactivsports.com to the Vercel project. Record the project-specific CNAME hash that Vercel provides — this hash is unknown until this step runs and is required to populate DNS-RECORDS.md. Set production NEXT_PUBLIC_SITE_URL env vars per market. Verify vercel.json has the correct $schema field for WAF rule compatibility in Plan 10-03.

Purpose: Unblocks Plan 10-03 (WAF config) and Plan 10-04 (DNS cutover). Domains must be added to Vercel BEFORE DNS is pointed to Vercel — cert provisioning requires Vercel to already know the domain.

Output: Three domains in Vercel project at "Pending Verification" status; CNAME hash recorded in DNS-RECORDS.md; production env vars set; vercel.json verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-hosting-dns-cutover-launch/10-CONTEXT.md
@.planning/phases/10-hosting-dns-cutover-launch/10-RESEARCH.md
@.planning/phases/10-hosting-dns-cutover-launch/10-UI-SPEC.md
@.planning/phases/10-hosting-dns-cutover-launch/DNS-RECORDS.md
@CLAUDE.md
</context>

<tasks>

<task type="human_action">
  <name>Task 1: Add three named domains to Vercel project</name>
  <files>.planning/phases/10-hosting-dns-cutover-launch/DNS-RECORDS.md</files>
  <read_first>
    - .planning/phases/10-hosting-dns-cutover-launch/10-CONTEXT.md (Decision 2: Three named per-domain certs; Decision 3: Cloudflare DNS record configuration)
    - .planning/phases/10-hosting-dns-cutover-launch/10-RESEARCH.md (Pattern 1: Vercel Domain Attachment — exact step sequence; Pitfall 3: cert provisioning fails if DNS points elsewhere — add to Vercel FIRST)
  </read_first>
  <action>
HUMAN-ACTION — requires Vercel Dashboard access.

Add each domain in Vercel Dashboard → Project → Settings → Domains:

1. Add `proactivsports.com`:
   - Click "Add Domain" → type `proactivsports.com` → Add
   - Vercel will show the A record it wants: note the IP address (likely `76.76.21.21` but verify the dashboard value — it is project-specific)
   - Status will show "Pending Verification" — this is correct. Do NOT point DNS yet.

2. Add `hk.proactivsports.com`:
   - Click "Add Domain" → type `hk.proactivsports.com` → Add
   - Vercel will show a CNAME target: `<hash>.vercel-dns-0xx.com`
   - CRITICAL: Record this exact CNAME hash — it is project-specific and cannot be computed in advance

3. Add `sg.proactivsports.com`:
   - Click "Add Domain" → type `sg.proactivsports.com` → Add
   - Vercel will show the same CNAME target as hk (same project hash)

4. Record in DNS-RECORDS.md:
   - Vercel apex A record IP: `<value from dashboard>`
   - Vercel CNAME hash: `<hash>.vercel-dns-0xx.com`
   - Update the placeholder rows in the DNS-RECORDS.md table with exact values
   - Set Status header from PENDING to CONFIGURED (once all three are added)

DO NOT set DNS records in Cloudflare yet — that happens in Plan 10-04.
  </action>
</task>

<task type="human_action">
  <name>Task 2: Set production environment variables in Vercel</name>
  <files></files>
  <read_first>
    - .planning/phases/10-hosting-dns-cutover-launch/10-CONTEXT.md (Decision 11: NEXT_PUBLIC_SITE_URL per market must be set before production build)
    - .planning/phases/10-hosting-dns-cutover-launch/10-UI-SPEC.md (Environment Variables section — exact variable names and values)
    - .planning/phases/10-hosting-dns-cutover-launch/10-RESEARCH.md (Pitfall 6: sitemap URLs must use production domain — set before build)
  </read_first>
  <action>
HUMAN-ACTION — requires Vercel Dashboard access.

In Vercel Dashboard → Project → Settings → Environment Variables:

Set the following variables scoped to Production environment only:

| Variable | Value | Environment |
|----------|-------|-------------|
| NEXT_PUBLIC_SITE_URL | https://proactivsports.com | Production |
| NEXT_PUBLIC_SITE_URL (hk) | https://hk.proactivsports.com | Production |
| NEXT_PUBLIC_SITE_URL (sg) | https://sg.proactivsports.com | Production |
| SANITY_WEBHOOK_SECRET | <value from 1Password / existing Vercel secrets> | Production |

NOTE on per-market NEXT_PUBLIC_SITE_URL: If the codebase uses a single NEXT_PUBLIC_SITE_URL env var (not market-scoped), set it to `https://proactivsports.com` for the root and confirm that hk/sg markets derive their base URLs from middleware or from separate variables. Check how sitemaps in Phase 7 used this variable and match that pattern.

Verify SANITY_WEBHOOK_SECRET is already set from Phase 6 webhook configuration. If not set, generate a strong random secret (e.g. `openssl rand -hex 32`) and set it in both Vercel and Sanity Dashboard → API → Webhooks → the existing webhook secret field.

Do NOT trigger a production build yet — production build happens in Plan 10-05 after DNS is live.
  </action>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify vercel.json exists and has correct $schema for WAF rules</name>
  <files>vercel.json</files>
  <read_first>
    - .planning/phases/10-hosting-dns-cutover-launch/10-UI-SPEC.md (vercel.json WAF deny rules — exact content required in Plan 10-03)
    - .planning/phases/10-hosting-dns-cutover-launch/10-RESEARCH.md (Code Examples: Vercel WAF Rule in vercel.json)
  </read_first>
  <action>
Check if `vercel.json` exists at the repo root. If it exists, read its current contents.

If vercel.json does NOT exist, create it with the minimal scaffold:
```json
{
  "$schema": "https://openapi.vercel.sh/vercel.json"
}
```

If vercel.json EXISTS and already has a `routes` array or other content:
- Preserve all existing configuration
- Ensure the `$schema` field is present: `"$schema": "https://openapi.vercel.sh/vercel.json"`
- Do NOT add WAF mitigate rules yet — those are added in Plan 10-03

If vercel.json EXISTS and has existing redirects (from Phase 9's .net 301 map):
- Preserve those redirects exactly as-is
- Do NOT modify them
- Just confirm $schema is present

Commit any change (or confirm no change needed) with message: `chore(10): verify vercel.json schema for WAF rule compatibility`
  </action>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update DNS-RECORDS.md with Vercel domain details</name>
  <files>.planning/phases/10-hosting-dns-cutover-launch/DNS-RECORDS.md</files>
  <read_first>
    - .planning/phases/10-hosting-dns-cutover-launch/10-UI-SPEC.md (DNS-RECORDS.md structure — exact format required)
    - .planning/phases/10-hosting-dns-cutover-launch/DNS-RECORDS.md (current state after Plan 10-01 tasks)
  </read_first>
  <action>
After the human actions in Tasks 1–2 are complete, update DNS-RECORDS.md to reflect the values collected:

1. Replace `<Vercel apex IP from dashboard>` with the actual IP confirmed in Task 1.
2. Replace `<hash>.vercel-dns-0xx.com` placeholders with the actual Vercel CNAME target recorded in Task 1.
3. Update the Status header: change from `PENDING` to `CONFIGURED` (records are documented but not yet active in Cloudflare).
4. Update the Last updated date to today.
5. Verify the "Pre-Cutover TTL State" section has the current TTL values from Plan 10-01 Task 3.

Commit: `docs(10): populate DNS-RECORDS.md with Vercel domain targets`

This document is now the authoritative reference for the Cloudflare DNS record configuration step in Plan 10-04.
  </action>
</task>

</tasks>

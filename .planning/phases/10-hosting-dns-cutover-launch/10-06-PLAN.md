---
plan: 06
phase: 10
wave: 1
autonomous: true
objective: "Create CUTOVER-RUNBOOK.md, SMOKE-TEST-CHECKLIST.md, and DNS-RECORDS.md skeleton as committed planning artifacts"
files_modified:
  - .planning/phases/10-hosting-dns-cutover-launch/CUTOVER-RUNBOOK.md
  - .planning/phases/10-hosting-dns-cutover-launch/SMOKE-TEST-CHECKLIST.md
  - .planning/phases/10-hosting-dns-cutover-launch/DNS-RECORDS.md
task_count: 3
requirements:
  - MIG-04
---

<objective>
Create the three planning/operational artifacts that are the primary deliverables of Phase 10. These documents must exist in version control before Plans 10-01 through 10-05 execute — Plan 10-01 populates DNS-RECORDS.md, Plan 10-02 fills in Vercel CNAME hash values, and Plans 10-04/10-05 use CUTOVER-RUNBOOK.md as the live execution document. Plan 10-06 runs in Wave 1 (before Plans 10-01 through 10-05) to create the skeleton documents.

Purpose: Satisfies MIG-04 (cutover runbook exists and is version-controlled). The CUTOVER-RUNBOOK.md is the operator's live execution document during the DNS cutover.

Output: Three Markdown files committed to .planning/phases/10-hosting-dns-cutover-launch/ — populated from the authoritative templates in 10-UI-SPEC.md and 10-RESEARCH.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-hosting-dns-cutover-launch/10-CONTEXT.md
@.planning/phases/10-hosting-dns-cutover-launch/10-RESEARCH.md
@.planning/phases/10-hosting-dns-cutover-launch/10-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create DNS-RECORDS.md skeleton</name>
  <files>.planning/phases/10-hosting-dns-cutover-launch/DNS-RECORDS.md</files>
  <read_first>
    - .planning/phases/10-hosting-dns-cutover-launch/10-UI-SPEC.md (DNS Records Specification — exact structure of DNS-RECORDS.md including all tables, locked rules, and open audit items)
    - .planning/phases/10-hosting-dns-cutover-launch/10-CONTEXT.md (Decision 3: Cloudflare DNS record configuration; Decision 10: manual audit required)
    - .planning/phases/10-hosting-dns-cutover-launch/10-RESEARCH.md (Pattern 1: Vercel domain attachment — exact record types; Anti-Patterns: never use orange cloud)
  </read_first>
  <action>
Create `.planning/phases/10-hosting-dns-cutover-launch/DNS-RECORDS.md` using the exact structure from 10-UI-SPEC.md DNS Records Specification section.

The file must include:
- Status header: PENDING
- Last updated date: today
- Vercel CNAME hash: PENDING (populated in Plan 10-02)
- Vercel-Pointing Records table with placeholder values `<Vercel apex IP from dashboard>` and `<hash>.vercel-dns-0xx.com`
- All records set to DNS-only / gray cloud
- Email Records table with placeholder rows for MX, SPF, DKIM (populated in Plan 10-01 Task 3)
- Verification Records table with `google-site-verification=<from Phase 8>` placeholder
- Pre-Cutover TTL State table with empty "Current TTL" and "Lowered at" columns
- Open Audit Items checklist (all unchecked)
- Locked DNS record rules table (the 6 locked rules from 10-UI-SPEC.md)

After creation, commit: `docs(10): create DNS-RECORDS.md skeleton — awaiting zone audit and Vercel domain attachment`
  </action>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create CUTOVER-RUNBOOK.md</name>
  <files>.planning/phases/10-hosting-dns-cutover-launch/CUTOVER-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/10-hosting-dns-cutover-launch/10-UI-SPEC.md (Cutover Runbook Specification — exact structure with all sections, bash commands, and checkbox items)
    - .planning/phases/10-hosting-dns-cutover-launch/10-RESEARCH.md (DNS Cutover Runbook (Authoritative) section — full runbook template)
    - .planning/phases/10-hosting-dns-cutover-launch/10-CONTEXT.md (Decision 9: cutover timing and TTL pre-lowering; Decision 15: artifacts as deliverables)
  </read_first>
  <action>
Create `.planning/phases/10-hosting-dns-cutover-launch/CUTOVER-RUNBOOK.md` using the exact structure from 10-UI-SPEC.md Cutover Runbook Specification section.

The runbook must include all sections from the spec:
- Execution Window (with placeholders for date, time, operator name)
- Pre-Cutover Checklist (T-72h to T-24h) with all checkbox items including the phase gate
- T-24h: Lower TTLs section with exact bash commands for dig verification
- T-0: Cutover Execution with all 6 steps:
  - Step 1: Set Cloudflare DNS records (table with exact record types, names, content placeholders)
  - Step 2: Update NS records at current registrar (NS pair placeholders)
  - Step 3: Verify NS propagation (exact dig commands)
  - Step 4: Monitor Vercel cert provisioning (three domain checklist)
  - Step 5: Trigger production build
  - Step 6: Publish first blog post per market
- T+30min: Smoke Tests (references SMOKE-TEST-CHECKLIST.md)
- T+60min: Post-Cutover Steps with:
  - GSC verification and sitemap submission (all three sitemaps)
  - WAF rules switch from LOG to DENY
  - Operational loop bootstrap (Day 90 calendar event, GBP review template, backlink outreach)
- Rollback Plan with both rollback scenarios (NS not yet propagated; NS has propagated)

Use the exact bash command blocks from the spec — do not paraphrase. Operators copy-paste from this document.

Commit: `docs(10): create CUTOVER-RUNBOOK.md — production execution document for DNS cutover`
  </action>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create SMOKE-TEST-CHECKLIST.md</name>
  <files>.planning/phases/10-hosting-dns-cutover-launch/SMOKE-TEST-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/10-hosting-dns-cutover-launch/10-UI-SPEC.md (Smoke Test Checklist Specification — exact structure with all sections, bash scripts, and sign-off table)
    - .planning/phases/10-hosting-dns-cutover-launch/10-RESEARCH.md (T+30min: Smoke Tests — per-domain test items)
  </read_first>
  <action>
Create `.planning/phases/10-hosting-dns-cutover-launch/SMOKE-TEST-CHECKLIST.md` using the exact structure from 10-UI-SPEC.md Smoke Test Checklist Specification section.

The checklist must include all sections with executable check items:
- Execution header (timestamp, operator, overall result placeholders)
- Domain Resolution section (3 dig commands with expected outputs)
- SSL/TLS section (curl + openssl verification commands)
- Root proactivsports.com section (homepage, title, JSON-LD, sitemap, llms.txt, OG preview)
- HK hk.proactivsports.com section (homepage, location pages, booking form test, WhatsApp GA4, GBP embed, sitemap)
- SG sg.proactivsports.com section (homepage, Katong Point, booking form, sitemap)
- WAF Rules section (exact curl commands for WordPress paths; complete rate limit test shell script)
- Sanity Webhook / ISR section (Studio publish test)
- 301 Redirects section (curl -sIL commands with .net sample URLs and expected 301 destinations)
- Analytics section (GA4 real-time verification)
- Sign-Off table (per-section result + overall PASS/FAIL + operator + timestamp)

Include the exact rate limit test script from 10-UI-SPEC.md (the `for i in $(seq 1 12)` loop).

Include the 301 redirect test section with placeholder .net URLs — note that actual URLs are populated from Phase 9's .net crawl map.

Commit: `docs(10): create SMOKE-TEST-CHECKLIST.md — T+30min post-cutover test matrix`
  </action>
</task>

</tasks>

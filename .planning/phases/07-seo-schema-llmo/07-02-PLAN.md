---
plan: "07-02"
phase: 07
wave: 1
autonomous: true
objective: "SEO route files — sitemap.ts, robots.ts, llms.txt/route.ts, and llms-full.txt/route.ts for all three properties (root, hk, sg)"
files_modified:
  - app/root/sitemap.ts
  - app/root/robots.ts
  - app/root/llms.txt/route.ts
  - app/root/llms-full.txt/route.ts
  - app/hk/sitemap.ts
  - app/hk/robots.ts
  - app/hk/llms.txt/route.ts
  - app/hk/llms-full.txt/route.ts
  - app/sg/sitemap.ts
  - app/sg/robots.ts
  - app/sg/llms.txt/route.ts
  - app/sg/llms-full.txt/route.ts
  - tests/unit/sitemap.test.ts
  - tests/unit/robots.test.ts
  - tests/unit/llms-txt.test.ts
task_count: 5
requirements:
  - SEO-02
  - SEO-03
  - SEO-04
depends_on:
  - "07-01"
---

# Plan 07-02: SEO Route Files — Sitemaps, Robots, llms.txt (All Three Properties)

## Objective

Create all 12 SEO route files for the three properties:
- 3 × `sitemap.ts` (served as `sitemap.xml` per property)
- 3 × `robots.ts` (served as `robots.txt` per property)
- 3 × `llms.txt/route.ts` (LLMO directory view)
- 3 × `llms-full.txt/route.ts` (LLMO full-content view)

Then promote the three RED test scaffold files from Plan 07-01 into real passing assertions.

**Prerequisite:** Plan 07-01 must be complete (middleware exclusion added, lib/schema.ts created).

---

<task id="07-02-T1">
## T1: Create sitemap.ts for all three properties

<read_first>
- `lib/hk-data.ts` — read all route slugs defined (HK_GYMNASTICS_PROGRAMMES slugs, venue slugs, etc.)
- `lib/sg-data.ts` — read SG route slugs (SG_ZONES slugs, SG_CAMP_TYPES slugs)
- `app/hk/` — run `find app/hk -name "page.tsx"` to get the full list of existing HK pages
- `app/sg/` — run `find app/sg -name "page.tsx"` to get full list of existing SG pages
- `app/root/` — run `find app/root -name "page.tsx"` to get full list of existing root pages
</read_first>

<action>
Create three `sitemap.ts` files. All URL entries must use hardcoded production origin strings — no `NEXT_PUBLIC_*` env vars, no preview URLs, EVER.

Priority scale (from CONTEXT.md and UI-SPEC §3.2):
- Homepage: `priority: 1`, `changeFrequency: 'weekly'`
- Location pages (wan-chai, cyberport, katong-point): `priority: 0.9`, `changeFrequency: 'monthly'`
- Pillar pages (gymnastics, weekly-classes, prodigy-camps): `priority: 0.8`, `changeFrequency: 'monthly'`
- Sub-pages (gymnastics/toddlers, weekly-classes/movement-zone): `priority: 0.7`, `changeFrequency: 'monthly'`
- Supporting content (blog hub, faq, coaches, book-a-trial): `priority: 0.6`, `changeFrequency: 'weekly'`
- Utility/legal pages (privacy, terms, careers): `priority: 0.3`, `changeFrequency: 'yearly'`

All URLs end with `/` (trailing slash).

**`app/root/sitemap.ts`:**

```typescript
import type { MetadataRoute } from 'next'

const ROOT_ORIGIN = 'https://proactivsports.com'

export default function sitemap(): MetadataRoute.Sitemap {
  return [
    {
      url: `${ROOT_ORIGIN}/`,
      lastModified: new Date(),
      changeFrequency: 'weekly',
      priority: 1,
    },
    {
      url: `${ROOT_ORIGIN}/brand/`,
      changeFrequency: 'monthly',
      priority: 0.6,
    },
    {
      url: `${ROOT_ORIGIN}/coaching-philosophy/`,
      changeFrequency: 'monthly',
      priority: 0.6,
    },
    {
      url: `${ROOT_ORIGIN}/news/`,
      changeFrequency: 'weekly',
      priority: 0.6,
    },
    {
      url: `${ROOT_ORIGIN}/careers/`,
      changeFrequency: 'monthly',
      priority: 0.3,
    },
    {
      url: `${ROOT_ORIGIN}/contact/`,
      changeFrequency: 'monthly',
      priority: 0.6,
    },
    {
      url: `${ROOT_ORIGIN}/privacy/`,
      changeFrequency: 'yearly',
      priority: 0.3,
    },
    {
      url: `${ROOT_ORIGIN}/terms/`,
      changeFrequency: 'yearly',
      priority: 0.3,
    },
  ]
}
```

**`app/hk/sitemap.ts`:**

```typescript
import type { MetadataRoute } from 'next'

const HK_ORIGIN = 'https://hk.proactivsports.com'

export default function sitemap(): MetadataRoute.Sitemap {
  return [
    { url: `${HK_ORIGIN}/`, lastModified: new Date(), changeFrequency: 'weekly', priority: 1 },
    // Location pages
    { url: `${HK_ORIGIN}/wan-chai/`, changeFrequency: 'monthly', priority: 0.9 },
    { url: `${HK_ORIGIN}/cyberport/`, changeFrequency: 'monthly', priority: 0.9 },
    // Gymnastics pillar
    { url: `${HK_ORIGIN}/gymnastics/`, changeFrequency: 'monthly', priority: 0.8 },
    { url: `${HK_ORIGIN}/gymnastics/toddlers/`, changeFrequency: 'monthly', priority: 0.7 },
    { url: `${HK_ORIGIN}/gymnastics/beginner/`, changeFrequency: 'monthly', priority: 0.7 },
    { url: `${HK_ORIGIN}/gymnastics/intermediate/`, changeFrequency: 'monthly', priority: 0.7 },
    { url: `${HK_ORIGIN}/gymnastics/advanced/`, changeFrequency: 'monthly', priority: 0.7 },
    { url: `${HK_ORIGIN}/gymnastics/competitive/`, changeFrequency: 'monthly', priority: 0.7 },
    { url: `${HK_ORIGIN}/gymnastics/rhythmic/`, changeFrequency: 'monthly', priority: 0.7 },
    { url: `${HK_ORIGIN}/gymnastics/adult/`, changeFrequency: 'monthly', priority: 0.7 },
    { url: `${HK_ORIGIN}/gymnastics/private/`, changeFrequency: 'monthly', priority: 0.7 },
    // Supporting content
    { url: `${HK_ORIGIN}/holiday-camps/`, changeFrequency: 'monthly', priority: 0.8 },
    { url: `${HK_ORIGIN}/birthday-parties/`, changeFrequency: 'monthly', priority: 0.8 },
    { url: `${HK_ORIGIN}/school-partnerships/`, changeFrequency: 'monthly', priority: 0.6 },
    { url: `${HK_ORIGIN}/competitions-events/`, changeFrequency: 'monthly', priority: 0.6 },
    { url: `${HK_ORIGIN}/coaches/`, changeFrequency: 'monthly', priority: 0.6 },
    { url: `${HK_ORIGIN}/blog/`, changeFrequency: 'weekly', priority: 0.6 },
    { url: `${HK_ORIGIN}/faq/`, changeFrequency: 'monthly', priority: 0.6 },
    { url: `${HK_ORIGIN}/book-a-trial/`, changeFrequency: 'weekly', priority: 0.6 },
    { url: `${HK_ORIGIN}/book-a-trial/free-assessment/`, changeFrequency: 'weekly', priority: 0.6 },
  ]
}
```

**`app/sg/sitemap.ts`:**

```typescript
import type { MetadataRoute } from 'next'

const SG_ORIGIN = 'https://sg.proactivsports.com'

export default function sitemap(): MetadataRoute.Sitemap {
  return [
    { url: `${SG_ORIGIN}/`, lastModified: new Date(), changeFrequency: 'weekly', priority: 1 },
    // Location
    { url: `${SG_ORIGIN}/katong-point/`, changeFrequency: 'monthly', priority: 0.9 },
    // Weekly classes pillar
    { url: `${SG_ORIGIN}/weekly-classes/`, changeFrequency: 'monthly', priority: 0.8 },
    { url: `${SG_ORIGIN}/weekly-classes/movement-zone/`, changeFrequency: 'monthly', priority: 0.7 },
    { url: `${SG_ORIGIN}/weekly-classes/sports-zone/`, changeFrequency: 'monthly', priority: 0.7 },
    { url: `${SG_ORIGIN}/weekly-classes/climbing-zone/`, changeFrequency: 'monthly', priority: 0.7 },
    // Prodigy camps pillar
    { url: `${SG_ORIGIN}/prodigy-camps/`, changeFrequency: 'monthly', priority: 0.8 },
    { url: `${SG_ORIGIN}/prodigy-camps/themes/`, changeFrequency: 'monthly', priority: 0.7 },
    { url: `${SG_ORIGIN}/prodigy-camps/multi-activity/`, changeFrequency: 'monthly', priority: 0.7 },
    { url: `${SG_ORIGIN}/prodigy-camps/gymnastics-camps/`, changeFrequency: 'monthly', priority: 0.7 },
    // Supporting content
    { url: `${SG_ORIGIN}/birthday-parties/`, changeFrequency: 'monthly', priority: 0.8 },
    { url: `${SG_ORIGIN}/school-partnerships/`, changeFrequency: 'monthly', priority: 0.6 },
    { url: `${SG_ORIGIN}/events/`, changeFrequency: 'monthly', priority: 0.6 },
    { url: `${SG_ORIGIN}/coaches/`, changeFrequency: 'monthly', priority: 0.6 },
    { url: `${SG_ORIGIN}/blog/`, changeFrequency: 'weekly', priority: 0.6 },
    { url: `${SG_ORIGIN}/faq/`, changeFrequency: 'monthly', priority: 0.6 },
    { url: `${SG_ORIGIN}/book-a-trial/`, changeFrequency: 'weekly', priority: 0.6 },
  ]
}
```
</action>

<acceptance_criteria>
- All three files exist at `app/root/sitemap.ts`, `app/hk/sitemap.ts`, `app/sg/sitemap.ts`
- `pnpm build` succeeds with all three sitemap files
- `grep "vercel.app" app/root/sitemap.ts app/hk/sitemap.ts app/sg/sitemap.ts` returns NOTHING (no preview URLs in sitemap output)
- `grep "trailing" app/hk/sitemap.ts` not needed — verify by inspection: every URL string ends with `'/'`
- `grep "proactivsports.com" app/hk/sitemap.ts | wc -l` returns ≥ 20 lines (all HK URLs)
</acceptance_criteria>
</task>

---

<task id="07-02-T2">
## T2: Create robots.ts for all three properties

<read_first>
- `app/root/sitemap.ts` (just created in T1) — verify the origin constant for root
- `app/hk/robots.ts` — check if a robots.ts file already exists from a prior phase
</read_first>

<action>
Create three `robots.ts` files. All must emit `allow: '/'` — NEVER branch on environment. The Phase 0 `X-Robots-Tag: noindex, nofollow` header is the correct preview-noindex mechanism; `robots.ts` must always allow-all.

**`app/root/robots.ts`:**

```typescript
import type { MetadataRoute } from 'next'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
    },
    sitemap: 'https://proactivsports.com/sitemap.xml',
    host: 'proactivsports.com',
  }
}
```

**`app/hk/robots.ts`:**

```typescript
import type { MetadataRoute } from 'next'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
    },
    sitemap: 'https://hk.proactivsports.com/sitemap.xml',
    host: 'hk.proactivsports.com',
  }
}
```

**`app/sg/robots.ts`:**

```typescript
import type { MetadataRoute } from 'next'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
    },
    sitemap: 'https://sg.proactivsports.com/sitemap.xml',
    host: 'sg.proactivsports.com',
  }
}
```

Important: If a `robots.ts` file already exists in any of these locations from a prior phase, read its content first and update it to match the pattern above exactly (do not delete other configs if they are already correct, but ensure there is no `disallow` or environment branching).
</action>

<acceptance_criteria>
- All three files exist at `app/root/robots.ts`, `app/hk/robots.ts`, `app/sg/robots.ts`
- `grep "disallow\|Disallow\|VERCEL_ENV\|process.env" app/root/robots.ts app/hk/robots.ts app/sg/robots.ts` returns NOTHING (no disallow rules, no environment branching)
- `grep "allow: '/'" app/root/robots.ts app/hk/robots.ts app/sg/robots.ts` returns 3 lines (one per file)
- `pnpm build` succeeds
</acceptance_criteria>
</task>

---

<task id="07-02-T3">
## T3: Create llms.txt route handlers for all three properties

<read_first>
- `lib/hk-data.ts` — read the full file to use real programme names, venue addresses, and coach names verbatim in the llms.txt content
- `lib/sg-data.ts` — read the full file to use real SG data verbatim
- UI-SPEC §9.1, §9.2, §9.3 — the content outlines are specified verbatim; use them exactly
</read_first>

<action>
Create six directories and route handler files. Each llms.txt route handler serves the directory view (link list only, per llmstxt.org spec). All use `export const revalidate = 86400` for 24h ISR.

Spec rules (NON-NEGOTIABLE from CONTEXT.md Locked Choice #12):
- H1: property/site name (required)
- Blockquote (`> `): one-paragraph business summary (required)
- H2: section headings only
- Link format: `- [Page Title](URL): one-sentence description`
- `## Optional` section: secondary content
- NO H3 or deeper
- NO HTML markup
- Pure Markdown only

**`app/root/llms.txt/route.ts`** (content from UI-SPEC §9.1):

```typescript
export const revalidate = 86400 // 24h ISR

export async function GET() {
  const content = `# ProActiv Sports

> ProActiv Sports is a premium children's gymnastics and multi-sport provider founded in Hong Kong in 2011. Operating under two sub-brands — ProGym (Hong Kong) and Prodigy (Singapore) — ProActiv runs structured weekly programmes, holiday camps, birthday parties, and school partnerships for children aged 2 to 16 across three venues: ProGym Wan Chai, ProGym Cyberport (HK), and Prodigy @ Katong Point (SG).

## About
- [Brand](https://proactivsports.com/brand/): Company history, leadership, and brand story
- [Coaching Philosophy](https://proactivsports.com/coaching-philosophy/): Safety, Progression, Confidence methodology

## Markets
- [Hong Kong](https://hk.proactivsports.com/): ProGym Wan Chai and ProGym Cyberport — weekly gymnastics, camps, parties
- [Singapore](https://sg.proactivsports.com/): Prodigy @ Katong Point — weekly classes, camps, MultiBall wall

## Contact
- [Contact](https://proactivsports.com/contact/): Market-routed enquiry form for HK and SG

## Optional
- [News](https://proactivsports.com/news/): Press and brand updates
- [Careers](https://proactivsports.com/careers/): Join the ProActiv coaching team
`

  return new Response(content, {
    headers: {
      'Content-Type': 'text/plain; charset=utf-8',
      'Cache-Control': 'public, max-age=86400, stale-while-revalidate=3600',
    },
  })
}
```

**`app/hk/llms.txt/route.ts`** (content from UI-SPEC §9.2):

```typescript
export const revalidate = 86400

export async function GET() {
  const content = `# ProActiv Sports Hong Kong

> ProActiv Sports Hong Kong operates ProGym Wan Chai (15/F The Hennessy, 256 Hennessy Road, Wan Chai) and ProGym Cyberport (5,000 sq ft purpose-built venue, Cyberport, Pokfulam, opened August 2025). A children's gymnastics and sports specialist operating since 2011, ProGym offers structured weekly classes from 12 months to adult, holiday camps, birthday parties, competitive pathways, and school partnerships.

## Programmes
- [Gymnastics](https://hk.proactivsports.com/gymnastics/): Structured weekly gymnastics for ages 12 months to adult — 8 levels from Toddlers to Competitive
- [Holiday Camps](https://hk.proactivsports.com/holiday-camps/): Easter, Summer, and Christmas camp programmes
- [Birthday Parties](https://hk.proactivsports.com/birthday-parties/): Exclusive venue hire, coach-hosted
- [School Partnerships](https://hk.proactivsports.com/school-partnerships/): In-school gymnastics and sports programmes

## Venues
- [ProGym Wan Chai](https://hk.proactivsports.com/wan-chai/): 15/F The Hennessy, 256 Hennessy Road, Wan Chai, Hong Kong
- [ProGym Cyberport](https://hk.proactivsports.com/cyberport/): 5,000 sq ft purpose-built venue, Cyberport, Pokfulam, Hong Kong (opened August 2025)

## Coaches
- [Hong Kong Team](https://hk.proactivsports.com/coaches/): Led by Monica, Director of Sports

## Book
- [Book a Free Trial](https://hk.proactivsports.com/book-a-trial/): Free 30-minute assessment for new students

## Optional
- [Blog](https://hk.proactivsports.com/blog/): Editorial content for Hong Kong families
- [FAQ](https://hk.proactivsports.com/faq/): Frequently asked questions
- [Competitions](https://hk.proactivsports.com/competitions-events/): Competitions and events calendar
`

  return new Response(content, {
    headers: {
      'Content-Type': 'text/plain; charset=utf-8',
      'Cache-Control': 'public, max-age=86400, stale-while-revalidate=3600',
    },
  })
}
```

**`app/sg/llms.txt/route.ts`** (content from UI-SPEC §9.3):

```typescript
export const revalidate = 86400

export async function GET() {
  const content = `# Prodigy by ProActiv Sports Singapore

> Prodigy by ProActiv Sports operates at Katong Point (451 Joo Chiat Road, Level 3, Singapore 427664) and is home to Singapore's only MultiBall wall. Prodigy offers weekly sports classes in three zones (Movement, Sports + MultiBall, Climbing), Prodigy holiday camps, birthday parties, and school partnerships including the IFS programme for children aged 2 to 16.

## Programmes
- [Weekly Classes](https://sg.proactivsports.com/weekly-classes/): Movement, Sports + MultiBall, and Climbing zones for ages 2–16
- [Prodigy Camps](https://sg.proactivsports.com/prodigy-camps/): Themed, multi-activity, and gymnastics holiday camps
- [Birthday Parties](https://sg.proactivsports.com/birthday-parties/): Exclusive Katong Point venue hire with MultiBall access
- [School Partnerships](https://sg.proactivsports.com/school-partnerships/): IFS and in-school sports programmes

## Venue
- [Katong Point](https://sg.proactivsports.com/katong-point/): 451 Joo Chiat Road, Level 3, Singapore 427664 — Singapore's only MultiBall wall

## Coaches
- [Singapore Team](https://sg.proactivsports.com/coaches/): Led by Haikel, Head of Sports

## Book
- [Book a Free Trial](https://sg.proactivsports.com/book-a-trial/): Free trial class for new students

## Optional
- [Blog](https://sg.proactivsports.com/blog/): Editorial content for Singapore families
- [FAQ](https://sg.proactivsports.com/faq/): Frequently asked questions
- [Events](https://sg.proactivsports.com/events/): Upcoming events at Katong Point
`

  return new Response(content, {
    headers: {
      'Content-Type': 'text/plain; charset=utf-8',
      'Cache-Control': 'public, max-age=86400, stale-while-revalidate=3600',
    },
  })
}
```
</action>

<acceptance_criteria>
- All three directories and files exist: `app/root/llms.txt/route.ts`, `app/hk/llms.txt/route.ts`, `app/sg/llms.txt/route.ts`
- `grep "###" app/root/llms.txt/route.ts app/hk/llms.txt/route.ts app/sg/llms.txt/route.ts` returns NOTHING (no H3 headings in content)
- `grep "<html\|<div\|<p " app/root/llms.txt/route.ts app/hk/llms.txt/route.ts app/sg/llms.txt/route.ts` returns NOTHING (no HTML)
- Each file's content string starts with `# ` (H1 heading)
- Each file's content string contains `> ` (blockquote summary)
- Each file contains `## Optional` section
- `pnpm build` succeeds
</acceptance_criteria>
</task>

---

<task id="07-02-T4">
## T4: Create llms-full.txt route handlers for all three properties

<read_first>
- `lib/hk-data.ts` — read in full; use programme descriptions, venue hours, FAQ answers verbatim
- `lib/sg-data.ts` — read in full; use SG zones, camps, FAQ answers verbatim
- `app/hk/llms.txt/route.ts` (just created) — use same structure, expand each section with full prose
</read_first>

<action>
Create three `llms-full.txt` route handlers. These expand each H2 section with the FULL prose content of each linked page — not just more links. An LLM consuming `llms-full.txt` must be able to answer factual questions about ProActiv without fetching additional URLs (UI-SPEC §9.4).

Minimum content for Phase 7 (static, from data files):
- Programme descriptions from HK_GYMNASTICS_PROGRAMMES / SG_ZONES / SG_CAMP_TYPES
- Venue addresses, opening hours, and NAP from venue data constants
- Coach bios from HK_COACHES / SG_COACHES
- FAQ answers from HK_FAQ_ITEMS / SG_FAQ_ITEMS

Use the actual data from the data files — copy the real descriptions, not placeholder text.

Structure for each llms-full.txt: Same H1 + blockquote as the matching llms.txt, then each H2 section has:
1. The link list (same as llms.txt)
2. Below each link: full prose expansion of that page's key content

**`app/root/llms-full.txt/route.ts`:**

```typescript
export const revalidate = 86400

export async function GET() {
  const content = `# ProActiv Sports

> ProActiv Sports is a premium children's gymnastics and multi-sport provider founded in Hong Kong in 2011. Operating under two sub-brands — ProGym (Hong Kong) and Prodigy (Singapore) — ProActiv runs structured weekly programmes, holiday camps, birthday parties, and school partnerships for children aged 2 to 16 across three venues: ProGym Wan Chai, ProGym Cyberport (HK), and Prodigy @ Katong Point (SG).

## About

- [Brand](https://proactivsports.com/brand/): Company history, leadership, and brand story

ProActiv Sports was founded in 2011 in Hong Kong. The company is led by Will (Founder), Monica (Director of Sports, HK), and Haikel (Head of Sports, SG). ProActiv believes in a coaching philosophy built on three pillars: Safety, Progression, and Confidence. The company has grown from a single gymnastics studio to a multi-venue, multi-market operation spanning Hong Kong and Singapore.

- [Coaching Philosophy](https://proactivsports.com/coaching-philosophy/): Safety, Progression, Confidence methodology

ProActiv's coaching methodology is structured around three principles: Safety (ensuring every child trains in a secure, age-appropriate environment), Progression (structured skill development from basic to advanced), and Confidence (building self-belief through achievement). Coaches are internationally trained and hold recognised gymnastics certifications.

## Markets

- [Hong Kong](https://hk.proactivsports.com/): ProGym Wan Chai and ProGym Cyberport — weekly gymnastics, camps, parties

ProActiv Sports in Hong Kong operates as ProGym. Two venues: ProGym Wan Chai (15/F The Hennessy, 256 Hennessy Road) and ProGym Cyberport (5,000 sq ft purpose-built venue, Cyberport, Pokfulam, opened August 2025). Programmes serve ages 12 months to adult with 8 levels of gymnastics, holiday camps, birthday parties, and school partnerships.

- [Singapore](https://sg.proactivsports.com/): Prodigy @ Katong Point — weekly classes, camps, MultiBall wall

ProActiv Sports in Singapore operates as Prodigy. One venue: Prodigy @ Katong Point (451 Joo Chiat Road, Level 3, Singapore 427664). Home of Singapore's only MultiBall wall. Programmes include weekly sports classes in Movement, Sports + MultiBall, and Climbing zones, Prodigy holiday camps (themed, multi-activity, gymnastics), birthday parties, and IFS school partnerships for ages 2 to 16.

## Contact

- [Contact](https://proactivsports.com/contact/): Market-routed enquiry form for HK and SG

ProActiv Sports can be contacted through the master contact form at proactivsports.com/contact/, which routes enquiries to the correct market (Hong Kong or Singapore) based on the user's selection. Telephone and WhatsApp contacts are available for each venue on their respective location pages.

## Optional

- [News](https://proactivsports.com/news/): Press and brand updates
- [Careers](https://proactivsports.com/careers/): Join the ProActiv coaching team
`

  return new Response(content, {
    headers: {
      'Content-Type': 'text/plain; charset=utf-8',
      'Cache-Control': 'public, max-age=86400, stale-while-revalidate=3600',
    },
  })
}
```

**`app/hk/llms-full.txt/route.ts`:**

Read `lib/hk-data.ts` to get the real programme descriptions, venue data, coach data, and FAQ items. Then construct the file with the same structure as the root example above, but expanding all HK sections with real content from the data files.

Key content to include:
- For `## Programmes`: each gymnastics sub-programme (toddlers through adult) with the age range and description from `HK_GYMNASTICS_PROGRAMMES`
- For `## Venues`: full NAP for Wan Chai (15/F The Hennessy, 256 Hennessy Road, Wan Chai) and Cyberport, opening hours from venue data
- For `## Coaches`: each coach's name, title, and bio from `HK_COACHES`
- For `## Optional`: FAQ answers from `HK_FAQ_ITEMS` (the top 5-6 most common questions)

**`app/sg/llms-full.txt/route.ts`:**

Same approach using `lib/sg-data.ts`. Key content:
- For `## Programmes`: zone descriptions from `SG_ZONES`, camp types from `SG_CAMP_TYPES`
- For `## Venue`: full Katong Point NAP (451 Joo Chiat Road, Level 3, Singapore 427664), MultiBall wall feature, transit info
- For `## Coaches`: SG coach bios from `SG_COACHES`
- For `## Optional`: SG FAQ answers from `SG_FAQ_ITEMS`
</action>

<acceptance_criteria>
- All three files exist: `app/root/llms-full.txt/route.ts`, `app/hk/llms-full.txt/route.ts`, `app/sg/llms-full.txt/route.ts`
- HK llms-full.txt content string contains programme descriptions (not just link URLs) — `grep "age" app/hk/llms-full.txt/route.ts` returns programme age range content
- SG llms-full.txt contains "MultiBall" in the content string
- All files use `revalidate = 86400`
- None contain H3 (`###`) headings
- None contain HTML markup
- `pnpm build` succeeds
</acceptance_criteria>
</task>

---

<task id="07-02-T5">
## T5: Promote RED test stubs to real passing assertions

<read_first>
- `tests/unit/sitemap.test.ts` (from Plan 07-01 T4) — the current stub version with TODO comments
- `tests/unit/robots.test.ts` (from Plan 07-01 T4) — current stub
- `tests/unit/llms-txt.test.ts` (from Plan 07-01 T4) — current stub
- `app/hk/sitemap.ts` (just created in T1) — verify the export signature
- `app/hk/robots.ts` (just created in T2) — verify the export signature
- `app/hk/llms.txt/route.ts` (just created in T3) — verify the export signature
</read_first>

<action>
Replace the TODO stub content in the three test files with real assertions that import and call the actual implementations.

**`tests/unit/sitemap.test.ts`** — replace entire file content:

```typescript
import { describe, it, expect } from 'vitest'
import hkSitemap from '../../app/hk/sitemap'
import sgSitemap from '../../app/sg/sitemap'
import rootSitemap from '../../app/root/sitemap'

describe('HK sitemap', () => {
  it('returns array of URLs all starting with https://hk.proactivsports.com/', () => {
    const urls = hkSitemap()
    expect(urls.every((entry) => entry.url.startsWith('https://hk.proactivsports.com/'))).toBe(true)
  })

  it('homepage entry has priority 1', () => {
    const urls = hkSitemap()
    const homepage = urls.find((e) => e.url === 'https://hk.proactivsports.com/')
    expect(homepage?.priority).toBe(1)
  })

  it('all URLs have trailing slashes', () => {
    const urls = hkSitemap()
    urls.forEach((entry) => {
      expect(entry.url.endsWith('/')).toBe(true)
    })
  })

  it('no vercel.app URLs appear in sitemap output', () => {
    const urls = hkSitemap()
    urls.forEach((entry) => {
      expect(entry.url).not.toContain('vercel.app')
    })
  })

  it('includes wan-chai location page at priority 0.9', () => {
    const urls = hkSitemap()
    const wanChai = urls.find((e) => e.url === 'https://hk.proactivsports.com/wan-chai/')
    expect(wanChai?.priority).toBe(0.9)
  })

  it('includes all 8 gymnastics sub-pages', () => {
    const urls = hkSitemap()
    const gymSubPages = urls.filter((e) => e.url.includes('/gymnastics/') && e.url !== 'https://hk.proactivsports.com/gymnastics/')
    expect(gymSubPages.length).toBe(8)
  })
})

describe('SG sitemap', () => {
  it('returns array of URLs all starting with https://sg.proactivsports.com/', () => {
    const urls = sgSitemap()
    expect(urls.every((entry) => entry.url.startsWith('https://sg.proactivsports.com/'))).toBe(true)
  })

  it('no vercel.app URLs appear', () => {
    const urls = sgSitemap()
    urls.forEach((entry) => {
      expect(entry.url).not.toContain('vercel.app')
    })
  })

  it('includes katong-point at priority 0.9', () => {
    const urls = sgSitemap()
    const katong = urls.find((e) => e.url === 'https://sg.proactivsports.com/katong-point/')
    expect(katong?.priority).toBe(0.9)
  })
})

describe('Root sitemap', () => {
  it('returns array of URLs all starting with https://proactivsports.com/', () => {
    const urls = rootSitemap()
    expect(urls.every((entry) => entry.url.startsWith('https://proactivsports.com/'))).toBe(true)
  })

  it('homepage has priority 1', () => {
    const urls = rootSitemap()
    const homepage = urls.find((e) => e.url === 'https://proactivsports.com/')
    expect(homepage?.priority).toBe(1)
  })
})
```

**`tests/unit/robots.test.ts`** — replace entire file content:

```typescript
import { describe, it, expect } from 'vitest'
import hkRobots from '../../app/hk/robots'
import sgRobots from '../../app/sg/robots'
import rootRobots from '../../app/root/robots'

describe('HK robots', () => {
  it('returns allow-all rule for all user agents', () => {
    const r = hkRobots()
    expect(r.rules).toMatchObject({ userAgent: '*', allow: '/' })
  })

  it('sitemap URL points to HK production origin', () => {
    const r = hkRobots()
    expect(r.sitemap).toBe('https://hk.proactivsports.com/sitemap.xml')
  })

  it('host field is hk.proactivsports.com', () => {
    const r = hkRobots()
    expect(r.host).toBe('hk.proactivsports.com')
  })
})

describe('SG robots', () => {
  it('returns allow-all and correct sitemap for SG', () => {
    const r = sgRobots()
    expect(r.rules).toMatchObject({ userAgent: '*', allow: '/' })
    expect(r.sitemap).toBe('https://sg.proactivsports.com/sitemap.xml')
  })
})

describe('Root robots', () => {
  it('returns allow-all and correct sitemap for root', () => {
    const r = rootRobots()
    expect(r.rules).toMatchObject({ userAgent: '*', allow: '/' })
    expect(r.sitemap).toBe('https://proactivsports.com/sitemap.xml')
  })
})
```

**`tests/unit/llms-txt.test.ts`** — replace entire file content:

```typescript
import { describe, it, expect } from 'vitest'
import { GET as hkGET } from '../../app/hk/llms.txt/route'
import { GET as sgGET } from '../../app/sg/llms.txt/route'
import { GET as rootGET } from '../../app/root/llms.txt/route'

describe('HK llms.txt route', () => {
  it('response Content-Type is text/plain; charset=utf-8', async () => {
    const res = await hkGET()
    expect(res.headers.get('Content-Type')).toBe('text/plain; charset=utf-8')
  })

  it('response body starts with # H1 heading (required by llmstxt.org spec)', async () => {
    const res = await hkGET()
    const text = await res.text()
    expect(text.trim().startsWith('# ')).toBe(true)
  })

  it('response body contains blockquote (> ) as required by spec', async () => {
    const res = await hkGET()
    const text = await res.text()
    expect(text).toContain('\n> ')
  })

  it('response body contains no H3 or deeper headings (spec violation)', async () => {
    const res = await hkGET()
    const text = await res.text()
    expect(text).not.toMatch(/^###/m)
  })

  it('response body contains no HTML markup', async () => {
    const res = await hkGET()
    const text = await res.text()
    expect(text).not.toMatch(/<[a-z][\s\S]*>/i)
  })

  it('contains ## Optional section', async () => {
    const res = await hkGET()
    const text = await res.text()
    expect(text).toContain('## Optional')
  })
})

describe('SG llms.txt route', () => {
  it('passes spec checks: H1, blockquote, no H3, no HTML', async () => {
    const res = await sgGET()
    const text = await res.text()
    expect(text.trim().startsWith('# ')).toBe(true)
    expect(text).toContain('\n> ')
    expect(text).not.toMatch(/^###/m)
    expect(text).not.toMatch(/<[a-z][\s\S]*>/i)
  })
})

describe('Root llms.txt route', () => {
  it('passes spec checks: H1, blockquote, no H3, no HTML', async () => {
    const res = await rootGET()
    const text = await res.text()
    expect(text.trim().startsWith('# ')).toBe(true)
    expect(text).toContain('\n> ')
    expect(text).not.toMatch(/^###/m)
    expect(text).not.toMatch(/<[a-z][\s\S]*>/i)
  })
})
```
</action>

<acceptance_criteria>
- `pnpm test:unit -- --grep "sitemap"` passes all assertions (no TODO stubs remain)
- `pnpm test:unit -- --grep "robots"` passes all assertions
- `pnpm test:unit -- --grep "llms"` passes all assertions including spec compliance checks
- `pnpm test:unit` full suite passes
</acceptance_criteria>
</task>

---

## Verification

After all 5 tasks complete:

```bash
pnpm build
pnpm test:unit
grep "vercel.app" app/hk/sitemap.ts app/sg/sitemap.ts app/root/sitemap.ts  # must return nothing
grep "disallow" app/hk/robots.ts app/sg/robots.ts app/root/robots.ts  # must return nothing
```

## must_haves

- [ ] 3 × sitemap.ts exist with hardcoded production origins and no preview URLs
- [ ] 3 × robots.ts always emit allow-all (no environment branching)
- [ ] 3 × llms.txt/route.ts pass the llmstxt.org spec (H1, blockquote, H2, Optional, no H3, no HTML)
- [ ] 3 × llms-full.txt/route.ts contain expanded prose content from data files
- [ ] All three test files promoted from RED stubs to real passing assertions
- [ ] `pnpm test:unit` full suite passes

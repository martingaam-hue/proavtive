---
plan: 08-04
phase: 8
wave: 3
autonomous: false
objective: >
  Operations and human-action phase. Audit all three GBP listings against
  lib/venues.ts, reconcile any NAP drift, upload fresh venue photos, and
  prepare the GSC Domain property TXT record in docs/gsc-txt-record.md for
  Phase 10 DNS cutover. No code changes except creating docs/gsc-txt-record.md.
files_modified:
  - docs/gsc-txt-record.md
task_count: 3
requirements:
  - SEO-09
  - SEO-10
---

# Plan 08-04 — GBP Audit + GSC TXT Record Preparation

## Context

This plan is an operations plan — it involves human tasks in Google Business
Profile admin and Google Search Console. The only code artifact it produces is
`docs/gsc-txt-record.md` (the stored GSC TXT verification value).

All three GBP listings must match `lib/venues.ts` exactly. The GSC Domain
property TXT value is retrieved and stored now so it is ready to paste into
Cloudflare DNS at Phase 10 without any additional research.

**Depends on:** Plan 08-03 (lib/venues.ts must be complete with real values before GBP audit)

**This plan is `autonomous: false`** — every task requires Martin's manual action
in a Google web UI. No task can be fully automated.

---

<task id="08-04-T01">
## T01 — GBP claim status checkpoint + initial audit

<read_first>
- lib/venues.ts (read entire file — the canonical values to match in GBP)
- .planning/inputs/strategy.md PART 8.3 (GBP checklist items 1-4)
</read_first>

<action>
**HUMAN-ACTION gate (D-11):** Before proceeding with the audit, confirm claim
status for all three GBP listings.

```
╔══════════════════════════════════════════════════════════════╗
║  CHECKPOINT: Action Required — GBP Claim Status              ║
╚══════════════════════════════════════════════════════════════╝

Verify in Google Business Profile (business.google.com) that you have
owner/manager access to:

1. ProGym Wan Chai — ProActiv Sports
   Expected primary category: Gymnastics Club
   Search: Google Maps → "ProGym Wan Chai" → Claim/Manage

2. ProGym Cyberport — ProActiv Sports
   Note: opened Aug 2025 — may not have a listing yet
   If no listing: create it at business.google.com/create

3. Prodigy by ProActiv Sports (Katong Point)
   Expected primary category: Sports Club
   Search: Google Maps → "Prodigy ProActiv Katong" → Claim/Manage

→ Confirm claim status for all three, then continue.
──────────────────────────────────────────────────────────────
```

Once claim status is confirmed, perform the GBP audit for each venue.

**For each GBP listing, compare against `lib/venues.ts` canonical values:**

| GBP Field | lib/venues.ts source | What to check |
|-----------|---------------------|---------------|
| Business name | `venues.name` | Exact match (e.g. 'ProGym Wan Chai — ProActiv Sports') |
| Address line 1 | `venues.address` | Exact match including floor format, building name, road suffix |
| Neighbourhood/locality | `venues.locality` | Exact match |
| Phone | `venues.phone` | Same number (E.164 or local format — just same digits) |
| Website URL | hk.proactivsports.com (HK) / sg.proactivsports.com (SG) | Correct market subdomain |
| Primary category | See notes below | Gymnastics Club (HK) / Sports Club or equiv (SG) |

**Primary category guidance:**
- ProGym Wan Chai + Cyberport: "Gymnastics Club" or "Sports Club" — verify in
  GBP category search; use the most specific available for gymnastics
- Prodigy Katong Point: "Sports Club" or "Gymnastics Club" — SG category taxonomy
  may differ from HK; use the most specific available

**For each discrepancy found:**
Document the before/after in a text note. Then correct the GBP field to match
`lib/venues.ts`. Wait for Google to process the update (typically minutes;
address changes may take longer).

Do NOT update `lib/venues.ts` to match GBP — GBP must match the code, not
vice versa. The code is the source of truth.
</action>

<acceptance_criteria>
- Access confirmed (owner/manager) for all three GBP listings
- Each GBP listing's Business Name matches `VENUES.{key}.name`
- Each GBP listing's Address matches `VENUES.{key}.address` (exact format, no 'Rd' vs 'Road' drift)
- Each GBP listing has a Website URL pointing to the correct market subdomain
- Primary category is set (Gymnastics Club or equivalent) for each listing
- Manual verification: GBP UI shows no "Suggested edits" pending that would change NAP fields
</acceptance_criteria>
</task>

---

<task id="08-04-T02">
## T02 — Upload fresh venue photos to GBP (strategy PART 8.3 checklist items 3-4)

<read_first>
- .planning/inputs/MEDIA-INVENTORY.md (identify which photos are hero-tier assets
  for each venue; at least 5 per venue required by Phase 8 success criterion 4)
- .planning/inputs/strategy.md PART 8.3 (exact checklist items for GBP photos)
</read_first>

<action>
For each of the three GBP listings, upload at least 5 fresh venue photos.

**Photo requirements per strategy PART 8.3:**
- At minimum: 1 exterior, 1 interior (equipment/apparatus visible), 1 class-in-session,
  1 team/coaches photo, 1 child participant photo (ensure parental consent)
- Photo orientation: landscape preferred for GBP cover/hero images
- Minimum resolution: 720px × 720px; preferred: 1080px+ wide
- File formats: JPG or PNG (GBP accepts both; avoid WebP for widest compatibility)

**For ProGym Wan Chai:**
- Source photos from: `/Users/martin/Downloads/ProActive/` (WC subfolder)
  or from Sanity/Mux if Phase 2 has already processed the media inventory
- Upload via: business.google.com → ProGym Wan Chai listing → Photos → Add Photos

**For ProGym Cyberport:**
- If the venue has no existing photos (opened Aug 2025), use general ProActiv
  equipment/apparatus photos that represent the brand until venue-specific shots
  are available
- Mark internally that venue-specific Cyberport photos are needed

**For Prodigy @ Katong Point:**
- Source photos from: `/Users/martin/Downloads/ProActive/` (SG/Katong subfolder)
- Ensure at least one MultiBall wall photo is included (distinctive feature per
  strategy PART 8.3)

After uploading, record the upload date and count per venue in a short note
(can be a comment in `docs/gsc-txt-record.md` or a separate `docs/gbp-audit-log.md`).

**Minimum required:** 5 photos per listing before Phase 8 can be marked complete.
</action>

<acceptance_criteria>
- ProGym Wan Chai GBP listing has ≥ 5 photos uploaded (verified in GBP UI)
- ProGym Cyberport GBP listing has ≥ 5 photos uploaded (or note explaining delay)
- Prodigy Katong Point GBP listing has ≥ 5 photos uploaded, including ≥ 1 MultiBall wall photo
- Photo uploads confirmed in GBP UI under Photos tab for each listing
</acceptance_criteria>
</task>

---

<task id="08-04-T03">
## T03 — GSC Domain property creation + TXT record storage

<read_first>
- docs/ directory (confirm docs/phase-0-verification.md exists — docs/ dir is present)
- .planning/phases/08-analytics-gbp-nap/08-UI-SPEC.md §6 (exact template for
  docs/gsc-txt-record.md)
</read_first>

<action>
**Step 1: Create the GSC TXT record file template first:**

Create `docs/gsc-txt-record.md` with the following content (Martin will fill
in the actual TXT value in Step 2):

```md
# Google Search Console — Domain Property Verification TXT Record

**Property type:** Domain
**Domain:** proactivsports.com
**Covers:** proactivsports.com + hk.proactivsports.com + sg.proactivsports.com

## TXT Record Value

<!-- HUMAN-ACTION: Replace with the actual value from GSC Admin (Step 2 below) -->
GSC_TXT_VALUE=google-site-verification=<REPLACE_WITH_ACTUAL_VALUE>

## How to retrieve this value

1. Go to https://search.google.com/search-console/
2. Click **Add Property** → choose **Domain** (NOT URL-prefix)
3. Enter: `proactivsports.com` (the root domain, no subdomain, no https://)
4. GSC shows a TXT record value in format:
   `google-site-verification=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX`
5. Copy the FULL value (including the `google-site-verification=` prefix) and
   paste it above, replacing `<REPLACE_WITH_ACTUAL_VALUE>`
6. **Do NOT click Verify yet** — DNS is not live until Phase 10

## Phase 10 action — paste into Cloudflare DNS

When Phase 10 begins and the domain zone is active in Cloudflare:

| Field | Value |
|-------|-------|
| Type | TXT |
| Name | @ (root, covers all subdomains via Domain property) |
| Content | `google-site-verification=<value from above>` |
| TTL | Auto |

Then return to GSC and click **Verify**.

## Security note

This TXT value is safe to commit to the repository. GSC TXT records are
intentionally public — they prove DNS ownership, not authenticate a service
account. The value has no access capabilities on its own.

## Status

- [ ] Domain property created in GSC
- [ ] TXT value retrieved and pasted above (replace placeholder)
- [ ] File committed to docs/
- [ ] Phase 10: TXT record added to Cloudflare DNS
- [ ] Phase 10: GSC Verify clicked → Domain property verified
```

**Step 2: Create the GSC Domain property:**

```
╔══════════════════════════════════════════════════════════════╗
║  CHECKPOINT: Action Required — GSC Domain Property           ║
╚══════════════════════════════════════════════════════════════╝

1. Go to: https://search.google.com/search-console/
2. Click Add Property
3. Choose DOMAIN (not URL-prefix) — this is the less-prominent option
4. Enter: proactivsports.com
5. GSC shows a TXT verification record — copy the FULL value
6. Paste into docs/gsc-txt-record.md replacing <REPLACE_WITH_ACTUAL_VALUE>
7. Do NOT click Verify yet (DNS is not live until Phase 10)

→ Complete steps 1-7, then commit docs/gsc-txt-record.md
──────────────────────────────────────────────────────────────
```

**Step 3: Commit the file with the real TXT value:**
```bash
git add docs/gsc-txt-record.md
git commit -m "docs(08): add GSC domain property TXT record for Phase 10 DNS cutover"
```

**Important verification before committing:**
```bash
grep "REPLACE_WITH_ACTUAL_VALUE" docs/gsc-txt-record.md
# Expected: zero results (placeholder replaced with real value)

grep "google-site-verification=" docs/gsc-txt-record.md
# Expected: one line containing the real verification string
```
</action>

<acceptance_criteria>
- `docs/gsc-txt-record.md` exists
- `grep "REPLACE_WITH_ACTUAL_VALUE" docs/gsc-txt-record.md` returns zero lines (real value inserted)
- `grep "google-site-verification=" docs/gsc-txt-record.md` returns exactly one line with the real value
- File is committed to git (visible in `git log --oneline -3`)
- GSC shows a Domain property for `proactivsports.com` in pending verification state
- GSC does NOT show the property as "Verified" yet (DNS not live — correct)
- `pnpm build` unaffected (docs/ files don't affect the build)
</acceptance_criteria>
</task>

---

## Verification

After all tasks, verify all Phase 8 success criteria manually:

**Success Criterion 3 — NAP consistency:**
- Open `lib/venues.ts` and compare each field character-for-character against
  the three GBP listings
- Open the HK footer on preview → compare phone/address to `VENUES.wanChai`
- Open the SG footer on preview → compare to `VENUES.katongPoint`
- Open HK JSON-LD (view-source on Wan Chai page) → check `address` and `telephone`

**Success Criterion 4 — GBP checklist:**
- GBP Wan Chai: name ✓, category ✓, hours ✓, ≥5 photos ✓
- GBP Cyberport: name ✓, category ✓, hours ✓, ≥5 photos ✓
- GBP Katong Point: name ✓, category ✓, hours ✓, ≥5 photos (incl. MultiBall) ✓

**Success Criterion 5 — GSC TXT record:**
- `docs/gsc-txt-record.md` contains real TXT value
- GSC shows Domain property for `proactivsports.com` (not yet verified — correct)

```bash
# Code check — no new build issues introduced
pnpm typecheck && pnpm lint && pnpm build
```

<must_haves>
- `docs/gsc-txt-record.md` exists with the real GSC TXT value (not placeholder)
- GSC Domain property exists for proactivsports.com (pending, not yet verified — correct)
- All three GBP listings have Business Name, Address matching lib/venues.ts exactly
- All three GBP listings have ≥ 5 photos uploaded
- All three GBP listings have a Website URL set to the correct market subdomain
- Phase 8 success criteria 3, 4, and 5 are all satisfied
</must_haves>

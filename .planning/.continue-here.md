---
context: default
phase: n/a — ad-hoc studio debug session
status: complete
last_updated: 2026-04-25T16:33:52Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: webpack alias bypass — Next.js 15 uses layer-based module resolution that ignores global `resolve.alias`. Do NOT attempt webpack alias fixes for React module issues; use the postinstall patch approach instead.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| webpack resolve.alias for React | Next.js 15 layer-based resolution bypasses global aliases entirely — both `react` and `react$` approaches silently failed | blocking | Use `scripts/patch-next-react.mjs` postinstall approach instead |
| Stale `.next` cache after config change | webpack config changes require `rm -rf .next` + server restart or old artifacts cause 500 errors | advisory | Always clear `.next` after changing `next.config.ts` |

<current_state>
Sanity Studio at http://localhost:3000/studio is working. Dev server running on port 3000 (webpack mode, `pnpm dev`).

All 3 previous debug sessions resolved:
1. Schema warnings (6→0): imageWithAlt duplicate validation, venue openingHours missing name, structure documentTypeListItem duplicates, Presentation tool singleton select
2. useEffectEvent crash: removed `patches/sanity@5.22.0.patch` (the patch introduced useEffectEvent calls); added `scripts/patch-next-react.mjs` as safety net
3. Presentation "Unable to connect": added `VisualEditing` from `next-sanity/visual-editing` to root layout behind draftMode guard
</current_state>

<completed_work>
- Fixed all 6 Sanity Studio schema warnings (commit 7f210ac)
- Removed Sanity patch that caused useEffectEvent crash (commit 7db8a7d)
- Added patch-next-react.mjs postinstall script as safety net
- Fixed CSP headers (unsafe-eval + core.sanity-cdn.com)
- Removed --turbopack from dev script
- Wired VisualEditing to root layout for Presentation tool (commit dbfa6db)
</completed_work>

<remaining_work>
- No outstanding studio issues known
- Presentation tool "Continue anyway" button works; full visual editing overlays on frontend pages are a future phase task
</remaining_work>

<decisions_made>
- Removed patches/sanity@5.22.0.patch — the patch introduced useEffectEvent into Sanity; unpatched Sanity 5.22.0 does not use it
- VisualEditing only renders in draft mode (isDraftMode guard) — zero production overhead
- dev script runs webpack (not turbopack); build uses turbopack — stable dev experience
</decisions_made>

<blockers>
None.
</blockers>

## Infrastructure State
- Dev server: running on port 3000 (`pnpm dev`, webpack mode)
- Studio: http://localhost:3000/studio — working, 0 issues
- Git: clean (3 commits ahead of last pre-session state, all pushed)

<next_action>
Studio is fully working. Resume normal phase work with `/gsd-resume-work` or `/gsd-next`.
</next_action>

---
context: default
phase: dark-theme-design-parity
task: null
status: paused
last_updated: "2026-05-12T09:37:43.957Z"
---

<current_state>
Ad-hoc dark theme design parity work. The site-wide dark cinematic redesign
(Space Grotesk, oklch(0.07) near-black background, brand-red primary) was
applied to root homepage + HK/SG navs in a prior session. This session fixed
the three location pages. HK/SG homepages and all inner pages have NOT been
audited yet.
</current_state>

<completed_work>

- `ad98a40` — Dark theme + Space Grotesk + cinematic hero slider on root homepage
- `7f05f91` — Dark nav dropdowns + market image 400 fixes
- `381c958` — HK/SG navs: bg-[#0a0a0f]/95, white text, dark dropdowns, cookie escape to root
- `8954d4b` — **Location pages parity** (this session):
  - `app/hk/wan-chai/page.tsx` — brand-navy → brand-red on all accents + icons + outline button
  - `app/hk/cyberport/page.tsx` — same + sizeNote text colour
  - `app/sg/katong-point/page.tsx` — brand-navy → brand-green on all accents + icons + outline button
  - Fixed pre-existing eslint-disable-next-line misplacement in all three files
</completed_work>

<remaining_work>

**Not yet audited for brand-navy dark-theme clashes:**

- `app/hk/page.tsx` — HK homepage. Known issues: CampsPartiesSection outline button uses `border-brand-navy`, AboutSnapshot heading uses `font-accent text-brand-navy`
- `app/sg/page.tsx` — SG homepage. Similar pattern likely
- All other HK inner pages: gymnastics pillar, coaches, camps, parties, booking hub
- All other SG inner pages: weekly classes, camps, coaches, parties, schools

**Quick audit command:**
```bash
grep -rn "brand-navy" app/hk/ app/sg/ app/root/ --include="*.tsx" | grep -v "bg-brand-navy\|opengraph\|\.test\." | grep -v "text-brand-navy.*bg-brand-yellow"
```
This shows all problematic brand-navy references (excludes Section bg=navy and the yellow badge intentional use).
</remaining_work>

<decisions_made>

- HK accent = `text-brand-red` — matches HKNav ProActiv HK red marker
- SG accent = `text-brand-green` — matches SGNav Prodigy SG green marker
- Outline buttons → `border-white/20 text-foreground hover:bg-white/8` on dark bg
- Yellow badge `bg-brand-yellow text-brand-navy` on Katong Point intentionally kept — navy on yellow is readable
- `bg="navy"` Section CTA strips kept — brand-navy (#0f206c) is visibly distinct from oklch(0.07) near-black
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `components/hk/hk-nav.tsx` — authoritative reference for HK dark design language
2. `components/sg/sg-nav.tsx` — authoritative reference for SG dark design language
3. `app/globals.css` lines 1–60 — dark token definitions (--background: oklch(0.07), brand-red as primary)

## Critical Anti-Patterns (do NOT repeat these)
- **Wrong eslint-disable placement**: `eslint-disable-next-line` was on the `const` line but the `any` was 4 lines later on the closing `})) as { data: any }`. Fix: place the comment on the line immediately before `})) as { data: any }`.

## Infrastructure State
- All changes committed and pushed to `main` (`8954d4b`)
- No dev server running
- No uncommitted files

<context>
The dark cinematic redesign touched root homepage + nav but left location pages
and inner pages with brand-navy references that are near-invisible on the dark
background. The pattern is consistent: replace brand-navy decorative/interactive
uses with brand-red (HK) or brand-green (SG), fix outline buttons to
border-white/20. The grep command in remaining_work will surface everything
that's left in one shot.
</context>

<next_action>
Run the grep audit command from remaining_work to find all outstanding brand-navy
references across app/hk/ and app/sg/. Apply the same red/green parity pattern
page by page. Start with app/hk/page.tsx (HK homepage) as it has the most traffic.
</next_action>

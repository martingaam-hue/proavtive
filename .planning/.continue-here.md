---
context: default
phase: bug-fix
task: null
total_tasks: null
status: paused
last_updated: 2026-05-12T13:04:30Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints discovered this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Diagnosing CI failures without Vercel logs | Assumed ESLint was the cause (it was last session) — spent time auditing any[] before confirming ESLint is clean | advisory | Always run `pnpm lint` locally first before assuming Vercel CI cause matches last session's cause |

<current_state>
Bug-fix session complete. Middleware navigation bug fixed and pushed (ebc4929). Vercel CI failures diagnosed as missing Sanity env vars — human action required before further progress. Design inconsistency on underpages is most likely empty Sanity states on Vercel (CSS tokens verified correct). No active phase or plan.
</current_state>

<completed_work>

- **Middleware navigation fix** (ebc4929): Query param `?__market=` now checked BEFORE cookie in precedence ladder. Fixes root cookie trapping users on root page when clicking HK/SG buttons.
- **Vercel CI root cause identified**: NOT ESLint. Missing `NEXT_PUBLIC_SANITY_PROJECT_ID`, `NEXT_PUBLIC_SANITY_DATASET`, `SANITY_API_READ_TOKEN` on Vercel project. Local `.env.local` has them; Vercel doesn't.
- **CSS token system verified**: All tokens dark (`--accent: oklch(0.14 0 0)`, `--card: oklch(0.11 0 0)`, `--muted: oklch(0.13 0 0)`). No stale light colors in page files.
</completed_work>

<remaining_work>

- **[HUMAN ACTION REQUIRED]** Add Sanity env vars to Vercel → Settings → Environment Variables:
  - `NEXT_PUBLIC_SANITY_PROJECT_ID` (from Sanity dashboard → Settings → API)
  - `NEXT_PUBLIC_SANITY_DATASET` (usually `production`)
  - `SANITY_API_READ_TOKEN` (Viewer-scoped token from Sanity → Settings → API → Tokens)
  - `SANITY_REVALIDATE_SECRET` (any random string)
  - Optional: `NEXT_PUBLIC_MUX_HK_HERO_PLAYBACK_ID`, `NEXT_PUBLIC_MUX_SG_HERO_PLAYBACK_ID`
- **[HUMAN ACTION REQUIRED]** Trigger Vercel redeploy after adding env vars — CI should go green
- **[HUMAN ACTION REQUIRED]** Clear browser cookies (x-market) then test HK/SG navigation buttons
- Verify design consistency after Sanity data loads on Vercel (if still broken, share screenshot)
- Decide next milestone: Phase 08 (Analytics/GBP), Phase 09 (Security), or Phase 10 (DNS cutover)
</remaining_work>

<decisions_made>

- **Middleware precedence**: Host > query param > cookie > default root. Changed from Host > cookie > query because `"root"` is a valid KNOWN_MARKET and a stale `root` cookie blocked the query param override entirely.
- **CI failure cause**: Not ESLint (local `pnpm lint` = 0 errors, only warnings). Root cause is missing Sanity env vars on Vercel causing build-time SSG fetch to fail.
- **Design inconsistency**: CSS token system is correct. Visual issues on Vercel underpages are empty Sanity states (no data = placeholder/empty UI), not CSS bugs.
</decisions_made>

<blockers>
- Vercel CI will remain broken until user adds Sanity env vars (human action, not code issue)
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — structured state with all completed/remaining tasks and env var list
2. `middleware.ts` — review ebc4929 diff if continuity around routing logic is needed

## Critical Anti-Patterns (do NOT repeat these)
- **ANTI-PATTERN**: Assuming Vercel CI fails for same reason as last session → always run `pnpm lint` + `pnpm build` locally first before diagnosing CI

## Infrastructure State
- Dev server: NOT running (user should `pnpm dev` to restart)
- Git: clean, main branch, pushed to origin (ebc4929 is latest)
- Vercel: builds failing (missing env vars) — human action required
- Sanity: configured locally via .env.local, NOT configured on Vercel

<context>
Started by investigating reported issues: (1) underpages with mixed design, (2) HK/SG nav buttons broken, (3) Vercel CI failures. Found the nav bug immediately in middleware — cookie check came before query param, and "root" is a valid market so clicking /?__market=hk while cookie=root sent you back to root. Fixed and pushed. The CI failures are pure missing env vars on Vercel — nothing wrong with the code. The design inconsistency is most likely empty Sanity states on Vercel rendering placeholder UI (no data), not actual CSS issues — all tokens were verified dark and correct.
</context>

<next_action>
Start with: Confirm user has added Sanity env vars to Vercel and redeploy is green. Then clear cookies and verify HK/SG navigation works. Then decide next milestone phase.
</next_action>
